---
context: default
phase: none (all 7 v0.0.2 phases complete and milestone-audited; quick-task follow-up work)
task: 1
total_tasks: 1
status: paused
last_updated: 2026-08-09T11:23:30.984Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

- [ ] CONSTRAINT: **NEVER merge v0.0.2, PR #16, or any other PR until the maintainer explicitly
      says so.** The merge is gated behind ADDITIONAL code and security reviews, which are
      themselves gated behind proving what the milestone claims. "All the evidence is in" is not
      an argument for merging -- it is the argument for STARTING those reviews.
      Pre-approved counterparts: pushing any branch, opening/closing/drafting any PR, and
      temporarily pushing to `main` with backup-before and restore-after.

- [ ] CONSTRAINT: **DO NOT touch `.planning/REQUIREMENTS.md`.** Two independent reviewers
      concluded unanimously against it. The decision not to edit it was ALREADY made earlier the
      same day, in commit `9251809`; the proposal to reverse it was a same-day reversal offering
      no changed circumstance, and the one thing that HAD changed turned out not to support it.
      ROBUST-04's checkbox STAYS TICKED, and legitimately: its traceability cell at `:736`
      carries no discharge evidence and nothing anywhere cites the all-MISS warning as proof the
      requirement was met, so the tick was never bought with the false sentence.

- [ ] CONSTRAINT: **Do not write that the drift gap is "closed".** It is not. Both reviewers
      refuted that independently, for two different reasons (see the anti-pattern table). Under
      drift the proportional warning is SILENT mid-month and MISATTRIBUTES the cause at shard
      rollover. Writing "now surfaced by the proportional branch" would replace a false sentence
      with a misleading one, in the same defect class this project has now caught seven times.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| The orchestrator's own claims were falsified SIX times in one session, all in this one area | (1) the gate is "dogfood-only"; (2) it is "effectively unreachable"; (3) drift makes publish unable to restore even its own writes; (4) Phase 10 invalidated a previously-true requirement; (5) the gap is now closed by the proportional branch; (6) "everything misses except the seeds" as that claim's input. | blocking | EVERY one was caught by an agent RE-DERIVING the claim from source, and NOT ONE by the agent that wrote it. Treat any claim in this area that has not been re-derived from code or git history as unverified -- including claims in this handoff. |
| A filtered view is not the artifact | Two of the six survived because the orchestrator inferred YAML job membership from a line-numbered `rg` excerpt. The reviewer that caught it extracted the whole file at both commits and found a DECOY `operation: seed` in a different job. | blocking | For any structural claim about YAML, read the raw block, not a filtered projection. Check the state at the commit that WROTE the claim, not just the current one. |
| Arguments lost to one measurement | A seven-agent panel produced a real option set and caught real errors, but BOTH of its numeric estimates were wrong and its consensus rested on a premise a measurement falsified. One 17-minute temporary `main` window settled what none of them could. | advisory | Prefer one observation to any number of arguments. The panel's value was the option set and the corrections, not its conclusion. |
| Rewriting a requirement to fit the implementation | The maintainer stopped this with: "To me it seems like we're rewriting requirements to fit our implementation." Both reviewers upheld the objection against the REVISED proposal too. | blocking | When a requirement's claim is false, the question is whether the requirement expressed an INTENT the implementation never met. If so it is an implementation gap, and documenting it away buries it. |
| Borrowing a precedent's legitimacy | XOS-05 was cited as precedent for an inline correction. It is the wrong shape twice over: its supersession lives in the traceability STATUS CELL not the requirement body, and it RE-DISCHARGED a claim that was never false -- only its witness was. | advisory | Read the precedent in full before leaning on it. XOS-05's actual spirit is "go make the claim true and prove it", not "annotate". |

<current_state>
Nothing is mid-execution. Tree clean, HEAD `9251809` level with the remote (0/0), `main` untouched
at `fe25a3f`, PR #16 OPEN with `mergedAt` null. Seven commits landed today across two quick tasks
(`260809-2s6` plans 01 and 02, and `260809-hcr`), all pushed.

Paused immediately AFTER a two-agent review REJECTED the correction the orchestrator proposed,
and BEFORE the single remaining edit is applied. That edit is docs-only, to ONE unfrozen file.
</current_state>

<completed_work>

- Quick `260809-2s6` plan 01: stopped the publish mirror re-enumerating prior runs' seed entries.
- Quick `260809-2s6` plan 02: replaced the partial-miss raw ratio with a Wilson lower bound,
  replaced every derived figure with one measured on run `31305961054`, and stripped a
  vendor-context leak from the shipped warning. 1073 -> 1079 tests.
- Quick `260809-hcr`: corrected `docs/advanced.md`, which promised consumers the all-MISS warning
  for a version bump when the proportional branch is what a bump produces. Guarded by
  `docs-same-os-claims.spec.ts`, proven by restoring the pre-edit prose (six failures).
- Filed `.planning/todos/pending/robust-04-all-restore-miss-clause-is-false.md`.
- Ran a two-agent review of a proposed ROBUST-04 correction. Result: REJECTED -- see below.
</completed_work>

<remaining_work>

ONE task. Amend `.planning/todos/pending/robust-04-all-restore-miss-clause-is-false.md`, which is
in `todos/pending` and therefore NOT frozen:

1. **Fix the present tense that flatters the past.** It says a drifted run "would actually show
   the PROPORTIONAL branch". Before `aee017c` (today) there was no partial branch at all, so
   drift was FULLY runtime-silent for the requirement's whole life, 2026-07-26 to 2026-08-09.
2. **Record that no drift actually shipped in the window.** Nine commits touched
   `start-cache-server/index.js` since 2026-07-26, each paired with the source change that
   required it; the unpaired ones are spec files or comment-only (`f6c91fd`), and esbuild strips
   comments. No `action-bundle-drift` catch is recorded anywhere in `.planning`. The gap was
   LATENT and UNREALISED -- without this sentence a reader assumes harm that did not occur.
3. **DELETE the closure claim.** See the third blocking constraint.
4. **Add what is established:** under drift the proportional warning is SILENT mid-month and
   MISATTRIBUTES the cause at rollover.

Optional, and recommended by one reviewer: file the misattribution as its own follow-up. It is an
open consumer-facing defect of the same species as the `docs/advanced.md` one just fixed.
</remaining_work>

<decisions_made>

- ROBUST-04's surfacing sentence was FALSE WHEN WRITTEN (2026-07-26), not invalidated later --
  publish-side seeding predates it by six days (`80fb65f`, 2026-07-20).
- That made it an IMPLEMENTATION GAP, not merely inaccurate prose: the requirement asserted drift
  had a runtime signal, and it had none.
- The gap is NOT closed. Today's Wilson branch does not close it.
- The checkbox stays ticked; the obligation (`build:action` in the same commit) and the mitigation
  (`action-bundle-drift`) are both intact.
- The correct venue for the requirement's fate is the maintainer's pending review, with the diff
  in hand.
</decisions_made>

<blockers>
- None blocking. The two entries above are recorded to stop settled ground being re-litigated.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` -- the same state, machine-readable, with the full evidence chain and
   every refuted claim spelled out with its refutation.
2. `.planning/todos/pending/robust-04-all-restore-miss-clause-is-false.md` -- the file to amend.
3. `packages/github-cache/src/publish/publish-mirror.ts` -- specifically the two warning branches
   and `wilsonLowerBound`. The proportional message's two candidate causes are at `:895-902`, and
   D3's membership-before-restore skip is at `:635`. Both are load-bearing for what is now known.

## Infrastructure State

- `origin/main` = `fe25a3f`. No window open. CI workflow `313666980` is `active`.
- PR #16 OPEN, `mergedAt` null. Seven `refs/backups/*`, all at `fe25a3f`.
- `nx format:check --all` reports two PRE-EXISTING failures under a gitignored
  `CLAUDE-SECURITY-*` directory -- untracked local scan output, absent from CI, not ours.
- `gh workflow disable` is DENIED to the agent by the auto-mode classifier. Any future temporary
  `main` window needs the maintainer to run the disable/enable pair; raise it at pre-flight, not
  mid-window.

<context>
The through-line of this session is that re-derivation catches what authorship does not. Six of
the orchestrator's own claims died that way, plus a false succession premise in a plan, a
threshold that would have fired on every correct run, an ungated fail-open path, five false
comments, a guard covering one of two sites, and a verify command whose cheapest fix was deleting
correct prose. None was caught by the agent that wrote it.

The remaining edit is small and its content is settled. Resist the pull to reopen the ROBUST-04
question itself -- it has been reviewed twice and belongs to the maintainer's review now.
</context>

<next_action>
Start with: apply the four edits in `<remaining_work>` to
`.planning/todos/pending/robust-04-all-restore-miss-clause-is-false.md`, then commit. Do NOT touch
`.planning/REQUIREMENTS.md`.
</next_action>
