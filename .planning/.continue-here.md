---
context: default
phase: none (all 7 v0.0.2 phases complete, milestone audited; item 1 of the operator plan is done)
task: 1
total_tasks: 3
status: item_1_complete
last_updated: 2026-08-08
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

- [ ] CONSTRAINT: **NEVER merge v0.0.2, any other milestone, PR #16, or any other PR until the
      operator explicitly says so.** No condition satisfies this implicitly -- not a green CI run,
      not a passed verification, not a clean audit, not a `MERGEABLE` PR. Stated once and restated
      TWICE after an initial narrower reading ("until items 2 and 3 are confirmed") proved wrong.
      Do not offer merging as an option, a recommendation, or a "natural next step".
      -- Counterpart, equally explicit: temporary pushes to `main` ARE approved for anything a PR
      cannot verify, with backup-before and restore-after; pushing any branch and opening/closing
      any PR are also approved. That exception exists so a merge is never needed as a measurement
      device.

- [ ] CONSTRAINT: **Item 2 must precede item 3.** Item 2 fixes the defect in the very procedure item
      3 uses -- the temporary-main-window restore force-push fires a full production `ci.yml` run,
      both `publish` legs writing for real, because `[skip ci]` is structurally unavailable to a
      re-push of an existing commit. Running item 3 first means improvising against a
      known-defective procedure.

- [ ] CONSTRAINT: **`--auto` is NOT a flag `/gsd-core:quick` parses.** MEASURED: `quick.md` parses
      only `--full`, `--validate`, `--discuss`, `--research`, `--text`. Passing `--auto` literally
      would land the string in the task DESCRIPTION and pollute the derived slug -- a silent
      corruption, not an error. The operator's intent is `discuss-phase --auto` SEMANTICS: single
      pass, auto-pick the recommended option per gray area, no human checkpoint. Apply the
      semantics, not the flag. -- The documented exception still binds: a gray area that is BOTH
      high-impact and not-high-confidence is written into CONTEXT.md as an explicit UNRESOLVED with
      its competing options, never auto-locked.

- [ ] CONSTRAINT: **Plan-check findings are yours to resolve.** Fix them, re-run the checker, or
      rule that they do not block -- then proceed. Do not present them as a question. Report
      outcomes and judgment calls, not line numbers or gate IDs; the detail belongs in the
      artifact file.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| A number asserted as MEASURED that was never measured | The verifier's single gap on item 1. The plan said a bullet postdates its task "by about five weeks"; the commits are one week apart, and five weeks after the base date is a FUTURE date, so the figure was unreachable. It was the one probe carried forward as text instead of executed, and it propagated into the summary twice and into STATE.md on the strength of the assertion. | blocking | If a claim says MEASURED, run the command. A downstream file inherits provenance along with the number. |
| Gate that reads as coverage but asserts nothing | Now eight instances across two tasks. Latest: a region gate greps `## Central Question` where the real heading is `### Central Question`, so unrelated prose satisfied it. | blocking | Ask of every gate: would an honest FALSIFIED still pass? would writing NOTHING pass? would DELETING the guarded text pass? Check heading LEVEL, not just heading text. |
| A fix that needs reviewing as hard as the original | Iteration 3 found both its blockers in the REVISION, not the plan. And the sweep correcting the five-weeks figure hit the very line-wrap trap it was fixing. | blocking | Plan-check the revision, not just the plan. |
| Confident false zero | Four measured shapes, each reading as absence: a literal spanning a line wrap (has now fired THREE times, including on the correction sweep); `rg -U` with `\s+` cannot bridge a wrap whose continuation carries a `#` (use `[\s#]+`); a SHAPE-anchored regex failing because a file records verdicts as headings, not table rows; and `git grep` with a dash-leading pattern erroring out (`error: unknown switch` -- use `-e`). | blocking | Check the EXIT CODE (1 = genuine no-match, 2+ = the command FAILED) and run a positive control on the same path before treating any zero as absence. |
| A verification step that is unsatisfiable by construction | Item 1's `<verification>` item 4 asked for all three gate scripts to pass together at the final tree. Two scope gates exclude `STATE.md`; the third requires it modified-and-unstaged. No tree state satisfies all three. | advisory | Report it as a finding. Do NOT weaken a gate to make a whole-task check go green. |
| Ungated precise count in a sealed artifact | Counts rot. Item 1's own census was right at one commit and wrong two commits later, the delta being the task's own transient handoff files. | blocking | Either gate the number, anchor it to a commit, or omit it and say the omission is deliberate. |

<current_state>
**ITEM 1 IS COMPLETE AND VERIFIED.** Quick task `260808-lpt` planned, hardened through three
plan-check iterations, executed on the main tree, verified 8/9 with the one gap closed.

Commits: `ccd8248` (plan revision), `c3fe74d` / `d5841c2` / `fed70f4` (the three task commits),
`142ea72` (the verifier's gap), `7f2ba26` (STATE.md). `origin/main` is `fe25a3f`, untouched, no
window open. PR #16 is OPEN and MERGEABLE and **must not be merged**.

Nothing outside `.planning/` was modified by item 1.
</current_state>

<remaining_work>

1. **ITEM 2 -- resolve the two genuine follow-ups.** Load-bearing half: the temporary-main-window
   restore force-push fires a full production `ci.yml` run, both `publish` legs writing for real,
   because `[skip ci]` is structurally unavailable to a re-push of an existing commit. Five stale
   `refs/backups/*` on origin, all at `fe25a3f`, suggest five prior undisclosed occurrences --
   **do not touch those refs**, they are part of the subject. Second half: one unattributed test
   failure at `69bd1b7`, not reproducible in 7 attempts, no output captured. It is likely
   UNACTIONABLE until a second occurrence WITH output -- say so plainly rather than manufacturing
   an investigation.
2. **ITEM 3 -- close the two open-by-design observations.** Only after item 2. Phase 9's first
   post-merge `publish-verify` (windows-11-arm) leg, and Phase 10's OBS-01 summary + shard census.
   Both need a real push to `main` that RUNS the publish legs; they are gated off `pull_request`
   and were skipped on run `30947535877`. Temporary main window, backup before, restore after.
   Never a merge.
</remaining_work>

## Required Reading (in order)

1. `.planning/HANDOFF.json` -- machine-readable state. `operator_plan` holds all three items;
   `decisions` holds every operator ruling.
2. `.planning/quick/260808-lpt-*/260808-lpt-SUMMARY.md` and `-VERIFICATION.md` -- what item 1
   actually closed, and the three discrepancies it found against its own plan.
3. `./CLAUDE.md` and `./AGENTS.md` -- `grep` is DENIED; `git commit -m` FAILS on this Dev Drive
   (use `-F`); never `git add .` / `-A` / `-u`; ASCII only.

## Infrastructure State

- `origin/main`: `fe25a3f865f20f3d4f8a40e96f8cb5717608ba8a`, verified untouched. No window open.
- Five pre-existing `refs/backups/*` on origin, all at `fe25a3f`. **Do not touch them** -- they
  are evidence for item 2, not cleanup targets.
- PRs: #16 OPEN (base `main`), #17 CLOSED never merged.
- No background processes, no async job manifests, no running servers, no open worktrees.

<next_action>
Start ITEM 2 via `/gsd-core:quick --full` with `discuss-phase --auto` semantics. Then ITEM 3,
never before. Never a merge.
</next_action>
