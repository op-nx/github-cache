---
context: default
phase: none (all 7 v0.0.2 phases complete; active work is quick task 260804-lc3)
task: 0
total_tasks: 1
status: paused
last_updated: 2026-08-04T13:59:18.259Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **The lc3 plan is revised but its new gates are UNREVIEWED. Do not execute until
      plan-check iteration 2 returns.** This constraint was originally "the plan is unrevised, do not
      execute"; the revision landed at 14:12Z, twelve minutes after this handoff was first written,
      and all four items are verified present (6 `if ...; then exit 1; fi` gates, the spec-file
      absence gate, all four per-site literals, the liveness-proof literal, the `awk` diff gate;
      gate count 19 -> 23). **The reason to still not execute changed rather than disappeared:** the
      four NEW gates have had no reviewer other than their author, and the planner itself found a
      defect in one of them while testing it -- the `SRV-05` count gate failed pre-edit with
      `test: : integer expected` because `git grep -c` prints nothing on zero matches. -- Mitigation:
      run plan-check iteration 2 scoped to the new gates, then execute.

- [ ] CONSTRAINT: **`rg` undercounts the prose this task audits.** `dead sidecar` reads as 2 lines
      with a naive `rg -c -F`, but is 4 occurrences in `ci.yml` -- one is uppercase (`DEAD SIDECAR`
      at `:1047`) and one is split across a hard `#` wrap (`:1030-1031`). I hit this: my count
      disagreed with the planner's and the planner was right. -- Mitigation: always `-i`, and `-U`
      with `\s+` for any phrase that might wrap. Check exit codes (2 = command FAILED, 1 = genuine
      no-match) and run a positive control before treating a zero as absence.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Gate that reads as coverage but asserts nothing | The session's recurring defect, caught three times and never by the author. h3b's append-only gate froze a file it then instructed editing, so it would have killed its own task 2. lc3's spec-file gate covered only the presence half, so a compliant run would ship a corrected `ci.yml` beside a still-false docstring in the guard file added to prevent exactly that. Four lc3 sites could be satisfied by deletion alone, leaving a dangling sentence. | blocking | Run the plan-checker on every plan and treat "would an honest FALSIFIED still pass?" and "would writing NOTHING still pass?" as mandatory questions. Do not let the plan author be the only reader. |
| Deletion-only gate | An absence gate with no matching presence gate passes when the executor removes the bad text and writes no replacement. | blocking | Every absence gate needs a paired required literal at the same site. lc3's `:1040` already does this (`INPUT DOES NOT EXIST`); four sites still need it. |
| `cmd; test $? -eq 1` under `set -e` | Empirically confirmed: `set -e` aborts on the correct absence-confirming nonzero exit before `test` runs, so the gate false-fails on correct work -- and the predictable response is weakening a working gate to reach green. | advisory | Use the POSIX-exempt form: `if git grep -q -F "X" -- <file>; then exit 1; fi`. Whether this harness applies `set -e` is EXPLICITLY OPEN; the safer form removes the question. |
| Stale artifact asserting a closed item as open | The milestone audit's own follow-up #1 was already fixed in code; the ROADMAP note just predated its fix by two hours. Cost a full quick task to discover. | advisory | Before treating any recorded follow-up as open, check `git merge-base --is-ancestor <note-commit> <fix-commit>`. A note is not evidence of a live defect. |
| Citing line numbers that move | `2df3af5` exists in this repo for this reason. This session found another: CONTEXT.md cited `:806` for a guard whose assertion is `:816` (`:806` is the `it()` line). | advisory | Re-derive every cited line number at commit time. Prefer symbol citations over `file.ext:NN` -- `ci.yml` carries 39 bare filenames and ZERO `:NN` citations, deliberately. |

<current_state>
Paused deliberately, by operator request, immediately before executing quick task 260804-lc3.
No active phase -- all 7 v0.0.2 phases are complete and the milestone is audited.

Two units of work COMPLETED and committed this session; a third is planned but not executed.

HEAD is `d1600f6`, 471 commits ahead of `origin/main` (`fe25a3f`). `main` is untouched and verified
at `fe25a3f`. PR #16 is OPEN with base `main`. Working tree is clean apart from three untracked lc3
task documents.
</current_state>

<completed_work>

- **Milestone v0.0.2 audit** (`3c67513`) -- Done. `status: tech_debt`: 51/51 requirements satisfied,
  7/7 phases verified, 8/8 integration seams wired, 4/4 target outcomes achieved, 7/7 Nyquist
  compliant, zero blockers, 11 debt items. The 3-source cross-reference needed a judgement call:
  SUMMARY frontmatter carries `requirements_completed` for only 21 of 51 IDs (Phases 7-9 never
  populate it), so the matrix row mapping "passed + frontmatter missing" to *partial* would have
  reported 30 partials on a house-style convention. Verified those 30 by hand instead.

- **Quick 260804-h3b** (`d4dc093`, `dd780f8`, `76a9e9b`, `d1600f6`) -- Done, Verified 8/8. Found its
  own subject already fixed: the ROADMAP note (`da462b5`, 08-03 01:09) predates both fixes,
  `40e4d21` (03:13) and `e5d3cd3` (22:27). Note superseded in place; audit corrected at four sites.
  Both sub-claims observed live and reported separately -- (a) delta allowance, run `30907575624`,
  `delta=9596s matched_ref=refs/pull/16/merge`, `headSha` equal to its own pre-registration commit;
  (b) `$defaultref` clause, run `30910935382` on stacked draft PR #17, `delta=1252s
  matched_ref=refs/heads/main`, with both other jq arms MEASURED empty. `main` window open 3m30s,
  restored behind a three-way gate. Teardown complete.

- **Quick 260804-lc3** -- IN PROGRESS. CONTEXT.md, RESEARCH.md and PLAN.md written (all untracked).
  Plan-check iteration 1 complete: 2 blockers, 5 warnings. Revision dispatched, NOT landed.
</completed_work>

<remaining_work>

1. Confirm the lc3 plan revision landed -- four items, listed in `HANDOFF.json`
   `human_actions_pending[0]`. If absent, apply them by hand.
2. Plan-check iteration 2 of 2 on the revised plan.
3. Execute lc3 with `gsd-executor` on the MAIN TREE (no worktree isolation): correct 7 `ci.yml`
   sites + the `docs-same-os-claims.spec.ts:522` docstring + add 2 lock phrases to row D, in ONE
   commit.
4. Verify with `gsd-verifier`, update STATE.md's Quick Tasks Completed table, commit artifacts.
</remaining_work>

<decisions_made>

- lc3 corrects SEVEN `ci.yml` sites plus one live spec docstring, not the one line the report named
  -- root-cause discipline plus the repo's own DOCS-09 same-commit precedent.
- The comment lock goes on the EXISTING row D of `docs-same-os-claims.spec.ts` as two phrases, not a
  new row. That file's read is RAW so it can see comments; `dogfood-cross-os.spec.ts` cannot,
  because its `codeLines` filter strips every `#` line (its own policy note at `:223-225`).
- Pin `UNCONDITIONAL cache.restoreCache` and `404 means the sidecar DID answer` -- the cause and the
  code discrimination, never the disjunction's incidental wording, which may legitimately be reworded.
- The prose MUST distinguish MEASURED from REASONED. The presence disjunction is measured (two runs);
  the failure-implication correction is derived from code and has NEVER been observed.
- Dead-sidecar negative gates are SENTENCE-scoped, never file-wide -- `:1015` and `:1047` are
  legitimate and must survive, and `:1047` is the paragraph that makes the correction true.
- `known-present` is deliberately NOT gated: it is REQUIREMENTS.md's own word and correct prose may
  reuse it.
- No behaviour change. Acceptance set (200 alone), no-retry-loop and step placement all unchanged.
- Run on the MAIN TREE, no worktree isolation -- a junctioned `node_modules` makes `check:action`
  report FALSE bundle drift, so only the main tree gives a true verdict.
</decisions_made>

<blockers>
- **The lc3 plan on disk is unrevised and must not be executed.** See BLOCKING CONSTRAINTS.
- **Whether the harness wraps `<automated>` gates in `set -e` is EXPLICITLY OPEN.** Not established
  from `execute-plan.md` by either the checker or me. The safer gate form removes the question.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` -- the machine-readable state; `human_actions_pending[0]` lists the four
   revision items verbatim.
2. `.planning/quick/260804-lc3-.../260804-lc3-CONTEXT.md` -- LOCKED decisions. Do not revisit them.
3. `.planning/quick/260804-lc3-.../260804-lc3-RESEARCH.md` -- settled the Q1 blocker and found the
   self-contradiction. Its verdicts are established; its DRAFT PROSE has three known defects the
   plan already corrects (rotting `file.ext:NN` citations, one lock phrase that wraps, and an
   ambiguous "all four integration legs").
4. `.planning/quick/260804-lc3-.../260804-lc3-PLAN.md` -- but read it to CHECK it, not to follow it.
5. `.planning/quick/260804-h3b-.../260804-h3b-SUMMARY.md` -- where this defect was found and why it
   was deferred.
6. `./CLAUDE.md` and `./AGENTS.md` -- `grep` is DENIED; `git commit -m` fails on this Dev Drive.

## Infrastructure State

- `origin/main`: `fe25a3f865f20f3d4f8a40e96f8cb5717608ba8a`, verified untouched. No window is open.
- Backup refs on origin: five pre-existing, all at `fe25a3f`. The h3b backup was deleted after its
  restore gate passed. Do not touch the five.
- PRs: #16 OPEN (base `main`, the feature branch). #17 CLOSED, never merged (the h3b probe).
- Stacks: `repos/op-nx/github-cache/stacks` length 0. The h3b stack was torn down.
- No background processes, no async job manifests, no running servers.
- Local test suite green: 24 `o3-witness` assertions pass; nothing outside `.planning/` changed
  across `3c67513..HEAD`.

## Pre-Execution Critique Required

- Design artifact: `.planning/quick/260804-lc3-close-the-ci-yml-positive-control-just-s/260804-lc3-PLAN.md`
- Critique focus: (1) Does every absence gate have a paired presence gate, so writing NOTHING fails?
  (2) Does the spec file have an absence gate for the failure-implication claim, not just the
  presence-mechanism half? (3) Would an honest partial result still pass every gate? (4) Do the two
  new lock phrases each sit within ONE raw `ci.yml` line, given the file wraps prose across `#`
  boundaries mid-sentence?
- Gate: Do NOT begin execution until plan-check iteration 2 returns and the four revision items are
  confirmed present.

<context>
The through-line of this session is that every gate which LOOKED like coverage and asserted nothing
was caught by an independent pass, never by its author. The milestone audit's own follow-up #1 turned
out to be already fixed -- the note simply predated the fix. h3b's append-only gate would have killed
its own task 2 by freezing a file it then instructed editing. lc3's checker found the guard file would
have kept the very false claim it was added to prevent. Keep spending the plan-checker; it has paid
for itself every single time, and cheaply.

The lc3 correction itself is factually settled and independently re-traced. Only its ENFORCEMENT is
under-specified, so the remaining work is gate carpentry, not analysis. Resist the urge to re-litigate
the prose.

One honest caveat to carry forward: the corrected failure-implication prose is REASONING from code,
not measurement. The control's FAIL direction has never been observed on a real run. Do not let the
shipped comment imply otherwise -- that is the plan's own top constraint and the thing most likely to
be quietly lost in execution.
</context>

<next_action>
Start with: plan-check iteration 2 of 2, scoped to the four NEWLY ADDED gates. The revision is
already landed and verified -- do not re-request it, and do not re-review what iteration 1 passed.

The four questions worth the checker's effort:

1. Does the spec-file absence gate cover the FAILURE-IMPLICATION claim, not just the
   presence-mechanism half? That gap was blocker 1.
2. Is each of the four per-site literals (`RESTORED OR WROTE`, `NO WRITE PERMISSION`,
   `a retry would HIDE`, `SRV-05`) genuinely single-line-safe against RAW `ci.yml`? The file wraps
   prose across `#` boundaries mid-sentence, and `docs-same-os-claims.spec.ts` reads raw.
3. Does the `SRV-05` count `-ge 2` gate actually distinguish "both sites reattributed" from
   "only PART A reattributed"? A presence gate could not, which is why it is a count.
4. Does the `awk` comment-only diff gate skip the `+++`/`---` headers, and is it scoped to `ci.yml`
   alone? The spec file legitimately adds non-comment array strings.

On `VERIFICATION PASSED`: execute with `gsd-executor` on the MAIN TREE (no worktree isolation),
verify with `gsd-verifier`, update STATE.md's Quick Tasks Completed table, commit the artifacts.
</next_action>
