---
context: research
phase: pre-roadmap (reader-adapter spike pending)
task: 9
total_tasks: 11
status: paused
last_updated: 2026-07-17T20:15:35.510Z
---

# Continue Here — reader-adapter spike (FOUND-01)

All foundational decisions and the CREEP control ledger are **persisted and triple-reviewed**. Exactly **one** decision remains open — the reader/cross-context storage adapter — and it is now **purely empirical**. The next session runs `/gsd:spike` on it.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Sunk-cost / "already built" bias | The current architecture, implementation, and delivery are a **spike/PoC** (sunk cost = 0). Weighing "Releases is already implemented/tested" as a reason to prefer it is invalid. | blocking | Decide the reader **on forward merits only** (latency, cost, caps, cold-read fan-out, size ceiling, remediation capability, Docker synergy). Every reader requires (re)building its own publish/cleanup subsystem — none is port-isolated. |
| Reopening resolved-on-paper items | Three unknowns are closed and MUST NOT be re-litigated in the spike. | advisory | (1) GHCR has **no** atomic create-if-absent — best-effort no-overwrite, **low-severity**, covered by C2 + CORR-01 + pull-by-digest. (2) Write-trust backstop is **host-detected fail-closed** (`GITHUB_SERVER_URL`: github.com/`*.ghe.com` ON, all GHES OFF; floor unpublished). (3) Nx PUT floor is a **hard `200`/Nx-21+** (client matches 200 strictly). |
| `--auto` auto-locking a roadmap on an unverified foundation | `new-project --auto` formed a roadmap + Phase-1 discussion before the foundational storage-primitive decision was verified; it had to be rewound. | advisory | Do not form the roadmap until FOUND-01 (reader adapter) is settled. The roadmap re-derives *after* the spike. |
| Asymmetric requirements ledger | Requirements were initially heavy on GHCR hazards, thin on Releases. | advisory | The spike must produce **both** readers' full operational + security failure ledger (symmetric), not a lopsided one. |

<current_state>
Pre-roadmap. `PROJECT.md`, `REQUIREMENTS.md`, and `ARCHITECTURE-DECISION.md` (control ledger C1-C18) are committed (tip `98da97a`) and reviewed via: the D1-D4 security review, a 6-member advisor panel (3 board + 3 critic) + triage, an independent Sonnet `/lz-security-review` + triage, and 3 targeted research items. Working tree clean. The premature roadmap + Phase-1 discussion were rewound earlier (recovery tag `pre-rewind-2026-07-17`). One open decision: **FOUND-01 — reader adapter GHCR/OCI vs Releases**, now empirical.
</current_state>

<completed_work>
- Project init (brownfield) + config + PROJECT.md — `cbd0537`, `f8d18e6`
- Domain research (STACK/FEATURES/ARCHITECTURE/PITFALLS + SUMMARY) — `3c24199`
- v1 requirements — `3c33c2b`
- Rewound premature roadmap/Phase-1; re-opened primitive selection; auth baseline (public+private) + distribution forms — `63582d5`
- 4-architecture comparison + full CVE/D1-D4 security review; ARCHITECTURE-DECISION.md + control ledger C1-C18 — `c0dd4bc`
- 6-member advisor panel + triage survivors — `dc85cc1`
- Independent Sonnet `/lz-security-review` + triage — `a408442`
- Targeted research (GHCR atomic-create; backstop detection; Nx PUT floor) folded in — `98da97a`
</completed_work>

<remaining_work>
- **Task 9 (next):** run `/gsd:spike` on the reader adapter (GHCR/OCI vs Releases) against the FOUND-01 forward-merits ledger.
- Task 10: re-derive REQUIREMENTS.md traceability + ROADMAP.md from the settled reader decision.
- Task 11: plan + execute phases (test safety net + Octokit robustness; trust widening + RW/RO modes; consumer adoption docs).
</remaining_work>

<decisions_made>
Canonical record: **`.planning/ARCHITECTURE-DECISION.md`** (read it — do not rely on this summary). Highlights:
- One backend per process (`selectBackend` + trust input); default = Actions-cache CI-RW only; reader + publish/cleanup are opt-in and reader-specific.
- Write-trust = allowlist-only, default-deny, no denylist; `pull_request`/`release` host-detected fail-closed.
- Sync gate = separate predicate `{push, schedule}` only; no content signing; no LRU manifest; digest-pin iff GHCR; OS-namespace by default (CORR-01).
- Reader adapter (GHCR vs Releases) = the one open decision, decided on forward merits by the spike.
</decisions_made>

<blockers>
- None. The reader decision is open but empirical, not blocked.
</blockers>

## Required Reading (in order)
1. `.planning/ARCHITECTURE-DECISION.md` — the decision record + control ledger C1-C18 + the FOUND-01 reader ledger. **Read first.**
2. `.planning/REQUIREMENTS.md` — FOUND-01 rubric + TRUST/RETAIN/TEST/DOCS/GOV requirements (GHCR- and Releases-conditional items marked).
3. `.planning/research/SUMMARY.md` — synthesized domain research (+ dimension files STACK/FEATURES/ARCHITECTURE/PITFALLS).
4. `.planning/codebase/{ARCHITECTURE,CONCERNS,TESTING,STACK}.md` — the PoC's shape (reference, not a constraint).
5. `.planning/PROJECT.md` — high-level context + Key Decisions table.

## Critical Anti-Patterns (do NOT repeat)
- Sunk-cost bias: decide the reader on forward merits only (see the table above).
- Do not reopen resolved items: GHCR atomic-create, backstop host-detection, Nx-200 floor.

## Infrastructure State
- No running services or background jobs (all research subagents completed).
- Local `nrwl/nx` clone at `D:\projects\github\nrwl\nx` — used to verify the Nx client (`packages/nx/src/native/cache/http_remote_cache.rs`); available if the spike needs to re-check client behavior.
- The spike will need `gh` / GHCR auth (docker/oras login to `ghcr.io`; a token with `read:packages`/`write:packages` on a throwaway test package) for hands-on GHCR experiments; Releases experiments use `gh`/Octokit.

## Pre-Execution Critique Required
- Not applicable — no design artifact awaits critique. The spike itself produces the reader findings; those (and the re-derived roadmap) are the next review gate.

<context>
The whole project has been de-risked down to a single empirical question. Everything analytical is done and reviewed three times over. The spike should be tightly scoped: measure GHCR vs Releases on CI/local read+write latency, cost (public + private), per-primitive size ceiling, cold-read API fan-out vs rate limits, cross-OS round-trip (OS-invariant + OS-sensitive hash from each CI OS), and — for GHCR — child-manifest cleanup + cleanup-credential capability + the >5000-download remediation gap. Do NOT re-derive the paper conclusions. Pick the reader, then re-derive requirements → roadmap.
</context>

<next_action>
Start with: `/gsd:spike` scoped to the FOUND-01 reader adapter (GHCR/OCI vs GitHub Releases) using the forward-merits ledger in `.planning/ARCHITECTURE-DECISION.md`. Decide on forward merits only (the current impl is a spike/PoC). Then re-derive REQUIREMENTS traceability + ROADMAP.
</next_action>
