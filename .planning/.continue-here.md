---
context: default
phase: post-v0.0.1 (milestone complete, not archived)
task: findings-A cleanup
total_tasks: 2
status: paused
last_updated: 2026-07-21T08:11:43.254Z
---

# Handoff: v0.0.1 Findings-A cleanup (post-milestone, pre-archival)

Milestone **v0.0.1 Greenfield MVP Rebuild** is functionally complete and audit-PASSED
(7/7 phases verified/nyquist/secure; 6/6 E2E flows wired; consumer-smoke live proof green
on CI run 29792990244). It is deliberately **NOT completed/archived** (the maintainer
reverted the archival to land via PR). This handoff scopes the two non-blocking
**Findings A** the milestone audit surfaced. Neither affects correctness or shippability.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Windows npm-install lockfile prune | A bare `npm install` on this Windows arm64 host drops the Linux `@emnapi` WASM-fallback optional subtrees from package-lock.json; CI `npm ci` then fails on every job (invisible locally). Bit this milestone via esbuild. | advisory | Only if A1 touches deps/lockfile: regenerate the lockfile in a linux/arm64 node:24 container (node_modules masked), validate `npm ci` exits 0 there. See the `windows-npm-install-prunes-linux-optional-deps` memory. A1's expected scope (removing source files + a CI job + npm scripts) does NOT change deps, so this likely does not apply. |
| Removing a security-adjacent guard without confirming intent | trust.generated.cjs is a write-trust artifact; deleting it blindly could drop a defense the design intended. | advisory | Before removing (A1), confirm via git grep that nothing `require()`s it AND that no pre-`npm ci` / GHES / older-runner path needs a dependency-free trust copy. `trust.ts` must remain the single source of truth for `isWriteTrusted`. |

<current_state>
- Branch: `gsd/v0.0.1-greenfield-rebuild` @ `2d4e940` (= `origin/gsd/v0.0.1-greenfield-rebuild`, in sync). Working tree clean.
- `origin/main` @ `98da97a` — pre-milestone baseline (restored after the live proof; its CI is red, pre-milestone state).
- The green, complete, audited milestone lives entirely on the feature branch.
- v0.0.1-MILESTONE-AUDIT.md is present at `.planning/v0.0.1-MILESTONE-AUDIT.md` (status: passed); phase dirs are still in `.planning/phases/` (not archived).
</current_state>

<completed_work>
This session (autonomous phase 6 + milestone audit):
- Phase 6 discuss (--analyze --auto, 1 escalation) -> plan (5 plans, checker-verified) -> execute (5 executors) -> code review + fix (3 warnings) -> verify PASSED -> secure (15/15 threats closed) -> validate (nyquist-compliant, 2 gaps filled) -> learnings (21 entries bridged to the global store).
- Live consumer-smoke proof: 5 pushes to main fixed 3 real distribution bugs (cross-OS lockfile drift, background-step export handshake, readiness-poll/cache-key collision); final CI run 29792990244 = all 18 jobs green.
- Milestone audit PASSED. main restored to baseline; archival reverted per maintainer.
</completed_work>

<remaining_work>

### Task 1 (Finding A1) - Orphaned `trust.generated.cjs` (TRUST-04) - TECH DEBT
The generated dependency-free write-trust copy has **zero runtime consumers**. Both actions
reach the gate via `trust.ts` compiled/bundled into their entry (dist for the internal
action; the esbuild single-file bundle for the Phase 6 consumer action); Phase 6 bundling
made the raw-`require()` scenario unnecessary. It is also outside the tsconfig `src/**/*.ts`
include, so it never ships to `dist/`. TRUST-04's actual intent (single source + no dual-root
drift + parity) is met by `trust.ts` + the selfcheck parity guard.

Involved files (if removing):
- `packages/github-cache/src/action/trust.generated.cjs` — the orphaned artifact
- `packages/github-cache/selfcheck.cjs` — generator
- `packages/github-cache/src/lib/trust.generated.spec.ts` — parity spec (147 cases)
- `packages/github-cache/src/selfcheck.spec.ts` — selfcheck test
- `.github/workflows/ci.yml` — the `selfcheck` job (~line 55)
- `package.json` — `selfcheck` + `generate:trust` scripts (~lines 14-15)
- `.fallowrc.jsonc` — the trust.generated ignore entry (~line 53)
- KEEP: `packages/github-cache/src/lib/trust.ts` (single source of truth)

Decision needed: remove (recommended, deliberate) vs document a keep. Do it via `/gsd:quick`.
After removing, run `npx nx test github-cache` + `npm run check:action` + `fallow:ci` green.

### Task 2 (Finding A2) - TRUST-07 checkbox bookkeeping
`REQUIREMENTS.md` shows TRUST-07 as `[ ]` (~line 54) while `04-VERIFICATION.md:225` marks it
SATISFIED (first-write-wins / no-overwrite; pre-list + 422 benign-skip, unit-tested).
04-VERIFICATION notes the `[ ]` is a documented project convention (VERIFICATION.md is
authoritative). Options: flip to `[x]` for tidiness, or leave per convention. Trivial; no
functional impact.
</remaining_work>

<decisions_made>
- v0.0.1 posture: pre-1.0 drift-guarded (semver 0.x). GOV-03 defines "breaking" against the DOCS-05 enumerated surface.
- main restored to baseline; milestone NOT archived — land via PR from the feature branch (maintainer's call).
- The 5 deferred items (Docker/FOUND-03, GHCR-01, PROV-01, TRUST-09, RETAIN-02) are intentional later-milestone scope, not gaps — see the audit's `deferred` block.
</decisions_made>

<blockers>
None. Both tasks are ready to start; nothing is stuck.
</blockers>

## Required Reading (in order)
1. `.planning/v0.0.1-MILESTONE-AUDIT.md` — the `tech_debt` block (A1) + `deferred` block + the integration-checker orphan finding for TRUST-04.
2. `packages/github-cache/src/lib/trust.ts` — the single source of truth to preserve.
3. `packages/github-cache/src/action/trust.generated.cjs` + `packages/github-cache/selfcheck.cjs` — the artifact + generator under review for A1.
4. `.planning/phases/04-publish-retention-observability/04-VERIFICATION.md` (~line 225 + 238) — TRUST-07 SATISFIED + the checkbox convention note (A2).

## Infrastructure State
- CI (`.github/workflows/ci.yml`) triggers on push to `main` OR `pull_request`. `consumer-smoke` is push-to-main-only. A PR from the feature branch would run the full pipeline minus consumer-smoke and would exercise the 2 validate-phase-generated specs (`consumer-action-runtime.spec.ts`, `governance-docs.spec.ts`) on CI for the first time (they passed locally, 481 tests).
- `origin/main` is at the pre-milestone baseline; its latest CI is red (pre-milestone), expected until the milestone lands.

<context>
These are cleanup/bookkeeping items, not feature work. A1 is the only one with any real
substance (removing dead-but-guarded weight); A2 is a one-character edit or a no-op. The
milestone itself needs no further verification — it is done and audit-passed.
</context>

<next_action>
Start with Task 1 (A1): `git grep -n "trust.generated" -- ':!*trust.generated*'` to confirm
zero consumers, confirm no pre-npm-ci/GHES path needs a dependency-free trust copy, then
remove the orphaned artifact + its generator + parity spec + the ci.yml selfcheck job + the
selfcheck/generate:trust npm scripts + the .fallowrc.jsonc ignore via `/gsd:quick`, keeping
`trust.ts` as the single source. Verify `nx test github-cache` + `check:action` + `fallow:ci`
green. Then Task 2 (A2): flip the TRUST-07 checkbox in REQUIREMENTS.md or leave per convention.
</next_action>
