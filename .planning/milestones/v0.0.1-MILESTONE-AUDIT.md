---
milestone: v0.0.1
milestone_name: Greenfield MVP Rebuild
audited: 2026-07-22
status: passed
re_audit: "2026-07-22 re-audit of the 2026-07-21 baseline; ~89 commits landed since (PR #3 review findings + quick-task hardening). Prior audit's two tech-debt items are both now RESOLVED (see below). Integration re-checked fresh against current HEAD."
scores:
  requirements: 39/39 phase-assigned in-scope satisfied (+ FOUND-01/-03 LOCKED groundings honored; 2 deferred N/A-for-Releases)
  phases: 7/7 verified passed
  integration: 6/6 E2E flows wired
  nyquist: 7/7 compliant
  security: 7/7 SECURITY present, all threats closed (threats_open: 0 at block_on=high)
gaps: []
tech_debt:
  - phase: 04-publish-retention-observability
    items:
      - "WARNING (non-blocking, pre-existing SRV-05 property, surfaced by new input validation): a consumer who sets CACHE_MIRROR_MAX_AGE_DAYS below 7 without CACHE_MIRROR_ALLOW_AGGRESSIVE_RETENTION gets a loud red cleanup job (correct) but the read path only logs the refusal to process.stderr once per process (releases-backend.ts) -- never a GitHub Actions annotation. An operator watching only the Actions UI could miss the cause of a sudden all-MISS build. Consistent with SRV-05 read-must-never-break-build discipline; not a regression."
resolved_since_prior_audit:
  - "TRUST-04 trust.generated.cjs / selfcheck.cjs orphan -- RESOLVED. Those files are no longer tracked in the repo (git ls-files finds none); the dual-root drift concern is moot. TRUST-04's single-source-of-truth intent remains satisfied via bundled/built trust.ts."
  - "TRUST-07 REQUIREMENTS.md checkbox lag -- RESOLVED. The checkbox is now [x], aligned with 04-VERIFICATION.md's SATISFIED verdict. The Phase-4 verification-time note about lagging checkboxes (TEST-03/04, ROBUST-01/02/05, TRUST-07) no longer applies -- all are [x]."
deferred:
  - "TRUST-09 (publish-time package-visibility fail-closed assert) -- later-milestone GHCR revisit trigger; N/A for Releases (assets inherit repo visibility). Correctly out of v0.0.1 scope; checkbox [ ] by design."
  - "RETAIN-02 (GHCR >5000-download delete-refusal handling) -- later-milestone GHCR revisit trigger; N/A for Releases (no deletion wall). Correctly out of v0.0.1 scope; checkbox [ ] by design."
  - "PROV-01 (cosign keyless attestation) -- later milestone."
  - "GHCR-01 (GHCR/OCI as an additional synced store) -- later-milestone revisit trigger (with PROV-01 + Docker)."
  - "FOUND-03 Docker container distribution form -- later milestone; the CI-sidecar motivation is covered by the DOCS-06 background-step pattern (proven live)."
---

# Milestone v0.0.1 Audit: Greenfield MVP Rebuild

**Audited:** 2026-07-22 (re-audit of 2026-07-21 baseline)
**Status:** PASSED
**Definition of done:** A correct, CREEP-safe, GitHub-native Nx remote cache (Actions-cache CI-RW + read-only GitHub Releases mirror), rebuilt greenfield across 7 phases, consumer-adoptable (npm + JS Action) with governance and a versioned public contract.

## Verdict

All 7 phases verified `passed`, all Nyquist-compliant, all secured (SECURITY.md present; `threats_open: 0` at `block_on: high`). Cross-phase integration is WIRED across all 6 end-to-end flows -- re-confirmed by a fresh, independent gsd-integration-checker inspection of current HEAD (`0288279`) that also ran the suites live (430 unit tests, 3 integration tests, clean bundle-drift diff, clean tarball). Every in-scope v0.0.1 requirement is satisfied. No critical gaps, no broken flows, no blockers.

**Both tech-debt items from the 2026-07-21 audit are now resolved** (the `trust.generated.cjs` orphan is gone; the TRUST-07 checkbox is `[x]`). One non-blocking WARNING remains (sub-7-day retention read-path visibility), plus the five intentionally deferred later-milestone items.

## Re-audit delta (vs 2026-07-21)

The prior audit was authored at `2d4e940`; HEAD is now `0288279` (~89 commits, +7673/-1253). The changes are entirely fix/hardening work inside already-wired flows -- no new requirement scope, no export/import path orphaned. Notable landed commits and their integration verdicts:

- `4acbafd` resilient Octokit (retry/throttle) -- constructed + consumed in both `action/index.ts` and `cleanup/index.ts` (spec call-count asserted). WIRED.
- `dc7431a` sub-7-day retention refusal -- preserves the single-knob coupling by construction (same `resolveMaxAgeDays`, same env, both call sites observe the throw). WIRED.
- `3a56c30` sync-gate narrowing -- **additive**: a new, narrower `isTrustedSyncEvent` (schedule-only) wired **exclusively** into cleanup (matches `cleanup.yml`'s schedule-only trigger). `isSyncTrusted` (`{push, schedule}`) is **unchanged**, so TRUST-02's documented predicate and the code still agree. No conflict.
- `d328dbf` oversized-entry count-and-continue -- aggregate `failed>0 -> core.setFailed` fail-loud path preserved (ROBUST-02). WIRED.
- `9d4d0f1` byte-compare read-back -- publish-verify now byte-compares, strictly stronger than the prior HIT-only check. WIRED.
- `263b512`/`1a874a1`/`1ca8c14`/`97fb4dd` server + action hardening -- guard-clause positions preserve auth/hash/body-cap ordering; `start-cache-server/index.js` rebuild is byte-identical (check:action drift gate clean).

## Phase Verification (3-source cross-reference)

| Phase | VERIFICATION | Nyquist | SECURITY | Notes |
|-------|--------------|---------|----------|-------|
| 0 Teardown | passed (5/5) | compliant | secured (0 open) | Spike/PoC removed; clean baseline CI. |
| 1 Walking Skeleton | passed (12/12) | compliant | secured (0 open) | SRV-01..05, TEST-07; Nx-contract server, zero runtime deps. |
| 2 Default Cache in CI | passed (10/10) | compliant | secured (0 open) | selectBackend, Actions-cache RW, write-trust gate, withHashLock. Live cross-job HIT confirmed (run 29685631933). |
| 3 Cross-Context Read | passed (22/22) | compliant | secured (0/19 open) | Authenticated Releases reader, OS-namespaced (CORR-01), FOUND-02. |
| 4 Publish + Retention + Observability | passed (37/37) | compliant | secured (0/22 open) | publish/cleanup engines, separate sync gate, one coupled retention knob; all 11 reqs SATISFIED. Live cross-OS round-trip confirmed (run 29726834220). |
| 5 Trust-Widening + PPE Gate | passed (21/22) | compliant | secured (0/12 open, 1 accepted) | Host-gated PR/release trust, server-produced-key filter, PPE gate. Live PPE findings confirmed (run 29772015309). |
| 6 Distribution + Docs + Governance | passed (21/21) | compliant | secured (0 open) | npm + JS Action, guarded public surface, docs, SECURITY/LICENSE/semver. Consumer background-step round-trip proven live (run 29792990244, all 18 jobs green). |

3-source cross-reference method: for each REQ-ID, VERIFICATION.md per-requirement SATISFIED verdict x SUMMARY `requirements_completed` frontmatter x REQUIREMENTS.md traceability checkbox. Phase 4's SUMMARY frontmatter is thin per-plan, but 04-VERIFICATION.md (L218-234) proves the union of all six plans' `requirements:` equals the traceability row exactly -- all 11 SATISFIED with evidence.

## End-to-End Flows (integration checker, fresh @ 0288279)

| # | Flow | Status | Evidence |
|---|------|--------|----------|
| 1 | CI read-write: consumer action -> serve -> selectBackend -> Actions-cache -> Nx round-trip | WIRED | serve.ts:90-119; select-backend.ts:29-60; actions-cache-backend.ts; byte-identical start-cache-server/index.js; consumer-smoke green (run 29792990244) |
| 2 | Local read: serve -> selectBackend (untrusted) -> Releases reader -> releaseAssetName (cross-OS) | WIRED | select-backend.ts:32-41 -> releases-backend.ts:68-105 -> release-asset-name.ts:50-55; read path never name-filters |
| 3 | Publish/sync: push/schedule -> isSyncTrusted (separate predicate) -> publishMirror -> publish-verify byte-compare | WIRED | action/index.ts:109-124 gate-first; publish-mirror.ts:161-168; read-back.ts byte-compare |
| 4 | Retention coupling: one CACHE_MIRROR_MAX_AGE_DAYS drives read-lookback AND cleanup | WIRED | single resolveMaxAgeDays (retention.ts:109-125), both call sites; sub-7-day floor throws for both -- coupling preserved |
| 5 | Trust chain: default-deny write gate -> host-gated widening; server-produced-key filter precedes mirror | WIRED | trust.ts:79-100 -> select-backend.ts:32; publish-mirror.ts:163 filter first; cleanup's isTrustedSyncEvent is a distinct narrower gate |
| 6 | Distribution surface consistency: DOCS-05 guard matches real exports/inputs/knobs; docs match code | WIRED | public-surface.spec.ts (new knob enumerated) + docs-adoption.spec.ts (39 assertions); pack-check + check:action clean |

**Overall integration score: 6/6 WIRED. No regressions.**

## Requirements Coverage

- **In scope, satisfied (39/39 phase-assigned):** SRV-01..05, TEST-01..07, ROBUST-01..05, TRUST-01..08, CORR-01, FOUND-02, RETAIN-01, RETAIN-03, OBS-01, DOCS-01..06, GOV-01..03. All `[x]` in REQUIREMENTS.md.
- **LOCKED foundational groundings honored:** FOUND-01 (reader = GitHub Releases), FOUND-03 (distribution = npm + JS Action; Docker deferred).
- **Unchecked and correctly deferred (out of v0.0.1 scope; N/A for Releases):** TRUST-09, RETAIN-02 -- both later-milestone GHCR revisit triggers. These are the only two `[ ]` in-scope-adjacent boxes, and both are correct.
- **Orphan detection:** no requirement in the traceability table is absent from all phase VERIFICATIONs. PROV-01 appears in the REQUIREMENTS body as a deferred later-milestone item, intentionally not phase-mapped for v0.0.1.
- **FAIL gate:** no `unsatisfied` requirement -> no forced `gaps_found`.

## Milestone Highlights

- The DOCS-06 consumer background-step pattern was proven on real GitHub Actions across 5 live pushes; the live proofs surfaced and fixed 3 real distribution bugs (cross-OS lockfile drift, the background-step export handshake, a readiness-poll/cache-key collision) that every local gate and the verifier passed. See 06-LEARNINGS.md.
- Full CI green on the milestone HEAD: all 18 jobs pass (build/typecheck/test/format/fallow, integration both OS, publish + publish-verify both OS, action-bundle-drift, pack-check, selfcheck, ppe, consumer-smoke, dogfood seed/verify) -- CI run 29792990244.
- Post-baseline hardening (PR #3 review + quick tasks) added resilient Octokit, a sub-7-day retention safety floor, byte-compare read-back, and defense-in-depth cleanup gating -- all inside already-wired flows, no scope drift.

## Next

Milestone is complete and ready to archive/ship. The green milestone lives on `origin/gsd/v0.0.1-greenfield-rebuild`; land via PR #3 (merge commit), then `/gsd:complete-milestone v0.0.1` and cut/tag the deferred v0 release.
