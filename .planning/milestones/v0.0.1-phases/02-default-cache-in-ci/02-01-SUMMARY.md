---
phase: 02-default-cache-in-ci
plan: 01
subsystem: infra
tags: [supply-chain, actions-cache, actions-core, exact-pin, npm, vitest]

# Dependency graph
requires:
  - phase: 01-server
    provides: "@op-nx/github-cache lib scaffold (empty dependencies) and the in-repo pinned-constant spec convention (MAX_CACHE_BODY_BYTES)"
provides:
  - "@actions/cache@6.2.0 and @actions/core@3.0.1 as the lib's first runtime dependencies, exact-pinned"
  - "src/pinned-deps.spec.ts: a build-breaking guard against either specifier widening to a range"
  - "npm-generated package-lock.json with registry-derived integrity hashes for both toolkit packages"
affects: [02-04, 02-05, 02-06, actions-cache-backend, cache-archive-path, dogfood-action]

# Tech tracking
tech-stack:
  added: ["@actions/cache@6.2.0", "@actions/core@3.0.1"]
  patterns:
    - "Exact-pin + committed spec guard for version-hash-sensitive deps (ROBUST-03)"
    - "Human legitimacy checkpoint before a SUS (too-new) install"

key-files:
  created:
    - packages/github-cache/src/pinned-deps.spec.ts
  modified:
    - packages/github-cache/package.json
    - package-lock.json

key-decisions:
  - "Pinned @actions/cache to 6.2.0 (latest) rather than the plan's more-baked 6.1.0 default; the too-new verdict was surfaced and explicitly accepted by the human at the legitimacy gate."
  - "Task 2 executed as type=auto (not TDD): a static supply-chain/config guard over package.json, GREEN-only per the plan's inline rationale, no RED-first spec commit."

patterns-established:
  - "Version-hash-sensitive dependency = exact pin (bare x.y.z) locked by its own spec; a range operator is a silent-MISS hazard, not a convenience."

requirements-completed: [ROBUST-03]

coverage:
  - id: D1
    description: "@actions/cache and @actions/core are declared as exact versions (bare x.y.z, no range operator) in the lib package.json, and a committed spec fails the build if either widens to a range"
    requirement: "ROBUST-03"
    verification:
      - kind: unit
        ref: "packages/github-cache/src/pinned-deps.spec.ts#@actions/cache is pinned to an exact version, never a range (ROBUST-03)"
        status: pass
      - kind: unit
        ref: "packages/github-cache/src/pinned-deps.spec.ts#@actions/core is pinned to an exact version, never a range (ROBUST-03)"
        status: pass
    human_judgment: false

# Metrics
duration: 2min
completed: 2026-07-19
status: complete
---

# Phase 2 Plan 01: Supply-chain gate (exact-pinned toolkit deps) Summary

**Took the project's first runtime dependencies -- `@actions/cache@6.2.0` and `@actions/core@3.0.1` -- under an exact, human-approved pin and locked the pin with a build-breaking spec (ROBUST-03).**

## Performance

- **Duration:** 2 min
- **Started:** 2026-07-19T02:22:03Z
- **Completed:** 2026-07-19T02:23:59Z
- **Tasks:** 2 (Task 1 human checkpoint pre-resolved before spawn; Task 2 executed)
- **Files modified:** 3

## Accomplishments
- `packages/github-cache/package.json` `dependencies` now declares `@actions/cache: "6.2.0"` and `@actions/core: "3.0.1"` -- both bare `x.y.z`, no range operator (the lib's first-ever runtime deps).
- `package-lock.json` regenerated by npm (not hand-edited), so both packages carry registry-derived integrity hashes; the root `package.json` `dependencies` stayed empty (deps belong to the lib workspace, not the root).
- `src/pinned-deps.spec.ts` reads the manifest at test time and asserts both specifiers match `/^\d+\.\d+\.\d+$/` -- a future `npm install` that widens either to a range fails the build.

## Task Commits

Each task was committed atomically:

1. **Task 1: Package legitimacy checkpoint** - pre-resolved by human before spawn (no commit; approval recorded below)
2. **Task 2: Install both toolkit packages exact-pinned and lock the pin with a spec** - `f64f05d` (feat)

**Plan metadata:** committed with SUMMARY/STATE/ROADMAP (docs: complete plan)

## Files Created/Modified
- `packages/github-cache/package.json` - added `@actions/cache@6.2.0` + `@actions/core@3.0.1` to `dependencies`, exact-pinned
- `package-lock.json` - npm-resolved lockfile for the new deps and their transitive tree (40 packages added)
- `packages/github-cache/src/pinned-deps.spec.ts` - static ROBUST-03 guard: both toolkit specifiers must be bare `x.y.z`

## Human Approval Gate (Task 1)

The Phase 2 package legitimacy audit returned **SUS (`too-new`)** for `@actions/cache` (its newest release was ~6 days old at research time -- a version-freshness artifact, not a supply-chain signal). A human explicitly approved the install before any package-manager command ran.

**Approved (2026-07-19):**
- `@actions/cache@6.2.0` -- the human chose LATEST over the plan's more-baked `6.1.0` default, with the `too-new` verdict explicitly surfaced and accepted.
- `@actions/core@3.0.1`.

**Legitimacy evidence (registry.npmjs.org, fetched 2026-07-19):**
- `@actions/cache`: repository `github.com/actions/toolkit`, publisher `actions` org, not deprecated, NO install/postinstall scripts. Latest `6.2.0` published 2026-07-13 (6 days before approval) -- this freshness produced the `too-new` verdict.
- `@actions/core`: same canonical repo, not deprecated, no install scripts. `3.0.1` published 2026-04-21 (~3 months old).

**Reason recorded:** latest chosen over the baked `6.1.0` default; the `too-new` verdict was surfaced and accepted by the human.

## Decisions Made
- **Pinned 6.2.0, not the plan's 6.1.0 default.** The human took latest with the freshness verdict acknowledged; the version string is load-bearing (the toolkit version-hashes the archive path), so the exact value is a deliberate, recorded choice, not a default.
- **Task 2 ran GREEN-only (type=auto, not TDD).** Per the plan's inline rationale, this is a static supply-chain/config guard, not business logic with defined I/O, and a RED-first ordering would place a failing spec commit between the human approval gate and the approved install. `pinned-deps.spec.ts` was written and committed within Task 2, GREEN.

## Deviations from Plan

None - plan executed exactly as written. (The 6.2.0-vs-6.1.0 version choice is the plan's own documented alternative, exercised via the Task 1 human checkpoint -- not a deviation.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `@actions/cache` and `@actions/core` are installed and typed, so Plans 02-04/05/06 can import the toolkit (`actions-cache-backend.ts`, `cache-archive-path.ts`, the dogfood action entry).
- The exact pin is guarded: any later plan that runs `npm install` and accidentally widens a specifier will fail `nx test github-cache`.
- Note for downstream bumps: `@actions/cache` version-hashes the literal archive path + compression choice, so the only end-to-end verification of a version bump is the Plan 06 CI dogfood canary.

## Self-Check: PASSED

- `packages/github-cache/src/pinned-deps.spec.ts` - FOUND
- `.planning/phases/02-default-cache-in-ci/02-01-SUMMARY.md` - FOUND
- Commit `f64f05d` - FOUND in git log
- Committed `package.json` deps both match `/^\d+\.\d+\.\d+$/` - OK

---
*Phase: 02-default-cache-in-ci*
*Completed: 2026-07-19*
