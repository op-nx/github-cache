---
checked: v0.0.2 OS-invariant cross-OS sharing (phases 07-13)
checked_at_commit: 0dbee202eb8020466d37e21770fdfb8f512d1573
integration_score: 9/9
flows_score: 5/5
status: passed
gaps: []
---

# v0.0.2 Cross-Phase Integration Check

Supersedes the prior audit at commit 2df3af5 (now ~110 commits stale). Re-verified against
code and config at HEAD 0dbee20, branch gsd/v0.0.2-os-invariant-cross-os-sharing.

## Scores

- Integration seams: 9/9 WIRED (0 BLOCKER, 1 WARNING - disclosed stale-bookkeeping drift,
  1 WARNING - stale planning-doc drift, both non-blocking)
- E2E flows: 5/5 COMPLETE - O1, O2, O3, O4, plus the fifth backend-selection outcome added
  by Phase 13 (distinct from, and not to be confused with, the milestones O1-O4 count, which
  the ROADMAP correctly keeps at four)

All 44 test files / 1145 tests pass at HEAD (npm test, run on 2026-08-10, success line reads:
Successfully ran target test for project op-nx/github-cache).

## Seam-by-seam

| # | Seam | Verdict | Evidence |
|---|------|---------|----------|
| 1 | Phase 7 -> 8 (inferred lint target -> hash-parity measurement) | WIRED | nx.json carries the lint target (targetDefaults.lint, inferred via @nx/eslint/plugin) exactly as Phase 7 shipped it; capture-hashes.mjs (Phase 8) and hash-parity/compare.ts both fold lint into the five-target EXPECTED_TARGETS set. ci.yml lint job demands the literal success line Successfully ran target lint (not just exit 0), so a silently-absent inferred target reddens the job rather than reading as coverage. |
| 2 | Phase 8 -> 9 (single OS discriminator hand-off) | WIRED | nx.json:106 carries exactly one runtime input, node --no-warnings -p process.platform, on targetDefaults.integration only. nx-target-inputs.spec.ts:562-640 asserts by exact-equality that integration is the ONLY target with a runtime input, and separately pins the runtime command string verbatim against nx.json (not a hand-copied literal) - a live spec, not prose. |
| 3 | Phase 9/10 two independent cache layers (Releases mirror vs Actions cache) | WIRED | select-backend.ts branches entirely on isWriteTrusted plus repo-identity plus token plus CACHE_READ_ONLY, and the two backends (releases-backend.ts, actions-cache-backend.ts) are constructed from disjoint factories with no shared mutable state. docs/advanced.md How the backend is selected table (5 rows) matches the 5 branches in select-backend.ts in the same order. |
| 4 | Phase 13 composes (spreads), not duplicates, Phase 9/10 read path | WIRED | actions-cache-backend.ts:284-287: createActionsCacheBackend spreads createReadOnlyActionsCacheBackend and adds put - one entry-reading cache.restoreCache call site, asserted by an ordered-member scan in actions-cache-backend.spec.ts that resolves the subject as a single named file (so a sibling module carrying its own restoreCache would produce zero failures, closed by a separate VER-09 package-scope clause). |
| 5 | Backend-selection consumer contract single-sourced across code, docs and tests | WIRED | src/test/consumer-contract.ts EXPECTED_ENV_KNOBS (9 knobs, matching DOCS-10 ninth knob) is asserted by exact equality in public-surface.spec.ts (inline sorted literal) and docs-adoption.spec.ts (each knob individually asserted present in docs/configuration.md) - confirmed all 9 present via a cross-check against docs/configuration.md, docs/advanced.md, docs/versioning.md. CACHE_READ_ONLY is verified the LAST branch in select-backend.ts both by code inspection and by a narrowing-table spec clause that checks position mechanically. |
| 6 | CI workflow wiring: needs graph plus windows legs plus remote-cache floor gates | WIRED | .github/workflows/ci.yml: build-windows, typecheck-windows and test-windows each declare needs on exactly one ubuntu producer (build, typecheck, test respectively); each sets CACHE_READ_ONLY=1 before starting the sidecar (the knob that makes the floor-count gate sound); each gates on a remote-cache label count with a floor of 1/2/1 plus a fail-closed non-decimal check that treats an unparseable count as a hard failure rather than a silent pass. The publish job needs build, typecheck, test and integration, and its if-condition requires not cancelled, event is push, and the push was not forced - all present verbatim. |
| 7 | Cross-OS discriminator recipe (docs/cross-os.md) self-checks against nx.json | WIRED | docs/cross-os.md states it is pinned by docs-cross-os.spec.ts, which reads the declared runtime command out of nx.json and fails the build on drift. Live-verified: docs-cross-os.spec.ts (10 tests) passes at HEAD. |
| 8 | Mandatory ordering: O1 PROVEN in Phase 11 before O4 ENABLED in Phase 12 | WIRED | Git history confirms no overlap: Phase 11 closes at d015ef6 (complete phase execution), and Phase 11 TEST-08 producer-attribution capture at ada339a predates Phase 12 f5dd429 (add the three windows-11-arm reuse legs) - the commit that actually creates Windows CI as a second producer of build, typecheck and test hashes. The two phases never share a commit range; 11-EVIDENCE.md O4 row was explicitly RESERVED, neither filled nor deleted, throughout Phase 11. |
| 9 | Quick-task consolidations, post-phase, roughly 110 commits, preserved cross-phase seams | WIRED | Verified each of the five named consolidations routes every phase-owned caller through the new shared module and the full suite stays green: repo-file.ts readRepoFile, stripLineComments and packageSourceFiles are called from Phase 9, 10 and 13-owned specs; mirroredByLabel in lib/mirrored-by-label.ts is the sole label-construction site, called from both publish-mirror.ts (Phase 10) and read-back.ts (Phase 11); the workspace-root-walk consolidation (260810-kuo) fixed a real regression it introduced, an orphaned SOURCE_ROOT_URL reference in cache-key.spec.ts, in the SAME commit, and the derived exception-set guard in repo-file.spec.ts keeps the walk-primitive claim honest by construction rather than by a hand-maintained list; the read-miss-cause-list dedup (260809-uge) is an intra-file publish-mirror.ts consolidation with a byte-identity acceptance proof, no cross-phase caller risk. npm test at HEAD: 44 of 44 files, 1145 of 1145 tests green. |

## E2E flow verification (the four target outcomes, plus the fifth backend-selection outcome)

| Flow | Status | Evidence |
|------|--------|----------|
| O1 - local Windows dev HITs build, typecheck and test produced by Linux CI | COMPLETE | 11-EVIDENCE.md:30: PROVEN, 1 occurrence per target, 3 of 3 named individually, all pre-registered counts met. |
| O2 - local Windows dev HITs integration produced by Windows CI | COMPLETE | 11-EVIDENCE.md:31: PROVEN, 1 occurrence, restored artifact carries a Windows runner path. |
| O3 - Windows CI MISSES integration produced by Linux CI | COMPLETE | 11-EVIDENCE.md:35: PROVEN on push run 30500255530; witness delta 144 seconds against a 30 second margin; positive control 200 on both legs. |
| O4 - Windows CI HITs build, typecheck and test produced by Linux CI | COMPLETE, with a disclosed doc-sync wart, see WARNING 1 | 12-VERIFICATION.md:200: observed on run 30586177358, PR 12, first run, per-leg counts build-windows 1, typecheck-windows 2, test-windows 1, total 4 - matching the 1/2/1 pre-registration fixed in f5d03b0 BEFORE the run existed. REQUIREMENTS.md XOS-05 row is Complete with this same observation. |
| Fifth outcome - selectBackend branch count, four moving to five after Phase 13 CACHE_READ_ONLY branch | COMPLETE | This is NOT a fifth milestone target outcome - ROADMAP.md lines 37 through 42, the O1-O4 table, correctly stays at four rows; the four-to-five language belongs to DOCS-10, a documentation-consistency requirement about how many outcomes selectBackend itself returns. Verified consistent: code has 5 branches in select-backend.ts, docs/advanced.md line 21 says FIVE outcomes with a matching 5-row table, docs-adoption.spec.ts lines 171 and 231 mutation-provenly pin both the prose count and the fifth table row, and a repo-wide sweep for stale four-outcomes language returns zero hits at HEAD. See WARNING 2 for one stale non-guarded copy. |

## Warnings (non-blocking)

WARNING 1 - 11-EVIDENCE.md O4 VERDICT slot still literally reads PENDING even though the
observation was made. At HEAD, .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md line 36
and line 1169 still read PENDING, live-CI, first run of the proving PR, while the actual
observation on run 30586177358 was written into 12-UAT.md and REQUIREMENTS.md XOS-05 row
instead of back into this reserved slot. This is disclosed by the phase's own audit trail -
12-VERIFICATION.md lines 217 through 223 name it explicitly as a separate, still-open
bookkeeping item in a Phase 11 artifact, outside this task's scope - so it is not a hidden
gap, but it is real drift between two artifacts that both claim to be the record of truth for
the same fact. Does not affect requirement traceability (REQUIREMENTS.md is the authoritative
source per ROADMAP.md line 755 and correctly shows XOS-05 Complete) or any runtime behavior.

WARNING 2 - .planning/codebase/INTEGRATIONS.md line 85 still says selectBackend has four
outcomes. This is a planning-time codebase-map document produced by the map-codebase workflow,
not part of the guarded consumer contract - docs-adoption.spec.ts only checks
docs/configuration.md, docs/advanced.md and docs/versioning.md, so this file's staleness was
never in that guard's scope and the Phase 13 four-to-five sweep in plan 13-04 had no reason to
touch it. Non-blocking: it is an internal reference doc, not shipped documentation, and does
not affect any consumer-facing contract or requirement. Worth a follow-up regeneration of
.planning/codebase/INTEGRATIONS.md before it misleads a future planning session.

## Requirements Integration Map

Only requirements with a genuine cross-phase touchpoint are itemized below; the full 57-row
traceability table lives around line 704 of .planning/REQUIREMENTS.md. All 57 are marked
Complete there; this table adds the WIRED, PARTIAL or UNWIRED integration classification this
audit exists to produce.

| Requirement | Integration path | Status | Issue |
|---|---|---|---|
| LINT-01, LINT-03, LINT-04 | Phase 7 nx.json lint target to Phase 8 capture-hashes.mjs and compare.ts EXPECTED_TARGETS to the ci.yml lint job | WIRED | none |
| CORR-06 | Phase 7 ambient-platform ban to the nx-target-inputs.spec.ts CORR-04 exact-equality guard added in Phase 9 | WIRED | none |
| PARITY-01 through PARITY-08, CORR-03, CORR-04 | Phase 8 root-cause to nx.json typecheck.outputs, the seven-entry list, plus the integration-only runtime input, to ci.yml hash-parity and hash-parity-compare jobs, to Phase 9 VER-01 and OBS-04 tripwires | WIRED | none |
| VER-01 through VER-07, OBS-04, ROBUST-04 | Phase 9 Actions-cache version to Phase 10 republish sequencing, ROBUST-04 build-action-in-same-commit discipline | WIRED | ROBUST-04 recursion into Phase 10 confirmed live: commits 47597a6 in Phase 9, 77f675c in Phase 10, and later quick-task commits d9435f0, 712e4f9, 6833706, 1c6faef, 1172887, cbf60e6 and 501bcb1 all regenerate start-cache-server/index.js in the same commit as the serve-reachable source edit, or document the erase-to-no-diff exception in 90c9623, a types-only change. |
| CORR-02, RETAIN-04, RETAIN-05, XOS-06, XOS-07, TRUST-10 through TRUST-13 | Phase 10 Releases mirror to Phase 11 O1 and O2 live proofs, which require the mirror to be warm | WIRED | git show --stat on 77f675c proves CORR-02 and RETAIN-04 landed in the same commit; the O1 and O2 proofs in 11-EVIDENCE.md depend on and confirm the warm mirror. |
| XOS-01 through XOS-03, TEST-08 through TEST-10, OBS-02 | Phase 11 O1, O2 and O3 live proofs to the TEST-08 producer-attribution capture to Phase 12's second-producer transition | WIRED | Ordering constraint independently re-verified via git log, see seam 8 above. |
| XOS-04, XOS-05, XOS-08, DOCS-07 | Phase 12 Windows-reuse legs to the ci.yml needs edges to the docs/cross-os.md recipe | WIRED, see WARNING 1 | The O4 observation exists and is correctly recorded in 12-UAT.md and REQUIREMENTS.md, but the 11-EVIDENCE.md reserved slot was never back-filled - disclosed bookkeeping drift, not a functional gap. |
| VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09, DOCS-10 | Phase 13 read-only backend, which composes Phase 9 and 10's read path, wired into the ci.yml three Windows legs' CACHE_READ_ONLY knob and into docs, namely configuration.md, advanced.md and versioning.md | WIRED | Fifth-outcome doc consistency verified across all three guarded surfaces; one unguarded planning-doc copy remains stale, see WARNING 2. |

Requirements not itemized above, for example LINT-02, LINT-05, LINT-06, most of Phase 7's
TDD-internal mechanics, and Phase 9/10 items already covered above by their owning phase's
seam row, are substantively phase-internal - each has at least one cross-phase touchpoint
captured in the seams above via its owning phase's row, so none is unwired in the
no-cross-phase-wiring-at-all sense the audit brief flags.

## What was NOT found broken

Despite the adversarial starting stance of assuming every seam is broken until proven
otherwise, no BLOCKER was found. Every cross-phase export and consumer pair named in the
milestone brief resolved to WIRED on direct inspection of code, config, and passing specs,
not merely on SUMMARY or VERIFICATION prose. In particular: the nx.json OS discriminator is
genuinely singular and enforced by a live spec, not just documented; selectBackend's
CACHE_READ_ONLY branch is genuinely last and mechanically checked; Phase 13's backend
genuinely spreads rather than duplicates Phase 9 and 10's read path; the O1-before-O4 ordering
genuinely held in git history; and the roughly 110 post-phase consolidation commits genuinely
routed every phase-owned caller through the new shared modules without leaving an orphaned
reference (the one near-miss, an orphaned SOURCE_ROOT_URL in cache-key.spec.ts, was caught and
fixed in the same consolidation commit, 5968d51).
