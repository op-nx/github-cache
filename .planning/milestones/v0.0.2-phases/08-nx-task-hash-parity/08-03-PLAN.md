---
phase: 08-nx-task-hash-parity
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
autonomous: false
requirements: [PARITY-01, PARITY-03, PARITY-04, PARITY-05, PARITY-06]

must_haves:
  truths:
    - "A two-leg matrix CI job runs the instrument on ubuntu-24.04-arm and windows-11-arm and uploads one record per leg, with a missing record failing the LEG rather than arriving as silence (D-17, CORR-03 capture half)."
    - "All FOUR observation points -- workstation cold, workstation warm-preexisting, windows-11-arm, ubuntu-24.04-arm -- are taken at ONE commit, and every pasted value carries its full `meta` block so a stale paste is detectable (D-07, PARITY-03)."
    - "\"Does a warm local box compute the hash cold CI published\" is answered as its OWN named question and is NOT resolved by a `nx reset` in the proof recipe (D-08, PARITY-04)."
    - "The `integration` hash is recorded for the workstation and for windows-11-arm as a same-OS pair (PARITY-05)."
    - "Which of the seven-entry or one-entry `typecheck` outputs list is CORRECT is determined from what the command actually emits, BEFORE anything is pinned (Research A6, open question 2)."
  artifacts:
    - ".github/workflows/ci.yml `hash-parity` two-leg matrix job with its rationale comment block"
    - "the anchor commit SHA, recorded in 08-ROOT-CAUSE.md"
    - "08-ROOT-CAUSE.md `## Observation points` filled with four meta-stamped sections"
    - "08-ROOT-CAUSE.md section enumerating what tsc --build actually emits for typecheck"
  key_links:
    - "the `hash-parity` job's checkout ref -> PARITY-03's one-commit requirement (the default pull_request checkout resolves a MERGE commit whose SHA no workstation can reproduce)."
    - "`if-no-files-found: error` on the upload -> D-20 clause (a) (without it a leg that produced nothing uploads silence, and the comparator reports the right verdict with the wrong blame)."
    - "`fail-fast: false` -> the compare job's record count (with fail-fast on, a Windows leg failure cancels the ubuntu leg and 'fewer than two records' means something unrelated to hash parity)."
---

<objective>
Anchor one commit and take all four observation points at it.

Purpose: PARITY-03 asks for FOUR recorded values per target at ONE commit. That is only achievable
if every hash-rotating change is already behind the anchor and the CI legs measure the same SHA the
workstation measured. Plans 08-01 and 08-02 put the hash-rotating changes behind the anchor; this
plan creates the anchor, adds the capture-only CI job, and collects the four records.

Output: the `hash-parity` two-leg matrix job, the anchor commit, and the record's
`## Observation points` section filled with four meta-stamped readings plus the `typecheck` outputs
enumeration the fix will need.

Only the CAPTURE job is added here. The build-gating compare job is plan 08-06, and the reason is
D-15 plus D-18: pre-fix, the roadmap's own success criterion 1 says cold-ubuntu differs from
cold-windows for every target, so a build-gating comparison wired now would be RED for a reason the
record has not established yet, and D-18 forbids a `continue-on-error` softening.

HARD ORDERING CONSTRAINT (D-06 / D-15): no commit in this plan may touch `nx.json`.

Why `autonomous: false`: observation points 1 and 2 are captured on the maintainer's own Windows
workstation against their real, long-lived `.nx/workspace-data`, which is the measurement subject
and cannot be reproduced by any runner; and points 3 and 4 require a Live-CI round trip. Both carry
`<human-check>` items.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-nx-task-hash-parity/08-CONTEXT.md
@.planning/phases/08-nx-task-hash-parity/08-RESEARCH.md
@.planning/phases/08-nx-task-hash-parity/08-PATTERNS.md
@.planning/phases/08-nx-task-hash-parity/08-VALIDATION.md
@.planning/phases/08-nx-task-hash-parity/08-01-SUMMARY.md
@.planning/phases/08-nx-task-hash-parity/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two-leg capture job, and the anchor commit</name>

  <files>
.github/workflows/ci.yml
  </files>

  <read_first>
- `.github/workflows/ci.yml` lines 1-12 (the triggers and the workflow-level `permissions`),
  lines 26-45 (the `lint` job's rationale-comment convention and the five-line boilerplate),
  lines 384-457 (the `integration` job -- the two-leg matrix skeleton this job copies, its
  `fail-fast: false` rationale at :392-393, its `shell: bash` rationale at :398-404, and the
  ~35-line sidecar block this job must NOT copy), and lines 655-672 (the job-level `permissions`
  trap recorded at :658-661).
- `08-CONTEXT.md` D-01, D-04, D-05, D-07, D-17.
- `08-RESEARCH.md` `## CI mechanics for the two-leg + compare job` in full -- the artifact action
  majors (upload v7, download v8, asymmetric on purpose), the unique-per-leg name requirement,
  `if-no-files-found: error`, and the `### The two-leg matrix shape already exists in this repo`
  paragraph. Also `### The dependentTasksOutputFiles node is a real hazard`.
- `08-PATTERNS.md` sections `.github/workflows/ci.yml -- the two-leg capture job` (all four settled
  points) and `## No Analog Found` (there is NO in-repo precedent for artifact actions -- say so in
  the job comment so a reviewer does not go looking).
- `.planning/codebase/TESTING.md` lines 229-262 -- the Live-CI first-push close pattern.
  </read_first>

  <action>
Add a `hash-parity` job to `.github/workflows/ci.yml`, positioned after the `integration` job whose
skeleton it copies.

SHAPE, copied from `integration` at :409-423 and then deliberately trimmed:
- `strategy.fail-fast: false` with `matrix.os` listing `ubuntu-24.04-arm` and `windows-11-arm`.
- `runs-on: ${{ matrix.os }}` and a `timeout-minutes` matching the sibling jobs.
- Steps: checkout, setup-node with the node-version file and the npm cache, `npm ci`, the capture
  step, the upload step. Nothing else.

FOUR THINGS THE SKELETON SETTLES, each of which needs a line in the job's comment block because each
looks like an oversight next to a sibling job:
1. `fail-fast: false` carries a NEW reason here on top of its existing one. With fail-fast on, a
   Windows leg failure cancels the ubuntu leg, and plan 08-06's compare job then reports "fewer than
   two records" for a reason that has nothing to do with hash parity.
2. `shell: bash` on the capture step. GitHub's DEFAULT shell on `windows-11-arm` is pwsh, which
   fails on the constructs this file's scripted steps use; the rationale is already recorded at
   `ci.yml:399-404`.
3. NO sidecar block. All four of `build`, `typecheck`, `test` and `integration` carry a pre-set /
   background `start-cache-server` / readiness-poll / `cancel:` block. This job runs NO cached Nx
   task -- it computes hashes -- so it needs none, and leaving it out removes an entire class of
   flake from a gate that must be trusted.
4. NO job-level `permissions` block. `ci.yml:658-661` records that a job-level block REPLACES the
   workflow grant wholesale rather than merging. The workflow-level `contents: read` covers checkout
   plus an artifact upload.
And a fifth, which is NOT settled by the skeleton: do NOT gate this job on the push event. Every job
in this file carrying that gate does so because it WRITES. This job only measures, and the read-side
jobs' comment at `:170-172` states the corresponding rule.

CHECKOUT REF -- the one deliberate deviation from every other job in this file, and it needs its own
paragraph in the comment. Pin the checkout to the pull-request head SHA when there is one and to
the pushed SHA otherwise, using a fallback expression so the same step works on both events. The
reason is PARITY-03: on a `pull_request` event `actions/checkout` resolves a MERGE commit whose SHA
no developer can reproduce locally, and whose TREE differs from the branch's whenever the default
branch has moved. Either would silently break "four values per target at ONE commit". The instrument
records BOTH the checked-out `HEAD` and the runner-provided event SHA precisely so this can be
checked rather than assumed -- state in the comment that a divergence between those two fields in a
record is the signal that this pin stopped working.

CAPTURE STEP: a named, `shell: bash` step with `set -euo pipefail`, running the instrument directly
as a node invocation (never through `nx run` -- D-01b), with `--install-mode ci` because the
preceding step ran a clean install, and `--out` pointing at a per-leg record filename. Pass the
record filename in through the step's `env:` block rather than interpolating the matrix value
directly into the script body, and set `NX_DAEMON` to false in the same `env:` block. Disabling the
daemon is the CALLER's job by design: the instrument MEASURES `daemonEnabled` rather than forcing
it, so a leg that forgot is visible in the record instead of silently reclassified.

NO BUILD STEP, and this is load-bearing rather than an omission. RESEARCH records that `typecheck`
carries a `dependentTasksOutputFiles` node hashing the CONTENT of the package's build output
directory, so a job that ran a build first hashes a populated directory and a job that did not
hashes an empty one. Both legs must therefore be identical in this respect, and "neither builds" is
the identical option that is also the fastest. Put this in the comment, because a reader comparing
against the `pack-check` and `publish-verify` jobs -- both of which DO build first -- will otherwise
read it as a missing step.

UPLOAD STEP: `actions/upload-artifact` at its current major, with a name unique per leg (the matrix
OS value is the natural discriminator, and v4 onward made artifact names immutable so two legs
uploading to one name is an error rather than a merge), the record path, and `if-no-files-found`
set to error. That last setting needs its own comment line: the default is warn, so without it a leg
whose instrument produced nothing uploads an empty artifact, the download merges it into nothing,
and the comparator reports "fewer than two records" -- the correct verdict with the wrong blame, one
job further from the cause than necessary.

Note in the comment that this repo has NO prior artifact usage: the complete third-party action
inventory across both workflows is checkout and setup-node, so there is no in-repo major to match
and no precedent to copy. Name the two majors chosen and that upload and download are asymmetric on
purpose (the download action's own documentation pairs them that way). A reviewer who expects a
precedent should find that sentence instead of going looking.

Do NOT write any spec that asserts on `ci.yml` content. `nx.json`'s `test` inputs list
`cleanup.yml` and NOT `ci.yml`, so such a spec would serve a stale cached PASS. Registering
`ci.yml` is PARITY-08 and is deferred to Phase 9 so its hash rotation collapses into VER-01's
window. This is the ONLY place the rationale for these jobs can live, which is why the comment
block carries so much of it.

FINALLY -- the anchor. Commit ONLY the `ci.yml` change. Confirm `git status --porcelain` is EMPTY
afterwards, including untracked files, because the instrument derives `meta.workingTreeClean` from
exactly that. Record the resulting SHA; it is the anchor commit and all four observation points must
carry it.

Then make the job reachable. `ci.yml` triggers on a push to the default branch and on
`pull_request`, and `gh pr list` currently reports no open pull request for this branch -- so
pushing the branch alone runs NOTHING. Open a DRAFT pull request for this branch against the default
branch (the repo is the maintainer's own, and PR #4 during quick 260725-rk4 is the in-repo precedent
for using a PR to reach CI mid-phase). Do NOT widen `on.push.branches` and do NOT add a
`workflow_dispatch` trigger as a workaround: both change the workflow's trigger surface, neither is
covered by any decision in `08-CONTEXT.md`, and the trigger surface is a trust-model surface in this
repo.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <human-check>Confirm the draft pull request is open for this branch and that the `hash-parity` job appears in its checks list on both legs.</human-check>
  </verify>

  <acceptance_criteria>
- `.github/workflows/ci.yml` contains a `hash-parity` job whose `strategy.matrix.os` lists exactly
  `ubuntu-24.04-arm` and `windows-11-arm` and whose `strategy.fail-fast` is false.
- That job's steps are exactly: checkout (with an explicit ref expression), setup-node, `npm ci`, a
  `shell: bash` capture step, and an upload step. It contains no background step, no `cancel:` step,
  no `permissions:` block, no `if:` event gate and no build step.
- The upload step sets `if-no-files-found` to error and uses an artifact name that includes the
  matrix OS value.
- The job carries a rationale comment block above its key covering, at minimum: the fail-fast
  reason, the `shell: bash` reason, why there is no sidecar, why there is no job-level permissions
  block, why there is no build step, why the checkout ref is pinned, and the statement that the repo
  had no prior artifact usage.
- `npm run format:check` exits 0.
- `git status --porcelain` is empty at the anchor commit and the anchor SHA is recorded in the plan
  summary.
- `git show --stat` for this commit lists `.github/workflows/ci.yml` and nothing else.
- A draft pull request for this branch is open and a workflow run has been triggered on it.
  </acceptance_criteria>

  <done>
Both runners can produce a record at a known SHA, and that SHA is the anchor every observation point
will carry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Observation points 1 and 2, and what typecheck actually emits</name>

  <files>
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <read_first>
- `08-ROOT-CAUSE.md` as plan 08-01 left it -- the Method section's cold recipe and the D-08 rule
  that every section declares which question it answers.
- `08-CONTEXT.md` D-04, D-07, D-08, D-10, D-11.
- `08-RESEARCH.md` `### The recommended cold recipe: env vars, not deletion` (the exact three
  environment variables and why deletion does not produce a cold state), `### Finding 4: STALENESS,
  not freshness` including the `warm-fresh` versus `warm-preexisting` distinction, `### Answer to
  PARITY-04, measured`, Pitfall 1 and Pitfall 7.
- `packages/github-cache/tsconfig.json` and `tsconfig.lib.json` and `tsconfig.spec.json` -- the
  solution config's two project references and each referenced config's output settings. These
  determine what the `typecheck` command actually emits.
- `packages/github-cache/src/nx-target-inputs.spec.ts` lines 10-18 -- the recorded statement of what
  the `typecheck` target's command is.
- `08-RESEARCH.md` `### Finding 2: the differing field is the inferred typecheck outputs` and
  assumption A6 in the `## Assumptions Log`.
  </read_first>

  <action>
STEP 1 -- observation point 1, native Windows workstation, COLD.

Use the env-var recipe, not `nx reset`. Create a fresh temporary directory OUTSIDE the repository,
point `NX_WORKSPACE_DATA_DIRECTORY` and `NX_NATIVE_FILE_CACHE_DIRECTORY` at two subdirectories of
it, set `NX_DAEMON` to false, and run the instrument with `--install-mode` set to whatever this
workstation's `node_modules` was ACTUALLY produced by, and `--out` pointing at a file inside that
same temporary directory so nothing lands in the working tree.

Before capturing, run a clean install so the local install mode matches the CI legs'. This is not
housekeeping: RESEARCH Pitfall 6 records that the node map carries several hundred external
dependency entries including platform-conditional optional packages, so an install-mode difference
produces `only-in-A` / `only-in-B` entries that look exactly like an OS difference. Match the mode,
then record it honestly.

Three reasons the env-var recipe beats deletion, all of which belong in the record beside the
reading: an empty directory is cold BY CONSTRUCTION and cannot half-work, whereas `nx reset` is an
operation that can fail -- Pitfall 1 records an EPERM on this exact platform because the daemon
holds the workspace-data database open and `--onlyWorkspaceData` does not stop the daemon; the
resulting state is PROVABLE, because the instrument reads the directory entry counts before building
the graph; and it is non-destructive, which matters because the SAME box must next produce a
warm-preexisting reading and `nx reset` would destroy exactly that state.

Verify from the emitted record that `meta.graphState` reads cold and that both measured entry counts
are zero. If either is non-zero the recipe did not take and the reading must be discarded, not
relabelled.

STEP 2 -- observation point 2, native Windows workstation, WARM-PREEXISTING.

Run the instrument again with NO environment overrides at all, against the repository's own
long-lived `.nx/` state. Do NOT run `nx reset` first, do NOT run a cold capture into the repo's own
directories first, and do NOT let any Nx command run in between that would repopulate the graph
from scratch. `warm-preexisting` is the state PARITY-04 is actually asking about, and a
warm-after-reset graph and a warm-since-months graph are different measurements wearing the same
label.

If the executor is running inside a git worktree rather than the main checkout, STOP and move to the
main checkout first. Nx 23.1.0's `sharedCacheDirectory` resolves the MAIN worktree root, so a
worktree reads the main tree's graph state and its "cold" reading is not cold (Pitfall 7). Record
which checkout the reading came from.

STEP 3 -- append `## Observation points` sections 1 and 2 to `08-ROOT-CAUSE.md`.

Each section carries: its FULL `meta` block verbatim, including the commit SHA, the measured graph
state, both measured directory paths with their entry counts, the install mode, the Nx version, the
Node version, and the daemon flag. Then a table of the five targets with their hashes. Then the
discriminator's `stdout` and `stderr` recorded verbatim including whitespace. Then, per D-08, an
explicit sentence naming which of the two questions this reading answers.

Do NOT paste the full node maps -- they are several hundred entries per target and about a quarter
of a megabyte per record. Paste the `meta` block, the five hashes, and the DIFF. The full records
stay as untracked local files and as CI artifacts; the record carries what a reader needs and what
makes a stale paste detectable.

STEP 4 -- diff point 1 against point 2 and record it. Run the instrument's diff mode over the two
local records and paste the result: per target, the `only-in` buckets, the `value-changed` list with
both hashes, and the `same` COUNT. Lead with the `only-in` buckets even when they are empty, because
that bucket is the one that cannot arise from staleness within a single machine and is therefore the
cleanest OS signal -- recording it as empty here is what makes it meaningful when the cross-OS diff
in the next task is read. Then paste the `projectConfiguration` field diff, noting that `metadata`
inside that node is NOT hashed so any difference there is informational.

Then record the D-10 SUPERSESSION explicitly rather than quietly ignoring it. D-10 directs the
investigation to start at the external-dependency nodes and then the project fileset. That ordering
was reasonable when written; if this diff names a single differing node, state which node it is,
state that the measurement supersedes D-10's ordering, and say so as a correction with its evidence
rather than silently starting somewhere else.

STEP 5 -- answer PARITY-04 as its OWN named question, in its own heading. State the question in
full. State the answer with both numbers side by side: the warm-preexisting reading and the cold
reading, same machine, same operating system, same commit, same Nx, same Node. If the answer is NO,
that is a FINDING and not a bug to fix in this phase -- PARITY-04's own text permits it and the
roadmap's success criterion 4 says so explicitly. Record the consequence: if a warm local box does
not compute the hash cold CI published, the O1 outcome is unreachable regardless of OS parity, and
the developer-facing mitigation is a `nx reset`, which DOCS-07 owns in Phase 12. State that the
record deliberately does NOT put a `nx reset` in the proof recipe, because the requirement names
that exact move as disqualifying.

STEP 6 -- determine which `typecheck` outputs list is CORRECT (Research assumption A6, open
question 2). This is a measurement, it belongs in the record, and it must happen before anything is
pinned. RESEARCH observed two forms in the persisted plugin cache -- a seven-entry list and a
one-entry list -- and assumed without checking that the seven-entry one is correct. If that
assumption is inverted, pinning would make `typecheck` stop caching its declaration output, which is
a silent correctness regression in the cache this whole project exists to make trustworthy.

Settle it empirically: on a clean tree with the package's build output and typecheck output
directories removed, run the `typecheck` target's actual command as recorded at
`nx-target-inputs.spec.ts:10-18`, then enumerate every file it wrote. Record the enumerated paths,
map each onto the pattern that would cover it, and state which of the two candidate lists matches
what was written. If neither matches exactly, record the ACTUAL correct list -- the goal is the
correct list, not a vote between the two candidates. Restore the tree afterwards.

Append this as its own section stating which question it answers: neither of the two, it is the
input the fix in plan 08-05 needs.

Do NOT touch `nx.json`. Do NOT apply any fix in this plan.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <human-check>Confirm observation points 1 and 2 came from YOUR Windows workstation's main checkout (not a worktree), that point 2's graph was the long-lived one and was NOT preceded by a reset, and that both meta blocks name the anchor commit with a clean working tree.</human-check>
  </verify>

  <acceptance_criteria>
- `08-ROOT-CAUSE.md` contains two observation-point sections, each with a complete `meta` block
  naming the anchor commit SHA, a measured graph state, both directory paths with entry counts, and
  a five-row hash table.
- Point 1's recorded `meta.graphState` is cold and both of its measured entry counts are zero.
- Point 2's recorded `meta.graphState` is warm and its `workspaceDataDirectory` is the repository's
  own, not a temporary directory.
- Both sections state, in a sentence, which of the two questions the reading answers.
- The record contains a diff section presenting the two local records with, per target, the
  `only-in` buckets first, the `value-changed` list with both hashes, and a `same` count.
- The record contains an explicit statement of whether the measurement supersedes D-10's search
  ordering, naming the node the measurement points at.
- The record contains a heading dedicated to PARITY-04's question, its answer, both numbers, and the
  statement that no `nx reset` appears in the proof recipe.
- The record contains an enumeration of the files the `typecheck` command actually writes and a
  stated conclusion about which outputs list is correct, with the enumeration shown rather than
  asserted.
- `npm run format:check` exits 0.
- `git show --stat` for this commit lists only the record file. `nx.json` is untouched.
  </acceptance_criteria>

  <done>
The workstation half of the record is complete, PARITY-04 has its own named answer, and the fix in
plan 08-05 has the outputs list it needs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Observation points 3 and 4 from the real runners</name>

  <files>
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <read_first>
- `08-ROOT-CAUSE.md` as task 2 left it, and the anchor SHA recorded in task 1.
- `08-CONTEXT.md` D-07, D-09, D-11, D-21.
- `08-RESEARCH.md` `### How to diff two maps usefully` (the three buckets and why `only-in-*` leads
  a cross-OS reading), `### Finding 3` (the Windows separator sensitivity in the TypeScript
  plugin's project-reference classification, offered as the strongest available lead and explicitly
  NOT proven cross-OS), and `## Open Questions` items 1, 3 and 5.
- `.planning/phases/07-lint-toolchain-and-the-ambient-platform-read-ban/07-EVIDENCE.md` lines
  498-523 -- D-35's recorded Windows-side baseline for the inferred `lint` target's hashed node
  values, and the note that `options.cwd` is the row most likely to diverge.
- `.planning/codebase/TESTING.md` lines 229-262 -- the Live-CI first-push close pattern and its
  requirement that these items be NAMED rather than left looking like skipped steps.
  </read_first>

  <action>
This task is a LIVE-CI CLOSURE, and it must be labelled as one in the summary rather than reading as
an ordinary verification step. Points 3 and 4 exist only on real GitHub-hosted runners; no local
mock can produce them without faking the exact thing under test.

STEP 1 -- get the run. Wait for the workflow run triggered by the anchor commit on the draft pull
request. Confirm BOTH `hash-parity` legs completed and both uploaded an artifact. Download both
artifacts.

If a leg failed, do NOT proceed by pasting one record and describing the other. Fix the leg and
re-run at the SAME anchor commit. If fixing the leg requires a `ci.yml` change, that change makes a
NEW anchor commit -- in which case points 1 and 2 must be re-taken at the new anchor, because
PARITY-03's "four values per target" is worthless across two commits. State in the summary which
anchor the four points share.

STEP 2 -- validate the records before reading them. For each downloaded record, check that
`meta.commit` equals the anchor SHA and that `meta.commit` equals `meta.githubSha`. The second check
is what proves the checkout pin from task 1 worked: on a `pull_request` event an unpinned checkout
resolves a merge commit, and these two fields would disagree. If they disagree, the pin is not
working and the reading is not comparable with points 1 and 2 -- fix the pin and re-anchor rather
than reconciling in prose.

Also check `meta.installMode`, `meta.nxVersion`, `meta.nodeVersion` and `meta.graphState` against
points 1 and 2. `.node-version` is the moving alias `lts/krypton`, so the Node version on a runner
can differ from the workstation's; PARITY-06 requires it recorded, and if it DIFFERS that is itself
a confound to name in the record rather than a detail to skip.

STEP 3 -- append observation-point sections 3 and 4 in the same shape as 1 and 2: the full `meta`
block, the five-target hash table, the discriminator's raw stdout and stderr, and the D-08 sentence
naming which question the reading answers.

STEP 4 -- the cross-OS diff, which is PARITY-01's actual deliverable. Run the instrument's diff mode
over the two CI records and paste the result per target, leading with the `only-in` buckets. Then
paste the `projectConfiguration` field diff.

Read it in this order and record the reading, not just the output:
- `only-in-A` / `only-in-B` FIRST. RESEARCH is explicit that this bucket cannot arise from staleness
  within a single machine and is the cleanest OS signal available, and that a differing
  platform-specific optional-dependency set is the likeliest shape of a genuine cross-OS
  difference. If entries appear here, name the packages and say whether they are
  platform-conditional optional dependencies.
- `value-changed` second. If the merged project configuration node is the one that moved, use the
  `projectConfiguration` field diff to localise it to a FIELD -- that localisation is the whole
  reason the instrument emits the merged node, because the node map carries one entry covering all
  five targets.
- `same` count last, as the scale against which the other two are read.

Then state PARITY-01's answer in one paragraph: which hash inputs differ cross-OS, node by node,
with the evidence above it. Where RESEARCH offered a prediction -- its Finding 3 names a Windows
separator sensitivity in the TypeScript plugin's project-reference classification as the strongest
available lead while stating that the Linux leg was never measured -- quote the prediction alongside
the measurement so the divergence, or its absence, is visible. Do NOT present RESEARCH's numbers as
this record's numbers.

STEP 5 -- the `lint` reading (D-21, Phase 7's D-35). Compare the `lint` hashes across the two legs
and record the verdict. Quote D-35's Windows-side baseline table from `07-EVIDENCE.md:505-513`
alongside, and where the merged project configuration diff touches the `lint` target, name the
field. D-35 flags `options.cwd` as the row most likely to diverge and confirms it IS hashed, so
check that field specifically. This is the empirical settlement of a question Phase 7 left
UNVERIFIED BY DESIGN, and it decides which branch of D-21 plan 08-06 wires: `lint` asserted as a
fourth IDENTICAL target, or -- if it diverges and the fix proves out of scope -- the clause
downgraded to recorded-with-a-named-finding, never deleted.

STEP 6 -- D-11: `typecheck`'s third variance source. Compare `typecheck` across all four points and
state whether a third source beyond OS and staleness is visible. The lead candidate RESEARCH names
is the dependent-task-outputs node, which hashes the CONTENT of the package's build output
directory: a job that never ran a build hashes an empty set, a workstation with a populated one
hashes whatever is there. Both CI legs deliberately skip the build step, so if the workstation
readings were taken with a populated build directory, that IS the third source and the record should
say so. D-11 permits recording it as OPEN with its evidence, and explicitly forbids it becoming a
phase blocker -- so record a verdict either way and move on.

STEP 7 -- two findings to SURFACE, not fix, each in its own short section:
- The worktree claim in `AGENTS.md` states that each git worktree gets its own Nx cache and
  workspace data. RESEARCH read Nx 23.1.0's cache-directory resolution and found it resolves the
  MAIN worktree root instead. That is a documentation defect outside this phase's stated boundary.
  Record it with its citation and leave it; do not drive-by-fix it.
- Phase 7's D-36 records three legitimate all-MISS hash-rotation windows in this milestone, and this
  phase's fix commits are one of them. Record that here so nobody reads the resulting cache misses
  as a defect, and so Phase 9's tripwire is authored as "two consecutive all-miss pushes with no
  version-affecting change in between" rather than firing on correct work.

Commit the record. Do NOT touch `nx.json`.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <human-check>Live-CI closure: confirm both `hash-parity` legs are green on the pull request, that both downloaded records name the anchor SHA in BOTH their commit fields, and that the pasted meta blocks match the artifacts you downloaded.</human-check>
  </verify>

  <acceptance_criteria>
- `08-ROOT-CAUSE.md` contains four observation-point sections in total, and all four `meta` blocks
  name the SAME commit SHA.
- Sections 3 and 4 each record `meta.commit` and `meta.githubSha` as equal, or state explicitly that
  they diverged and that the reading was re-taken.
- The record contains a cross-OS diff section presenting, per target, the `only-in` buckets first,
  then `value-changed` with both hashes, then a `same` count, plus a merged project configuration
  field diff.
- The record states PARITY-01's node-by-node answer in prose, quoting RESEARCH's prediction beside
  the measurement wherever one exists, and attributes no difference to the operating system without
  the staleness axis being pinned first.
- The record states a verdict for `lint` cross-OS and quotes Phase 7's Windows-side baseline table
  beside it, naming which branch of D-21 plan 08-06 must wire.
- The record states a verdict for `typecheck`'s third variance source -- root-caused or explicitly
  OPEN with its evidence.
- The record contains a section recording the `AGENTS.md` worktree claim as a surfaced, unfixed
  finding with its citation, and a section pre-recording this phase's legitimate all-MISS rotation
  window.
- `npm run format:check` exits 0.
- `git show --stat` for this commit lists only the record file. `nx.json` is untouched by every
  commit in this plan.
  </acceptance_criteria>

  <done>
All four observation points exist at one commit, the cross-OS divergence is named node by node, and
the `lint` and `typecheck` open questions have recorded verdicts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository state -> a downloadable CI artifact | The capture job uploads a JSON document derived from repo state and runner environment to an artifact that anyone with the run URL can fetch on a public repository. |
| Workflow trigger surface -> the trust model | This repo's write and sync gates are keyed on event names; widening triggers to reach CI would move a security boundary for a convenience reason. |
| A CI leg's silence -> the record | A leg that produced nothing must fail its own leg rather than arriving downstream as an absent record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Information disclosure | the uploaded `hash-parity-<os>` artifact | low | accept | Content is task hashes, npm package node names, runner paths and the discriminator's stdout. No secret, token, or credential is in scope, and the job requests no secret. The workflow-level `contents: read` grant is unchanged. |
| T-08-12 | Elevation of privilege | a job-level `permissions` block | high | mitigate | No job-level block is added. `ci.yml:658-661` records that such a block REPLACES the workflow grant wholesale rather than merging, so adding one to "be explicit" would silently drop scopes. The workflow-level `contents: read` covers checkout and upload. |
| T-08-13 | Spoofing | a leg that uploads nothing | high | mitigate | `if-no-files-found` is set to error, so a leg whose instrument produced nothing fails its OWN leg rather than delivering silence that the comparator later blames on record count. |
| T-08-14 | Tampering | reaching CI by widening the trigger surface | high | mitigate | A draft pull request is used. Widening `on.push.branches` or adding `workflow_dispatch` is explicitly forbidden by this plan: the trigger allowlist is a trust-model surface in this repo (the write and sync gates key on event names) and no decision in `08-CONTEXT.md` authorises changing it. |
| T-08-15 | Tampering | a record measured at a different commit than it claims | high | mitigate | The checkout is pinned to the head SHA so the runner measures the branch tree rather than a merge commit, and the instrument records BOTH the checked-out HEAD and the runner-provided event SHA so a pin failure is visible in the artifact. Task 3 validates the equality before any reading is trusted. |
| T-08-16 | Information disclosure | host paths pasted into a committed record | low | accept | The pasted `meta` blocks carry a repository path and an OS temp path. No credential and no work-domain string is in scope; the `<human-check>` on task 2 is where the maintainer confirms it. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new npm package. Two first-party `actions/*` GitHub Actions are new to this repo; `08-RESEARCH.md`'s Package Legitimacy Audit records both as OK with zero `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts, so no install checkpoint is required. |
</threat_model>

<verification>
- All NINE battery commands green at this plan's final commit.
- `nx.json` is untouched by every commit in this plan, and by every commit in plans 08-01 and 08-02.
  This is D-06's proof and it is checkable with a single `git log --stat` over the phase's commits.
- Both `hash-parity` legs are green on the pull request at the anchor commit.
- All four observation points in the record name the same commit SHA.
</verification>

<success_criteria>
- A two-leg capture job produces one meta-stamped record per runner OS at a known commit.
- The record holds four observation points at that one commit, each declaring which question it
  answers.
- PARITY-04 has its own named answer, PARITY-01 has a node-by-node cross-OS reading, and the `lint`
  and `typecheck` open questions have recorded verdicts.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See plan 08-01's identical section for the full phase-wide list. This plan contributes:

**New CI job** (`.github/workflows/ci.yml`)
- `hash-parity` -- the two-leg matrix capture job

**New CI artifact names**
- `hash-parity-ubuntu-24.04-arm`
- `hash-parity-windows-11-arm`

**New third-party action in this repo** (there was no prior artifact usage)
- `actions/upload-artifact`

**New record sections** (`.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md`)
- `## Observation points` with four meta-stamped subsections
- the local staleness diff, the cross-OS diff, PARITY-04's named answer, the `typecheck` outputs
  enumeration, the `lint` cross-OS verdict, the D-11 verdict, the surfaced `AGENTS.md` finding, and
  the pre-recorded all-MISS rotation window
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-nx-task-hash-parity/08-03-SUMMARY.md` when done.
</output>
