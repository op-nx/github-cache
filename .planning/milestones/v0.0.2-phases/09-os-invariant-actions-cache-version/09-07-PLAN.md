---
phase: 09-os-invariant-actions-cache-version
plan: 07
type: execute
wave: 6
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified:
  - .planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md
autonomous: false
requirements: [VER-06, OBS-04]

must_haves:
  truths:
    - "The producer-attribution snapshot is captured BEFORE the merge to `main` -- the last moment it can be taken, because once the version is OS-invariant the ubuntu publish leg starts mirroring the Windows `integration` entry too and the shard stops being 'everything here is ubuntu's' (D-34, TEST-08's attribution-window clause)."
    - "The gate is the MERGE, not the phase's first code commit: `ci.yml:3-7` filters pushes to `main` and `publish` is `if: github.event_name == 'push'`, so nothing on this branch mirrors anything (C-05, H12)."
    - "The snapshot is evidence PRESERVATION for TEST-08 (Phase 11) and must NOT grow into the O1 proof -- TEST-08 is not in this phase's eleven requirements (D-34, H12)."
    - "VER-06's cross-OS read-back and OBS-04's rotation signal are named as `human_needed` at the merge, with the reason neither can be observed pre-merge, and NO check in this phase claims either (C-06, H5)."
    - "Every requirement miscount and stale citation this phase encountered is recorded rather than silently fixed, so the verifier and the milestone audit do not read a content-located edit as drift."
    - "The recorded corrections are audited against REQUIREMENTS.md's OWN WORDS, never against ROADMAP.md's `:537-544` traceability table or its `:575` count -- both are wrong for this phase (the table omits PARITY-08, VER-07 and ROBUST-04; the count says eight where REQUIREMENTS says eleven). This is how Phase 8 caught ROADMAP's stale PARITY numbering, and it is why `init.plan-phase`'s nine-ID answer was not trusted (D-28, D-00)."
  artifacts:
    - "`.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md` with `## Producer-attribution snapshot (D-34)`, `## Live-CI closures`, and `## Recorded corrections`"
  key_links:
    - "the snapshot -> Phase 11's TEST-08, which requires per-hit producer attribution and states the attribution window closes at Phase 9. This plan preserves the input; it does not perform the proof."
    - "`CACHE_MIRROR_MAX_AGE_DAYS` = 30 -> the snapshot's shelf life. Taking it late (close to the merge) is deliberate: assets created between the snapshot and the merge would otherwise be missing from the record, and waiting past the merge ages the record out."
    - "`09-ROTATION-SIGNAL.md` -> the `## Live-CI closures` OBS-04 row. The prediction lives there; this file records that the reading is still open and where to do it."
---

<objective>
Preserve the evidence that this phase destroys, and name what only a real default-branch push can
settle.

Purpose: two separate obligations converge at the end of this phase. First, TEST-08 (Phase 11) needs
per-asset producer attribution, and it states that the attribution window closes at Phase 9 rather
than Phase 12 -- once the version is OS-invariant the ubuntu publish leg starts mirroring the Windows
`integration` entry too, so the current month's shard stops being "everything here is ubuntu's". One
`gh api` pass is the only moment that record can be taken. Second, VER-06 and OBS-04 both have clauses
no pre-merge run can produce, and a phase that quietly claimed them closed would be worse than one
that names them.

Output: one committed record with the snapshot, the Live-CI closures, and the phase's recorded
corrections.

Why `autonomous: false`: the snapshot reads live repository state through `gh api`, and the two
`human_needed` items are confirmed by the maintainer reading real runs after the merge.

Vertical slice: the phase's hand-off closed end-to-end -- the perishable measurement, the open items
with where to read them, and the citation corrections a later audit would otherwise mistake for drift.
No code, no workflow, no `serve()`-reachable file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-os-invariant-actions-cache-version/09-CONTEXT.md
@.planning/phases/09-os-invariant-actions-cache-version/09-RESEARCH.md
@.planning/phases/09-os-invariant-actions-cache-version/09-VALIDATION.md
@.planning/phases/09-os-invariant-actions-cache-version/09-ROTATION-SIGNAL.md
@.planning/phases/09-os-invariant-actions-cache-version/09-05-SUMMARY.md
@.planning/phases/09-os-invariant-actions-cache-version/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the producer-attribution snapshot</name>

  <files>
.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md
  </files>

  <read_first>
- `.planning/REQUIREMENTS.md` `:473-491` -- TEST-08's own words, in particular the attribution clause
  ("The attribution window closes at Phase 9, not Phase 12 ... Capture `created_at` and the OBS-03
  label per asset, not just the asset list") and the sentence stating that enabling O4 permanently
  destroys the ability to re-derive it.
- `packages/github-cache/src/lib/retention.ts` -- `resolveMaxAgeDays` (default 30) and the
  `cache-mirror-YYYYMM` shard-tag scheme, so the shard tag in the snapshot is derived from the one
  source rather than hand-guessed.
- `packages/github-cache/src/publish/publish-mirror.ts` and
  `packages/github-cache/src/cleanup/cleanup-mirror.ts` -- which fields the publisher writes per asset
  and what cleanup prunes on, so the snapshot captures the fields that actually matter.
- `.github/workflows/ci.yml` `:3-7` and the `publish` job's `if:` -- confirm FIRST-HAND that pushes are
  filtered to `main` and `publish` is push-gated, which is what makes the merge (not this branch) the
  real gate.
- `09-CONTEXT.md` D-34 and C-05.
- `09-RESEARCH.md` `### Hazard C (NEW): D-34's snapshot does not gate the whole phase`.
- `~/.claude/CLAUDE.md`'s contact rule -- this is a PUBLIC repository and this file will be committed.
  No work email and no bare work domain may appear in the content, and `git config user.email` must be
  the approved public address before committing.
  </read_first>

  <action>
Create `.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md` and write its first
section, `## Producer-attribution snapshot (D-34)`.

STEP 1 -- resolve the inputs rather than hardcoding them:
- owner/repo from `gh repo view --json nameWithOwner`, not from memory;
- the current month's shard tag from `retention.ts`'s scheme (`cache-mirror-YYYYMM`), stated
  explicitly in the record with the month it resolved to;
- the tool versions: `gh --version` and the Nx version, per PARITY-06 discipline.

STEP 2 -- capture two lists. **Confirm the exact endpoint path and pagination shape before running
anything** -- check `gh api --help` or the REST reference rather than guessing flags. Note that the
Actions-cache list endpoint wraps its array in an `actions_caches` field, so a naive `-q '.[]'` will
not work.

- **the current month shard's assets:** resolve the release by tag, then list its assets, capturing per
  asset: `name`, `created_at`, `size` and `label` (the OBS-03 label field, which is EMPTY today --
  `mirrored-by` is Phase 10 -- and recording that it is empty is itself the point: this snapshot is
  what covers the window before the label exists).
- **the repository's Actions-cache entries:** per entry, `key`, `ref`, `created_at`,
  `last_accessed_at` and `size_in_bytes`.

Record the EXACT commands run, verbatim, so the capture is reproducible and a reader can see what was
and was not asked for.

**Capture only those fields.** Do NOT paste raw API JSON: it carries uploader identities, node ids and
signed URLs, none of which the record needs and all of which are permanent once committed to a public
repository. Render each list as a small table or TSV block of the named fields only.

If the shard release does not exist (a 404 -- possible if no default-branch push has happened this
month), record that verbatim as the result rather than substituting a different month. An honest empty
snapshot is a valid record; a silently widened query is not.

STEP 3 -- write the section's framing, which is as load-bearing as the data:

- **WHEN and WHY now.** The gate is the MERGE to `main`, not this phase's first code commit: pushes are
  filtered to `main` and `publish` is push-gated, so on this branch VER-01 can land without the ubuntu
  leg starting to mirror Windows entries. Taking it LATE is deliberate -- assets created between the
  snapshot and the merge would otherwise be missing -- and `CACHE_MIRROR_MAX_AGE_DAYS` is 30, so
  waiting past the merge ages the record out instead.
- **WHAT this is and is NOT.** It is evidence PRESERVATION for TEST-08, a Phase 11 requirement that is
  NOT in this phase's eleven. It is NOT the O1 proof, it does not attempt per-hit attribution, and it
  must not grow into either. State that plainly, because the natural next step from this data is
  exactly the proof this phase must not perform.
- **WHY it expires.** Once the version is OS-invariant the ubuntu publish leg can restore and mirror
  the Windows `integration` entry, so from the first post-merge push the shard is no longer
  single-producer. Every asset in this snapshot predates that, which is the whole value of the
  timestamp.
- **The OBS-03 gap.** Until `mirrored-by` lands in Phase 10 the mirrored assets carry no attribution at
  all, and this snapshot is what covers that window. Do NOT write that the label answers "whose bytes
  did the developer get" -- that claim is explicitly RETRACTED, and the label will record the
  PUBLISHING leg's OS, which from this phase forward can differ from the producing OS.
  </action>

  <verify>
    <automated>gh repo view --json nameWithOwner</automated>
    <automated>npm run format:check</automated>
  </verify>

  <acceptance_criteria>
- `09-EVIDENCE.md` exists with a `## Producer-attribution snapshot (D-34)` section carrying the capture
  timestamp, the resolved owner/repo, the resolved shard tag with its month, and the `gh` and Nx
  versions.
- The exact commands run are recorded verbatim.
- The shard-asset list records `name`, `created_at`, `size` and `label` per asset, and the
  Actions-cache list records `key`, `ref`, `created_at`, `last_accessed_at` and `size_in_bytes` per
  entry -- or, if the shard release 404s, that result recorded verbatim without widening the query.
- No raw API JSON is pasted; no uploader identity, node id or signed URL appears in the file.
- The only email-shaped token in the file, if any, is the approved public address; no work email and no
  bare work domain appears.
- The section states that the gate is the merge and not the first code commit, with the push filter and
  the `publish` gate cited.
- The section states that this is evidence preservation for TEST-08 and is NOT the O1 proof, and that
  TEST-08 is not in this phase's requirement list.
- The section states why the window closes and records the OBS-03 attribution gap WITHOUT the retracted
  whose-bytes claim.
- `npm run format:check` exits 0.
  </acceptance_criteria>

  <done>
The perishable half of Phase 11's evidence is on record, dated, scoped to what it is, and taken at the
last moment it could be.
  </done>
</task>

<task type="auto">
  <name>Task 2: Name the Live-CI closures and the phase's recorded corrections, then commit</name>

  <files>
.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md
  </files>

  <read_first>
- Every `09-0N-SUMMARY.md` written so far in this phase -- the closures section must name what those
  plans actually recorded as open, not what this plan predicts.
- `.planning/codebase/TESTING.md` -- the Live-CI first-push close pattern and its requirement that
  these items be NAMED explicitly and closed by a job that fails loud, never by a passive log line.
- `.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md` `## Live-CI closures` -- the precedent:
  one row per item, each with a run reference or an explicit open-with-reason statement, and nothing
  omitted.
- `.planning/phases/08-nx-task-hash-parity/deferred-items.md` -- if `test` failed once during this
  phase for an unattributed reason, it belongs in this record with what is and is not known.
- `09-VALIDATION.md` `## Manual-Only Verifications` -- the four rows and their sampling rates.
- `09-RESEARCH.md` `### A gap in the proof VER-06 does NOT close`.
  </read_first>

  <action>
STEP 1 -- write `## Live-CI closures`. One row per item, each with either a reference or an explicit
open-with-reason. Nothing omitted, and no item stated as closed that is not.

OPEN, `human_needed` at the merge:
- **VER-06's cross-OS read-back.** After the merge to `main`: `dogfood-seed` (ubuntu, SINGLE leg) then
  `dogfood-verify` (two-leg matrix, `fail-fast: false`). A MISS fails the job, so a green
  `windows-11-arm` leg IS the proof. Reason it cannot be observed earlier: `ci.yml:3-7` filters pushes
  to `main` and both dogfood jobs are `if: github.event_name == 'push'`, so neither runs on this branch
  or on any pull request. Sampling rate: every push to `main`.
- **OBS-04's rotation signal.** Read both `publish` legs' job summaries on the FIRST `main` push after
  the merge and compare against `09-ROTATION-SIGNAL.md`'s prediction. Same gate. State that the signal
  exists on exactly ONE run and cannot be re-sampled -- the next push is a normal all-HIT push -- which
  is what made recording it in advance a control rather than paperwork.

AVAILABLE pre-merge, and record how each stood:
- **ROBUST-04.** `action-bundle-drift` (`ci.yml:99-116`) carries no `if:`, so it runs on pull requests.
  Record its result on this branch's PR. Also record the caveat that makes it load-bearing: a green
  `dogfood-verify` is NOT ROBUST-04 evidence, because the dogfood jobs build the action from source
  in-job and never execute the committed `start-cache-server/index.js`, which is what four of the five
  bundle sites run. `action-bundle-drift` is the only control tying them together.
- **VER-04's cwd / `GITHUB_WORKSPACE` identity on a real Windows runner.** Already MEASURED 2026-07-26
  (`research/v0.0.2/PROBE-RESULTS.md` Q2); this phase makes it ENFORCED rather than assumed, and
  `integration`'s Windows leg exercises the guard on every pull request. Record whether it stayed green.

INFORMATIONAL, push-gated, and deliberately not a gate:
- **VER-05's resolved compression method per runner image.** Read from both `publish` legs' summaries
  after the merge. Surfaced, never gated -- no branch reads the value, so a surprising value is a
  finding to record rather than a failure.

If any item did NOT close, state it as open with the reason. An honest residual beats a claimed
observation.

STEP 2 -- write `## Recorded corrections`. Every miscount and stale citation this phase encountered,
recorded rather than silently fixed, so the verifier and the milestone audit's three-source
cross-reference do not read a content-located edit as unauthorised drift. At minimum:

- **The requirement list.** `gsd-tools query init.plan-phase 9` returns NINE; the authoritative list is
  ELEVEN. `OBS-04` and `DOCS-08` are dropped because ROADMAP's `**Requirements**:` line wraps at
  `:256-257` and the query stops at the newline. Phase 8 lost `CORR-03`/`CORR-04` to the identical
  defect.
- **ROADMAP.md is wrong twice more for this phase.** `:575` says 8, and the traceability rows at
  `:537-544` carry only eight, omitting `PARITY-08`, `VER-07` and `ROBUST-04`. REQUIREMENTS.md is
  self-consistent at eleven. Coverage was audited against REQUIREMENTS.md's own words throughout.
- **ROBUST-04's sidecar-job count.** `REQUIREMENTS.md:335` and ROADMAP SC1d say four; there are FIVE
  (`ci.yml:236`, `:310`, `:357`, `:432`, `:895`), and `integration` is a two-leg matrix so six
  job-legs.
- **DOCS-08's site count.** DOCS-08 names four sites-in-scope; the real shape is three corrections plus
  three additive preconditions = six sites touched, and `docs/advanced.md`'s fault-degradation line
  fails DOCS-08's own membership criterion for a correction (D-32).
- **The construction census.** `09-RESEARCH.md` says 15 constructions in
  `actions-cache-backend.spec.ts` and 16 overall while listing sixteen line numbers, and marks
  `select-backend.spec.ts` "probably". Measured: 17 direct and 21 total across THREE files, with
  `select-backend.spec.ts` CONFIRMED.
- **`dogfoodBody`'s call sites.** `09-CONTEXT.md`'s canonical-refs line cites "(`:224`, `:276`)"; there
  is ONE call, at `:224`, and `:276` is a reuse of its buffer (C-01).
- **The `@actions/cache` bump checklist does not exist.** VER-05 and D-13 both direct a note to it;
  zero hits repo-wide. The note went into `pinned-deps.spec.ts`'s `@actions/cache` assertion instead.
- **`ci.yml` line-number drift.** Phase 8 inserted `hash-parity` (`:573`) and `hash-parity-compare`
  (`:742`), so REQUIREMENTS' cited `ci.yml:577-583` now lands inside the hash-parity job. Every DOCS-08
  site was located by phrase.
- **`packages/github-cache/project.json` EXISTS**, falsifying the premise Phase 8's D-12 and Phase 7's
  D-02 both assert. Recorded again here because PARITY-08's merged-configuration guard depends on it.
- **U-01 CLOSED, D-01 stands.** Nx 23.1.0 tolerates a foreign `.tar` in `.nx/cache` on every axis
  measured, with a positive control proving the instrument fires. The version is recorded beside the
  literal's comment lock.

Also record, in one line each, the two facts a future reader would otherwise misread:
- this phase's commit 09-03 is the milestone's **rotation window 2 of 3** (Phase 7's inferred `lint`
  target was 1; Phase 10's CORR-02 asset rename is 3), so its all-MISS push is expected and no tripwire
  authored here may fire on it;
- the both-legs all-MISS comes from the PATH change, not from `enableCrossOsArchive` -- which alone
  rotates only WINDOWS entries.

If `npm run test` failed once during this phase for an unattributed reason, record it here with what is
and is not known and cross-reference
`.planning/phases/08-nx-task-hash-parity/deferred-items.md`; do not guess at the cause.

STEP 3 -- before committing, confirm `git config user.email` is the approved PUBLIC address. This is a
public repository and this is the phase's closing commit. Then commit `09-EVIDENCE.md` on its own,
staged by name.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <automated>git config user.email</automated>
    <automated>npm run test &amp;&amp; npm run lint &amp;&amp; npm run check:action &amp;&amp; npm run pack:check</automated>
    <human-check>Confirm the two `human_needed` items are understood as open: VER-06's cross-OS read-back and OBS-04's one-time rotation signal, both readable only on the first `main` push after the merge. Neither has a pre-merge check and neither may be marked closed until you have read the real run.</human-check>
    <human-check>Confirm the snapshot's shard-asset and Actions-cache lists look plausible for the current month, and that no uploader identity, signed URL or work-domain string appears in the committed file.</human-check>
  </verify>

  <acceptance_criteria>
- `09-EVIDENCE.md` contains a `## Live-CI closures` section with a row per item: VER-06 and OBS-04 as
  OPEN / `human_needed` at the merge with the push-filter reason cited; ROBUST-04 and VER-04 with their
  pre-merge results; VER-05 as informational and push-gated.
- The VER-06 row states that a MISS fails the job and that a green Windows leg IS the proof.
- The OBS-04 row states the signal is once-only and points at `09-ROTATION-SIGNAL.md`.
- The ROBUST-04 row records that a green `dogfood-verify` is NOT ROBUST-04 evidence, with the
  from-source reason.
- `09-EVIDENCE.md` contains a `## Recorded corrections` section covering all ten named items plus the
  two one-line reader notes.
- Nothing is stated as closed that is open, and any item that did not close is stated as open with its
  reason.
- `git config user.email` returns the approved public address.
- The commit contains exactly `09-EVIDENCE.md`, staged by name.
- `npm run test`, `npm run lint`, `npm run check:action` and `npm run pack:check` exit 0.
- No source file, workflow file, or `start-cache-server/index.js` is modified by this plan.
  </acceptance_criteria>

  <done>
The phase closes with its two open items named rather than claimed, and every citation a later audit
would trip over is recorded with the count that is right.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live GitHub API responses -> a permanently committed public file | The response carries uploader identities, node ids and signed URLs. Anything pasted is public and permanent, in a repository where the maintainer's contact surface is deliberately narrow. |
| a perishable measurement -> a Phase 11 proof taken later | Once the merge lands, this data cannot be re-derived. A snapshot that omitted a field is not a snapshot that can be topped up. |
| "the phase is complete" -> two clauses no pre-merge run can produce | A phase that claimed VER-06 and OBS-04 closed would hand the milestone a proof that was never observed. |
| evidence preservation -> the proof it preserves evidence for | The natural next step from this data is exactly the O1 proof that belongs to a later phase and a requirement not in this phase's list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-52 | Information disclosure | raw `gh api` JSON committed to a public repository | high | mitigate | Only the named fields are captured -- asset `name`/`created_at`/`size`/`label` and cache `key`/`ref`/`created_at`/`last_accessed_at`/`size_in_bytes` -- rendered as tables. No raw JSON, no uploader identity, no node id, no signed URL. A `<human-check>` confirms it before the commit stands. |
| T-09-53 | Information disclosure | a work email or bare work domain in a public commit | high | mitigate | The record carries no contact data by construction, an acceptance criterion asserts the only email-shaped token present is the approved public one, and `git config user.email` is verified before the closing commit. Detection is by allowlist-inversion; the forbidden value is never written down. |
| T-09-54 | Repudiation | claiming VER-06 or OBS-04 closed pre-merge | high | mitigate | Both are recorded as OPEN / `human_needed` with the `main`-only push filter and both `if:` gates cited. No plan in this phase authors an acceptance check either could satisfy, and two `<human-check>` items make the maintainer's reading the closure event. |
| T-09-55 | Repudiation | a once-only signal lost because nobody read it | high | mitigate | The prediction is already committed in `09-ROTATION-SIGNAL.md` and this record names WHERE to read it and WHEN, with the once-only nature stated. The next push after the signal is a normal all-HIT push, so a missed reading is unrecoverable. |
| T-09-56 | Tampering | the snapshot growing into the O1 proof | medium | mitigate | Stated explicitly in the section framing and in this plan's must-haves: TEST-08 is a Phase 11 requirement and is NOT in this phase's eleven. The record preserves an input and performs no attribution. |
| T-09-57 | Repudiation | a content-located edit read as unauthorised drift | medium | mitigate | `## Recorded corrections` lists every stale citation and miscount with the correct value, including the `ci.yml` drift caused by Phase 8's insertions, so the verifier and the milestone audit can reconcile the cited numbers with the real locations. |
| T-09-58 | Denial of service | a 404 on the shard release read as a failure | low | mitigate | An absent shard is recorded verbatim as the result. Widening the query to another month to produce data would substitute a different measurement for the one requested, which is worse than an honest empty snapshot. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package, no script, no dependency. Two read-only `gh api` calls and one markdown file. `gh` is pre-existing tooling and no flags are guessed -- the endpoint path and pagination shape are confirmed from `gh api --help` or the REST reference before running. |
</threat_model>

<verification>
- All NINE battery commands green at this plan's commit: `format:check`, `build`, `typecheck`,
  `typecheck:action`, `test`, `lint`, `fallow:ci`, `check:action`, `pack:check`.
- `git diff` for this plan touches exactly
  `.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md`.
- No file under `packages/github-cache/`, no workflow file, and not `start-cache-server/index.js` is
  modified by this plan.
- The snapshot's capture timestamp precedes any merge of this branch to `main`.
- `git config user.email` is the approved public address at the commit.
- VER-06 and OBS-04 are recorded as `human_needed`, not as satisfied.
</verification>

<success_criteria>
- The producer-attribution snapshot exists, is dated, records the named fields for both lists, and is
  scoped explicitly as evidence preservation rather than as the O1 proof.
- Every Live-CI item carries a reference or an explicit open-with-reason, and the two merge-gated ones
  are named as `human_needed`.
- Every miscount and stale citation this phase encountered is recorded with the correct value.
- Nothing in the record is claimed that was not observed.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See plan 09-01's identical section for the full phase-wide list. This plan contributes:

**New files**
- `.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md` -- with
  `## Producer-attribution snapshot (D-34)`, `## Live-CI closures` and `## Recorded corrections`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-os-invariant-actions-cache-version/09-07-SUMMARY.md` when done.
</output>
