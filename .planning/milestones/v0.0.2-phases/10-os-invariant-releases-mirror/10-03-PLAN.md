---
phase: 10-os-invariant-releases-mirror
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .github/workflows/ci.yml
  - packages/github-cache/src/dogfood-cross-os.spec.ts
  - packages/github-cache/src/docs-same-os-claims.spec.ts
autonomous: true
requirements: [XOS-07, OBS-03]
must_haves:
  truths:
    - The `publish` job declares `needs: [build, typecheck, test, integration]`, so one
      default-branch push mirrors that push's full task set instead of racing job completion.
    - A spec asserts all four dependency names AND that `build` survived, because a `toMatch`
      against a `needs:` list is satisfied by any superset.
    - The `needs: build (NOT test)` claim in the `publish` job's leading comment is CORRECTED
      with a supplied replacement reason (`!cancelled()`), not merely deleted.
    - The retraction guard's scan scope covers every file this phase edits that exists today.
  artifacts:
    - .github/workflows/ci.yml (publish job `needs:` + its leading comment)
    - packages/github-cache/src/dogfood-cross-os.spec.ts (the first `needs:` VALUE guard in
      the repo, with its non-vacuity positive control)
    - packages/github-cache/src/docs-same-os-claims.spec.ts (the `!cancelled()` replacement
      reason row, and the widened `EDITED_FILES` scope)
  key_links:
    - `jobBlock('publish')` -> the `publish` job's own block. `jobBlock` throws when the job
      key is absent, so a renamed or deleted job fails loud here instead of silently
      satisfying a match.
    - `nx.json`'s `targetDefaults.test.inputs` already lists
      `{workspaceRoot}/.github/workflows/ci.yml` (PARITY-08), so both specs re-run when
      `ci.yml` changes. Without that input a `ci.yml` edit replays a PASS computed before the
      edit existed.
  prohibitions:
    - MUST NOT assert the `needs:` list with a bare `toMatch` for one member and stop there --
      any superset satisfies it, so `needs: [integration]` alone would pass. Pin every member
      AND pin that `build` is still present.
    - MUST NOT put a COMMENT phrase assertion in `dogfood-cross-os.spec.ts`. Its `codeLines`
      reader strips every `#` line, so `jobBlock` structurally cannot see a comment; a
      comment lock placed there is vacuous by construction.
    - MUST NOT put a YAML VALUE assertion in `docs-same-os-claims.spec.ts`. Its `read(file)`
      is raw, so a value match there is also satisfied by `ci.yml`'s own comments.
    - MUST NOT build a third `ci.yml` harness. Exactly two exist and each answers exactly one
      of the two question kinds.
    - MUST NOT delete the existing `needs: build (NOT test)` justification without supplying a
      replacement reason. Deleting it leaves a future reader holding a documented argument for
      narrowing `needs:` straight back (Phase 9's hardest-won lesson).
    - MUST NOT add a file to `EDITED_FILES` that does not exist yet. `read(file)` is a
      `readFileSync` and throws on a missing path; `mirror-seed.ts` and
      `release-asset-name.integration.spec.ts` are created by later plans and are added to the
      scope by those plans.
    - MUST NOT spell, in any new `forbidden` regex, the phrase it forbids. Use the
      single-character character-class trick already used by every `forbidden` row in that
      file -- read one of those rows and copy the technique. Spelling the phrase plants it in
      the very file that proves it is gone.
    - MUST NOT touch `max-parallel: 1` or its comment here. XOS-06's comment lock is only TRUE
      once `read-back.ts` carries the label assertion, so plan 10-05 owns it.
  human_needed:
    - "LIVE CI (push-to-`main` only): one default-branch push must mirror that push's FULL
      task set under the widened `needs:`. `publish` is `push`-gated to `main`, so NO PR run
      samples it at any rate. A real runner must show: the `publish` job starting only after
      `integration (windows-11-arm)` completes, and a post-push
      `gh api repos/op-nx/github-cache/releases/tags/cache-mirror-<YYYYMM>` census containing
      every task hash from that push. There is deliberately NO pre-merge acceptance check for
      this -- any check that could pass pre-merge would be passing for the wrong reason."
---

<objective>
Make `publish` wait for every job that produces a mirrored entry, and stop the `publish`
job's comment from arguing against that.

Purpose: XOS-07. The race is MEASURED, not projected. On run `30400231720`,
`publish (ubuntu-24.04-arm)` enumerated the Actions cache at ~`21:21:11Z` while
`integration (windows-11-arm)` did not finish until `21:23:13Z` -- 122 seconds earlier -- and
the shard census carries the fingerprint: task hash `8059758544828235640` exists ONLY as
`8059758544828235640-windows`, never under `-linux`. The mirror is currently completing only
because the Windows publish leg happens to run second and slowly. Cite that run; it is
stronger than the requirement's own hypothetical framing.

Output: the widened `needs:`, its corrected comment with a supplied replacement reason, the
repo's FIRST `needs:` value guard, and the retraction guard's widened scan scope.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md
@.planning/phases/10-os-invariant-releases-mirror/10-PATTERNS.md
@.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / value | Location |
|------|----------------|----------|
| changed CI value | `needs: [build, typecheck, test, integration]` (was `needs: build`) | `.github/workflows/ci.yml`, `publish` job |
| new spec describe | the `publish` job `needs:` VALUE guard plus its non-vacuity positive control | `packages/github-cache/src/dogfood-cross-os.spec.ts` |
| new drift-table row | the `!cancelled()` replacement-reason row | `DOCS_08_SITES` in `packages/github-cache/src/docs-same-os-claims.spec.ts` |
| widened constant | `EDITED_FILES` gains this phase's already-existing edited source files | `packages/github-cache/src/docs-same-os-claims.spec.ts` |

## Line-number drift -- located by CONTENT and JOB NAME, never by an inherited citation

Recorded so the verifier does not read a content-located edit as unauthorised drift. Every
citation in this phase's requirements predates ~220 lines of `ci.yml` churn plus Phase 9's
edits; the research measured drift up to **+416 lines**.

| Cited as | ACTUAL location at research time | Drift | How to locate |
|----------|----------------------------------|-------|---------------|
| `ci.yml:1003-1115` (`publish`) | `publish:` at `:1003`; block `:1003-1101`; leading comment starts `:983` | 0 | job key `^  publish:$` |
| `ci.yml:1117-1150` (`publish-verify`) | `publish-verify:` at `:1117`; block `:1117-1137` | 0 | job key |
| `ci.yml:336-337` (platform discriminator) | `:384` | +47 | phrase `real OS surface` |
| `ci.yml:577-583` (DOCS-08 site 2) | `:990`, `:992`, `:995` | **+413** | phrase `keep BOTH legs` |
| `ci.yml:652` (captured terminal output) | `:1068` | **+416** | phrase `TERMINAL OUTPUT` |
| `ci.yml:693` (Phase-10-owned same-OS claim) | `:1109` | **+416** | phrase `own-OS asset` |
| `nx.json:69` (PARITY-08 `ci.yml` test input) | `:69` | 0 | content |

<tasks>

<task type="auto">
  <name>Task 1: Widen the publish job's needs and correct the comment that argued against it</name>

  <files>.github/workflows/ci.yml</files>

  <read_first>
    - `.github/workflows/ci.yml` -- the `publish` job located by the job key `^  publish:$`,
      and its ENTIRE leading comment block above it (the comment starts well before the job
      key; read up to the previous job's end).
    - `.planning/REQUIREMENTS.md` XOS-07 (the authoritative text).
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-16.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `### XOS-07` and the
      Assumptions Log entry A-1.
    - The `build`, `typecheck`, `test` and `integration` job blocks in `ci.yml`, to confirm
      first-hand that none of them declares a `needs:` that would create a cycle.
  </read_first>

  <action>
    Change the `publish` job's `needs: build` to `needs: [build, typecheck, test, integration]`.

    Then REWRITE the clause in the leading comment that currently argues for the narrow
    dependency (`needs: build (NOT test): a failing test leg must never skip the mirror and
    drop already-built entries`). Locate it by that phrase, never by a line number.

    The rewrite must supply a REPLACEMENT reason rather than merely deleting the old one --
    deleting a justification leaves a future reader holding a documented argument for
    narrowing `needs:` straight back. It must carry, in this order:
    1. The old concern is still real and is now handled by a DIFFERENT mechanism: the job
       carries `if: ${{ !cancelled() && github.event_name == 'push' }}`, and `!cancelled()`
       is GitHub's documented way to run a job even when a `needs:` dependency FAILED (not
       merely when it was skipped). So a failing `test` leg does NOT skip the mirror.
    2. Why the widening is needed at all, with the measurement: on run `30400231720`,
       `publish (ubuntu-24.04-arm)` enumerated the Actions cache ~122 seconds BEFORE
       `integration (windows-11-arm)` finished, so the Windows `integration` entry did not yet
       exist when the enumeration snapshot was taken -- and the shard shows task hash
       `8059758544828235640` present ONLY under a `-windows` suffix. Each leg's view of the
       Actions-cache key set is a SINGLE `listCacheEntries()` call taken at its publish step's
       start and is never re-read (`publish-mirror.ts`, the `const entries` line), so the set a
       leg can mirror is a function of that leg's start time.
    3. The failure mode if the `!cancelled()` citation is ever wrong or changes: `publish`
       gets SKIPPED on a red-`test` push, which is a mirror GAP and never a wrong artifact.
       Say this explicitly -- it is the bounded downside and it is the thing a future reader
       needs in order to judge the trade.
    4. The wall-clock consequence: `publish` used to start ~40 s into the run and now waits
       for `integration (windows-11-arm)`, roughly 3 minutes. Acceptable for a push-only
       background mirror job.
    5. The downstream consequence, because it changes which pushes sample OBS-05:
       `publish-verify` carries `if: github.event_name == 'push'` with NO `!cancelled()`, so it
       is skipped whenever `publish` fails. Widening `needs:` makes `publish` run on a
       red-`test` push, so `publish-verify` now runs there too. That is desirable and is worth
       naming rather than discovering.

    Do NOT touch `max-parallel: 1`, its comment, the shard-growth estimate, or the
    `publish-verify` job comment in this task. Those belong to plans 10-05 and 10-07, and each
    is only TRUE after a change those plans make.

    ASCII only.
  </action>

  <acceptance_criteria>
    - The `publish` job declares all four dependencies. Locate the job by the key
      `^  publish:$` and read the `needs:` line; assert it names `build`, `typecheck`, `test`
      and `integration`.
    - `build` is still present -- assert it explicitly. A `needs:` list that dropped `build`
      would satisfy a naive check for `integration`.
    - No `needs:` cycle: confirm by reading that `build`, `typecheck`, `test` and `integration`
      each declare no `needs:` of their own.
    - The corrected comment carries the replacement mechanism:
      `rg -c 'cancelled' .github/workflows/ci.yml` >= 1 within the `publish` job's leading
      comment (locate the comment block first, then count within it).
    - The corrected comment carries the bounded failure mode. Assert on the CONTENT: the
      comment states that the consequence of the mechanism changing is a skipped mirror rather
      than a wrong artifact.
    - The corrected comment cites the measurement by run id:
      `rg -c '30400231720' .github/workflows/ci.yml` >= 1.
    - The old narrow-dependency argument no longer stands unqualified. Assert on the surviving
      CONTENT (the four clauses above), not on the absence of a token -- an absence check here
      would be satisfied by deleting the whole comment, which is the failure this
      requirement's own history warns about.
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache` passes. Note the existing
      `docs-same-os-claims.spec.ts` rows already read `ci.yml`; confirm none of them reddened.
    - `git diff -- .github/workflows/ci.yml` shows changes ONLY inside the `publish` job's
      `needs:` line and its leading comment block. Any other hunk is out of scope for this
      task.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- dogfood-cross-os docs-same-os-claims</automated>
  </verify>

  <done>
    `publish` waits on every producer, and the comment that used to argue for the narrow
    dependency now documents `!cancelled()`, the measured race, the bounded failure mode, and
    the wall-clock and `publish-verify` consequences.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The repo's first needs: VALUE guard, with a superset-proof clause</name>

  <files>packages/github-cache/src/dogfood-cross-os.spec.ts</files>

  <read_first>
    - `packages/github-cache/src/dogfood-cross-os.spec.ts` -- the `codeLines` reader (note it
      FILTERS OUT every line whose trimmed form starts with `#`), the `jobBlock(name)` helper
      including its throw-on-absent-job behaviour, and the existing "scopes to a real,
      non-empty job block" non-vacuity control.
    - `.planning/phases/10-os-invariant-releases-mirror/10-PATTERNS.md` Tier 3 item 10 --
      including its closing note that the analog asserts a job is NARROW while this assertion
      is that a list is WIDE, and why the positive control matters MORE in the wide direction.
    - `nx.json` -- confirm `{workspaceRoot}/.github/workflows/ci.yml` is in
      `targetDefaults.test.inputs`, or this guard serves a stale cached PASS.
  </read_first>

  <behavior>
    - `jobBlock('publish')` returns a non-empty block: it matches the `publish` job's
      `if:` expression, proving the extraction captured the right job before anything is
      asserted about its contents.
    - The extracted block's `needs:` value names `build`, `typecheck`, `test` and
      `integration` -- each asserted individually, so dropping any ONE reddens.
    - Reverting the value to `needs: build` reddens three of those four assertions and leaves
      the `build` one green. Predict that split; it is the evidence the guard is not a
      superset-satisfied tautology.
  </behavior>

  <action>
    Add a new `describe` to `dogfood-cross-os.spec.ts` asserting the `publish` job's `needs:`
    VALUE. Reuse the module's existing `jobBlock` helper -- do NOT extract it to a shared
    module, and do NOT build a second job-block parser. Record in a comment why this guard
    lives in this file: `jobBlock` is the only such helper in the repo, extracting it would be
    a new mechanism, and RESEARCH's Don't-Hand-Roll table names that as the smell.

    The describe must carry:
    - A positive control FIRST, copying the shape of the existing non-vacuity control: assert
      `jobBlock('publish')` matches something that is unambiguously the publish job (its
      `if:` expression), so no later assertion can be satisfied by an empty string.
    - FOUR separate assertions, one per dependency name, each anchored to a word boundary so
      `test` cannot be satisfied by `typecheck` or by a substring of another token.
    - The `build`-SURVIVES clause called out by name in a comment: a `toMatch` against a
      `needs:` list is satisfied by any SUPERSET, so `needs: [integration]` alone would pass a
      check that only looked for `integration`. Asserting each of the four independently is
      what closes that hole.
    - A comment recording that this guard did NOT exist before this phase, so before it,
      reverting XOS-07's widening reddened NOTHING. State that plainly rather than implying
      prior coverage.

    Put NO comment-phrase assertion here. `codeLines` strips `#` lines, so a comment lock
    placed in this file is vacuous by construction; the comment locks go in
    `docs-same-os-claims.spec.ts`, whose `read(file)` is raw.

    RED first: run the new describe against a `ci.yml` with `needs: build` restored (or
    against the pre-task-1 state if this task is executed before task 1 lands) and record that
    three of the four dependency assertions failed. That is the assertion-level RED, and its
    3-of-4 shape is the superset proof.
  </action>

  <acceptance_criteria>
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache -- dogfood-cross-os` passes.
    - The new describe contains a positive control that asserts `jobBlock('publish')` is
      non-empty and is the publish job:
      `rg -c "jobBlock\('publish'\)" packages/github-cache/src/dogfood-cross-os.spec.ts` >= 2
      (the control plus at least one subject assertion).
    - Each of the four dependency names is asserted separately -- read the describe and confirm
      four distinct assertions, not one combined regex over the whole list.
    - MUTATION CHECK, run and recorded in the SUMMARY: reverting `ci.yml` to `needs: build`
      reddens exactly three of the four dependency assertions (the `build` one stays green)
      and reddens nothing else. Restore `ci.yml` afterwards and confirm the suite returns to
      green.
    - MUTATION CHECK 2, run and recorded: renaming the `publish` job key makes `jobBlock`
      THROW rather than returning an empty string that trivially satisfies the assertions.
      Restore afterwards.
    - `rg -c 'max-parallel' packages/github-cache/src/dogfood-cross-os.spec.ts` returns 0 --
      XOS-06's value guard belongs to plan 10-05 and adding it here would collide.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- dogfood-cross-os</automated>
  </verify>

  <done>
    Reverting XOS-07's widening now reddens a spec, and the guard is proven superset-proof by a
    predicted 3-of-4 mutation split rather than asserted to be.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the replacement reason as a drift row and widen the retraction scope</name>

  <files>packages/github-cache/src/docs-same-os-claims.spec.ts</files>

  <read_first>
    - `packages/github-cache/src/docs-same-os-claims.spec.ts` in full -- the header (its
      requirement-miscount reconciliation, its line-number-drift rationale, and its HARD
      DEPENDENCY note about `nx.json`'s `test` inputs), one complete `DOCS_08_SITES` row
      including its rationale doc-comment, the `required` / `forbidden` mechanics, the
      `bucket` field, the `EDITED_FILES` constant, and the `retracted` regex at the bottom.
    - `.planning/phases/10-os-invariant-releases-mirror/10-PATTERNS.md` Tier 3 item 7 -- the
      four load-bearing mechanics, including the self-evading `forbidden` regex technique and
      the `forbidden: []` present-not-absent rule on additive rows.
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-21.
  </read_first>

  <behavior>
    - A new `DOCS_08_SITES` row keyed on `.github/workflows/ci.yml` requires BOTH the
      corrected `needs:` claim AND its replacement justification, so a bare deletion of either
      half reddens.
    - `EDITED_FILES` covers every already-existing source file this phase edits, so the
      `retracted` producer-attribution scan reaches all of them.
    - Deleting the replacement justification from `ci.yml` while leaving the widened `needs:`
      in place reddens the new row.
  </behavior>

  <action>
    Add ONE `DOCS_08_SITES` row, `bucket: 'correction'`, `file: '.github/workflows/ci.yml'`,
    whose `required` array carries the phrases task 1 wrote: the `!cancelled()` mechanism, the
    bounded skipped-mirror failure mode, and the cited run id. Model the row's rationale
    doc-comment on the existing site-2 row, which records WHY a bare deletion would be
    destructive -- here: the old narrow-`needs:` argument was a documented case for undoing
    XOS-07, so the row requires the replacement reason to be present, not merely the old
    argument to be gone.

    If the row needs a `forbidden` entry, build it with the single-character character-class
    technique every existing `forbidden` regex in this file uses, and carry a one-line comment
    saying why the spelling is contorted -- so a later tidy does not "fix" it and thereby plant
    the forbidden phrase in the file that proves it is absent. If no `forbidden` entry is
    warranted, set `forbidden: []` EXPLICITLY: the property must be present, because `as const`
    would otherwise drop it and break the destructure.

    Then widen `EDITED_FILES` so the `retracted` producer-attribution scan covers this phase's
    edits. Add ONLY files that exist right now:
    `packages/github-cache/src/publish/publish-mirror.ts`,
    `packages/github-cache/src/action/index.ts`,
    `packages/github-cache/src/roundtrip/read-back.ts`,
    `packages/github-cache/src/lib/release-asset-name.ts`,
    `packages/github-cache/src/lib/cache-key.ts`,
    `packages/github-cache/src/cleanup/cleanup.ts`.
    Do NOT add `packages/github-cache/src/lib/mirror-seed.ts` or
    `packages/github-cache/src/lib/release-asset-name.integration.spec.ts` -- neither exists
    yet, `read(file)` is a `readFileSync`, and it throws on a missing path. Add a comment
    stating that later plans in this phase extend this list when they create those files, so
    the omission reads as sequencing rather than as an oversight.

    Paths in this constant are resolved from `WORKSPACE_ROOT_URL`, so they are repo-relative;
    match the existing entries' style exactly.
  </action>

  <acceptance_criteria>
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache -- docs-same-os-claims` passes.
    - The new row exists and is a correction:
      `rg -c "bucket: 'correction'" packages/github-cache/src/docs-same-os-claims.spec.ts`
      is one greater than before this task.
    - The row requires the replacement mechanism:
      `rg -c 'cancelled' packages/github-cache/src/docs-same-os-claims.spec.ts` >= 1.
    - `EDITED_FILES` names all six added paths. Assert each one individually:
      `rg -c 'publish/publish-mirror\.ts' <spec>` >= 1, `rg -c 'action/index\.ts' <spec>` >= 1,
      `rg -c 'roundtrip/read-back\.ts' <spec>` >= 1,
      `rg -c 'lib/release-asset-name\.ts' <spec>` >= 1,
      `rg -c 'lib/cache-key\.ts' <spec>` >= 1, `rg -c 'cleanup/cleanup\.ts' <spec>` >= 1.
    - `EDITED_FILES` does NOT name a file that does not exist. For each entry, confirm the
      path resolves on disk; the guard would otherwise throw rather than fail informatively.
    - MUTATION CHECK, run and recorded in the SUMMARY: deleting the replacement-reason clause
      from `ci.yml` (keeping the widened `needs:`) reddens exactly the new row's `required`
      assertions. Restore afterwards.
    - Every new `forbidden` regex, if any, carries the single-character character class and a
      comment explaining the contortion. Verify by reading, not by counting.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- docs-same-os-claims</automated>
  </verify>

  <done>
    The replacement reason is a drift-guarded row rather than prose, and the retraction scan's
    scope now covers this phase's existing edited sources.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow `needs:` graph -> `publish` job execution | job ORDERING only; it is not the write gate |
| `publish` job -> Actions cache (per-ref scope) | unchanged by this plan; the enumeration is still `ref`-scoped and still gated in-process by `isSyncTrusted` |
| Nx task -> sidecar -> mirrored Release asset | widening which JOBS gate `publish` does not change which TASKS route through the sidecar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Elevation of privilege | the widened `publish` `needs:` list | low | accept | `needs:` gates job ordering, never capability. The load-bearing write gate is `isSyncTrusted` re-checking the default branch IN-PROCESS (C2), plus the job-level `if:` on the push trigger. Neither is touched, and the job's `permissions` block is unchanged |
| T-10-14 | Denial of service | `publish` skipped on a red-`test` push if `!cancelled()` ever stops covering a FAILED dependency | medium | mitigate | RESEARCH A-1 records this as CITED from GitHub docs and NOT reproduced in this repo (no run on record has a failed `publish` ancestor). Bounded downside: a mirror GAP, never a wrong artifact. The mitigation is that the rewritten comment NAMES this failure mode, so a future operator diagnosing a missing mirror finds the cause written down |
| T-10-15 | Information disclosure | more jobs gating `publish` -> more task outputs reaching the anonymously-readable mirror | medium | accept | The set of tasks routed through the sidecar is UNCHANGED; widening `needs:` only removes a timing race that was already resolving in favour of mirroring them (measured on run `30400231720`). The governing invariant is unchanged: "no mirrored task ever prints a secret", NOT `::add-mask::`, which redacts logs only and never a cache payload. Offered as INPUT to TRUST-12's classification |
| T-10-16 | Repudiation | a guard that passes vacuously (`toMatch` on a superset; `jobBlock` on an empty string) | high | mitigate | A positive control asserted BEFORE any subject assertion, four independent per-dependency assertions, and two recorded mutation checks -- the 3-of-4 revert split and the job-rename throw. This is the failure mode that DISPROVED two Phase 9 guards that looked fine |
| T-10-SC | Tampering | npm / pip / cargo installs | low | accept | ZERO new packages. No `package.json` or lockfile edit; no installer runs in this plan |
</threat_model>

<verification>
- `npx nx run @op-nx/github-cache:test --skip-nx-cache` green, with `dogfood-cross-os` and
  `docs-same-os-claims` both exercised.
- `npm run typecheck` and `npm run lint` green.
- All three mutation checks run, predicted in advance, and recorded in the SUMMARY with their
  observed redness.
- `git diff --stat -- start-cache-server/index.js` prints nothing: no `serve()`-reachable
  source is touched, so the bundle delta must be ZERO.
</verification>

<success_criteria>
- Reverting XOS-07's widening reddens a spec. Before this plan it reddened nothing.
- The `publish` job's comment documents the mechanism, the measurement, the bounded failure
  mode, and the two downstream consequences.
- The retraction scan reaches every source file this phase has already edited.
</success_criteria>

<output>
Create `.planning/phases/10-os-invariant-releases-mirror/10-03-SUMMARY.md` when done.
</output>
