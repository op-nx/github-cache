---
phase: 11-live-proofs-o1-o2-o3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - capture-hashes.mjs
  - read-integration-hash.mjs
  - .planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json
autonomous: true
requirements: [TEST-08]

must_haves:
  truths:
    - "Running the new capture-hashes mode resolves the Windows leg's actual command (nx run-many -t integration) and asserts, over the RESOLVED task graph, that no build, typecheck or test task appears in it (D-12)."
    - "The same run carries a negative control that DOES resolve a forbidden target, so a resolver that resolves nothing cannot pass (D-13)."
    - "The assertion output is written to a JSON record on disk, not printed and discarded (TEST-08's own words)."
    - "A committed root-level reader extracts the integration task hash and cacheStatus from .nx/cache/run.json and THROWS on absence rather than yielding an empty hash (D-17 step b)."
    - "Neither edit rotates any Nx task hash, so the perishable O1/O2 window is untouched (D-10 row 4, D-11)."
  artifacts:
    - "capture-hashes.mjs with an --assert-graph-premise mode"
    - "read-integration-hash.mjs at the workspace root"
    - ".planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json"
  key_links:
    - "capture-hashes.mjs --assert-graph-premise -> createTaskGraph(projectGraph, {}, [PROJECT], ['integration'], undefined, {}) -> Object.keys(taskGraph.tasks)"
    - "read-integration-hash.mjs -> .nx/cache/run.json tasks[].target === 'integration' -> integration-hash.txt + a printed cacheStatus"
    - "both files are workspace-root, so they match no nx.json workspace-root input and rotate nothing"
---

<objective>
Build the two hash-neutral instruments the rest of the phase depends on, BEFORE the perishable
O1/O2 measurement window is touched.

1. TEST-08's mechanical premise assertion: a new `--assert-graph-premise` mode on the existing
   root-level `capture-hashes.mjs` that resolves the Windows CI leg's ACTUAL command and asserts
   over the RESOLVED task graph that it contains no `build`, `typecheck` or `test` task (D-12),
   paired with a mandatory non-vacuity negative control (D-13).
2. D-17 step (b)'s reader: a committed root-level `read-integration-hash.mjs` that lifts the
   `integration` task's hash and `cacheStatus` out of `.nx/cache/run.json` and throws on absence.

Purpose: the premise assertion is one of the four independent producer-attribution means
(D-14 row 1) and is the ONLY one that is structural rather than observational, so it must be
ASSERTED rather than assumed. The reader is what makes the O3 `ci.yml` work possible in wave 6
without adding a file under `packages/` (D-10 row 3 would rotate all four proof hashes).

Output: `capture-hashes.mjs` gains one flag; `read-integration-hash.mjs` is created;
`11-task-graph-premise.json` is the captured evidence TEST-08 requires.

Why this is wave 1: both files are workspace-root, and `nx.json` enumerates every workspace-root
input explicitly with no `{workspaceRoot}/*.mjs` glob, so BOTH edits rotate NOTHING (D-10 row 4,
independently re-derived by RESEARCH.md). D-11 therefore permits them to precede the perishable
local proof, and this plan is the only place in the phase where a code edit is free.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-CONTEXT.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-RESEARCH.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-PATTERNS.md
@capture-hashes.mjs
@nx.json
</context>

## Decisions implemented

| Decision | Where |
|---|---|
| D-04 (`.planning`-only commits rotate no hash) | the JSON record lands under `.planning/`, so it is free |
| D-10 (rotation table) | both edits are workspace-root and rotate nothing |
| D-11 (O1/O2 measured first; hash-neutral instrument may precede) | this plan is wave 1 precisely because it is hash-neutral |
| D-12 (new mode on `capture-hashes.mjs`, not a new spec) | task 1 |
| D-13 (mandatory negative control) | task 1, the `typecheck` control and the three non-vacuity assertions |
| D-14 (attribution: the graph premise is row 1 and is load-bearing) | task 1's record is what makes row 1 an assertion rather than an assumption |
| D-17 (step b: read the leg's own hash from `run.json`) | task 2 |

Deliberately NOT done here, with the reason, so it is not read as an oversight:
an every-commit Vitest version of the premise assertion. CONTEXT.md's `<deferred>` list defers it
with a reason (TEST-08 asks for a mechanical assertion whose output is captured, not a permanent
gate) and a spec under `packages/` would rotate three of the four proof hashes (D-10 row 2).
The instrument's own negative control is the runnable check that stands in its place, and it runs
on every invocation.

## Artifacts this phase produces

Every symbol Phase 11 creates. The plan-review source-grounding pass reads this list to exclude
newly-created symbols from drift verification.

**New CLI surface**
- `capture-hashes.mjs` flag `--assert-graph-premise` (plan 11-01)
- `capture-hashes.mjs` internal helpers `resolvedTaskIds`, `assertGraphPremise`, and the constants
  `FORBIDDEN_TARGETS` and `CONTROL_TARGET` (plan 11-01)

**New root-level files**
- `read-integration-hash.mjs` (plan 11-01)

**New `ci.yml` step names, in the existing `integration` job (plan 11-06)**
- `Run the integration target and tee its output`
- `Read this leg's integration hash from run.json`
- `Record the remote-cache label occurrence count for this leg`
- `Positive control on this leg's own key, which must return 200`
- `Record whether step debug logging is active`

**New `ci.yml` job and its step names (plan 11-06)**
- job `o3-witness`
- `Read H_linux from the ubuntu integration leg's record`
- `Assert the H_linux cache entry existed before the Windows integration step started`

**New CI artifact names (plan 11-06)**
- `integration-hash-${{ matrix.os }}`

**New files written at runtime, not committed (plans 11-06, 11-07)**
- `integration-hash.txt`, `integration-nx.log` (inside the `integration` job)
- `o3-witness.log` (inside the `o3-witness` job)

**New anchored success literal (plan 11-06)**
- `o3-witness: EXISTENCE OK`

**New spec surface (plan 11-05)**
- `packages/github-cache/src/dogfood-cross-os.spec.ts`: a new `describe` block
  `ci.yml o3-witness job exists and keeps its shape (XOS-03, TEST-09)`
- `packages/github-cache/src/docs-same-os-claims.spec.ts`: new additive `DOCS_08_SITES` rows keyed
  on `.github/workflows/ci.yml`

**New `.planning` artifacts**
- `.planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json` (plan 11-01)
- `.planning/phases/11-live-proofs-o1-o2-o3/11-hashes-warm.json` (plan 11-02)
- `.planning/phases/11-live-proofs-o1-o2-o3/11-hashes-cold.json` (plan 11-02)
- `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md` (plans 11-03, 11-04, 11-07)

## Multi-Source Coverage Audit

Every item from all four source artifacts, and the plan that covers it. No item is MISSING.

**GOAL (ROADMAP Phase 11 success criteria)**

| Item | Plan |
|---|---|
| SC1 cold Windows box logs `[remote cache]` for `build`, `typecheck`, `test`, named per target | 11-02, 11-03 |
| SC2 same run HITs `integration`, compared against the pre-rename baseline | 11-03 |
| SC3 producer attribution at proof time + the mechanical graph premise | 11-01, 11-04 |
| SC4(a) cite `H_linux != H_win` | 11-07 |
| SC4(b) Windows `integration` EXECUTED with no label, in a run where `nx-cache-<H_linux>` existed | 11-06, 11-07 |
| SC4(c) positive control returns 200 in the same job | 11-06, 11-07 |
| SC5 soundness probe timestamped as preceding; `ACTIONS_STEP_DEBUG` on; pre-registered counts; `Cache: n/m hit` marked non-discriminating | 11-03, 11-06, 11-07 |
| Scoping correction: allocate plan capacity for real `ci.yml` code and new tooling | 11-01, 11-05, 11-06 |

**REQ (REQUIREMENTS.md, the authoritative text)**

| ID | Plan |
|---|---|
| XOS-01 | 11-02, 11-03, 11-04 |
| XOS-02 | 11-02, 11-03, 11-04 |
| XOS-03 | 11-05, 11-06, 11-07 |
| TEST-08 | 11-01, 11-03, 11-04, 11-05, 11-06, 11-07 |
| TEST-09 | 11-05, 11-06, 11-07 |
| TEST-10 | 11-02, 11-03 |
| OBS-02 | 11-03, 11-07 |

**RESEARCH (11-RESEARCH.md findings that change the work)**

| Finding | Plan |
|---|---|
| U-01 answered YES; ship D-17 as written; do NOT build the dedicated-job fallback | 11-06 |
| `?key=` is a PREFIX match, compare `.key` for equality | 11-06 |
| `run.json` `cacheStatus` replaces `Cache: n/m hit` as the corroborator | 11-01, 11-03, 11-06, 11-07 |
| an `ACTIONS_STEP_DEBUG` SECRET overrides the VARIABLE; `runner.debug` is the better in-run check | 11-06, 11-07 |
| D-16 confirmed sound; acceptance set is `{200}` ALONE | 11-06 |
| D-19 is TWO spec edits (presence in `dogfood-cross-os`, prose in `docs-same-os-claims`) | 11-05 |
| the `typecheck` negative control is strictly stronger than `test` | 11-01 |
| a new/edited `.spec.ts` rotates `test`/`typecheck`/`integration`/`lint` but not `build` | 11-05 |
| editing root `package.json` rotates `test`, so use a flag not a new npm script | 11-01 |
| D-06's cold hashes are a live pre-flight, warm capture FIRST | 11-02 |
| `ci.yml`'s two `test`-inputs comment blocks are STALE | 11-06 |
| `dist/` survives `nx reset`, so no rebuild before the measurement | 11-03 |

**CONTEXT (CONTEXT.md D-02..D-24; D-00 and D-01 are `[informational]`)**

| Decision | Plan |
|---|---|
| D-02, D-06, D-07 | 11-02 |
| D-03 | 11-02, 11-04 |
| D-04 | 11-01, 11-02 |
| D-05 | 11-02, 11-03 |
| D-08, D-09 | 11-03 |
| D-10 | 11-01, 11-02, 11-04, 11-05, 11-06 |
| D-11 | 11-01, 11-02, 11-04 |
| D-12, D-13 | 11-01 |
| D-14 | 11-01, 11-03, 11-04 |
| D-15 | 11-04 |
| D-16 | 11-06 |
| D-17 | 11-01, 11-05, 11-06, 11-07 |
| D-18, D-20 | 11-07 |
| D-19 | 11-05, 11-06 |
| D-21 | 11-06, 11-07 |
| D-22 | 11-03, 11-04, 11-07 |
| D-23, D-24 | 11-03, 11-07 |

**Exclusions, which are not gaps:** every item on CONTEXT.md's `<deferred>` list, every row of
REQUIREMENTS.md `## Out of Scope`, and all of XOS-04 / XOS-05 / XOS-08 / DOCS-07, which ROADMAP
assigns to Phase 12.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the --assert-graph-premise mode to capture-hashes.mjs (D-12, D-13)</name>

  <files>capture-hashes.mjs, .planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json</files>

  <read_first>
    - `capture-hashes.mjs` in full. The three shapes to reuse are `parseArgs` (hand-rolled, throws
      a usage block on an unknown flag), `captureTargets` (the existing `createTaskGraph` call site
      and its missing-task throw), and the `--out` writer plus the mode dispatch at the file tail.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-PATTERNS.md` section 1, which excerpts all four
      of those shapes and names the trap (split the task id on the LAST colon).
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-RESEARCH.md` `## TEST-08 / D-12`, which gives the
      resolved-set call, the verified inferred `typecheck` dependsOn, and why the control must be
      `typecheck` rather than `test`.
    - `nx.json` targetDefaults for `integration`, `typecheck` and `test`, to confirm the resolved-set
      expectations are a property of the graph rather than of the config.
    - `packages/github-cache/src/nx-target-inputs.spec.ts` around the two non-vacuity controls, for
      the in-repo statement of the vacuity trap D-13 exists to avoid.
  </read_first>

  <behavior>
    Write these expectations down before implementing; they are the mode's contract.

    - Given the resolved task graph for `targets = ['integration']` (the Windows leg's actual
      command, `npm run integration` = `nx run-many -t integration`), the emitted task-id set is
      NON-EMPTY. An empty set fails.
    - No member of that set has a target segment equal to `build`, `typecheck` or `test`. The target
      segment is the substring after the LAST colon, because the project name is scoped
      (`@op-nx/github-cache`) and a future `@op-nx/github-cache:build-deps` would false-positive a
      substring match.
    - Given the resolved task graph for `targets = ['typecheck']` (the negative control), the
      emitted set has EXACTLY two members, `<PROJECT>:build` and `<PROJECT>:typecheck`, where
      `<PROJECT>` is the derived project name. Both membership and the count-of-two are asserted,
      because it is a property of the RESOLVED graph and an Nx upgrade that changes the inferred
      dependsOn must fail loud rather than quietly weaken the control.
    - The control set INTERSECTS the forbidden set. This is the clause that makes the absence
      assertion over the `integration` set meaningful.
    - The control set DIFFERS from the `integration` set. A resolver returning the same thing for
      every input fails here.
    - Every failure message enumerates the whole observed set, mirroring the existing missing-task
      throw.
    - `--assert-graph-premise` is mutually exclusive with `--install-mode` and rejects it with a
      message saying why (the mode measures no hash, so accepting an install-mode value would make
      the record lie). It is COMPATIBLE with `--out`, because TEST-08 requires the assertion output
      captured as evidence and `--out` is the existing evidence channel.
  </behavior>

  <action>
    Extend `capture-hashes.mjs` with a fourth mode. Concretely:

    Add `assertGraphPremise: undefined` to `parseArgs`'s `parsed` object, one `if (flag ===
    '--assert-graph-premise')` branch that sets it to `true` and does NOT consume a following
    argument, and one usage line reading
    `node capture-hashes.mjs --assert-graph-premise [--out <path>]`
    inside the existing unknown-flag throw. Keep the hand-rolled parser; do not add an argument
    library (the existing `ponytail: three flags, no arg library.` comment is the house posture).

    Add module-level constants `FORBIDDEN_TARGETS = ['build', 'typecheck', 'test']` and
    `CONTROL_TARGET = 'typecheck'`. Do not spell the project name anywhere: reuse the existing
    derived `PROJECT` constant.

    Add `function resolvedTaskIds(projectGraph, targets)` that calls
    `createTaskGraph(projectGraph, {}, [PROJECT], targets, undefined, {})` and returns
    `Object.keys(taskGraph.tasks).sort()`. The second argument MUST stay `{}`; the existing comment
    at the `captureTargets` call site records the measured `flatMap is not a function` failure that
    passing `nxJson.targetDefaults` there produces.

    Add `async function assertGraphPremise(args)` that builds the project graph the same way
    `capture` does (`createProjectGraphAsync`), computes the `['integration']` set and the
    `[CONTROL_TARGET]` set via `resolvedTaskIds`, evaluates the six assertions in the `<behavior>`
    block, and throws with the full observed set on any failure. Split each task id on its LAST
    colon to get the target segment.

    On success, build a record with `mode` set to the string `assert-graph-premise`, a `meta` block
    carrying the same fields the existing capture record uses for provenance (commit, project, os,
    arch, node version, nx version, capturedAt), `forbiddenTargets`, an `integration` object holding
    the resolved command string and its task-id set, a `control` object holding the same for the
    control target, and a `verdict` field whose value is the string `PREMISE OK`. Serialise it with
    the existing `--out`-or-stdout writer verbatim, and print the one-line human summary to stderr
    exactly as `capture` does, so the machine-readable record stays parseable on stdout.

    Wire the mode into the dispatch at the file tail: `--assert-graph-premise` branches before the
    `--diff` branch or beside it, and rejects `--install-mode` with its own reason, mirroring how
    `--diff` rejects it.

    Do NOT add an npm script. Root `package.json` is an `nx.json` `test` input, so a new script would
    rotate the `test` hash mid-window; `npm run capture:hashes -- --assert-graph-premise` already
    works because the existing script forwards `--` arguments.

    Then run the mode with `--out` pointed at
    `.planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json` and commit that record.
    TEST-08 requires the assertion OUTPUT captured as evidence, not discarded as a pre-flight check.

    Finally, prove the assertion can fail: temporarily add `integration` to `FORBIDDEN_TARGETS`,
    re-run, observe the predicted non-zero exit and the message naming the `integration` task, then
    revert the mutation. Record the observed failure message in the plan SUMMARY. A predicted-only
    redness is not evidence; this is the project's predict-then-observe-then-revert pattern.
  </action>

  <verify>
    <automated>node capture-hashes.mjs --assert-graph-premise --out .planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json &amp;&amp; node -e "const r=require('node:fs').readFileSync('.planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json','utf8');const j=JSON.parse(r);if(j.verdict!=='PREMISE OK')throw new Error('verdict='+j.verdict);if(j.control.taskIds.length!==2)throw new Error('control set size='+j.control.taskIds.length);if(j.integration.taskIds.length===0)throw new Error('integration set is empty');console.log('premise record OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `node capture-hashes.mjs --assert-graph-premise` exits 0 and writes a JSON record whose
      `verdict` field is the exact string `PREMISE OK`.
    - The record's `integration.taskIds` array is non-empty and contains no entry whose
      post-last-colon segment is `build`, `typecheck` or `test`.
    - The record's `control.taskIds` array has exactly 2 entries, and they are the derived project
      name suffixed with `:build` and `:typecheck`.
    - `node capture-hashes.mjs --assert-graph-premise --install-mode ci` exits non-zero with a
      message naming the mutual exclusion and its reason.
    - `node capture-hashes.mjs --bogus-flag` still prints the usage block and that block now lists
      the `--assert-graph-premise` form.
    - With `integration` temporarily added to `FORBIDDEN_TARGETS`, the mode exits non-zero and the
      message enumerates the observed `integration` task-id set. The mutation is reverted and
      `git diff -- capture-hashes.mjs` shows no trace of it in the committed state.
    - `git status --porcelain packages/ .github/ start-cache-server/` prints nothing: this task
      touches no hashed input under those paths.
  </acceptance_criteria>

  <done>
    `capture-hashes.mjs` has a fourth mode that asserts TEST-08's premise over the resolved task
    graph, its non-vacuity control is proven to bite by an observed-then-reverted mutation, and the
    captured record is committed under `.planning/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create read-integration-hash.mjs, the run.json reader (D-17 step b)</name>

  <files>read-integration-hash.mjs</files>

  <read_first>
    - `capture-hashes.mjs` header comment block `WHY A ROOT-LEVEL DEV-ONLY SCRIPT RATHER THAN A
      MODULE OF THE PUBLISHED PACKAGE`, whose reason (b) applies verbatim here: an Nx-cached
      instrument replays a stale record instead of measuring.
    - `capture-hashes.mjs` around the missing-task throw, for the enumerate-what-was-observed
      failure shape to mirror.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-RESEARCH.md` `## Code Examples` first block, and
      the `## .nx/cache/run.json` section, which give the verified field names, the exact three
      `cacheStatus` values, and the reason absence must throw.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-PATTERNS.md` section 4, for the root-level
      argument and the do-not-copy list.
  </read_first>

  <behavior>
    Three fixture cases, all runnable with no `nx` invocation whatsoever. Build them under the
    session scratchpad, OUTSIDE the repository, so the working tree stays clean.

    - `run-integration-ok.json`: `run.command` is the string `nx run-many -t integration` and
      `tasks` holds one entry with `target` set to `integration`, a `hash`, `cacheStatus` set to
      `remote-cache-hit`, and `status` 0. Expect exit 0, the bare hash written to the output path,
      and a stdout line naming the hash, the cacheStatus and the status.
    - `run-lint.json`: `run.command` is `nx run-many -t lint` and `tasks` holds a `lint` entry.
      Expect a non-zero exit and a message quoting the observed command. This is guard 1: every
      `nx` invocation overwrites `run.json`, so a record from a different command must never be
      uploaded as the integration hash.
    - `run-integration-missing-task.json`: `run.command` contains `integration` but `tasks` holds
      only a `build` entry. Expect a non-zero exit and a message enumerating the observed target
      names. This is guard 2: Nx writes `run.json` inside a try/catch that swallows every error
      unless `NX_VERBOSE_LOGGING`, so a missing file or task is otherwise SILENT and an empty hash
      would upload cleanly.
  </behavior>

  <action>
    Create `read-integration-hash.mjs` at the workspace root. It takes two optional positional
    arguments: `process.argv[2]` is the `run.json` path and defaults to the literal
    `.nx/cache/run.json`; `process.argv[3]` is the output path and defaults to the literal
    `integration-hash.txt`. No flags, no argument library. The two defaults are what the `ci.yml`
    steps rely on, and the overrides are what make the three fixture cases testable without an `nx`
    invocation.

    Body, using `readFileSync` and `writeFileSync` from `node:fs`:

    Parse the record. Guard 1: if the parsed `run.command` does not contain the substring
    `integration`, throw a message that quotes the observed command value and says the record is
    from a different `nx` invocation. Guard 2: find the entry in `tasks` whose `target` equals the
    string `integration`; if there is none, throw a message enumerating every observed `target`
    value. Use the `target` and `projectName` fields directly; do not split or parse `taskId`.

    On success write the bare hash with no trailing newline to the output path, and print one line
    to stdout naming the hash, the `cacheStatus` and the `status`. `cacheStatus` is one of exactly
    three values, `remote-cache-hit`, `local-cache-hit` or `cache-miss`, and printing it is what
    feeds D-17 step (a)'s RECORDED observation and O1/O2's structured corroborator in the same line
    at no extra cost.

    Root-level is deliberate and is what keeps this file hash-neutral (D-10 row 4): `nx.json`
    enumerates every workspace-root input explicitly and there is no `{workspaceRoot}/*.mjs` glob,
    and root `nx.includedScripts` is an empty array so the file cannot become an Nx target.

    Do NOT add this file to `docs-same-os-claims.spec.ts`'s `EDITED_FILES` list. That list holds
    docs, `ci.yml` and files under `packages/github-cache/src/`; `capture-hashes.mjs` is a root
    dev-only instrument this milestone also edits and is likewise absent, so the established scope
    of that list excludes root instruments. Adding an entry would also be a spec edit, which
    rotates three of the four proof hashes (D-10 row 2) in a wave where that cost is forbidden.

    Do not copy the `spawnSync(command, { shell: true })` form from `runDiscriminator`; this reader
    needs no shell and no child process.

    Verify with the three fixture cases, then delete the output file the OK case produced and
    confirm the working tree is clean.
  </action>

  <verify>
    <automated>node read-integration-hash.mjs "$SCRATCH/run-integration-ok.json" "$SCRATCH/integration-hash.txt" &amp;&amp; test "$(cat "$SCRATCH/integration-hash.txt")" = "1234567890123456789" &amp;&amp; ! node read-integration-hash.mjs "$SCRATCH/run-lint.json" "$SCRATCH/out.txt" &amp;&amp; ! node read-integration-hash.mjs "$SCRATCH/run-integration-missing-task.json" "$SCRATCH/out.txt" &amp;&amp; echo "reader OK"</automated>
  </verify>

  <acceptance_criteria>
    - `read-integration-hash.mjs` exists at the workspace root and is committed.
    - Against `run-integration-ok.json` it exits 0, the output file contains exactly the fixture's
      hash value with no surrounding whitespace, and stdout carries one line containing the hash,
      the literal `remote-cache-hit` and the status.
    - Against `run-lint.json` it exits non-zero and stderr contains the observed command string
      `nx run-many -t lint`.
    - Against `run-integration-missing-task.json` it exits non-zero and stderr enumerates the
      observed target names, including `build`.
    - Run with no arguments in a tree whose `.nx/cache/run.json` is absent, it exits non-zero rather
      than writing an empty output file.
    - `git status --porcelain packages/ .github/ start-cache-server/` prints nothing, and
      `git status --porcelain` shows no leftover `integration-hash.txt` in the repository root.
    - `docs-same-os-claims.spec.ts` is unchanged by this task.
  </acceptance_criteria>

  <done>
    A committed root-level reader lifts the `integration` hash and `cacheStatus` out of
    `run.json`, throws on both silent-absence paths, and is proven against three fixtures with no
    `nx` invocation and no repository mutation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nx internals to instrument | `capture-hashes.mjs` imports `nx/src/*` internals; an import break or an API change is untrusted input to the record |
| `.nx/cache/run.json` to reader | a file Nx writes inside an error-swallowing try/catch, so absence and staleness are both possible and both silent |
| instrument to evidence record | the JSON record becomes committed evidence, so a false pass is a false proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Repudiation | `assertGraphPremise` in `capture-hashes.mjs` | high | mitigate | Every assertion throws; none warns. The negative control asserts non-emptiness, forbidden-set intersection and set inequality, so a resolver returning nothing fails three ways. The mutation check proves the assertion can fail before it is trusted |
| T-11-09 | Spoofing | `read-integration-hash.mjs` guard 1 | high | mitigate | `run.command` must contain `integration`, so a `run.json` left behind by any other `nx` invocation cannot masquerade as the integration record |
| T-11-10 | Repudiation | `read-integration-hash.mjs` guard 2 | high | mitigate | A missing file or missing task THROWS and enumerates what was observed; the reader never writes an empty hash |
| T-11-03 | Information disclosure | `11-task-graph-premise.json` | medium | mitigate | The record carries only commit, project, platform, versions, task ids and the verdict. No token, no REST payload, no email address. This is a public repository |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs no package. RESEARCH.md `## Package Legitimacy Audit` states N/A and records the benefit: a dependency edit would touch root `package.json`, an `nx.json` `test` input, and rotate the `test` hash mid-window. No install task exists in any plan, so there is nothing to gate |
</threat_model>

<verification>
- `node capture-hashes.mjs --assert-graph-premise` exits 0 and the committed record's `verdict` is
  the exact string `PREMISE OK`.
- The three reader fixture cases behave as the `<behavior>` block specifies: one pass, two throws.
- `git status --porcelain packages/ .github/ start-cache-server/` prints nothing after both tasks,
  which is the mechanical statement that this plan rotated no task hash.
- No `nx run-many`, `nx test`, `nx build` or `nx reset` invocation occurs anywhere in this plan.
  The premise mode and the reader are plain `node` invocations. This matters because plan 11-02
  performs an irreversible `nx reset` and the perishable window opens there, not here.
</verification>

<success_criteria>
- `capture-hashes.mjs --assert-graph-premise` exists, asserts TEST-08's premise over the resolved
  task graph, carries D-13's `typecheck` negative control with an explicit two-element expectation,
  and has been shown to fail on a reverted mutation.
- `.planning/phases/11-live-proofs-o1-o2-o3/11-task-graph-premise.json` is committed and is the
  captured assertion output TEST-08 requires.
- `read-integration-hash.mjs` exists at the workspace root, throws on both silent-absence paths,
  and is verified against three fixtures without any `nx` invocation.
- Both edits are provably hash-neutral, so the O1/O2 measurement window that plan 11-02 opens is
  untouched.
</success_criteria>

<output>
Create `.planning/phases/11-live-proofs-o1-o2-o3/11-01-SUMMARY.md` when done. Record the observed
failure message from the mutation check verbatim, and the four target hashes are NOT part of this
plan's record: plan 11-02 owns them.
</output>
