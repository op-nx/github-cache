---
phase: 12-windows-ci-reuse-o4-consumer-recipe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/github-cache/src/dogfood-cross-os.spec.ts
  - packages/github-cache/src/windows-regression-detector.spec.ts
  - packages/github-cache/src/nx-target-inputs.spec.ts
  - nx.json
autonomous: true
requirements: [XOS-04, XOS-08, XOS-05]

must_haves:
  truths:
    - "A deleted or renamed build-windows / typecheck-windows / test-windows job reddens the suite instead of passing silently (D-02)"
    - "A copy-paste leaving the wrong `npm run <target>` line in a Windows leg reddens the suite (PATTERNS clause 5)"
    - "A Windows leg that lost its `needs:` edge, its runner label, its timeout or its sidecar reddens the suite (XOS-08)"
    - "A deleted or gutted scheduled detector workflow reddens the suite (XOS-05's conditional clause, D-08)"
    - "The detector-workflow guard cannot replay a stale cached PASS, because the workflow file is an nx.json test input from the same commit as the guard (D-09, PARITY-08)"
    - "Each new assertion was OBSERVED failing with the right cause before any implementation landed (D-14, S-7)"
  artifacts:
    - path: "packages/github-cache/src/dogfood-cross-os.spec.ts"
      provides: "three new describes, one per Windows leg, each anchored per-job-block"
      contains: "build-windows"
    - path: "packages/github-cache/src/windows-regression-detector.spec.ts"
      provides: "shape guard over the new scheduled detector workflow file"
      contains: "--skip-nx-cache"
    - path: "nx.json"
      provides: "test-input registration for the detector workflow file"
      contains: "windows-regression-detector.yml"
    - path: "packages/github-cache/src/nx-target-inputs.spec.ts"
      provides: "literal pin asserting the detector workflow is a declared test input"
      contains: "windows-regression-detector.yml"
  key_links:
    - from: "packages/github-cache/src/windows-regression-detector.spec.ts"
      to: ".github/workflows/windows-regression-detector.yml"
      via: "new URL('../../../.github/workflows/windows-regression-detector.yml', import.meta.url) read, existsSync-guarded"
      pattern: "windows-regression-detector\\.yml"
    - from: "nx.json"
      to: "packages/github-cache/src/windows-regression-detector.spec.ts"
      via: "targetDefaults.test.inputs entry -- an edit to the workflow file rotates the test hash so the guard re-runs"
      pattern: "\\{workspaceRoot\\}/\\.github/workflows/windows-regression-detector\\.yml"
---

<objective>
RED for the whole Windows-reuse slice: author every guard that the three Windows legs and the
scheduled regression detector must satisfy, and OBSERVE each one failing for the right cause while
the jobs and the workflow file still do not exist.

Purpose: this repo has already shipped a CI gate that could be deleted with nothing going red
(`dogfood-cross-os.spec.ts:301-306` records why the `jobBlock` THROW is the presence guard), and it
has already shipped a `needs:` assertion that was a tautology because it was unanchored
(`dogfood-cross-os.spec.ts:135-138`). Authoring the guards FIRST, and watching them fail, is what
stops both defects from recurring on three new jobs at once.

Output: three new describes in `dogfood-cross-os.spec.ts`, one new spec file for the detector
workflow, one `nx.json` `test` input registration, and one literal pin for that registration.

Note on the wave design, because it deliberately breaks the shape Phases 9-11 used: there is NO
perishable-window ordering constraint in this phase, and saying so IS the decision (D-17). Phases 10
and 11 each guarded a measurement their own edits would destroy; O4 is a SAME-RUN property, so the
commit that invalidates the `test` hash is the same commit that proves the HIT. Every `depends_on` in
this phase's six plans is a genuine build-order or file-ownership dependency -- none of them exists to
protect a measurement window, and none should be added for that reason.
</objective>

## Phase Goal

**Verbatim from `.planning/ROADMAP.md:523-524` (`**Goal**` line for Phase 12):**

> Windows CI reuses Linux CI's portable artifacts, and an outside project can copy the recipe
> without inheriting a wrong-result risk.

> **MVP_MODE discrepancy, surfaced rather than papered over.** ROADMAP marks this phase
> `**Mode:** mvp`, but its `**Goal**` line is NOT in `As a ... I want to ... so that ...` form, so
> no user story can be emitted without inventing one -- which the planner is forbidden to do. The
> goal above is reproduced verbatim instead. **Operator item: run `/gsd:mvp-phase 12` if a
> user-story-shaped goal is wanted in ROADMAP.** This does not block execution; the vertical-slice
> task decomposition is applied regardless (see `## Vertical slice` below).

## Vertical slice

This plan is the failing end-to-end test for slice 1 of two: *Windows CI reuses Linux artifacts*.
Slice 1 completes across plans 12-01 (RED), 12-02 (the legs), 12-03 (the detector) and 12-06 (the
live proof). Slice 2 (*a consumer can copy the recipe safely*) is plans 12-04 and 12-05.

The split is deliberately NOT "all the YAML in one wave, all the specs in another" -- that is the
horizontal shape MVP mode exists to prevent. Each plan carries its guard and its artifact together,
except this one, which is the phase's single RED plan and exists because `type: tdd` discipline is
enabled and the guards are the only TDD-eligible work in the phase (there is no domain logic here:
the phase ships CI YAML, a consumer doc, and a one-line config value).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-CONTEXT.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-VALIDATION.md
</context>

## Artifacts this phase produces

Listed once here for the whole phase, so a source-grounding pass can exclude newly-created symbols
from drift verification. Nothing in this list exists in the tree before Phase 12.

| Kind | Symbol / path | Created by plan |
|---|---|---|
| CI job name | `build-windows` | 12-02 |
| CI job name | `typecheck-windows` | 12-02 |
| CI job name | `test-windows` | 12-02 |
| Workflow file | `.github/workflows/windows-regression-detector.yml` | 12-03 |
| Workflow job key | `detect` (inside the detector workflow) | 12-03 |
| Log filename | `detector.log` (runner-side, inside the detector job) | 12-03 |
| Doc | `docs/cross-os.md` | 12-05 |
| Spec file | `packages/github-cache/src/windows-regression-detector.spec.ts` | 12-01 |
| Spec file | `packages/github-cache/src/docs-cross-os.spec.ts` | 12-05 |
| `nx.json` `test` input literal | `{workspaceRoot}/.github/workflows/windows-regression-detector.yml` | 12-01 |
| `nx.json` `test` input literal | `{workspaceRoot}/docs/cross-os.md` | 12-05 |
| Discriminator literal | `node --no-warnings -p process.platform` | 12-04 |
| `ci.yml` comment phrase | `all seven wired jobs` | 12-02 |
| Describe name | `ci.yml build-windows job exists and keeps its shape (XOS-04, XOS-08)` | 12-01 |
| Describe name | `ci.yml typecheck-windows job exists and keeps its shape (XOS-04, XOS-08)` | 12-01 |
| Describe name | `ci.yml test-windows job exists and keeps its shape (XOS-04, XOS-08)` | 12-01 |
| Describe name | `windows-regression-detector.yml workflow config -- the XOS-05 detector` | 12-01 |
| `11-EVIDENCE.md` section | `## O4 (XOS-04, XOS-05)` (the RESERVED block, filled in place) | 12-06 |

## Multi-source coverage audit (whole phase)

Audited against ALL FOUR source types. `REQUIREMENTS.md:658-665` is AUTHORITATIVE on the count:
**FOUR** requirements. `ROADMAP.md:609-611`, `:624` and `:684` undercount at THREE (the XOS-08 row
is missing) -- CONTEXT D-01, and REQUIREMENTS wins.

```
SOURCE    | ID      | Feature/Requirement                                              | Plan     | Status
--------- | ------- | ---------------------------------------------------------------- | -------- | ---------
GOAL      | --      | Windows CI reuses Linux CI's portable artifacts                   | 01,02,03,06 | COVERED
GOAL      | --      | an outside project can copy the recipe without a wrong-result risk | 04,05   | COVERED
REQ       | XOS-04  | ci.yml runs build/typecheck/test on a windows-11-arm leg          | 01,02    | COVERED
REQ       | XOS-05  | those legs HIT for all three targets; write decision recorded; attribution loss recorded; scheduled --skip-nx-cache detector REQUIRED | 01,02,03,06 | COVERED
REQ       | XOS-08  | the Windows legs declare needs: on the corresponding ubuntu jobs  | 01,02    | COVERED
REQ       | DOCS-07 | consumer cross-OS recipe, stderr-immune discriminator, registered, drift-guarded | 04,05 | COVERED
RESEARCH  | U-01    | discriminator = `node --no-warnings -p process.platform`, single-sourced | 04  | COVERED
RESEARCH  | Corr 1  | pull_request is the ONLY vehicle (ci.yml is on: push[main] + pull_request); FIRST run never a re-run | 06 | COVERED
RESEARCH  | Corr 2  | workflow_dispatch cannot prove pre-merge; prove the COMMAND + post-merge first-run close | 03,06 | COVERED
RESEARCH  | Corr 3  | --assert-graph-premise stays true; only the inference dies         | 02       | COVERED
RESEARCH  | F-3     | needs: is a strictly stronger durability barrier than XOS-08 needs | 02       | COVERED
RESEARCH  | F-5     | test passes on win32/arm64 today (40 files / 856 tests)            | 03       | COVERED
RESEARCH  | F-6     | --skip-nx-cache skips READ and WRITE at Nx 23.1.0                  | 03       | COVERED
RESEARCH  | F-7     | docs/cross-os.md not yet an nx.json test input; inputs REPLACE     | 05       | COVERED
RESEARCH  | Pitfall 5 | the unattributed `test` failure at 69bd1b7 may surface on Windows | 03,06   | COVERED
RESEARCH  | Sec     | eight T-12-xx threat statements, ASVS L1                           | 01..06 (threat_model blocks) | COVERED
PATTERNS  | M-1     | the rg sweep needs --hidden or it silently drops .github/          | 04       | COVERED
PATTERNS  | M-2     | the naive multi-target success-line needle is VACUOUS               | 01,03    | COVERED
PATTERNS  | --      | ci.yml sidecar step occurs FIVE times today, not four; invariant governs FOUR wired jobs | 02 | COVERED
PATTERNS  | --      | nx-target-inputs.spec.ts:375-393 ARGUES for byte-identity: a SIXTH correction site | 04 | COVERED
PATTERNS  | --      | capture-hashes.mjs has TWO attribution sites, not one              | 02       | COVERED
PATTERNS  | --      | build-windows's needs: assertion must be INDENT-ANCHORED           | 01       | COVERED
CONTEXT   | D-00    | [informational] four IDs; never write the trailing-period literal  | --       | EXEMPT (informational)
CONTEXT   | D-01    | [informational] REQUIREMENTS wins over ROADMAP's undercount        | --       | EXEMPT (informational)
CONTEXT   | D-02    | THREE dedicated jobs, each needs: its ONE ubuntu counterpart       | 01,02    | COVERED
CONTEXT   | D-03    | sidecar block COPIED VERBATIM; invariant comment four -> seven      | 02       | COVERED
CONTEXT   | D-04    | port 3000 on every new leg, unchanged                             | 02       | COVERED
CONTEXT   | D-05    | timeout-minutes: 15 on each new leg                                | 01,02    | COVERED
CONTEXT   | D-06    | the Windows legs WRITE, and it is FORCED not chosen                | 02,06    | COVERED
CONTEXT   | D-07    | both consequences fire: attribution record AND the detector        | 02,03    | COVERED
CONTEXT   | D-08    | the detector is a NEW workflow file, hard fail, no sidecar         | 01,03    | COVERED
CONTEXT   | D-09    | register the detector workflow in nx.json in the SAME COMMIT       | 01       | COVERED
CONTEXT   | D-10    | a NEW docs/cross-os.md, not a section in advanced.md               | 05       | COVERED
CONTEXT   | D-11    | safe default FIRST, portability checklist SECOND                   | 05       | COVERED
CONTEXT   | D-12    | checklist items INHERITED from 08-ROOT-CAUSE.md:1777-1828; item 6 STRUCK | 05 | COVERED
CONTEXT   | D-13    | architecture and libc named, with the honest arm64-only limit      | 05       | COVERED
CONTEXT   | D-14    | drift guard is phrase-keyed and proven RED first; a twice-occurring phrase is half-locked | 01,05 | COVERED
CONTEXT   | D-15    | the documented command and nx.json's command are ONE string        | 04,05    | COVERED
CONTEXT   | D-16    | the rotation table                                                | 04,06    | COVERED
CONTEXT   | D-17    | NO perishable-window ordering; keep depends_on for build order only | (wave design) | COVERED
CONTEXT   | D-18    | a SAME-REPO pull-request run is the vehicle; no temporary main push | 06      | COVERED
CONTEXT   | D-19    | counts PRE-REGISTERED in the PLAN                                  | 06       | COVERED
CONTEXT   | D-20    | `Cache: n/m hit` marked NON-DISCRIMINATING in both directions      | 06       | COVERED
CONTEXT   | D-21    | keep --assert-graph-premise, correct its CLAIM, in the SAME COMMIT | 02       | COVERED
CONTEXT   | D-22    | fill 11-EVIDENCE.md's RESERVED O4 in place; update the :34 table   | 06       | COVERED
```

**No `MISSING` rows.** Exclusions applied, and each is a documented non-gap: CONTEXT's nine
`## Deferred Ideas` (the sidecar-block drift guard, an every-commit graph-premise spec, collapsing
the publish matrix, archive file-mode handling, PARITY-04's everyday-box question, macOS,
regenerating `.planning/codebase/*`, and the two GSD tooling defects) are developer-deferred, not
gaps. `lint` is absent from the phase BY DESIGN -- XOS-04 names three targets, `lint` has no sidecar
(D-33) and is absent from the mirror by design, so a coverage audit reading three-of-four must NOT
report a gap.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- three per-job describes for the Windows legs</name>

  <files>packages/github-cache/src/dogfood-cross-os.spec.ts</files>

  <read_first>
    - `packages/github-cache/src/dogfood-cross-os.spec.ts` IN FULL (546 lines). It is the only
      job-block extractor in the repo. You need: the `codeLines` read and `#`-strip (`:49-77`), the
      `jobBlock(name)` helper and its THROW, the `o3-witness` describe (`:283-352`) as the clause
      template, the recorded anchoring bug (`:135-138`), and the per-job-scoping rule (`:36-42`).
    - `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md`, section
      `### packages/github-cache/src/dogfood-cross-os.spec.ts -- extend`, plus `## Shared Patterns`
      S-3 (positive control first) and S-5 (anchor by indent level).
  </read_first>

  <behavior>
    Three describes, one per Windows leg. For each of `build-windows` (producer `build`, command
    `npm run build`), `typecheck-windows` (producer `typecheck`, command `npm run typecheck`) and
    `test-windows` (producer `test`, command `npm run test`), the following must hold and each must
    be an independently-named `it`:

    - Test 1 (POSITIVE CONTROL, FIRST): `jobBlock('<leg>')` returns a real non-empty block whose
      body carries the leg's own `- run: npm run <target>` line at SIX spaces. `jobBlock` THROWS
      when no job is keyed `  <leg>:`, and that throw IS the presence guard -- it is what stops a
      new CI job from being a silently deletable gate.
    - Test 2: the block matches `/^ {4}runs-on: windows-11-arm$/m`.
    - Test 3: the block matches `/^ {4}needs: <producer>$/m` -- exactly ONE producer, bare scalar,
      never a list.
    - Test 4: the block matches `/^ {4}timeout-minutes: 15$/m`.
    - Test 5: the block matches `/^ {6}- run: npm run <target>$/m` AND does not match the other two
      targets' `npm run` lines. This is the clause that catches a copy-paste leaving `npm run test`
      in `build-windows`; nothing else in the file would catch it.
    - Test 6: the block carries the sidecar -- it matches `/^ {6}- uses: \.\/start-cache-server$/m`
      and `/^ {6}- cancel: cache-server$/m`. A leg without a sidecar cannot exhibit a HIT.
    - Test 7 (absence, and it comes AFTER the Test 1 control): the block does NOT match
      `/^ {4}if:/m`. `build`, `typecheck` and `test` carry no job-level `if:`, which is exactly what
      makes them PR-eligible and therefore what makes plan 12-06's proving-run vehicle work.

    Expected RED, per describe: Test 1 fails with the `jobBlock` THROW naming the absent job key.
    Tests 2-7 fail the same way, because `jobBlock` throws before any assertion runs.
  </behavior>

  <action>
    Append three describes to `packages/github-cache/src/dogfood-cross-os.spec.ts`, after the
    existing `ci.yml o3-witness job exists and keeps its shape (XOS-03, TEST-09)` describe. Name
    them exactly `ci.yml build-windows job exists and keeps its shape (XOS-04, XOS-08)`,
    `ci.yml typecheck-windows job exists and keeps its shape (XOS-04, XOS-08)` and
    `ci.yml test-windows job exists and keeps its shape (XOS-04, XOS-08)` -- each contains the token
    `windows`, which is what `-t "windows"` selects.

    Reuse the file's existing module-scope `codeLines` and `jobBlock` helpers. Do NOT add a second
    reader and do NOT widen `jobBlock`. The file's own rule (`:117-120`) is "the guard comes to the
    helper rather than the helper going to the guard".

    ANCHOR EVERY PATTERN AT ITS INDENT LEVEL. Job-level keys sit at FOUR spaces, their step children
    at SIX. This is not style: an unanchored `\bbuild\b` on `build-windows`'s `needs:` is satisfied
    by that same job's own `- run: npm run build` step, which is the exact tautology this file
    records having shipped once (`:135-138`). Equally, a whole-file `toContain('windows-11-arm')`
    is unconditionally true -- that token occurs 19 times in `ci.yml` (MEASURED) -- so every clause
    must be scoped to `jobBlock(<leg>)`, never to the file.

    Give each `expect` a failure message in the register the file already uses: say what the clause
    protects and say "if the job was legitimately renamed, update this describe in the SAME commit;
    do not delete the assertion to make the suite green".

    Do NOT put any comment-phrase assertion in this file. `codeLines` strips every `#` line, so a
    comment lock here is vacuous BY CONSTRUCTION (`:271-275`). Do NOT add the sidecar-block
    byte-identity guard -- it is an explicit Deferred Idea and `12-RESEARCH.md`'s test map says "Do
    NOT add it".
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "windows" 2>&1 | rg -q -F "no job keyed"</automated>
  </verify>

  <acceptance_criteria>
    - `rg -o -F "build-windows" packages/github-cache/src/dogfood-cross-os.spec.ts | wc -l` is at
      least 7 (one describe name plus at least six `jobBlock` call sites / messages).
    - `rg -c "jobBlock\('(build|typecheck|test)-windows'\)" packages/github-cache/src/dogfood-cross-os.spec.ts`
      returns a non-zero count, and `rg -o "jobBlock\('(build|typecheck|test)-windows'\)" packages/github-cache/src/dogfood-cross-os.spec.ts | wc -l`
      is at least 18 (>= 6 clauses x 3 legs).
    - Every new regex in the three describes is anchored: `rg -n "toMatch\(/\^" packages/github-cache/src/dogfood-cross-os.spec.ts | wc -l`
      increases by at least 18 relative to `git show HEAD:packages/github-cache/src/dogfood-cross-os.spec.ts`,
      and `rg -c "toMatch\(/\\\\b" packages/github-cache/src/dogfood-cross-os.spec.ts` returns
      nothing added by this task (no unanchored word-boundary pattern).
    - OBSERVED RED (record the exact output in the SUMMARY, do not predict it):
      `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "build-windows"` exits non-zero and
      its output contains `no job keyed` and the string `build-windows`. Repeat for
      `typecheck-windows` and `test-windows`. Three named RED observations, not one.
    - No `it` in the three new describes asserts on a `#`-comment phrase (`codeLines` strips them):
      `rg -n "toMatch|toContain" ` over the three new describes shows no pattern containing `#`.
    - `npx nx format:check --all` passes.
  </acceptance_criteria>

  <done>
    Three describes exist, all clauses are indent-anchored and job-block-scoped, and all three
    were OBSERVED failing with the `jobBlock` THROW naming the absent job key. The SUMMARY records
    the three RED outputs verbatim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED -- the detector-workflow shape guard, registered in the same commit</name>

  <files>packages/github-cache/src/windows-regression-detector.spec.ts, nx.json, packages/github-cache/src/nx-target-inputs.spec.ts</files>

  <read_first>
    - `packages/github-cache/src/cleanup/cleanup-workflow.spec.ts` IN FULL (58 lines). It is the
      in-repo precedent for a spec over a workflow FILE rather than over a job inside `ci.yml`.
      Note the `#`-strip and its stated reason, and note its `../../../../` depth -- it lives in
      `src/cleanup/`. The new spec lives in flat `src/`, so its depth is `../../../`.
    - `packages/github-cache/src/docs-trust.spec.ts:20-30` for the `existsSync`-guarded read plus a
      named existence `it` FIRST. Use that shape so the RED is a NAMED assertion rather than a
      module-load ENOENT.
    - `nx.json:50-72` -- the `targetDefaults.test.inputs` string-literal region, and the fact that
      `{workspaceRoot}/nx.json` is itself entry 6, which is why a registration is effective on its
      OWN commit.
    - `packages/github-cache/src/nx-target-inputs.spec.ts:537-614` -- the `{workspaceRoot}` literal
      pin shape and the comment lock explaining why an explicit path beats a glob.
    - `12-PATTERNS.md` sections `### .github/workflows/<detector>.yml (NEW)` and
      `## Two measurements ... M-2`, and `## Shared Patterns` S-2 and S-8.
  </read_first>

  <behavior>
    A new spec file `packages/github-cache/src/windows-regression-detector.spec.ts` reads
    `.github/workflows/windows-regression-detector.yml` via
    `new URL('../../../.github/workflows/windows-regression-detector.yml', import.meta.url)`,
    guarded by `existsSync` so a missing file yields `''` rather than a module-load crash, then
    strips `#`-prefixed lines into `codeLines`. Describe name:
    `windows-regression-detector.yml workflow config -- the XOS-05 detector`.

    - Test 1 (POSITIVE CONTROL / existence, FIRST): `existsSync(detectorUrl)` is `true`. Every
      clause below is a `toMatch`/`not.toMatch` against `codeLines`, so an empty string would make
      the absence clauses pass trivially. This test is what makes them non-vacuous.
    - Test 2: triggers -- `codeLines` matches `/^on:\s*\n\s*schedule:/m` and matches
      `/^\s*workflow_dispatch:/m` and does NOT match `/^\s*(push|pull_request):/m`.
      `workflow_dispatch` is REQUIRED (D-08) and must NOT appear in the forbidden-trigger list.
    - Test 3: the cron is off the top of the hour -- `codeLines` matches
      `/^\s*- cron: '(?!0 )\d{1,2} \d{1,2} \* \* \*'$/m`, i.e. the minute field is not `0`.
    - Test 4: exactly one job, and it runs on the Windows runner --
      `/^ {4}runs-on: windows-11-arm$/m`.
    - Test 5: the cache is bypassed -- `codeLines` contains `--skip-nx-cache`.
    - Test 6: the run is proven, not inferred -- `codeLines` contains `NO_COLOR` and contains the
      exact plural multi-target success needle
      `Successfully ran targets build, typecheck, test for project`.
    - Test 7 (absence, after the Test 1 control): NO sidecar and no remote cache tier. `codeLines`
      does not match `/start-cache-server/` and does not match `/NX_SELF_HOSTED_REMOTE_CACHE/`.
    - Test 8 (absence): no write permission -- `codeLines` does not match
      `/permissions:[\s\S]{0,60}contents:\s*write/` and does not match `/actions:\s*(read|write)/`.

    Expected RED: Test 1 fails with `expected false to be true`, naming the absent workflow path.
    Tests 2-6 fail against the empty string. Tests 7-8 pass trivially -- which is exactly why Test 1
    exists and why it is asserted FIRST.
  </behavior>

  <action>
    Create `packages/github-cache/src/windows-regression-detector.spec.ts` implementing the
    `<behavior>` above, mirroring `cleanup-workflow.spec.ts` clause for clause with the
    substitutions named there. Carry a docstring that states BOTH standing reasons this file's
    assertions are non-vacuous: (a) only non-comment lines are matched, because the workflow's own
    prose comments will repeat `--skip-nx-cache` and the success needle verbatim while explaining
    the rationale; and (b) the workflow file is registered as an `nx.json` `test` input in THIS
    commit, so an edit to it rotates the `test` hash and re-runs this guard instead of replaying a
    stale cached PASS.

    The success needle is the plural form naming ALL THREE targets in `-t` argument order. Do NOT
    pin `Successfully ran target` alone: source-traced at
    `node_modules/nx/dist/src/tasks-runner/life-cycles/formatting-utils.js:37`, Nx FILTERS the
    printed target list down to targets that actually resolved a task, so if `typecheck` silently
    stopped resolving the line becomes `Successfully ran targets build, test for project ...` and
    the short needle still matches. The short needle is VACUOUS for a three-target run. Record in
    the spec's failure message that this needle also pins the `-t` ARGUMENT ORDER, so reordering the
    flags in the workflow reddens a correct run and the fix is to update this needle in the same
    commit.

    In the SAME COMMIT (D-09 / PARITY-08, and this is the whole point of the commit boundary):
    add `"{workspaceRoot}/.github/workflows/windows-regression-detector.yml"` to
    `nx.json`'s `targetDefaults.test.inputs`, positioned IMMEDIATELY AFTER the existing
    `"{workspaceRoot}/.github/workflows/ci.yml"` entry so the two workflow entries stay adjacent.
    Use the explicit path, NEVER `{workspaceRoot}/.github/workflows/**` -- the reason is
    comment-locked at `nx-target-inputs.spec.ts:583-591`: a glob "would silently adopt whatever
    lands there next". Do not reorder or reformat any existing entry; `nx.json` is a `test` input,
    so an unrelated reorder is diff noise on a hash-rotating file.

    Also in the same commit, add ONE `it` to `packages/github-cache/src/nx-target-inputs.spec.ts` in
    the existing registration-pin shape: `nx.json declares the windows-regression-detector workflow
    as a test input`, asserting `nxJson.targetDefaults.test.inputs` contains the literal
    `{workspaceRoot}/.github/workflows/windows-regression-detector.yml`. Minimum viable is the
    literal pin -- the merged-configuration clause is already discharged for the whole `test` list
    by the existing `mergedTest` cases, so do not add a second copy per new entry.

    Registering a `test` input for a file that does not yet exist is intentional and safe: an Nx
    fileset tolerates an absent path, and the registration landing WITH the guard is what removes
    the stale-pass window entirely. There is no ordering instruction for anyone to remember.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "detector" 2>&1 | rg -q -F "windows-regression-detector.yml"</automated>
  </verify>

  <acceptance_criteria>
    - `packages/github-cache/src/windows-regression-detector.spec.ts` exists and contains
      `../../../.github/workflows/windows-regression-detector.yml` (THREE `../`, not four -- the
      spec is in flat `src/`). `rg -c "\.\./\.\./\.\./\.\./" packages/github-cache/src/windows-regression-detector.spec.ts`
      returns 0.
    - `rg -o -F "Successfully ran targets build, typecheck, test for project" packages/github-cache/src/windows-regression-detector.spec.ts | wc -l`
      equals 1, and `rg -c -F "'Successfully ran target '" packages/github-cache/src/windows-regression-detector.spec.ts`
      returns 0 (the vacuous short needle is absent as a standalone pin).
    - `node -e "const j=require('./nx.json');const i=j.targetDefaults.test.inputs;const a=i.indexOf('{workspaceRoot}/.github/workflows/ci.yml');const b=i.indexOf('{workspaceRoot}/.github/workflows/windows-regression-detector.yml');if(b!==a+1){console.error('not adjacent',a,b);process.exit(1)}console.log('adjacent at',b)"`
      exits 0.
    - `rg -c -F "{workspaceRoot}/.github/workflows/**" nx.json` returns 0 (no glob).
    - `rg -o -F "windows-regression-detector.yml" packages/github-cache/src/nx-target-inputs.spec.ts | wc -l`
      is at least 1.
    - OBSERVED RED (record verbatim in the SUMMARY): `npx nx run @op-nx/github-cache:test
      --skip-nx-cache -- -t "detector"` exits non-zero, and the failing test is the NAMED existence
      `it` (Test 1), not a module-load ENOENT. Confirm the output names
      `windows-regression-detector.yml`.
    - `npx nx format:check --all` passes.
  </acceptance_criteria>

  <done>
    The detector guard exists with an existsSync-guarded read and a named existence control first;
    the workflow file is a declared `nx.json` `test` input and is pinned by a literal assertion; all
    three edits are in ONE commit; and the RED was observed as a named assertion failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content -> Nx task hash | an `nx.json` input list change alters which files invalidate a cached verdict; a wrong list is a stale-PASS vector, not an authorization one |
| spec file -> CI gate | a vacuous assertion is a gate that cannot fail, i.e. a deleted control that still looks present |

**Delta introduced by this plan: none on any security path.** No credential, no write path, no
workflow trigger, no network egress, no runtime code. Two spec files and one config line.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10 | Tampering | the three new describes in `dogfood-cross-os.spec.ts` | medium | mitigate | A vacuous guard is a deletable control. Every clause is indent-anchored (`^ {4}` / `^ {6}`) and scoped to `jobBlock(<leg>)`; `windows-11-arm` occurs 19 times in `ci.yml` so no file-wide clause is permitted; the positive control is asserted FIRST, before any `not.toMatch` |
| T-12-11 | Tampering | `windows-regression-detector.spec.ts` success-line needle | medium | mitigate | The plural three-target needle, source-traced to `formatting-utils.js:37`. The short needle is forbidden by an acceptance criterion because Nx filters unresolved targets out of the printed list, making it pass on a two-of-three run |
| T-12-12 | Tampering | `nx.json` `test` input registration | medium | mitigate | Registration lands in the SAME COMMIT as the guard (D-09), so there is no window in which the guard can replay a stale cached PASS. Explicit path, never a glob -- a glob would silently adopt whatever workflow lands next |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package is installed by this plan.** VERIFIED rather than asserted: `files_modified` contains no `package.json` and no `package-lock.json`, and `12-RESEARCH.md`'s Package Legitimacy Audit records "Not applicable -- this phase installs no external packages". If any task reaches for a dependency, that is a scope escape and the legitimacy gate applies from scratch, including a blocking human checkpoint for any `[ASSUMED]`/`[SUS]` verdict |
| T-12-04 | Elevation of privilege | `$GITHUB_ENV` writes | critical **if present** | mitigate | CHECK, do not assume. This plan writes no workflow YAML at all, so the T-11-27 shape (a record-controlled value written into `$GITHUB_ENV` in a fork-PR-reachable job) is structurally absent. Re-checked here rather than deferred because T-11-27 is the most recent live Critical finding on this repo |
</threat_model>

<verification>
- `npx nx run @op-nx/github-cache:test --skip-nx-cache` is RED, and every failure is one of the new
  assertions. NO pre-existing test may break. If an unrelated spec fails, STOP and investigate --
  this plan changes no production code, so an unrelated failure is a real signal.
- Every RED is OBSERVED and transcribed into the SUMMARY, never predicted (D-14, S-7).
- `npx nx format:check --all` passes.
- `npm run lint` passes.
</verification>

<success_criteria>
- Three per-leg describes exist, all clauses indent-anchored and job-block-scoped.
- The detector guard exists with a named existence control asserted FIRST.
- `nx.json` declares the detector workflow as a `test` input, adjacent to `ci.yml`, explicit path.
- `nx-target-inputs.spec.ts` pins that registration by literal.
- Four named RED observations recorded verbatim (three legs plus the detector).
- Two commits: one per task, `test(12-01): ...` for each (both are RED-gate commits).
</success_criteria>

<output>
Create `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-01-SUMMARY.md` when done.
Record, in this order: the exact RED output for each of the four guards; the `nx.json` insertion
index; and any clause you had to weaken, with the reason.
</output>
