---
phase: 12-windows-ci-reuse-o4-consumer-recipe
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .github/workflows/windows-regression-detector.yml
autonomous: true
requirements: [XOS-05]

must_haves:
  truths:
    - "A scheduled windows-11-arm job EXECUTES build, typecheck and test with the cache bypassed, on a daily cadence, whether or not anyone pushes (XOS-05's conditional clause, D-08)"
    - "The job HARD FAILS on a red target -- it is the one gate in this phase that SHOULD fire, because a red leg here means a real Windows-only regression, never correct work (D-08)"
    - "Exit 0 is not accepted as evidence the targets ran: the plural three-target success LINE is demanded, so a run where one target silently stopped resolving fails (PATTERNS M-2)"
    - "No cache tier participates at all: no sidecar, no remote-cache env, so the detector adds no cache producer (D-08, RESEARCH F-6)"
    - "The command itself is proven on Windows arm64 BEFORE merge, since the workflow cannot be dispatched until its file reaches the default branch (RESEARCH Correction 2)"
  artifacts:
    - path: ".github/workflows/windows-regression-detector.yml"
      provides: "the XOS-05 --skip-nx-cache windows-11-arm regression detector"
      contains: "--skip-nx-cache"
  key_links:
    - from: ".github/workflows/windows-regression-detector.yml"
      to: "nx.json targetDefaults.test.inputs"
      via: "registered by plan 12-01 in the same commit as its guard, so the guard cannot replay a stale cached PASS"
      pattern: "windows-regression-detector\\.yml"
    - from: ".github/workflows/windows-regression-detector.yml"
      to: "the three Nx targets on a real Windows runner"
      via: "npm exec -- nx run-many -t build typecheck test --skip-nx-cache, which skips READ and WRITE at Nx 23.1.0"
      pattern: "--skip-nx-cache"
---

<objective>
Add the scheduled `windows-11-arm` regression detector, and close its substantive risk before merge
by proving the COMMAND rather than the job.

Purpose, in XOS-05's own words and stated HERE rather than in a distant rationale: **the success
signal for O4 -- every target `[remote cache]`, wall time collapsing to sidecar overhead -- is the
IDENTICAL observation to a Windows-only regression being invisible forever.** A green O4 leg IS a
Windows leg that did not run the code. Reading a green O4 CI as evidence the targets are PORTABLE is
CIRCULAR, and `REQUIREMENTS.md`'s Out of Scope table names it as such: a restored task does not
execute. Once plan 12-02 lands, this workflow is the ONLY path in the entire repository that
executes `build`, `typecheck` or `test` on a Windows runner. That is its whole justification.

Output: one new workflow file, plus two recorded measurements from this Windows arm64 workstation --
the three-target command's own green, and the observed RED that proves the success-line needle is
not vacuous.
</objective>

## Phase Goal

**Verbatim from `.planning/ROADMAP.md:523-524`:**

> Windows CI reuses Linux CI's portable artifacts, and an outside project can copy the recipe
> without inheriting a wrong-result risk.

> MVP_MODE discrepancy (ROADMAP's `**Goal**` is not in user-story form) is recorded once in
> `12-01-PLAN.md` `## Phase Goal`. Operator item: `/gsd:mvp-phase 12`.

## Vertical slice

Second slice of *Windows CI reuses Linux artifacts*, and it is the half that keeps the first half
honest. Plan 12-02 makes Windows stop executing these targets; this plan makes something still
execute them. After Task 1 a real daily signal exists that did not exist before; after Task 2 the
command inside it is measured green on Windows arm64 rather than assumed.

## Artifacts this phase produces

See `12-01-PLAN.md` `## Artifacts this phase produces`. Created by THIS plan:
`.github/workflows/windows-regression-detector.yml`, its job key `detect`, and the runner-side log
filename `detector.log`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-CONTEXT.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the scheduled windows-11-arm regression detector workflow</name>

  <files>.github/workflows/windows-regression-detector.yml</files>

  <read_first>
    - `.github/workflows/cleanup.yml` IN FULL (47 lines). This is the whole skeleton: `name:` at the
      top, a rationale comment block IMMEDIATELY under it recording why the concern is NOT a `ci.yml`
      job, a comment above `cron:` giving the off-the-hour reason, one job, and the four-step
      checkout / setup-node / `npm ci` prelude.
    - `.github/workflows/ci.yml`, the `lint` job -- BOTH its leading comment block (the one
      explaining that exit 0 is not sufficient and that `NO_COLOR` is load-bearing rather than
      tidiness) and its scripted step. Locate by the job key `lint:`, not by line number.
    - `packages/github-cache/src/windows-regression-detector.spec.ts` as written by plan 12-01. It
      IS the contract for this file; every clause there must be satisfied by what you write.
    - `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md`, sections
      `### .github/workflows/<detector>.yml (NEW)` and `## Two measurements ... ### M-2`. M-2 is
      load-bearing: it source-traces why the obvious needle is vacuous.
    - `.planning/REQUIREMENTS.md:363-369` (XOS-05, including the "identical observation" sentence)
      and `:592-602` (Out of Scope -- the circularity argument and the divergence-detection row).
  </read_first>

  <action>
    Create `.github/workflows/windows-regression-detector.yml` with `name: Windows regression
    detector` and ONE job keyed `detect`.

    **The header comment block, immediately under `name:`.** Mirror `cleanup.yml`'s form and record
    three things: (1) why this is a SEPARATE scheduled workflow and not a job inside `ci.yml` --
    adding `schedule:` to `ci.yml` would fire all nineteen of its jobs on a schedule unless every one
    of them grew an `if:`, and `cleanup.yml`'s own header already records the reason a scheduled
    concern lives in its own file; (2) XOS-05's sentence, in XOS-05's own words, that the success
    signal for O4 is the identical observation to a Windows-only regression being invisible forever,
    and that a green O4 leg IS a Windows leg that did not run the code; (3) that this workflow is
    therefore the ONLY path that executes these three targets on a Windows runner once XOS-04 lands,
    and that it HARD FAILS by design -- it is not a tripwire that fires on correct work (the class
    OBS-04 records disabling), because a red leg here means a genuine Windows-only regression in the
    code under test.

    **Triggers.** `schedule:` with a single daily cron OFF the top of the hour -- use
    `'23 4 * * *'`, and carry the in-repo reason verbatim in a comment above it: GitHub delays
    scheduled runs under top-of-hour load, and this check has no deadline, so an off-peak minute is
    fine. Pick a minute and hour distinct from `cleanup.yml`'s so the two scheduled workflows do not
    contend. THEN `workflow_dispatch:` with a comment stating its ACTUAL justification: it is how the
    detector gets re-run ON DEMAND AFTER merge. It does NOT close the "does this go green on a real
    runner" question before merge, and the comment must say so, because GitHub only dispatches a
    workflow whose file exists on the DEFAULT branch -- a brand-new file on a feature branch does not
    appear in the UI and the API rejects it. Note in the same comment that `cleanup.yml`, the shape
    precedent, carries `schedule:` only, so the addition is deliberate. Do NOT add `push:` or
    `pull_request:`.

    **Permissions.** `permissions: contents: read` at workflow level, with a comment: this job checks
    out, installs and runs three targets, and writes nothing -- no Releases asset, no Actions-cache
    entry, no package. Do NOT copy `cleanup.yml`'s `contents: write`, and do NOT add `actions: read`.

    **Do NOT copy `cleanup.yml`'s `concurrency:` block.** It needs single-writer serialization
    because concurrent DELETES are unsafe. This job deletes nothing and writes nothing, so two
    overlapping runs are harmless, and adding the block would be an unrequested mechanism.

    **Do NOT copy the sidecar dogfood block from `ci.yml`, and do not set any remote-cache client
    env var.** `--skip-nx-cache` skips remote READ and remote WRITE at Nx 23.1.0 (source-traced:
    `doNotSkipCache` gates the read AND is passed as `postRunSteps`' `shouldCache` argument), so a
    cache server here would be dead weight and an extra cache producer this milestone does not want.
    With no remote-cache client variable in the environment there is no remote tier at all,
    regardless. Also do NOT add a separate `- run: npm run build` step -- the detector's own
    `run-many` invocation IS the build.

    **The job.** `runs-on: windows-11-arm`. `timeout-minutes: 20`, with a comment giving the reason
    the number differs from the 15 the `ci.yml` jobs use: this is the only job in the repository
    whose wall time is FULL cold execution of all three targets rather than a cache restore, so it
    needs more headroom than a leg that HITs. Then the four-step prelude byte-identical to
    `cleanup.yml`'s / `ci.yml`'s: `actions/checkout@v7`; `actions/setup-node@v6` with
    `node-version-file: '.node-version'` and `cache: 'npm'`; `- run: npm ci`.

    **The one substantive step.** Name it so a red leg says what broke. It MUST declare
    `shell: bash` -- the default shell on `windows-11-arm` is pwsh, which fails on `set -euo
    pipefail`, on `|`, and on `tee`. Declare `env: NO_COLOR: '1'` at step level. Body:
    `set -euo pipefail`; then `npm exec -- nx run-many -t build typecheck test --skip-nx-cache 2>&1 |
    tee detector.log`; then a `grep -q` against `detector.log` demanding the exact needle
    `Successfully ran targets build, typecheck, test for project`.

    THE NEEDLE IS THE POINT, and the obvious one is vacuous. `nx run-many` on a MISSING target prints
    "NX No tasks were run" and EXITS 0 (measured in this repo), which is why this is a scripted step
    rather than a bare `- run:`. Worse, source-traced at
    `node_modules/nx/dist/src/tasks-runner/life-cycles/formatting-utils.js:37`, Nx FILTERS the printed
    target list down to targets that actually resolved a task -- so a needle matching only
    `Successfully ran target` still PASSES when only two of the three ran. Name all three, in `-t`
    argument order. Put the trade in a comment beside it: this needle also pins the `-t` ARGUMENT
    ORDER, so reordering the flags reddens a correct run and the fix is to update the needle and its
    spec clause in the same commit. This shape was chosen over three separate single-target steps
    because one invocation shares one graph load on the slowest runner in the matrix, and Nx already
    names the failing target in its own output.

    `NO_COLOR` is MORE load-bearing here than in the `lint` job, not less: each target name is bolded
    INDIVIDUALLY while the `, ` separators are not, so with colour on the combined line carries three
    escape-sequence pairs interleaved with the commas and a plain-text match cannot fire. Say that in
    the comment. A gate that always fails gets deleted just as fast as one that never fires.

    Note for the reader, in a comment: the `grep` here is runner-side POSIX inside a workflow body and
    is correct. This project's never-use-`grep` rule governs the agent's own shell, not workflow
    bodies -- `ci.yml`'s `lint` job already uses `grep -q` for exactly this purpose.

    Pure ASCII only: `--` never an em dash, `->` never an arrow glyph.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "detector"</automated>
  </verify>

  <acceptance_criteria>
    - Every clause of `packages/github-cache/src/windows-regression-detector.spec.ts` is GREEN,
      including its Test 1 existence control, its `workflow_dispatch`-present clause, its
      `push`/`pull_request`-absent clause, its no-sidecar clause and its no-write-permission clause:
      `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "detector"` exits 0.
    - `rg -c "^  detect:$" .github/workflows/windows-regression-detector.yml` equals 1, and
      `rg -c "^  [a-z]" .github/workflows/windows-regression-detector.yml` equals 1 (exactly ONE job).
    - `rg -c "^    runs-on: windows-11-arm$" .github/workflows/windows-regression-detector.yml`
      equals 1.
    - `rg -c "^    timeout-minutes: 20$" .github/workflows/windows-regression-detector.yml` equals 1.
    - `rg -c "^        shell: bash$" .github/workflows/windows-regression-detector.yml` >= 1, and the
      substantive step declares it (a pwsh default would fail on `set -euo pipefail`).
    - `rg -o -F -- "--skip-nx-cache" .github/workflows/windows-regression-detector.yml | wc -l` >= 1.
    - `rg -o -F "Successfully ran targets build, typecheck, test for project" .github/workflows/windows-regression-detector.yml | wc -l`
      >= 1.
    - `rg -c "cron: '[1-9]" .github/workflows/windows-regression-detector.yml` equals 1 (minute field
      is not 0 -- off the top of the hour), and the cron differs from `cleanup.yml`'s:
      `rg -o "cron: '[^']*'" .github/workflows/cleanup.yml .github/workflows/windows-regression-detector.yml`
      returns two DIFFERENT values.
    - `rg -c "contents: read" .github/workflows/windows-regression-detector.yml` equals 1.
    - `rg -c -F "concurrency:" .github/workflows/windows-regression-detector.yml` equals 0.
    - The file is pure ASCII: `rg -c '[^\x00-\x7F]' .github/workflows/windows-regression-detector.yml`
      returns 0 matches (exit 1).
    - `npx nx format:check --all` passes.
  </acceptance_criteria>

  <done>
    The workflow exists, satisfies every plan 12-01 clause, hard-fails by design, demands the plural
    three-target success line, carries no cache tier, and its header records XOS-05's
    identical-observation sentence at the point the detector is added.
  </done>
</task>

<task type="auto">
  <name>Task 2: prove the COMMAND on Windows arm64, and OBSERVE the needle's red</name>

  <files>(no file changes -- two recorded measurements written into the SUMMARY)</files>

  <read_first>
    - `.github/workflows/windows-regression-detector.yml` as written by Task 1, so the command you
      run locally is character-identical to the one the runner will run.
    - `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md` `### M-2`, whose final
      paragraph names the exact mutation: delete one target from the `-t` list, NOT from the workflow.
    - `.planning/phases/08-nx-task-hash-parity/deferred-items.md` item 1 -- the unattributed `test`
      failure at `69bd1b7`, whose output was discarded once already.
  </read_first>

  <action>
    Two measurements on this Windows arm64 workstation, both recorded verbatim in the SUMMARY. This
    is RESEARCH Correction 2's path (2) -- prove the COMMAND, not the job -- because the job itself
    cannot be dispatched until its file reaches the default branch, and the substantive risk ("do
    these three targets pass on Windows with the cache bypassed?") is closable now.

    **Measurement A -- the positive control.** Run the detector's command exactly as the workflow
    declares it, with `NO_COLOR=1` in the environment:
    `npm exec -- nx run-many -t build typecheck test --skip-nx-cache`. Record: the exit code, the
    per-target pass counts, the wall time, the `Cache:` line Nx prints, and the FULL final success
    line verbatim. Then confirm the workflow's needle matches that line as a literal substring.
    If the exact printed line differs in ANY character from
    `Successfully ran targets build, typecheck, test for project @op-nx/github-cache`, the workflow's
    needle is wrong -- fix the needle and the plan 12-01 spec clause in ONE commit and re-run. Do not
    adjust the observation to fit the prediction.

    **Measurement B -- the observed RED for the needle's non-vacuity.** Re-run with ONE target
    dropped from the `-t` list: `npm exec -- nx run-many -t build test --skip-nx-cache` with
    `NO_COLOR=1`. Record the success line it prints. Confirm TWO things: the run EXITS 0 (so the exit
    code is not the gate), and the three-target needle does NOT appear in that output. That pair IS
    the red observation -- it is what proves the guard would catch a target that silently stopped
    resolving. Then confirm the naive short needle DOES appear in the same output, which is the
    positive demonstration that the obvious needle is vacuous and the plural one is not. Do not
    predict either result; run both and transcribe them.

    **If the `test` target fails on either run, STOP and capture the output BEFORE any re-run.** The
    `69bd1b7` `test` failure has never been attributed and its output was discarded once already;
    Nx's flaky-task detection then recorded a FAILURE and a SUCCESS at the same hash. A Windows box
    with the cache bypassed is the likeliest place for it to surface. Record the full output, the Nx
    version, the Node version and the install mode in the SUMMARY, append the observation to
    `.planning/phases/08-nx-task-hash-parity/deferred-items.md` item 1, and report it -- do not
    re-run to get a green.

    Do NOT commit `detector.log` or any other run artifact. If a log file lands in the working tree,
    delete it and confirm `git status --porcelain` is clean apart from the intended changes.
  </action>

  <verify>
    <automated>NO_COLOR=1 npm exec -- nx run-many -t build typecheck test --skip-nx-cache 2>&1 | rg -q -F "Successfully ran targets build, typecheck, test for project"</automated>
  </verify>

  <acceptance_criteria>
    - Measurement A: the command exits 0, and its output contains the exact literal
      `Successfully ran targets build, typecheck, test for project` -- verified by the `<automated>`
      command above, whose exit status IS the assertion.
    - Measurement A: the SUMMARY records the exit code, the full final success line verbatim, the
      per-target result, and the `Cache:` line, with `Cache:` explicitly marked NON-DISCRIMINATING
      in both directions (it prints an identical `0%` for a run with no remote tier at all, and a
      non-zero count includes local hits).
    - Measurement B: `NO_COLOR=1 npm exec -- nx run-many -t build test --skip-nx-cache 2>&1 | rg -q -F "Successfully ran targets build, typecheck, test for project"`
      exits NON-ZERO (the needle is absent) while the underlying `nx` invocation itself exited 0.
      Both facts recorded.
    - Measurement B: the SUMMARY records the two-target success line verbatim, demonstrating that a
      needle matching only the singular prefix would have passed on a two-of-three run.
    - `git status --porcelain` shows no `detector.log` and no other stray artifact.
    - If `test` failed: the full output is captured in the SUMMARY and appended to
      `deferred-items.md` item 1 BEFORE any re-run, and the plan reports rather than retries.
  </acceptance_criteria>

  <done>
    The three-target command is MEASURED green on Windows arm64 with the cache bypassed, and the
    plural needle is MEASURED non-vacuous by a two-target run that exits 0 while failing the needle.
    Both observations are transcribed, not predicted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a new workflow trigger -> a job holding the default `GITHUB_TOKEN` | any new trigger widens the set of contexts in which repository code executes with a token |
| the detector job -> (nothing) | deliberately terminal: no cache tier, no credential beyond the implicit checkout, no write of any kind |

**Delta: one new workflow file with two triggers, both collaborator-only, and a job that writes
nothing.** No new credential, no new write path, no package, no network egress beyond `npm ci` and
`actions/checkout`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Elevation of privilege | the detector's `schedule` + `workflow_dispatch` triggers | low | mitigate | Both are in the trusted, collaborator-only set per the 2026-06-26 changelog. `schedule` is already in `TRUSTED_EVENTS`; `workflow_dispatch` is deliberately absent from BOTH allowlists and stays that way. The detector needs no cache write, so the distinction never matters here. Workflow-level `permissions: contents: read`, no `actions:` scope, no `concurrency:` mechanism |
| T-12-04 | Elevation of privilege | `$GITHUB_ENV` writes in the detector | critical **if present** | mitigate | CHECKED: the detector writes NOTHING to `$GITHUB_ENV`. `NO_COLOR` is a step-level `env:` literal, which is a different mechanism. **BLOCKER if any task adds an `echo` into `$GITHUB_ENV` of a value derived from downloaded or checked-out content** -- that is the T-11-27 shape, the most recent live Critical finding on this repo |
| T-12-13 | Tampering | a detector that passes on a partial run | medium | mitigate | The plural three-target needle, source-traced to `formatting-utils.js:37`, plus Task 2's MEASURED two-target red. Exit 0 is explicitly not accepted: `nx run-many` on a missing target prints "NX No tasks were run" and exits 0 |
| T-12-14 | Denial of service | an extra cache producer / cache pressure | low | mitigate | `--skip-nx-cache` skips READ and WRITE at Nx 23.1.0 (source-traced through all five `postRunSteps` call sites), and no remote-cache client variable is set, so no remote tier participates. The detector adds zero entries to the 10 GB per-repo Actions-cache budget and zero assets to the Releases mirror |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package installed by this plan.** VERIFIED: `files_modified` is one workflow file; no `package.json`, no `package-lock.json`. `12-RESEARCH.md`'s Package Legitimacy Audit records the phase installs nothing. `npm ci` inside the workflow installs the EXISTING pinned lockfile and adds no specifier. Any new dependency is a scope escape and the gate applies from scratch |
</threat_model>

<verification>
- `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "detector"` exits 0.
- The three-target command is measured green on Windows arm64 and the plural needle is measured
  non-vacuous. Both transcribed in the SUMMARY.
- `npx nx format:check --all` and `npm run lint` pass.
- The full-suite green check belongs to the WAVE gate, not to this plan in isolation: plan 12-02 runs
  in the same wave and closes the `ci.yml` Windows-job guards. After the wave merges, run
  `npm run test && npm run typecheck && npm run lint && npx nx format:check --all`.
- ROBUST-04: this plan edits no `serve()`-reachable source. Confirm rather than assume with
  `npm run check:action` FROM THE MAIN WORKING TREE, never a junctioned worktree (esbuild rewrites
  689 module paths there and reports false drift with no source edit).

## What this plan explicitly does NOT close

Stated so a reader does not look for evidence that cannot exist yet:

- **That the detector goes green on a real `windows-11-arm` runner.** `workflow_dispatch` only fires
  for a workflow file present on the DEFAULT branch, so a brand-new file on a feature branch cannot
  be dispatched at all. This is a POST-MERGE first-run close, the pattern this repo already uses by
  name for `ppe`, `dogfood-*` and `consumer-smoke`. Surface it as a human-verify item.
- **That the runner label resolves and the YAML parses on GitHub's side.** Structurally guarded by
  plan 12-01's spec; the parse itself is post-merge.
</verification>

<success_criteria>
- `.github/workflows/windows-regression-detector.yml` exists with one `windows-11-arm` job, daily
  off-the-hour cron plus `workflow_dispatch`, `contents: read`, no sidecar, no concurrency block,
  `shell: bash`, `NO_COLOR: '1'`, `--skip-nx-cache`, and the plural three-target success needle.
- Its header records XOS-05's identical-observation sentence and the hard-fail rationale.
- `workflow_dispatch`'s comment states its real justification (on-demand re-run AFTER merge) and
  explicitly disclaims the pre-merge-proof claim.
- Two commits: `feat(12-03): ...` for Task 1 and `test(12-03): ...` (or `docs(12-03): ...` if no file
  changes) recording Task 2's measurements.
</success_criteria>

<output>
Create `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-03-SUMMARY.md` when done.
Record: the chosen cron; the exact success line printed by the three-target run; the exact success
line printed by the two-target run; the exit codes of both; and whether the `69bd1b7` `test` failure
surfaced.
</output>
