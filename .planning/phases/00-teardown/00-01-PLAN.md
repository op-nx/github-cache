---
phase: 00-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/op-nx-github-cache/
  - start-cache-server/
  - publish-mirror/
  - .verdaccio/
  - tsconfig.json
  - nx.json
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npx nx show projects` lists only `@op-nx/source` (no `@op-nx/github-cache`)"
    - "`test ! -d packages/op-nx-github-cache && test ! -d start-cache-server && test ! -d publish-mirror && test ! -d .verdaccio` all pass (dirs gone)"
    - "`npx nx sync:check` exits 0 (TS project references match the graph; root tsconfig cleaned)"
    - "`git grep -nE 'op-nx-github-cache|@actions/cache|@octokit|verdaccio' -- nx.json package.json tsconfig.json` returns no matches (dangling PoC refs scrubbed)"
    - "`npm ci` exits 0 (package.json and package-lock.json back in sync)"
    - "`git grep -F 'node -p process.platform' -- nx.json` returns a match AND `git grep -F 'eol=lf' -- .gitattributes` returns a match (dormant Phase-3 invariants preserved, D-03)"
  artifacts:
    - "nx.json (scrubbed; `integration` targetDefault + discriminator preserved)"
    - "package.json (`local-registry` target + `verdaccio` devDep removed)"
    - "package-lock.json (resynced by npm install; PoC deps pruned)"
    - "tsconfig.json (generator-updated; PoC project reference removed)"
  key_links:
    - "nx.json `integration` targetDefault `{ runtime: node -p process.platform }` discriminator preserved -> Phase 3 cross-OS correctness foundation (D-03)"
    - "package.json <-> package-lock.json in sync -> every CI job's `npm ci` gate"
---

<objective>
Remove the spike/PoC project `@op-nx/github-cache` and its non-graph siblings, scrub the
dangling references the Nx generator leaves behind, and resync the lockfile - leaving the Nx
workspace shell graph-clean (SC1) and green (SC4).

Purpose: SC1 (project + siblings gone, graph resolves with zero dangling references) and the
config half of SC4 (workspace shell intact and green). This is the core teardown; plans 02
and 03 tear down the coupled CI and stale docs in parallel.

Output: a shell-only Nx workspace whose only project is `@op-nx/source`, with no dangling
`nx.json` / `package.json` / `tsconfig.json` references and a resynced `package-lock.json`.
</objective>

## Artifacts this plan leaves

This plan deletes far more than it creates. It LEAVES:
- `nx.json` - scrubbed of the two dangling `typecheck.inputs` paths and the two PoC
  `externalDependencies` (`@actions/cache`, `@octokit/rest`), with the `integration`
  targetDefault + `node -p process.platform` discriminator and all generic parts KEPT (D-02/D-03).
- `package.json` - with the `nx.targets.local-registry` block and the `verdaccio` devDep
  removed (D-04), and `nx.includedScripts: []` plus the `build`/`typecheck`/`test`/`integration`/
  `format*` scripts KEPT (they become green no-ops, not dangling).
- `package-lock.json` - resynced (verdaccio, `@actions/cache`, `@octokit/*`, and the workspace
  package pruned).
- `tsconfig.json` - the generator-updated root tsconfig (PoC project reference removed).
It DELETES: `packages/op-nx-github-cache/`, `start-cache-server/`, `publish-mirror/`, `.verdaccio/`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-teardown/00-CONTEXT.md
@.planning/phases/00-teardown/00-RESEARCH.md
@nx.json
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the PoC project via the Nx generator and delete its non-graph siblings</name>
  <files>packages/op-nx-github-cache/, tsconfig.json, start-cache-server/, publish-mirror/, .verdaccio/</files>
  <read_first>.planning/phases/00-teardown/00-RESEARCH.md (Q1 generator contract), .planning/phases/00-teardown/00-CONTEXT.md (D-01)</read_first>
  <action>
    Invoke the `nx-generate` skill first (AGENTS.md rule for generator work). Confirm the
    generator contract at execute time: run `npx nx g @nx/workspace:remove --help` (NEVER guess
    flags), then `npx nx g @nx/workspace:remove @op-nx/github-cache --dry-run --no-interactive`
    and confirm the preview DELETEs the whole `packages/op-nx-github-cache/` tree and UPDATEs
    only root `tsconfig.json` (removing the project reference). Then run the real removal:
    `npx nx g @nx/workspace:remove @op-nx/github-cache --no-interactive`. Do NOT pass
    `--forceRemove` (the graph shows zero dependents per D-01; run without it so an unexpected
    in-use report surfaces rather than being forced past). The generator does NOT own the sibling
    directories, so delete the three tracked siblings by name with `git rm -r start-cache-server
    publish-mirror .verdaccio` (specific paths only - never `git add -A`/`git rm` by glob). Do
    NOT touch `.gitattributes`, `vitest.workspace.ts`, `tsconfig.base.json`, or the `integration`
    targetDefault - those are load-bearing shell (handled/preserved elsewhere).
  </action>
  <acceptance_criteria>
    - `npx nx show projects` -> lists `@op-nx/source` only; does NOT contain `@op-nx/github-cache`
    - `test ! -d packages/op-nx-github-cache` -> exit 0
    - `test ! -d start-cache-server && test ! -d publish-mirror && test ! -d .verdaccio` -> exit 0
    - `git grep -F 'op-nx-github-cache' -- tsconfig.json` -> no match (exit 1; generator cleaned the reference)
  </acceptance_criteria>
  <verify>
    <automated>npx nx show projects</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Scrub dangling PoC residue from nx.json and package.json, preserving the shell</name>
  <files>nx.json, package.json</files>
  <read_first>nx.json, package.json, .planning/phases/00-teardown/00-RESEARCH.md (Q2 scrub checklist), .planning/phases/00-teardown/00-CONTEXT.md (D-02, D-03, D-04)</read_first>
  <action>
    In `nx.json` make exactly three edits (D-02): (1) in `targetDefaults.typecheck.inputs` DELETE
    the two relative-path fileset strings that point into the deleted package (the two
    `^{projectRoot}/../...` entries referencing `packages/op-nx-github-cache/tsconfig.lib.json`
    and `tsconfig.spec.json`, nx.json lines 124-125) - they become dangling after removal; (2) in
    `targetDefaults.build.inputs` remove `@actions/cache` and `@octokit/rest` from the
    `externalDependencies` array, KEEPING `typescript`, `tslib`, `@types/node`; (3) do the same in
    `targetDefaults.typecheck.inputs` `externalDependencies`. KEEP everything else: `namedInputs`,
    `plugins`, `analytics: false`, `release.version.preVersionCommand`, the generic `test`/`build`/
    `typecheck` defaults, and - critically (D-03) - the entire `integration` targetDefault
    INCLUDING its `node -p process.platform` runtime discriminator. Scoping `externalDependencies`
    to the real toolchain closure (not the whole graph) is a cross-OS hash-parity guard, so keep
    the explicit list; just drop the two PoC deps. In `package.json` make two edits (D-04): remove
    the entire `nx.targets.local-registry` block (it points at the now-deleted `.verdaccio/config.yml`),
    KEEPING `nx.includedScripts: []`; and remove the `verdaccio` devDependency line. KEEP the
    `build`/`typecheck`/`test`/`integration`/`format`/`format:check` scripts (they become green
    no-ops, not dangling) and the `workspaces: ["packages/*"]` glob.
  </action>
  <acceptance_criteria>
    - `git grep -nE 'op-nx-github-cache|@actions/cache|@octokit|verdaccio' -- nx.json package.json` -> no matches (exit 1)
    - `git grep -F 'local-registry' -- package.json` -> no match (exit 1)
    - `git grep -F 'node -p process.platform' -- nx.json` -> match present (integration discriminator preserved, D-03)
    - `git grep -nE '"typescript"|"tslib"|"@types/node"' -- nx.json` -> matches present (toolchain closure kept)
    - `node -e "JSON.parse(require('fs').readFileSync('nx.json','utf8')); JSON.parse(require('fs').readFileSync('package.json','utf8'))"` -> exit 0 (both still valid JSON)
  </acceptance_criteria>
  <verify>
    <automated>git grep -nE 'op-nx-github-cache|@actions/cache|@octokit|verdaccio' -- nx.json package.json; test $? -eq 1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Resync package-lock.json so npm ci passes</name>
  <files>package-lock.json</files>
  <read_first>.planning/phases/00-teardown/00-RESEARCH.md (Pitfall 1; Runtime State Inventory - build artifacts row)</read_first>
  <action>
    The `nx g @nx/workspace:remove` generator runs NO install step, so removing the `verdaccio`
    devDep and the workspace package left `package-lock.json` out of sync - `npm ci` (every CI job)
    then errors on package/lock mismatch. Run `npm install` to prune `verdaccio` (~133 lockfile
    refs), `@actions/cache`, `@octokit/*`, and the `packages/op-nx-github-cache` workspace entry
    from the lockfile and node_modules. This installs zero NEW packages - it is a prune-only resync
    (RESEARCH Environment Availability: no new dependency is introduced). Stage `package-lock.json`
    by name.
  </action>
  <acceptance_criteria>
    - `npm ci` -> exit 0 (lockfile in sync with package.json)
    - `git grep -nE 'op-nx-github-cache|verdaccio|@octokit' -- package-lock.json` -> no matches (exit 1)
  </acceptance_criteria>
  <verify>
    <automated>npm ci</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (removal-only) | Teardown is a net attack-surface REDUCTION; no new input, auth, or crypto surface. The only security-relevant risk is losing a dormant control while deleting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Tampering | nx.json `integration` targetDefault + discriminator; `.gitattributes eol=lf` | medium | mitigate | D-03: KEEP both. Acceptance asserts `node -p process.platform` survives in nx.json and `eol=lf` survives in `.gitattributes`; a diff touching either is a teardown regression (Phase-3 cross-OS foundation). |
| T-00-02 | Tampering (supply chain) | `npm install` lockfile resync | low | accept | Resync PRUNES only (verdaccio/@actions/cache/@octokit/workspace pkg); adds zero new packages (RESEARCH: "No NEW external dependency is introduced"). Package-legitimacy gate N/A - no `[ASSUMED]`/`[SUS]` package to verify. |
| T-00-03 | Information disclosure | removed package + resynced lockfile | low | accept | The PoC stored no repo secrets (runner-injected tokens only); the removal commits no credential. |
</threat_model>

<verification>
Structural + graph-clean gate for this plan (the comprehensive cross-tree SC1 grep and the
green-CI battery run in plan 04 after all Wave-1 work merges):
- `npx nx show projects` -> `@op-nx/source` only
- `npx nx sync:check` -> exit 0
- sibling dirs + package dir absent (`test ! -d ...`)
- `git grep -nE 'op-nx-github-cache|@actions/cache|@octokit|verdaccio' -- nx.json package.json tsconfig.json package-lock.json` -> no matches
- `npm ci` -> exit 0
- dormant invariants present (`node -p process.platform` in nx.json; `eol=lf` in `.gitattributes`)
</verification>

<success_criteria>
SC1 (project + siblings gone, graph resolves with zero dangling references to any of them) and
the config half of SC4 (shell intact, `nx run-many` resolves green) are met when every
acceptance criterion above passes.
</success_criteria>

<output>
Create `.planning/phases/00-teardown/00-01-SUMMARY.md` when done.
</output>
