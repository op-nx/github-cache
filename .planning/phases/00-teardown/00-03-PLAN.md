---
phase: 00-teardown
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .prettierignore
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`.prettierignore` contains `.planning/`, `CLAUDE.md`, `AGENTS.md`, and `.claude/`, plus the pre-existing entries (`/dist`, `/coverage`, `/.nx/cache`, `/.nx/workspace-data`, `.nx/self-healing`)"
    - "`npx nx format:check --all` exits 0"
    - "`README.md` contains no reference to `op-nx-github-cache` and no link to `packages/op-nx-github-cache/README.md`"
  artifacts:
    - ".prettierignore (agent + planning docs added, existing entries kept)"
    - "README.md (neutral workspace-shell placeholder)"
  key_links:
    - "`.prettierignore` scopes `format:check --all` to real source -> SC3 format gate stays green through future `.planning` churn (incl. SC5's map-codebase writes)"
---

<objective>
De-prime the two stale docs the teardown would otherwise leave red or misleading: make
`nx format:check --all` green by ignoring the churny agent/planning docs (D-07), and trim the
root `README.md` to a neutral shell placeholder with no PoC references or dead links (D-08).

Purpose: the format half of SC3 (a green `format:check --all` gate) and the README half of SC5
(no rebuild-priming artifact in the root README).

Output: a `.prettierignore` that keeps the format gate on real source only, and a README that
describes the empty workspace shell.
</objective>

## Artifacts this plan leaves

- `.prettierignore` - existing entries KEPT, with `.planning/`, `CLAUDE.md`, `AGENTS.md`,
  `.claude/` added. Rationale (D-07): `nx format:check --all` scans the whole tree and currently
  exits 1 on unformatted TRACKED docs (none are workspace source), and SC5's map-codebase writes
  more `.planning` markdown - so without this the kept `--all` mechanic ships a red gate. These are
  GSD/agent docs, not part of the consumer contract (dogfood-safe). NOT a repo-wide reformat.
- `README.md` - a minimal neutral placeholder (project name + "greenfield rebuild in progress",
  no PoC references, no dead links). It does NOT pre-write Phase 6's adoption docs.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-teardown/00-CONTEXT.md
@.planning/phases/00-teardown/00-RESEARCH.md
@.prettierignore
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agent + planning docs to .prettierignore</name>
  <files>.prettierignore</files>
  <read_first>.prettierignore, .planning/phases/00-teardown/00-CONTEXT.md (D-07), .planning/phases/00-teardown/00-RESEARCH.md (Pitfall 2, Open Question 1)</read_first>
  <action>
    Append four entries to `.prettierignore` (D-07): `.planning/`, `CLAUDE.md`, `AGENTS.md`,
    `.claude/`. KEEP every existing entry (`/dist`, `/coverage`, `/.nx/cache`,
    `/.nx/workspace-data`, `.nx/self-healing`). Do NOT run a repo-wide `nx format:write` and do NOT
    reformat the docs in place - the point is to scope the gate to real workspace source so future
    GSD/agent doc writes (including SC5's map-codebase output) do not re-redden CI. Keep the file
    ASCII, LF-terminated.
  </action>
  <acceptance_criteria>
    - `git grep -F '.planning/' -- .prettierignore` AND `git grep -F 'CLAUDE.md' -- .prettierignore` AND `git grep -F 'AGENTS.md' -- .prettierignore` AND `git grep -F '.claude/' -- .prettierignore` -> all match
    - `git grep -F '/.nx/cache' -- .prettierignore` -> match present (existing entries retained)
    - `npx nx format:check --all` -> exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx nx format:check --all</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md as a neutral workspace-shell placeholder</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/00-teardown/00-CONTEXT.md (D-08), .planning/phases/00-teardown/00-RESEARCH.md (Q2 item 10)</read_first>
  <action>
    Replace `README.md` with a minimal neutral placeholder (D-08). The current README calls the
    deleted package "the only package" and links `packages/op-nx-github-cache/README.md` (now a
    broken link) - both rebuild-priming artifacts SC5 forbids. Keep only: the workspace name
    (`@op-nx/source`), a one-line "greenfield rebuild in progress" note, and the generic
    `npx nx run-many -t build test` build/test line. Remove all PoC prose, the "only package"
    claim, and the broken package-README link. Do NOT pre-write Phase 6 adoption docs, install
    instructions, or a feature list. Keep it ASCII (no emojis / non-ASCII per project rules), and
    do not include any email address or the work domain (public repo).
  </action>
  <acceptance_criteria>
    - `git grep -F 'op-nx-github-cache' -- README.md` -> no match (exit 1)
    - `git grep -F 'packages/op-nx-github-cache/README.md' -- README.md` -> no match (exit 1, broken link gone)
    - `git grep -F 'the only package' -- README.md` -> no match (exit 1)
    - README.md is non-empty and mentions `@op-nx/source`
  </acceptance_criteria>
  <verify>
    <automated>git grep -F 'op-nx-github-cache' -- README.md; test $? -eq 1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (docs only) | `.prettierignore` and `README.md` carry no runtime trust boundary; teardown is net attack-surface reduction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-07 | Information disclosure | README.md content | low | accept | Neutral placeholder only; action forbids secrets, any email address, and the work domain (public-repo hygiene). |
| T-00-08 | Tampering | `.prettierignore` scope creep | low | accept | Only four doc paths added; the gate stays on real source. No consumer-contract file is ignored (dogfood-safe, D-07). |
</threat_model>

<verification>
- `.prettierignore` contains the four new entries + all existing entries
- `npx nx format:check --all` -> exit 0
- `README.md` free of `op-nx-github-cache`, the broken package-README link, and the "only package" claim
</verification>

<success_criteria>
The format half of SC3 (`format:check --all` green) and the README half of SC5 (no
rebuild-priming artifact in the root README) are met when every acceptance criterion passes.
</success_criteria>

<output>
Create `.planning/phases/00-teardown/00-03-SUMMARY.md` when done.
</output>
