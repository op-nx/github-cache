---
phase: 00-teardown
plan: 04
type: execute
wave: 2
depends_on:
  - 00-01
  - 00-02
  - 00-03
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npx nx sync:check` exits 0 (`The workspace is up to date`)"
    - "`npx nx show projects` lists only `@op-nx/source`"
    - "`npx nx graph --print` emits valid JSON and exits 0 (graph builds, no dangling references)"
    - "`npx nx run-many -t build test typecheck integration` exits 0 (green no-op baseline)"
    - "`npm ci && npm run build && npm run typecheck && npm run test && npm run integration` all exit 0"
    - "`npx nx format:check --all` exits 0"
    - "`git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|verdaccio|@actions/cache|@octokit' -- ':!.planning'` returns no matches across the merged tree (SC1 no-dangling gate)"
  artifacts: []
  key_links:
    - "a graph-clean + committed teardown is the precondition for plan 05's `/gsd:map-codebase` (D-06/Q6): a broken graph or out-of-sync lockfile would poison the regenerated map"
---

<objective>
Run the SC1-SC4 acceptance-command battery across the fully-merged, torn-down workspace to
prove the teardown is graph-clean and green with no remote cache - the "no dangling reference"
gate (SC1/SC4) and the local-cache-only green proof (SC3). This is a verification-only plan; it
writes no source, only the clean-state proof that gates the de-priming in plan 05.

Purpose: SC1 (graph resolves with zero dangling references), SC3 (5 targets green on local cache
only), SC4 (shell intact + green). It is the last gate before de-priming, because
`/gsd:map-codebase` must run against a committed, graph-clean workspace (D-06).

Output: the SUMMARY recording every acceptance command's exit code - the clean-state proof.
</objective>

## Artifacts this plan leaves

Nothing on disk (verification-only). It LEAVES a SUMMARY documenting the green battery. If any
command fails, treat it as a teardown regression from Wave 1 and fix it (or route back through
`/gsd:plan-phase 0 --gaps`) before plan 05 runs - do NOT run `/gsd:map-codebase` against a
red/out-of-sync workspace.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-teardown/00-CONTEXT.md
@.planning/phases/00-teardown/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graph-clean + no-dangling-reference gate (SC1, SC2, SC4)</name>
  <files>(none - verification only)</files>
  <read_first>.planning/phases/00-teardown/00-RESEARCH.md (Q3 verification battery; Validation Architecture SC map)</read_first>
  <action>
    Invoke the `nx-workspace` skill first (AGENTS.md rule for workspace queries). On the merged
    tree (Wave-1 plans 01/02/03 complete), run the structural gate that proves zero dangling
    references. Run `npx nx sync:check` (expect `The workspace is up to date`, exit 0);
    `npx nx show projects` (expect `@op-nx/source` only); `npx nx graph --print` (expect valid
    JSON, exit 0). Then run the cross-tree dangling grep EXCLUDING `.planning` (its historical docs
    intentionally still mention the PoC per D-06): `git grep -nE
    'op-nx-github-cache|start-cache-server|publish-mirror|verdaccio|@actions/cache|@octokit' --
    ':!.planning'` and expect no matches. Also confirm SC2 on the merged ci.yml:
    `git grep -nE 'start-cache-server|nx reset|windows-selfcheck|publish-mirror' --
    .github/workflows/ci.yml` -> no matches, and `test ! -f .github/workflows/mirror-cleanup.yml`.
    Record every exit code in the SUMMARY. Treat any failure as a teardown regression to fix
    before plan 05.
  </action>
  <acceptance_criteria>
    - `npx nx sync:check` -> exit 0
    - `npx nx show projects` -> lists `@op-nx/source`; no `@op-nx/github-cache`
    - `npx nx graph --print` -> exit 0, valid JSON
    - `git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|verdaccio|@actions/cache|@octokit' -- ':!.planning'` -> no matches (exit 1)
    - `git grep -nE 'start-cache-server|nx reset|windows-selfcheck|publish-mirror' -- .github/workflows/ci.yml` -> no matches (exit 1)
    - `test ! -f .github/workflows/mirror-cleanup.yml` -> exit 0
    - dormant invariants still present: `git grep -F 'node -p process.platform' -- nx.json` and `git grep -F 'eol=lf' -- .gitattributes` both match
  </acceptance_criteria>
  <verify>
    <automated>git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|verdaccio|@actions/cache|@octokit' -- ':!.planning'; test $? -eq 1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Green local-cache-only battery (SC3, SC4)</name>
  <files>(none - verification only)</files>
  <read_first>.planning/phases/00-teardown/00-RESEARCH.md (Q3 exit-code gotcha; Q4 no-op matrix; Validation Architecture full gate)</read_first>
  <action>
    Prove the reworked baseline is green on Nx's LOCAL cache only (no remote cache present). Run
    `npm ci` (expect exit 0 - lockfile in sync), then `npm run build`, `npm run typecheck`,
    `npm run test`, `npm run integration` - each an `nx run-many -t <target>` that no remaining
    project has, so each exits 0 as a green no-op printing `No tasks were run` (RESEARCH Q3
    verified this exit-0 behavior; it is NOT an error). Then `npx nx run-many -t build test
    typecheck integration` as a single combined check (exit 0). Finally `npx nx format:check --all`
    (exit 0, relies on plan 03's `.prettierignore`). Record each exit code in the SUMMARY. Do NOT
    add any special-casing or `if:` guards - the no-op green is the intended baseline.
  </action>
  <acceptance_criteria>
    - `npm ci` -> exit 0
    - `npm run build` / `npm run typecheck` / `npm run test` / `npm run integration` -> each exit 0
    - `npx nx run-many -t build test typecheck integration` -> exit 0 (`No tasks were run`)
    - `npx nx format:check --all` -> exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm ci && npm run build && npm run typecheck && npm run test && npm run integration && npx nx format:check --all</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (read-only verification) | No new surface. This gate re-confirms the security-relevant invariants Wave 1 preserved (reduced permissions, dormant discriminator). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-09 | Tampering | dormant Phase-3 invariants surviving teardown | medium | mitigate | The gate asserts `node -p process.platform` (nx.json) and `eol=lf` (.gitattributes) are still present on the merged tree - a Wave-1 regression that dropped either fails this plan before de-priming. |
| T-00-10 | Elevation of Privilege | ci.yml permission posture (cross-check) | low | accept | Re-greps ci.yml for `contents: write`/`actions:` as a merged-tree cross-check of plan 02's least-privilege mitigation. |
</threat_model>

<verification>
The full acceptance battery (this plan IS the verification):
- Quick: `npx nx sync:check && npx nx show projects`
- Full: `npm ci && npm run build && npm run typecheck && npm run test && npm run integration && npx nx format:check --all`
- No-dangling: `git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|verdaccio|@actions/cache|@octokit' -- ':!.planning'` -> no matches
All must pass before plan 05 (de-priming) runs.
</verification>

<success_criteria>
SC1 (zero dangling references), SC3 (5 targets green on local cache only), and SC4 (shell
intact + green) are proven when every acceptance command exits as specified. This clean,
committed state is the precondition for plan 05.
</success_criteria>

<output>
Create `.planning/phases/00-teardown/00-04-SUMMARY.md` when done, recording every acceptance
command's exit code.
</output>
