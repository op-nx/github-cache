---
phase: 00-teardown
plan: 05
type: execute
wave: 3
depends_on:
  - 00-04
files_modified:
  - .planning/codebase/
autonomous: false
requirements: []
must_haves:
  truths:
    - "`/gsd:map-codebase` has regenerated `.planning/codebase/*.md` against the torn-down, shell-only workspace"
    - "`git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|selectBackend|CacheBackend|@actions/cache|@octokit' -- '.planning/codebase/**'` returns no matches (no PoC trace in the regenerated map)"
  artifacts:
    - ".planning/codebase/{ARCHITECTURE,STACK,STRUCTURE,CONVENTIONS,INTEGRATIONS,CONCERNS,TESTING}.md (regenerated; thin empty-shell map)"
  key_links:
    - "map-codebase runs LAST, after the plan 01-04 teardown commits land and the graph is proven clean (D-06/Q6) - so the regenerated map reflects the deleted-PoC state"
---

<objective>
Complete de-priming (SC5): regenerate the codebase map against the torn-down, shell-only
workspace so `.planning/codebase/*` no longer describes the deleted PoC, then confirm no
rebuild-priming artifact remains.

Purpose: SC5 (codebase map regenerated, no PoC trace). Sequenced LAST (D-06) because
`/gsd:map-codebase` analyzes the LIVE workspace and must run against the committed, graph-clean
teardown proven by plan 04 - a broken graph or out-of-sync lockfile would poison the map.

Output: a regenerated, thin `.planning/codebase/*` describing an empty Nx workspace shell, plus
a grep-based confirmation that no PoC trace survives.
</objective>

## Artifacts this plan leaves

- `.planning/codebase/{ARCHITECTURE,STACK,STRUCTURE,CONVENTIONS,INTEGRATIONS,CONCERNS,TESTING}.md`
  - regenerated against the shell-only workspace. Expect a THIN map describing an empty Nx
  workspace (no `src/`, no cache project, no actions) - that is correct, not a defect.

Note: PoC mentions in `.planning/ROADMAP.md`, `PROJECT.md`, and `.planning/research/*` are
intentional historical/target-spec context, NOT rebuild-priming artifacts, and are out of SC5's
regeneration scope (D-06). The SC5 grep is scoped to `.planning/codebase/**` only.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-teardown/00-CONTEXT.md
@.planning/phases/00-teardown/00-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Orchestrator/human runs /gsd:map-codebase against the torn-down workspace</name>
  <action>
    Orchestrator/human step (D-06, SC5 caveat): the executor subagent cannot invoke a GSD slash
    command, so pause here and have the orchestrator/human run `/gsd:map-codebase` at the repo root
    AFTER confirming the plan 01-04 teardown commits have landed and plan 04's battery is green.
    It regenerates `.planning/codebase/*` against the shell-only workspace. Resume with task 2 once
    the map is regenerated.
  </action>
  <what-built>
    Plans 01-04 tore down the PoC and proved the workspace graph-clean + green. The remaining
    de-priming step is to regenerate the codebase map against that torn-down state. `/gsd:map-codebase`
    is a GSD slash command that spawns mapper agents - it cannot be invoked from inside the executor
    subagent, so the orchestrator/human runs it.
  </what-built>
  <how-to-verify>
    1. Confirm the plan 01-04 teardown commits have LANDED and plan 04's battery is green
       (`npx nx sync:check` exit 0, `npx nx show projects` -> `@op-nx/source` only). Do NOT proceed
       against a red or uncommitted workspace.
    2. Run `/gsd:map-codebase` at the repo root. It regenerates
       `.planning/codebase/{ARCHITECTURE,STACK,STRUCTURE,CONVENTIONS,INTEGRATIONS,CONCERNS,TESTING}.md`
       against the shell-only workspace.
    3. Expect a thin map describing an empty Nx workspace shell (no PoC, no `src/`, no actions).
  </how-to-verify>
  <resume-signal>Type "mapped" once `/gsd:map-codebase` has regenerated `.planning/codebase/*`, or describe any error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm no rebuild-priming artifact remains (SC5 final check)</name>
  <files>(none - verification only)</files>
  <read_first>.planning/phases/00-teardown/00-RESEARCH.md (Q6 de-priming mechanics + concrete verification grep)</read_first>
  <action>
    After the map is regenerated, run the SC5 no-trace grep scoped to the regenerated docs only:
    `git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|selectBackend|CacheBackend|@actions/cache|@octokit'
    -- '.planning/codebase/**'` and expect ZERO matches. (Scope is `.planning/codebase/**` ONLY -
    PoC mentions in ROADMAP.md, PROJECT.md, and `.planning/research/*` are intentional historical
    context per D-06 and are NOT in scope.) If any current-state PoC trace remains in the
    regenerated map, treat it as an SC5 failure and re-run `/gsd:map-codebase` (or hand-correct the
    offending doc) until the grep is clean. Record the result in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|selectBackend|CacheBackend|@actions/cache|@octokit' -- '.planning/codebase/**'` -> no matches (exit 1)
    - `.planning/codebase/ARCHITECTURE.md` exists and is non-empty (map was regenerated, not deleted)
  </acceptance_criteria>
  <verify>
    <automated>git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|selectBackend|CacheBackend|@actions/cache|@octokit' -- '.planning/codebase/**'; test $? -eq 1</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (docs regeneration) | No runtime surface. Concern is only that stale PoC prose (a rebuild-priming artifact) survives into the map that primes Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-11 | Information disclosure / priming | regenerated `.planning/codebase/*` | low | accept | The final grep confirms no PoC trace / rebuild-priming artifact leaks forward into the map Phase 1 reads; re-run map-codebase if any remains. |
</threat_model>

<verification>
- `/gsd:map-codebase` regenerated `.planning/codebase/*` against the torn-down workspace (checkpoint)
- `git grep -nE 'op-nx-github-cache|start-cache-server|publish-mirror|selectBackend|CacheBackend|@actions/cache|@octokit' -- '.planning/codebase/**'` -> no matches
- `.planning/codebase/ARCHITECTURE.md` present and non-empty
</verification>

<success_criteria>
SC5 (de-priming complete: codebase map regenerated against the torn-down workspace, no PoC
trace, no rebuild-priming artifact remaining) is met when the checkpoint is resolved and the
final grep returns no matches.
</success_criteria>

<output>
Create `.planning/phases/00-teardown/00-05-SUMMARY.md` when done.
</output>
