---
phase: 08-nx-task-hash-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - capture-hashes.mjs
  - package.json
  - .fallowrc.jsonc
  - .planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
autonomous: true
requirements: [PARITY-01, PARITY-02, PARITY-06]

must_haves:
  truths:
    - "A root-level dev-only ESM instrument computes an Nx task hash for all five targets and emits the per-NODE `details.nodes` map, which is what `nx show target inputs` cannot produce (PARITY-02, D-01, D-02, D-05)."
    - "The instrument's hash is proven byte-identical to what Nx itself writes into `.nx/cache/run.json` for the same task at the same commit, so the record measures Nx's arithmetic rather than a re-implementation (PARITY-02)."
    - "Every emitted record carries the Nx version, Node version, install mode, and a MEASURED (not asserted) graph state (PARITY-06, D-04)."
    - "The instrument is NOT an Nx target and cannot be Nx-cached, so a capture is never a replay (D-01b)."
    - "`08-ROOT-CAUSE.md` exists with its method sections, including the recorded proof that `nx show target inputs` is insufficient (D-03, D-06, PARITY-01)."
  artifacts:
    - "capture-hashes.mjs at the repo root"
    - "root package.json `capture:hashes` script"
    - ".fallowrc.jsonc `entry` list gains capture-hashes.mjs"
    - ".planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md with its Method, D-03 and D-09 sections"
  key_links:
    - "capture-hashes.mjs -> `nx/src/hasher/create-task-hasher.js` (an internal subpath; an Nx major that moves it must break this file at IMPORT time, never silently)."
    - "capture-hashes.mjs -> nx.json `targetDefaults.integration.inputs` runtime entry (the discriminator command string is READ from the config, never re-spelled -- D-14)."
    - "root package.json `nx.includedScripts` is `[]`, which is what structurally prevents the new script from becoming an Nx target (D-01b)."
---

<objective>
Build the measurement instrument and prove it measures Nx, then open the root-cause record with its
method sections.

Purpose: D-15's ordering is measure -> record -> fix -> gate, and nothing downstream is trustworthy
until the instrument is proven to compute the SAME number Nx computes. This plan is the "measure"
half's foundation: the tool, its self-validation against Nx's own arithmetic, and the record file
that every later plan appends to.

Output: `capture-hashes.mjs`, a `capture:hashes` npm script, the fallow entry-point declaration,
and `08-ROOT-CAUSE.md` opened with its Method, D-03-insufficiency and D-09-confound sections.

HARD ORDERING CONSTRAINT for this plan and plans 08-02, 08-03 and 08-04: **no commit may touch
`nx.json`.** D-06 requires the record to predate every fix commit and D-15 makes the ordering
load-bearing; git history is the proof, so a single stray `nx.json` hunk in any of these four plans
destroys PARITY-01's central claim.

TDD exemption, stated rather than assumed: `workflow.tdd_mode` is enabled, but this plan's artifact
is a root-level `.mjs` that is deliberately NOT linted and NOT typechecked (`eslint.config.mjs`
records the lint scope deviation; `tsconfig.action.json` covers only the esbuild-reachable graph),
and D-19 records that a spec importing an untyped root `.mjs` would not compile under `typecheck`.
So it cannot carry a co-located vitest spec. Its correctness is proven by a STRONGER check than a
unit test over a mock: Task 2 asserts byte-equality against the hash Nx itself wrote.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-nx-task-hash-parity/08-CONTEXT.md
@.planning/phases/08-nx-task-hash-parity/08-RESEARCH.md
@.planning/phases/08-nx-task-hash-parity/08-PATTERNS.md
@.planning/phases/08-nx-task-hash-parity/08-VALIDATION.md
@.planning/research/v0.0.2/PROBE-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The capture instrument</name>

  <files>
capture-hashes.mjs
package.json
.fallowrc.jsonc
  </files>

  <read_first>
- `esbuild.action.mjs` in full -- the only root-level dev-only `.mjs` in the repo. Copy its FORM:
  no shebang, top-level `await`, no `process.exit` on the happy path, and a header comment that
  states why the script exists as a script and names the rejected alternative.
- `packages/github-cache/src/nx-target-inputs.spec.ts` lines 1-44 -- the internal-subpath import
  block (`nx/src/...`, not `nx/dist/src/...`) and the posture paragraph at :35-37 that the
  instrument's header must restate.
- `packages/github-cache/pack-check.cjs` lines 32-65 and 126-171 -- the fail-loud CLI shape, the
  JSON shape-check-before-index at :56-64, and the OS-normalisation line at :63-64.
- `08-RESEARCH.md` `## Verified call sequence` (the executed four-import sequence, the three traps
  table, and the signature notes), `## The record's meta block` material inside
  `## Graph state: cold, warm, and the third state nobody names` (the `workspaceDataDirectory` /
  `getNativeFileCacheLocation` measurement snippet), `## The Hash.details.nodes key vocabulary`,
  and Pitfalls 1, 2, 4, 6, 7.
- `08-CONTEXT.md` D-01, D-02, D-03, D-04, D-05, D-14.
- `08-PATTERNS.md` section `capture-hashes.mjs (root-level dev-only ESM instrument, D-01)` -- all
  three analogs and BOTH scope facts (a root `.mjs` is neither linted nor typechecked).
- `nx.json` lines 88-103 -- `targetDefaults.integration.inputs`, whose last entry is the runtime
  discriminator string the instrument must READ rather than re-spell.
- `package.json` lines 5-20 (the scripts block, grouped by concern) and lines 50-52
  (`nx.includedScripts` is an empty array).
- `.fallowrc.jsonc` lines 9-42 -- the `entry` array, specifically the `esbuild.action.mjs` and
  `pack-check.cjs` entries and their comments.
  </read_first>

  <action>
Create `capture-hashes.mjs` at the repo ROOT, beside `esbuild.action.mjs` (D-01). It is dev-only,
never a module of the published package, and never an Nx target.

HEADER COMMENT. Carry all four elements the house convention requires, and in particular BOTH of
D-01's independent reasons, because the second is the non-obvious one: (a) it imports
`nx/src/hasher/*` and `nx` is a devDependency, so a shipped module importing it would break every
consumer install; (b) an Nx-CACHED instrument would REPLAY a stale record instead of measuring, and
a cached capture is not a capture. Restate the internal-subpath posture verbatim in spirit from
`nx-target-inputs.spec.ts:35-37`: an Nx major that moves the subpath must break this file at IMPORT
time, loud and immediate, never a silent pass. Name D-03's rejected alternative
(`HashPlanInspector` / `nx show target inputs`) and why it is rejected: it SKIPS
`ProjectConfiguration` per its own NAPI doc and reports file PATHS rather than content hashes.
Name `08-ROOT-CAUSE.md` as the artifact this feeds.

IMPORTS, exactly the four RESEARCH executed, each through the package's `./src/*` export condition
with an explicit `.js` extension: `readNxJson` from `nx/src/config/nx-json.js`;
`createProjectGraphAsync` from `nx/src/project-graph/project-graph.js`; `createTaskGraph` from
`nx/src/tasks-runner/create-task-graph.js`; `createTaskHasher` from
`nx/src/hasher/create-task-hasher.js`. Plus `workspaceDataDirectory` from
`nx/src/utils/cache-directory.js` and `getNativeFileCacheLocation` from
`nx/src/native/native-file-cache-location.js` for the measured graph state, and node builtins
`node:fs`, `node:child_process`, `node:url`.

ARGUMENTS, hand-parsed over `process.argv` -- no arg library (mark it `ponytail:`):
- `--install-mode <ci|install>` is REQUIRED with NO default. RESEARCH Open Question 4 and Pitfall 6
  are explicit: `process.env.npm_config_*` is unavailable when the script is invoked directly, so a
  defaulted flag would record a GUESS as a measurement. A missing or unrecognised value must throw
  before any measurement runs.
- `--out <path>` is OPTIONAL. When absent, the record JSON goes to STDOUT. Diagnostics ALWAYS go to
  stderr, never stdout, so stdout stays parseable JSON.
- `--diff <recordA.json> <recordB.json>` selects the alternate DIFF mode described below. It is
  mutually exclusive with capture mode.

PROJECT NAME, derived not guessed (RESEARCH Pattern 2 and trap 1): read the `name` field out of
`packages/github-cache/package.json` resolved relative to `import.meta.url`. Hardcoding the
directory name produces a `TypeError` deep inside Nx's `resolveConfiguration`; hardcoding the
scoped name works but drifts silently on a rename.

TARGETS, the five D-05 requires, as a module-level `const` array in this order: `build`,
`typecheck`, `test`, `integration`, `lint`. `lint` is present because Phase 7's D-35 hands it over
explicitly and Phase 8 is where "does `@nx/eslint` infer `lint` identically on both OSes?" is
settled empirically.

MEASURE THE GRAPH STATE BEFORE BUILDING THE GRAPH. Read `workspaceDataDirectory` and
`getNativeFileCacheLocation()`, then for each: whether it exists and how many entries it holds.
Derive `graphState` as `cold` when BOTH are absent or empty and `warm` otherwise. This must be
MEASURED, not accepted from a flag -- D-04 requires it and Pitfall 1 is the reason (a `nx reset`
that failed with EPERM leaves a warm graph behind a record that claims cold). Record the resolved
DIRECTORY PATHS as well as the verdict, so a caller that redirected them via
`NX_WORKSPACE_DATA_DIRECTORY` / `NX_NATIVE_FILE_CACHE_DIRECTORY` is visible in the record instead of
silently reclassified. Record the paths RAW, not forward-slash-normalised: a backslash IS the
Windows fact and this record is evidence. Also record `daemonEnabled` as
`process.env.NX_DAEMON !== 'false'` -- MEASURE it, do NOT force it. RESEARCH measured both daemon
states agreeing to the digit on all five targets, so the daemon is not an arithmetic hazard; it is
a fourth staleness surface with no inspectable file, which is why the CALLER disables it and the
record reports whether the caller did.

CALL SEQUENCE, per target, exactly as RESEARCH executed it: build the project graph ONCE outside
the loop with `createProjectGraphAsync({ exitOnError: false })`; read `readNxJson()` once; then per
target call `createTaskGraph(projectGraph, {}, [PROJECT], [target], undefined, {})`. The second
argument is `extraTargetDependencies`, a target-to-array map -- passing `nxJson.targetDefaults`
there throws `flatMap is not a function` (trap 2). Look up `taskGraph.tasks` at the
`<project>:<target>` key and THROW if it is undefined, with an error naming the target, the likely
causes (renamed, deleted, or its inferring plugin not registered) and the available task ids. That
error is the direct analogue of Phase 7's `nx run-many -t <missing>` learning: a deleted inferred
target must never read as "measured, no difference" (Pitfall 4). Then
`createTaskHasher(projectGraph, nxJson)` and `await hasher.hashTask(task, taskGraph, process.env)`.
Use the METHOD, not the free `hashTask` from `nx/src/hasher/hash-task.js`: the free function mutates
the task, returns void, and writes to the task-details SQLite DB during a measurement.

RECORD from each hash: `hash.value` as `hash`, `hash.details.command` as `command`, and
`hash.details.nodes` as `nodes`. Do NOT read `hash.details.runtime` for the discriminator -- in Nx
23.1.0 `createHashDetails` always emits it as an empty object, and the runtime input lands in
`nodes` under a key beginning `runtime:`.

ALSO EMIT THE MERGED PROJECT NODE. Add a top-level `projectConfiguration` key holding
`projectGraph.nodes[PROJECT].data` verbatim. This is the single most important addition beyond
D-04's minimum and it is what makes PARITY-01 answerable: the node map carries exactly ONE
`<project>:ProjectConfiguration` entry covering ALL five targets, so a difference there cannot be
localised to a field without the merged node itself -- and localising it is precisely what
RESEARCH's Finding 2 did and what Phase 7's D-35 lint hand-off needs. Note in a comment that
`metadata` inside that node is NOT hashed, so a difference there is informational rather than a
hash divergence.

DISCRIMINATOR, per D-04 and CORR-03. READ the command string out of nx.json's
`targetDefaults.integration.inputs` -- the entry carrying a `runtime` key -- rather than re-spelling
it; D-14 requires that string to stay byte-identical, and reading it means the record cannot drift
from the config. Throw if no runtime entry is found. Run it through a shell the way Nx does,
capturing stdout and stderr SEPARATELY, and record `command`, `stdout`, `stderr` and the exit
`status`. Never merge the streams: Nx's runtime hashing covers both and a shell-level stream merge
destroys the distinction CORR-03 asked for. Record the raw strings VERBATIM including trailing
newlines -- a trimmed value is a different value.

META, the D-04 seven plus the measured extras: `os` (`process.platform`), `arch`
(`process.arch`), `nxVersion` read from the INSTALLED `node_modules/nx/package.json` version (not
the declared devDependency range -- PARITY-06 wants what actually ran), `nodeVersion`
(`process.version`), `installMode` (the required flag), `graphState` (measured), `commit`
(`git rev-parse HEAD`), plus `workingTreeClean` from `git status --porcelain` being empty,
`githubSha` from `process.env.GITHUB_SHA` or null, `runnerOs` from `process.env.RUNNER_OS` or null,
`capturedAt` as an ISO timestamp, and the four graph-state directory/count fields plus
`daemonEnabled`. Measure `workingTreeClean` BEFORE writing any output file, so a capture that writes
into the workspace does not report itself dirty.

`githubSha` matters and must not be dropped as noise: on a `pull_request` event
`actions/checkout` resolves a MERGE commit whose SHA no developer can reproduce, so recording both
`commit` and `githubSha` is what makes a cross-observation-point SHA mismatch visible instead of
hidden. Plan 08-03 pins the checkout so they agree; this field is how that is checked.

OUTPUT SHAPE, top level: `meta`, `targets` (keyed by target name, each `{ hash, command, nodes }`),
`projectConfiguration`, `discriminator`. Serialise with two-space indentation so a committed excerpt
is readable and a diff is line-oriented.

DIFF MODE. Given two record paths, print to stdout a three-bucket partition of the node maps PER
TARGET -- `only-in-A`, `only-in-B`, `value-changed` (with both hashes), and a COUNT only for `same`
-- plus a field-level diff of `projectConfiguration`. RESEARCH is explicit that a raw diff drowns in
the 422-plus `npm:` keys and that the `only-in-*` bucket is the one to LEAD with in a cross-OS
reading, because it cannot arise from staleness within a single machine and is the shape a differing
platform-specific optional-dependency set takes. Print the buckets in that order and label each with
the target name. Mark this the ONE node-partition implementation in the repo and explain why the
comparator in `packages/github-cache/src/hash-parity/` cannot import it: the instrument must be
runnable immediately after `npm ci` with NO build step (the capture job has none, by design), so it
cannot import from `dist/`, and a typechecked module cannot import an untyped root `.mjs` (D-19).

FAILURE POSTURE: rely on top-level `await` rejecting for a non-zero exit, as `esbuild.action.mjs`
does. Every throw must name what failed and then name the suspects, in the voice of
`read-back.ts:50-57`.

WIRE IT UP:
- Add `"capture:hashes": "node capture-hashes.mjs"` to the root `package.json` scripts, positioned
  with `build:action` / `check:action` / `typecheck:action` / `pack:check` (the tool scripts), not
  with the five target scripts. The root `package.json` declares `nx.includedScripts` as an empty
  array, so adding a script does NOT create an Nx target -- D-01(b) is satisfied STRUCTURALLY, not
  by discipline. Say so in the summary.
- Add `capture-hashes.mjs` to `.fallowrc.jsonc`'s `entry` array with a comment in the voice of the
  `esbuild.action.mjs` and `pack-check.cjs` entries: it is invoked as a standalone node bin by the
  `capture:hashes` script and by the ci.yml capture job, and is never imported, so reachability
  analysis cannot infer it. Without this, `npm run fallow:ci` goes red.

Do NOT touch `nx.json`. Do NOT add a spec for this file. Do NOT set `NX_DAEMON` from inside the
script.
  </action>

  <verify>
    <automated>node capture-hashes.mjs --install-mode ci | rg -q 'ProjectConfiguration'</automated>
  </verify>

  <acceptance_criteria>
- `node capture-hashes.mjs --install-mode ci` exits 0 and writes a single JSON document to stdout
  whose top-level keys are exactly `meta`, `targets`, `projectConfiguration` and `discriminator`.
- The emitted `targets` object has exactly five keys: `build`, `typecheck`, `test`, `integration`
  and `lint`. Each carries a non-empty string `hash`, a string `command`, and a `nodes` object with
  more than 400 entries.
- The emitted `discriminator` object carries `command`, `stdout`, `stderr` and `status`, and its
  `command` string is byte-equal to the runtime entry read out of `nx.json`
  `targetDefaults.integration.inputs`. Verifiable by comparing the two strings.
- Only the `integration` target's `nodes` map contains a key beginning with `runtime:`. This is the
  live confirmation of CORR-04 from the measurement side.
- `meta.graphState` is one of `cold` or `warm` and was DERIVED from the measured directory entry
  counts, not from any CLI flag. Verifiable by grepping the source for a `--graph-state` flag and
  finding none.
- `node capture-hashes.mjs` with no `--install-mode` exits NON-ZERO and its stderr names the missing
  flag.
- `node capture-hashes.mjs --install-mode ci --out <tmpfile>` writes the same document to the file
  and prints nothing to stdout.
- `node capture-hashes.mjs --diff <a.json> <b.json>` over two records captured in different graph
  states prints, per target, an `only-in` pair, a `value-changed` list and a `same` COUNT, and a
  `projectConfiguration` field diff.
- `npm run capture:hashes -- --install-mode ci` works, and root `package.json`
  `nx.includedScripts` is still an empty array.
- `npm run fallow:ci` exits 0.
- `nx.json` is untouched by this commit. Verifiable with `git show --stat`.
  </acceptance_criteria>

  <done>
The instrument exists, emits the per-node map for all five targets with a measured graph state, and
is neither linted-into-existence nor Nx-cacheable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the instrument computes Nx's number, and open the record</name>

  <files>
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <read_first>
- `.planning/phases/07-lint-toolchain-and-the-ambient-platform-read-ban/07-EVIDENCE.md` lines 1-30 --
  the preamble's "MEASURED on this repo, never predicted" declaration and the per-section stamp
  (date, host OS/arch/node/npm, base commit SHA). This is D-06's named model.
- `08-CONTEXT.md` D-03, D-06, D-08, D-09.
- `08-RESEARCH.md` `## Validation against Nx's own arithmetic`,
  `## nx show target inputs is insufficient -- the citations for the record`, Pitfall 2, and
  `### Finding 4: STALENESS, not freshness`.
- `.planning/ROADMAP.md` Phase 8 success criterion 1 (the wording the record must correct).
- `.planning/STATE.md` -- locate the recorded pair attributed to "ubuntu CI" versus "windows CI"
  that D-09 requires the record to name as a confounded reading.
  </read_first>

  <action>
STEP 1 -- validate the instrument against Nx's own arithmetic. In ONE shell session, on a clean
working tree: run `node capture-hashes.mjs --install-mode ci --out <tmpfile>`, then IMMEDIATELY run
`npx nx run @op-nx/github-cache:build`, then IMMEDIATELY read `.nx/cache/run.json` and compare the
`build` task's `hash` there against the instrument's `targets.build.hash`. They must be
byte-identical. Pitfall 2 is the hazard: `StoreRunInformationLifeCycle` overwrites `run.json`
unconditionally on EVERY `nx` invocation, so the read must happen before any other `nx` command --
do not interleave a `nx show`, a `nx reset`, or an editor-triggered graph build. Repeat for at least
one second target (`test` is a good choice because its input list is the longest). Record both
comparisons.

If they do NOT match, STOP and treat it as a blocker rather than adjusting the instrument to agree:
a mismatch means either the call sequence is wrong or `run.json` was overwritten, and both need to
be distinguished before anything downstream is trusted.

STEP 2 -- run and capture the D-03 evidence. Run the CLI surface D-03 rejects, using RESEARCH's
executed invocation `npx nx show target @op-nx/github-cache:build --inputs`, and capture its stdout
VERBATIM. If that subcommand does not resolve at Nx 23.1.0, capture the exact error verbatim
instead -- either outcome is admissible evidence for the record; an invented one is not. Also
capture the NAPI doc line from `node_modules/nx/dist/src/native/index.d.ts` stating that
`ProjectConfiguration` is skipped, with its line number.

STEP 3 -- create `.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md`. Follow
`07-EVIDENCE.md`'s structure. Write these sections and no measurement sections yet (those arrive in
plan 08-03):

`# Phase 8 - Root-Cause Record`, then a preamble declaring that every number in this document is
MEASURED on this repo at the stated commit and never predicted, that where a prediction from
`08-RESEARCH.md` exists it is quoted alongside so the divergence or its absence is visible, and
that `08-RESEARCH.md`'s own numbers were taken at commit `c61ef40` as a MAP for the investigation
and are explicitly NOT carried forward as this record's values (CONTEXT `<specifics>`: Phase 8
re-takes the reading at its own commit rather than citing an earlier one as current).

`## Method` -- the instrument, its four-import call sequence, the fact that `graphState` is measured
rather than asserted and why (Pitfall 1: a `nx reset` that failed with EPERM leaves a warm graph
behind a record that claims cold), the env-var cold recipe (`NX_DAEMON=false`,
`NX_WORKSPACE_DATA_DIRECTORY` and `NX_NATIVE_FILE_CACHE_DIRECTORY` pointed at a fresh temp
directory) and why it beats deletion: the native file cache lives in the OS temp directory OUTSIDE
the repo, so removing `.nx/` does not produce a cold state at all. State that `nx reset` is the
right thing to DOCUMENT for a developer whose box misses everything and the wrong thing to build a
measurement on.

`## The instrument computes Nx's number` -- Step 1's two comparisons, with the target, the
instrument's value, the `run.json` value, and a pass column. State Pitfall 2 as the method caveat.

`## Why nx show target inputs is not evidence (D-03, PARITY-02)` -- both citations: the NAPI doc
line stating `ProjectConfiguration` is skipped and `Cwd` is skipped as ambient, with its file and
line; and Step 2's captured output showing declared PATTERNS with no hashes and no
`ProjectConfiguration`. Name the programmatic form `HashPlanInspector.inspectTaskInputs()` as well
and record that it is built on the same NAPI inspector and carries the same limitation, so "maybe
the API is better than the CLI" is closed before it is raised. Record `.nx/cache/run.json` as the
COMPLEMENTARY task-level surface, not a substitute, and note that it is overwritten by every `nx`
invocation.

`## Every prior cross-OS measurement in this repo read a confounded variable (D-09)` -- name the
pair recorded in `STATE.md` attributed to "ubuntu CI" versus "windows CI" and state that no
difference may be attributed to the OS until freshness is pinned. Then name the confound PRECISELY,
in the form RESEARCH's Finding 4 established: not "the graph was warm" but "the graph was carrying
an inference result that no longer matched the tree".

`## The second axis is STALENESS, not freshness` -- this is a CORRECTION to the roadmap's own
wording and it must be stated as one. The roadmap's success criterion 1 and REQUIREMENTS PARITY-01
both call it a FRESHNESS axis. RESEARCH measured that cold and warm produce the SAME hash when
`.nx/workspace-data` is fresh -- two runs against a brand-new workspace-data directory, the first
cold by construction and the second warm because the first populated it, agreeing on all five
targets -- while the repo's long-lived directory produces a different value. So `cold != warm` is
false as a general statement and `stale != fresh` is what is true. Record that `nx reset` is the
only known cure and not a control, and that a warm-after-reset graph and a warm-since-March graph
are different measurements wearing the same label, which is why plan 08-03 captures observation
point 2 as warm-PREEXISTING specifically. Quote the roadmap's wording so the correction is
attributable rather than a silent rewrite.

`## Which question each proof answers (D-08)` -- state the two questions up front: cross-OS parity,
and "does a warm local box compute the hash cold CI published" (PARITY-04). State that the second
must NOT be silently resolved by `nx reset`, because TEST-10's mandated reset clears
`.nx/workspace-data` and forces COLD -- which answers the first question while looking like it
answered the second. Every later section in this document is required to state which of the two it
answers.

Add a placeholder heading `## Observation points` with a one-line note that plan 08-03 fills it at
the anchor commit.

Do NOT paste RESEARCH's measured hash values anywhere in the record as this phase's values. Quote
them ONLY inside an explicitly-labelled "research prediction" callout where a comparison is being
drawn.

Do NOT touch `nx.json`.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <human-check>Confirm the record's Method section names YOUR workstation's host details and the base commit it was written at, and that no value in it was carried over from 08-RESEARCH.md as a current measurement.</human-check>
  </verify>

  <acceptance_criteria>
- `.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md` exists and contains headings matching
  `## Method`, `## The instrument computes Nx's number`, a D-03 section, a D-09 section, a staleness
  section, and a D-08 section.
- The record states an instrument-versus-`run.json` comparison for at least two of the five targets,
  each with both values printed and an explicit pass column.
- The record cites the NAPI doc's skipped-`ProjectConfiguration` line with a file path and line
  number, and quotes the captured output of the rejected CLI surface verbatim.
- The record names the `STATE.md` pair attributed to the two CI operating systems and states that
  it read a confounded variable.
- The record quotes the roadmap's "FRESHNESS axis" wording and states the correction to
  staleness-of-persisted-inference, with the reason.
- The record states both PARITY-04 and cross-OS parity as SEPARATE named questions and requires
  every later section to declare which it answers.
- `npm run format:check` exits 0.
- `git show --stat` for this plan's commits lists no change to `nx.json`.
  </acceptance_criteria>

  <done>
The instrument is proven to compute Nx's own number, and the record is open with its method and its
two anti-requirements (the rejected CLI surface and the confounded prior readings) written down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nx internal subpath -> the instrument | `nx/src/*` carries no semver guarantee; an Nx major can move it. The failure must be at import, not at read. |
| Measurement -> committed evidence | A record that silently replays a cached or stale value would make every downstream conclusion wrong while looking correct. |
| Local host paths -> a committed document | `meta` carries resolved directory paths, and the native file cache resolves under the OS temp directory, which on Windows contains the account name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `capture-hashes.mjs` as an Nx target | high | mitigate | The script is a root-level `.mjs` outside every project, and root `package.json` declares `nx.includedScripts` as an empty array, so adding the `capture:hashes` script cannot create a target. A cached capture would replay instead of measure (D-01b). Structural, not disciplinary. |
| T-08-02 | Spoofing | the instrument's hash versus Nx's hash | high | mitigate | Task 2 asserts byte-equality against `.nx/cache/run.json` for at least two targets, read in the same session that produced it (Pitfall 2). A re-implementation that merely looks plausible is rejected by construction. |
| T-08-03 | Tampering | `nx/src/*` internal subpath drift | medium | accept | The import fails loudly at module load on any Nx major that moves it; `nx` is exact-pinned at 23.1.0 and `pinned-deps.spec.ts` fails the build if a specifier widens. Same accepted posture and precedent as `nx-target-inputs.spec.ts:35-37`. |
| T-08-04 | Information disclosure | host paths pasted into `08-ROOT-CAUSE.md` | low | accept | The recorded paths are a repo path and an OS temp path; the native-file-cache segment is a truncated sha256, not an account name, though the temp root itself carries one on Windows. No credential, token or work-domain string is in scope. The `<human-check>` on Task 2 is where the maintainer confirms it. |
| T-08-05 | Denial of service | a missing inferred target read as "no difference" | medium | mitigate | The instrument THROWS on an absent `taskGraph.tasks` entry, naming the target and the available ids. This is Phase 7's `nx run-many -t <missing>` learning applied at the measurement layer (Pitfall 4). |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new npm package is introduced by this plan; every import resolves to an already-installed exact-pinned devDependency. `08-RESEARCH.md`'s Package Legitimacy Audit records zero `[ASSUMED]`, zero `[SUS]` and zero `[SLOP]` verdicts, so no install checkpoint is required. |
</threat_model>

<verification>
- All NINE battery commands green at this plan's final commit: `format:check`, `build`, `typecheck`,
  `typecheck:action`, `test`, `lint`, `fallow:ci`, `check:action`, `pack:check`.
- `node capture-hashes.mjs --install-mode ci` succeeds on this workstation.
- `nx.json` is untouched by every commit in this plan.
- `packages/github-cache/` is untouched by every commit in this plan.
</verification>

<success_criteria>
- The instrument emits the per-NODE `details.nodes` map for all five targets plus the merged project
  configuration node, with a MEASURED graph state and a complete `meta` block.
- The instrument's hash is proven byte-identical to Nx's own for at least two targets.
- `08-ROOT-CAUSE.md` records the rejected CLI surface with citations, the confounded prior readings,
  the staleness correction, and the two separate questions.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is CREATED by phase 8 and did not exist before it. Drift verification must not
flag any of them as an unresolved reference.

**New files**
- `capture-hashes.mjs` (plan 08-01)
- `.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md` (plan 08-01, appended by 08-03, 08-04,
  08-05, 08-06)
- `packages/github-cache/src/hash-parity/compare.ts` (plan 08-02)
- `packages/github-cache/src/hash-parity/compare.spec.ts` (plan 08-02)
- `packages/github-cache/src/hash-parity/assert-parity.ts` (plan 08-02)

**New npm script**
- `capture:hashes` in the root `package.json`, defined as `node capture-hashes.mjs` (plan 08-01)

**New exported symbols** (all in `packages/github-cache/src/hash-parity/compare.ts`, plan 08-02)
- `EXPECTED_TARGETS`, `INVARIANT_TARGETS`, `DIVERGENT_TARGET`, `REQUIRED_META_KEYS`
- `HashParityRecord`, `ParityFailureReason`, `ParityVerdict` (types)
- `compareHashParity`

**New fallow entry points**
- `capture-hashes.mjs` (plan 08-01)
- `packages/github-cache/src/hash-parity/assert-parity.ts` (plan 08-02)

**New package-manifest entries**
- `!dist/hash-parity` in `packages/github-cache/package.json` `files` (plan 08-02)
- a `dist/hash-parity/` predicate in `packages/github-cache/pack-check.cjs` `FORBIDDEN`, plus the
  `DIST_SUBTREES` single-source constant it and the success message are derived from (plan 08-02)

**New nx.json keys**
- `targetDefaults.typecheck.outputs` (plan 08-05)

**New CI jobs** (`.github/workflows/ci.yml`)
- `hash-parity` (two-leg matrix capture, plan 08-03)
- `hash-parity-compare` (build-gating comparison, plan 08-06)

**New CI artifact names**
- `hash-parity-ubuntu-24.04-arm`, `hash-parity-windows-11-arm` (plan 08-03)

**New spec describe-block subjects**
- the comparator's four D-20 clauses, their per-clause negatives and the empty-`targets` vacuity
  control (plan 08-02)
- the `typecheck.outputs` pin in `packages/github-cache/src/nx-target-inputs.spec.ts` (plan 08-05)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-nx-task-hash-parity/08-01-SUMMARY.md` when done.
</output>
