---
phase: 08-nx-task-hash-parity
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - nx.json
  - packages/github-cache/src/nx-target-inputs.spec.ts
  - .planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
autonomous: false
requirements: [PARITY-03, PARITY-05, PARITY-07, CORR-04]

must_haves:
  truths:
    - "`build`, `typecheck` and `test` compute a byte-identical hash on ubuntu-24.04-arm and windows-11-arm at one commit, and the workstation agrees in the cold graph state (PARITY-03)."
    - "`integration` computes a byte-identical hash between the native Windows workstation and windows-11-arm (PARITY-05)."
    - "Every fix lands in `nx.json` `targetDefaults` -- no `project.json`, no plugin-option patching (D-12), and each fix's rationale lock lives in the guard spec that pins it (D-13)."
    - "`integration`'s runtime discriminator is byte-identical to what it was before this phase, and remains the ONLY declared platform discriminator in the file (D-14, CORR-04)."
    - "U-01 is RESOLVED by a maintainer decision taken with both measurements in hand, never auto-selected."
    - "The public-surface guard passes UNCHANGED -- no new env knob, no new action input, no new package export (PARITY-07, D-16)."
  artifacts:
    - "nx.json `targetDefaults` fix, derived from the record's `## Proposed fix` section"
    - "packages/github-cache/src/nx-target-inputs.spec.ts pin, extending the existing describe blocks"
    - "08-ROOT-CAUSE.md post-fix re-measurement sections and the U-01 resolution"
  key_links:
    - "`nx.json` `targetDefaults` -> the merged project-graph node Nx folds into every task hash (a targetDefaults entry normalises ITS field regardless of what inference produced -- that is the whole mechanism the fix relies on)."
    - "the pin in `nx-target-inputs.spec.ts` -> `{workspaceRoot}/nx.json` being a declared `test` input (without that wiring, the pin replays a cached PASS after someone reopens the hole -- the same bug one level up)."
    - "`targetDefaults.integration.inputs` runtime entry -> `capture-hashes.mjs`, which READS it (a re-spelling breaks the instrument's discriminator capture as well as CORR-04)."
---

<objective>
Apply the fix the record proposed, pin its rationale, re-measure, and let the maintainer close U-01.

Purpose: D-15's ordering reaches its third step. The record is complete and committed; this is the
first plan in the phase permitted to touch `nx.json`. Everything here is derived from the record's
`## Proposed fix` section rather than re-decided, and the outcome is checked against the U-01
trigger condition that plan 08-04 committed IN ADVANCE.

Output: the `nx.json` edit, its pin in the existing cross-cutting drift guard, a post-fix
re-measurement at all four observation points, and a recorded U-01 resolution.

Why `autonomous: false`: U-01 is HIGH impact and NOT-HIGH confidence by the user's own
classification, and it is the one item `/gsd:discuss-phase --auto` deliberately withheld from
auto-lock. It closes on a maintainer decision taken with the measurements in hand -- not on this
plan's judgement about whether the numbers look good enough.

This plan also carries a Live-CI closure: the cross-OS half of the re-measurement exists only on
real runners.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-nx-task-hash-parity/08-CONTEXT.md
@.planning/phases/08-nx-task-hash-parity/08-RESEARCH.md
@.planning/phases/08-nx-task-hash-parity/08-PATTERNS.md
@.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
@.planning/phases/08-nx-task-hash-parity/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the fix RED, apply it, and re-measure locally</name>

  <files>
packages/github-cache/src/nx-target-inputs.spec.ts
nx.json
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <read_first>
- `08-ROOT-CAUSE.md` `## Root cause`, `## Proposed fix`, the U-01 trigger-condition section, and the
  `typecheck` outputs enumeration. THIS is the source of what to change. Do not re-derive it.
- `nx.json` lines 47-164 in full -- all five `targetDefaults` entries. Note the ordering convention:
  in the only entry that has one, `outputs` sits FIRST, before `inputs`.
- `packages/github-cache/src/nx-target-inputs.spec.ts` in full -- the header at :10-44 (including
  the recorded warning at :28-43 about the rejected single-project input expander), the parse cast
  at :55-61 whose `targetDefaults` type ALREADY admits an optional `outputs` array so no type edit
  is needed, the `describe('lint declares its full input set (LINT-04)')` block at :206-261, the
  `outputs` pin at :229-234 with its rationale comment, the CORR-04 guard at :241-260 with its
  defensive optional-chaining rationale at :243-255, and the stale-pass describe at :263-327.
- `08-CONTEXT.md` D-12, D-13, D-14, D-16, and U-01.
- `08-PATTERNS.md` sections `nx.json -- targetDefaults.typecheck.outputs (the fix, D-12/D-13)` and
  `packages/github-cache/src/nx-target-inputs.spec.ts (extend, do not re-author)`.
- `08-RESEARCH.md` `### Finding 2` consequence 2 (why `outputs` is load-bearing beyond the hash) and
  `## Don't Hand-Roll` (the row instructing that CORR-04's guard be EXTENDED, never re-authored).
  </read_first>

  <behavior>
- The `typecheck` target declares the outputs list the record's enumeration determined to be
  correct, pinned by a deep-equality assertion.
- `integration` is still the ONLY target in `targetDefaults` declaring a platform runtime input.
- `integration`'s runtime input string is byte-identical to its pre-phase value.
- `{workspaceRoot}/nx.json` is still a declared `test` input, so the pin cannot replay a cached PASS.
- Any narrowing applied to a target's `externalDependencies` is pinned by a deep-equality assertion
  on the sorted list, in the same shape as the existing four-package assertion at :213-227.
  </behavior>

  <action>
STEP 1 -- write the pins FIRST and OBSERVE the RED.

Extend `packages/github-cache/src/nx-target-inputs.spec.ts`. Add a describe block for the target
the record's proposed fix names, in the voice of the existing LINT-04 block, and put the assertions
from this task's `<behavior>` list in it. Before the `nx.json` edit exists, an assertion on an
absent `outputs` key returns undefined and the deep-equality assertion fails -- run
`npx nx test @op-nx/github-cache` and record the failure, with its count.

Note the one place a naive RED goes wrong here: `targetDefaults.typecheck` ALREADY EXISTS, so the
helper does not throw the way it did when `targetDefaults.lint` was absent in Phase 7. The failure
is a value mismatch, not a `TypeError`. That is a WEAKER signal, so make it unambiguous by asserting
deep equality against the full expected list rather than a `toBeDefined`.

EXTEND the existing file. Do NOT create a new spec for this and do NOT put the pin in
`src/hash-parity/compare.spec.ts`. `nx-target-inputs.spec.ts` is this package's cross-cutting
`nx.json` drift guard, `TESTING.md` places cross-cutting guards at the package-source root, and
re-authoring CORR-04's existing guard elsewhere would create a second copy to drift. The parse cast
at :55-61 already admits an optional `outputs` array, so no type edit is needed -- check that before
adding one.

For CORR-04, do NOT write a new assertion: the guard at :241-260 already asserts that `integration`
is the only target with a runtime input, and it already carries the defensive optional chaining and
the written rationale for it. Confirm it still passes and say so; adding a second copy is the
failure mode `08-RESEARCH.md`'s Don't-Hand-Roll table names by ID.

Add ONE genuinely new CORR-04-adjacent assertion that does not exist today and that D-14 needs: the
`integration` runtime input's command STRING, pinned by exact equality to its current value. The
existing guard proves only that exactly one target HAS a runtime input; it does not prove the string
was not re-spelled. D-14 requires it byte-identical because after VER-03 it is the sole mechanism
separating OS-sensitive targets, and `capture-hashes.mjs` READS that string rather than re-spelling
it, so a re-spelling silently changes what the instrument captures as well. Comment-lock both
reasons.

STEP 2 -- apply the fix to `nx.json`, in the SAME commit as the pins.

Take the change from the record's `## Proposed fix` section. Name the key it named, set the value it
determined. Do not widen the change beyond what the record proposed, and do not add a second fix
"while in there" -- every extra `nx.json` hunk in this commit is one more variable in the re-
measurement that follows.

Follow the file's ordering convention: where a target gains an `outputs` key, it sits FIRST in the
entry, before `inputs`, matching the only existing precedent.

Prefer NARROWING over adding (D-13). If the record's proposal is to narrow a declared
`externalDependencies` list, remove entries rather than adding a new input; if it is to declare an
`outputs` list, that IS an addition, and it is the narrowing kind -- it replaces whatever inference
produced with one fixed list.

Do NOT create a `project.json`. This workspace is deliberately free of them (Phase 7's D-02 and
Phase 1's 01-01 both record it) and D-12 forbids it. Do NOT patch plugin options in the `plugins`
array as a first resort -- that is U-01's escalation path, not this plan's default. Do NOT re-spell
`integration`'s runtime input.

STEP 3 -- GREEN and re-measure locally, both graph states, at the fix commit.

Run `npx nx test @op-nx/github-cache` to green. Then re-take observation points 1 and 2 with the
instrument, using the same recipes plan 08-03 used: the env-var cold recipe into a fresh temporary
directory outside the repo for point 1, and no overrides at all for point 2.

Point 2 is the one that matters here and it has a trap. The persisted plugin cache does NOT
self-heal, so a warm-preexisting reading taken immediately after the fix may still be carrying the
old inference. Take the reading anyway and record it AS THAT -- it is the measured developer
experience the fix has to survive, and it is the evidence behind the Phase 12 hand-off note. Then
run a full `nx reset` and take a THIRD local reading, labelled warm-after-reset, and record all
three. Do not overwrite point 2 with the post-reset value; a reset that silently replaces the
warm-preexisting reading is exactly the move PARITY-04 names as disqualifying, one plan later.

Diff the new cold reading against the pre-fix cold reading with the instrument's diff mode, and
diff the new cold reading against the new warm reading. The confirming outcome is that the node the
record named stops moving between graph states. Record both diffs.

Append a `## Post-fix re-measurement (local)` section to `08-ROOT-CAUSE.md` with the three readings,
their full `meta` blocks, and the two diffs. State which of D-08's two questions each answers.

Run the full nine-command battery. Note for the record, and do not treat it as a defect: this commit
legitimately rotates every task hash and rotates `test` twice over, because `{workspaceRoot}/nx.json`
is already an explicit `test` input. Phase 7's D-36 pre-recorded this as one of the milestone's three
legitimate all-MISS rotation windows.
  </action>

  <verify>
    <automated>npx nx test @op-nx/github-cache</automated>
  </verify>

  <acceptance_criteria>
- The RED was observed and its failure count recorded BEFORE the `nx.json` edit, and the assertion
  that failed was a deep-equality one rather than a presence check.
- `nx.json` contains the key the record's `## Proposed fix` section named, carrying the value that
  section specified.
- `packages/github-cache/src/nx-target-inputs.spec.ts` contains a deep-equality pin for that value
  with a comment stating WHY that value and not another, citing the record's enumeration. The file
  was EXTENDED; no new spec file was created for this pin.
- The file contains a new exact-equality pin on `integration`'s runtime input command string, with a
  comment naming both reasons: D-14's byte-identical requirement and the fact that
  `capture-hashes.mjs` reads that string.
- The pre-existing CORR-04 guard at :241-260 is UNMODIFIED and still passes. Verifiable with
  `git diff` over that range.
- No `project.json` exists anywhere in the workspace, and `nx.json`'s `plugins` array is unchanged.
- `08-ROOT-CAUSE.md` gained a post-fix local re-measurement section with THREE readings -- cold,
  warm-preexisting and warm-after-reset -- each with a full `meta` block, plus two instrument diffs.
- The warm-preexisting reading was NOT replaced by the post-reset one; all three are present.
- `npx nx test @op-nx/github-cache` exits 0 and all nine battery commands exit 0 at the commit.
- `npx vitest run src/public-surface.spec.ts` passes with that file unmodified.
  </acceptance_criteria>

  <done>
The fix is applied inside `nx.json` only, its rationale is locked in the guard that pins it, and the
local half of the re-measurement is recorded including the state a real developer's box will be in.
</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure across both runners at the fix commit</name>

  <files>
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <read_first>
- `08-ROOT-CAUSE.md` `## Post-fix re-measurement (local)` as task 1 left it, and the U-01
  trigger-condition section from plan 08-04.
- `.github/workflows/ci.yml` -- the `hash-parity` job as plan 08-03 added it, specifically its
  checkout ref pin.
- `08-CONTEXT.md` D-07, D-08, D-21, U-01.
- `.planning/codebase/TESTING.md` lines 229-262 -- the Live-CI first-push close pattern.
  </read_first>

  <action>
This task is a LIVE-CI CLOSURE. Label it as one in the summary; it cannot close locally without
faking the thing under test.

STEP 1 -- push the fix commit to the draft pull request and wait for both `hash-parity` legs.
Download both artifacts.

STEP 2 -- validate before reading, exactly as plan 08-03's task 3 did: `meta.commit` equals the fix
commit SHA on both records, and `meta.commit` equals `meta.githubSha` on both (which is what proves
the checkout pin still holds). Compare `meta.nxVersion`, `meta.nodeVersion` and `meta.installMode`
against the local readings and record any difference as a named confound rather than skipping it --
`.node-version` is the moving alias `lts/krypton`, so the runner's Node version can legitimately
differ from the workstation's.

STEP 3 -- append `## Post-fix re-measurement (CI)` to the record: both `meta` blocks, both
five-target hash tables, both discriminator captures, and the instrument's cross-OS diff.

STEP 4 -- state the four verdicts plainly, each as its own line, because these are the phase's
outcome and they must not be buried in a diff dump:
- `build`, `typecheck` and `test`: identical across the two legs, or not. This is PARITY-03's
  cross-OS half.
- `integration`: differs across the two legs, or not. If it does NOT differ, that is a failure of
  the discriminator and NOT a success of parity -- say so; a matching `integration` hash means the
  one target that is supposed to be OS-sensitive stopped being so.
- `integration` again, this time the same-OS pair: the workstation's cold reading against the
  `windows-11-arm` reading. This is PARITY-05, and it is a DIFFERENT comparison from the one above.
  Present it as its own row so it cannot be reported by accident.
- `lint`: identical across the two legs, or not. This is D-21's branch point and Phase 7's D-35
  settlement. Quote D-35's Windows-side baseline table beside it, and if the merged project
  configuration diff touches the `lint` target, name the field -- D-35 flags the resolved working
  directory as the row most likely to diverge and confirms it IS hashed.

STEP 5 -- compare each verdict against plan 08-04's PRE-COMMITTED U-01 trigger condition and state,
in one sentence per verdict, whether it confirms the proposal or triggers U-01. Do NOT decide U-01
here. State the evidence and stop; task 3 is where it closes.

If `build`, `typecheck` or `test` still diverge, do NOT iterate on `nx.json` looking for something
that makes the numbers agree. That is precisely the improvisation U-01 forbids, and an undirected
search would also destroy the attribution the record spent four plans establishing. Record the
divergence with its node-level evidence and take it to task 3.

If `lint` diverges and the fix proves out of Phase 8's scope, record it as a named finding with its
evidence. D-21's fallback DOWNGRADES `lint`'s clause to recorded-with-a-named-finding; it never
deletes it, because a deleted clause is indistinguishable from a clause that never existed. Name
which branch plan 08-06 must wire.

Commit the record.
  </action>

  <verify>
    <automated>npm run format:check</automated>
    <human-check>Live-CI closure: confirm both `hash-parity` legs are green at the fix commit and that the four verdicts in the record match the two artifacts you downloaded.</human-check>
  </verify>

  <acceptance_criteria>
- `08-ROOT-CAUSE.md` contains a `## Post-fix re-measurement (CI)` section with both runner `meta`
  blocks, both hash tables, both discriminator captures, and a cross-OS instrument diff.
- Both CI records' `meta.commit` equals the fix commit SHA and equals their `meta.githubSha`.
- The record states FOUR separately-labelled verdicts: the three invariant targets cross-OS,
  `integration` cross-OS, `integration` workstation-versus-windows-11-arm as its own row, and
  `lint` cross-OS.
- The `integration` cross-OS row states explicitly that a MATCHING hash there is a discriminator
  failure and not a parity success.
- Each verdict carries a one-sentence statement of whether it confirms plan 08-04's pre-committed
  U-01 proposal or triggers U-01.
- The record names which branch of D-21 plan 08-06 must wire for `lint`, and if it is the fallback
  branch, the clause is recorded as downgraded rather than described as removed.
- No further `nx.json` edit was made in this task. Verifiable with `git show --stat`.
- `npm run format:check` exits 0.
  </acceptance_criteria>

  <done>
The cross-OS half of the re-measurement exists at the fix commit, with four separately-stated
verdicts and their bearing on U-01.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Close U-01 -- is the fix location settled?</name>

  <read_first>
- `08-ROOT-CAUSE.md` `## Proposed fix`, the U-01 trigger-condition section, `## Post-fix
  re-measurement (local)` and `## Post-fix re-measurement (CI)`.
- `08-CONTEXT.md`'s UNRESOLVED U-01 block in full.
- `08-RESEARCH.md`'s `> Research update on U-01` callout -- specifically its closing instruction
  that the confirming experiment is NOT a licence to treat U-01 as auto-resolved.
  </read_first>

  <files>
.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md
  </files>

  <action>
STOP and present the decision below to the maintainer. Do NOT select an option, do NOT make any
further `nx.json` edit, and do NOT proceed to plan 08-06 until a selection is returned. U-01 is the
one item `/gsd:discuss-phase --auto` deliberately withheld from auto-lock as HIGH impact and
NOT-HIGH confidence, and `08-CONTEXT.md` instructs that it be re-opened with the maintainer at
exactly this point rather than auto-selected (D-12, U-01).

Assemble the checkpoint message from measured values only: the four verdicts from task 2 verbatim,
the three local readings from task 1 with their `meta` blocks, and plan 08-04's PRE-COMMITTED U-01
trigger condition quoted verbatim beside them. Add exactly one line stating which way the evidence
points, and no more -- the point of the pre-commitment is that the reading is checked against a
condition written before the experiment, not argued for after it.

When the selection returns, append it to `08-ROOT-CAUSE.md` under a heading recording U-01 as
RESOLVED, with the date, the selected option id, the maintainer's reasoning, and an explicit
statement of whether the selection matched the pre-committed confirming condition or departed from
it. Leave `08-CONTEXT.md`'s UNRESOLVED block AS WRITTEN -- the resolution belongs in the record, and
editing the context document after the fact would erase the evidence that the item was genuinely
open when the phase started.

If the selection is anything other than `confirm-d12`, record the scope the maintainer authorised
and STOP. Plans 08-05 and 08-06 were written against `confirm-d12`; a different selection needs its
own gap-closure plan rather than an improvised extension of this one, because pinning an inferred
target, patching plugin options, or escalating upstream each change what the phase delivers.
  </action>

  <decision>
Whether PARITY-03's byte-identical goal is reachable through `nx.json` alone -- that is, whether
D-12's "fixes land in `nx.json` `targetDefaults` only" is a sufficient fix location for this
workspace, or whether the fix location must be re-opened.
  </decision>

  <context>
U-01 was withheld from `/gsd:discuss-phase --auto`'s auto-lock as HIGH impact and NOT-HIGH
confidence, and `08-CONTEXT.md` instructs that it be re-opened with the maintainer rather than
auto-selected. It is HIGH impact because the O1 and O4 outcomes both depend on PARITY-03 and the
choice freezes how this workspace configures targets for the rest of the milestone.

`08-RESEARCH.md` found in-repo evidence favouring the `nx.json` route and then said, in its own
words, that this is a hypothesis with a named cheap confirming experiment and NOT a licence to treat
U-01 as auto-resolved. Plan 08-04 committed the trigger condition in advance; tasks 1 and 2 ran the
experiment. This checkpoint is where a human reads the result.

Present, in the checkpoint message: the four verdicts from task 2 verbatim; the local three-state
re-measurement from task 1; the pre-committed trigger condition from plan 08-04; and a one-line
statement of which way the evidence points. Do not editorialise beyond that line.
  </context>

  <options>
    <option id="confirm-d12">
      <name>Confirm D-12 -- the fix location is settled inside nx.json</name>
      <pros>The measurement matches the pre-committed confirming condition; no further change is
      needed; the workspace stays free of `project.json` files and of plugin-option patching; plan
      08-06 wires the gate as designed.</pros>
      <cons>None if the verdicts are clean. If any verdict is ambiguous, choosing this closes U-01
      on a reading rather than on evidence.</cons>
    </option>
    <option id="pin-inferred-target">
      <name>Pin the inferred target explicitly</name>
      <pros>Removes the dependency on plugin inference for the diverging field entirely; the most
      direct answer if inference itself is the divergence.</pros>
      <cons>Departs from D-02's no-`project.json` posture or expands `targetDefaults` into
      restating inferred values, which increases the surface that must be kept in step with plugin
      upgrades.</cons>
    </option>
    <option id="patch-plugin-options">
      <name>Patch the inferring plugin's options in nx.json's plugins array</name>
      <pros>Stays inside `nx.json`; can change what inference produces at its source rather than
      overriding the result.</pros>
      <cons>D-12 names plugin-option patching as explicitly not a first resort; couples the
      workspace to plugin-internal option semantics that carry no semver guarantee.</cons>
    </option>
    <option id="escalate-upstream">
      <name>Escalate upstream and carry the divergence as a recorded, unfixed finding</name>
      <pros>Correct when the cause is a third-party plugin defect that no local configuration can
      normalise; the record already holds the evidence an upstream report needs.</pros>
      <cons>PARITY-03 stays unmet for this milestone, which makes the O1 and O4 outcomes unreachable
      until upstream ships; requires an explicit decision about what the phase then delivers.</cons>
    </option>
  </options>

  <resume-signal>
Select: confirm-d12, pin-inferred-target, patch-plugin-options, or escalate-upstream. If anything
other than confirm-d12 is selected, state the scope you are authorising, because plans 08-05 and
08-06 were written against confirm-d12 and a different selection needs its own gap-closure plan
rather than an improvised extension of this one.
  </resume-signal>

  <verify>
    <automated>rg -q 'U-01' .planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md</automated>
    <human-check>Confirm the recorded resolution names the option you selected, carries your reasoning, and states whether it matched plan 08-04's pre-committed confirming condition.</human-check>
  </verify>

  <acceptance_criteria>
- The maintainer's selection and its reasoning are appended to `08-ROOT-CAUSE.md` under a heading
  recording U-01 as RESOLVED, with the date and the selection id.
- The record states whether the selection matched plan 08-04's pre-committed confirming condition
  or departed from it, and where it departed, why.
- If the selection is not `confirm-d12`, no further `nx.json` edit is made in this plan; the
  authorised scope is recorded and the work is routed to a gap-closure plan.
- `08-CONTEXT.md`'s UNRESOLVED block is left as written -- the resolution lives in the record, not
  by editing the context document after the fact.
  </acceptance_criteria>

  <done>
U-01 is closed by a maintainer decision taken with both measurements in hand, and the decision is
recorded with its date and its reasoning.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `nx.json` -> every task hash | A `targetDefaults` edit rotates every task hash and changes what Nx caches and restores. A wrong `outputs` list is a silent cache-correctness regression, not a build failure. |
| Inference -> the merged project node | Third-party plugin inference produces the node Nx hashes. The fix relies on `targetDefaults` overriding one of its fields; if that assumption is wrong the fix location itself is wrong. |
| Measured result -> a settled architectural decision | U-01 freezes how this workspace configures targets. A decision auto-taken from a favourable-looking number is the trap quadrant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-21 | Tampering | a pinned `outputs` list that is stable but WRONG | high | mitigate | `outputs` is what Nx caches and restores, so pinning the shorter list would silently stop `typecheck` caching its declaration output. The value comes from plan 08-03's enumeration of what the command actually writes, not from either candidate's entry count, and it is pinned by deep equality with the rationale comment-locked in the guard spec (D-13, RESEARCH A6). |
| T-08-22 | Tampering | `integration`'s runtime discriminator being re-spelled | high | mitigate | A new exact-equality pin on the command string, comment-locked with both reasons: D-14 requires it byte-identical because after VER-03 it is the sole mechanism separating OS-sensitive targets, and `capture-hashes.mjs` READS that string, so a re-spelling changes what the instrument captures too. The pre-existing CORR-04 guard is extended, never re-authored. |
| T-08-23 | Tampering | a pin that replays a cached PASS | high | mitigate | `{workspaceRoot}/nx.json` is already a declared `test` input and that wiring is itself asserted in the same file, so editing `nx.json` re-runs the pin. This repo has shipped the stale-cached-PASS class three times; the wiring is checked rather than assumed. |
| T-08-24 | Elevation of privilege | U-01 auto-resolved from a favourable number | high | mitigate | A blocking `checkpoint:decision` with the four options U-01 itself names, presented with the pre-committed trigger condition beside the measured result. The research's own instruction that the confirming experiment is not a licence to auto-resolve is quoted in the checkpoint context. |
| T-08-25 | Tampering | an undirected search for a value that makes the numbers agree | high | mitigate | Task 2 forbids iterating on `nx.json` when a verdict fails and routes the case to the checkpoint instead. Improvised iteration would destroy the attribution four plans of measurement established, and would produce a fix nobody can explain. |
| T-08-26 | Information disclosure | the public surface widening | medium | mitigate | No env knob, no action input, no package export is added; `public-surface.spec.ts` and `src/index.ts` are untouched and the guard's passing UNCHANGED is the proof (PARITY-07, D-16). The Nx environment variables used by the cold recipe are Nx's own, process-scoped inside one shell or CI step, and read by no shipped code. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new npm package and no manifest change. `08-RESEARCH.md`'s Package Legitimacy Audit stands unchanged. |
</threat_model>

<verification>
- All NINE battery commands green at every commit in this plan, not only the last.
- `packages/github-cache/src/public-surface.spec.ts`, `src/index.ts` and
  `src/test/consumer-contract.ts` are untouched, and `npm run pack:check` exits 0.
- No `project.json` exists in the workspace and `nx.json`'s `plugins` array is unchanged.
- `git diff` over `nx-target-inputs.spec.ts:241-260` shows the pre-existing CORR-04 guard unmodified.
- Both `hash-parity` legs are green at the fix commit.
- The record carries three local post-fix readings and both CI post-fix readings, all at the fix
  commit.
</verification>

<success_criteria>
- `build`, `typecheck` and `test` compute one hash across both runner legs at the fix commit, and
  the workstation's cold reading agrees.
- `integration` differs across the legs and matches between the workstation and `windows-11-arm`.
- Every fix landed in `nx.json` `targetDefaults`, each with its rationale locked in the guard spec
  that pins it.
- U-01 is resolved by a recorded maintainer decision.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See plan 08-01's identical section for the full phase-wide list. This plan contributes:

**New nx.json keys**
- the `targetDefaults` entry the record's `## Proposed fix` section named -- `outputs` on the
  affected target, and any narrowing of a declared `externalDependencies` list

**New spec describe-block subjects** (`packages/github-cache/src/nx-target-inputs.spec.ts`)
- the pinned outputs list for the fixed target, with its rationale comment
- the exact-equality pin on `integration`'s runtime input command string

**New record sections** (`.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md`)
- `## Post-fix re-measurement (local)` with cold, warm-preexisting and warm-after-reset readings
- `## Post-fix re-measurement (CI)` with the four labelled verdicts
- the U-01 resolution, with its date and the selected option id
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-nx-task-hash-parity/08-05-SUMMARY.md` when done.
</output>
