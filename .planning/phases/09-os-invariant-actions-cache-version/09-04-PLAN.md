---
phase: 09-os-invariant-actions-cache-version
plan: 04
type: tdd
wave: 3
depends_on: ["09-03"]
files_modified:
  - packages/github-cache/src/lib/compression-method.ts
  - packages/github-cache/src/lib/compression-method.spec.ts
  - packages/github-cache/src/action/index.ts
  - packages/github-cache/src/action/index.spec.ts
  - packages/github-cache/src/pinned-deps.spec.ts
autonomous: true
requirements: [VER-05]

must_haves:
  truths:
    - "`resolveCompressionMethod()` reproduces `getCompressionMethod`'s observable contract EXACTLY: argv `zstd` `['--quiet','--version']`, stdout AND stderr accumulated into ONE string, the exit code STRUCTURALLY never consulted, a spawn fault yielding `''`, `.trim()`, and the branch on `=== '' ? 'gzip' : 'zstd-without-long'` (VER-05, D-13, C-02)."
    - "It uses `spawnSync`, not `spawn` and never `promisify(execFile)` -- the latter REJECTS on non-zero exit, so the idiomatic `.catch(() => '')` reports gzip for a broken-but-present zstd, the exact inversion VER-05 forbids (C-02, RESEARCH Q6)."
    - "The parsed semver is computed-and-unused upstream and is NOT reintroduced as a condition here (VER-05)."
    - "There is NO timeout, deliberately, and the omission is RECORDED as the one place hardening was declined (D-15)."
    - "The value is surfaced as a separate `core.summary` line after the count table plus a `core.info`, and is NEVER gated -- no branch anywhere reads it (D-16)."
    - "`compression-method.ts` is imported ONLY from `runPublish` in `action/index.ts`, so it stays out of `start-cache-server/index.js` and out of ROBUST-04's rebuild obligation -- proven by `check:action`'s diff staying EMPTY (D-17)."
  artifacts:
    - "`packages/github-cache/src/lib/compression-method.ts` -- `resolveCompressionMethod` and the `CompressionMethod` type, comment-locked to `@actions/cache` 6.2.0 `cacheUtils.js`"
    - "`packages/github-cache/src/lib/compression-method.spec.ts` -- the six-case matrix over a mocked `node:child_process` plus the argv/options assertion"
    - "`packages/github-cache/src/action/index.ts` -- the appended summary line and the `core.info`, with the append mechanism comment-locked"
    - "`packages/github-cache/src/pinned-deps.spec.ts` -- the bump note above the `@actions/cache` assertion, and the record that the checklist VER-05 names does not exist"
  key_links:
    - "the non-zero-exit case -> `local-context.ts:94-104`. `runHelper` resolves `undefined` on any non-zero exit and attaches no stderr listener (`:85-86`), both comment-locked as deliberate -- which is why reusing it would report gzip in precisely the case VER-05 names."
    - "the second `core.summary.write()` -> `action/index.spec.ts:123`, which asserts `write` was called ONCE. That assertion breaks unless it is updated in this commit, and the summary mock at `:16-19` has no `addRaw` method to call."
    - "`resolveCompressionMethod`'s single import site -> the bundle's single entry (`start-cache-server/entry.ts`), which does not reach `action/index.ts`. `npm run check:action` returning an EMPTY diff is the structural proof that D-17 holds."
---

<objective>
Report which compression method `@actions/cache` actually resolved, without being able to ask it.

Purpose: the compression method is a third cache-version component, pushed into the version
UNCONDITIONALLY at `cacheUtils.js:163` -- before and independent of the `enableCrossOsArchive` branch
-- so `enableCrossOsArchive` cannot rescue a mismatch. `getCompressionMethod` is unreachable through
`@actions/cache`'s exports map (the same `ERR_PACKAGE_PATH_NOT_EXPORTED` wall VER-02 documents), so
the value must be independently re-derived. Getting the re-derivation subtly wrong is worse than not
having it: a probe that reports `gzip` where the library computed `zstd` sends the next reader
looking in the wrong place.

Output: one comment-locked leaf, a six-case spec whose control is the non-zero-exit case, one summary
line, and a bump note in the file a bumper must edit anyway.

Vertical slice: one behavioural property (the resolved compression method reaches a human) closed
end-to-end -- the probe, the four-way discrimination that proves it is a faithful port rather than a
`runHelper`-shaped one, and the surfacing. Independent of every other plan in the phase except its
own wave ordering: it touches the publish path only and carries no bundle obligation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-os-invariant-actions-cache-version/09-CONTEXT.md
@.planning/phases/09-os-invariant-actions-cache-version/09-RESEARCH.md
@.planning/phases/09-os-invariant-actions-cache-version/09-PATTERNS.md
@.planning/phases/09-os-invariant-actions-cache-version/09-VALIDATION.md
@.planning/phases/09-os-invariant-actions-cache-version/09-03-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): the six-case matrix over a mocked child process</name>

  <files>
packages/github-cache/src/lib/compression-method.spec.ts
  </files>

  <read_first>
- `node_modules/@actions/cache/lib/internal/cacheUtils.js` `:100-136` -- `getVersion` and
  `getCompressionMethod`, read first-hand. Note four things: `additionalArgs.push('--version')`
  MUTATES the caller's array so the real invocation is `zstd --quiet --version`;
  `ignoreReturnCode: true` means the exit code is never inspected; BOTH stdout and stderr listeners
  append to the same string; and `semver.clean(versionOutput)` is computed and then NOT used.
- `node_modules/@actions/cache/lib/internal/constants.js` `:6-13` -- the three `CompressionMethod`
  string values `gzip`, `zstd-without-long`, `zstd`. `getCompressionMethod` returns only the first
  two; the third exists upstream and is never reached by this code path.
- `packages/github-cache/src/lib/local-context.ts` `:13-18`, `:42-118` -- read it to understand WHY
  it is NOT reused. `:52` and `:65-66` are the two hardening options to borrow (`shell: false`,
  `windowsHide: true`). `:85-86` (no stderr listener, comment-locked as deliberate because stderr is
  localized and credential-adjacent) and `:94-104` (resolves `undefined` on any non-zero exit) are
  the two axes VER-05 INVERTS. `HELPER_TIMEOUT_MS` at `:13-18` is the timeout D-15 declines.
- `packages/github-cache/src/lib/dogfood-body.spec.ts` -- the co-located leaf-spec register: three
  tests, no harness.
- `packages/github-cache/src/lib/local-context.spec.ts` and
  `packages/github-cache/src/lib/select-backend.spec.ts` `:24` -- both mock at the MODULE level, which
  is the nearest thing to a precedent and is NOT sufficient here: module-mocking
  `compression-method.ts` would test nothing. The seam has to be at `node:child_process`.
- `packages/github-cache/src/lint-scope-drift.spec.ts` `:132-141` -- the register for a
  recorded-deliberate-omission ("NO explicit timeout argument, and that is the considered value
  rather than an omission"). Reuse that phrasing for D-15.
- `09-RESEARCH.md` `## Q6` in full, including the primitive comparison table and the
  `promisify(execFile)` row.
- `09-PATTERNS.md` sections `### A5` and `## 4. Patterns with NO in-repo analog` (the
  child-process-fixture row).
- `09-CONTEXT.md` D-12 through D-17, and C-02.
  </read_first>

  <action>
Create `packages/github-cache/src/lib/compression-method.spec.ts`. Mock `node:child_process` so
`spawnSync` is controllable -- there is NO precedent for this in the repo, so state in the header why
a module-level mock of `compression-method.ts` would test nothing and why the seam has to sit at the
child-process boundary.

SIX behavioural cases, each its own `it`:

1. **empty output, clean exit** -- `{ status: 0, stdout: '', stderr: '' }` -> `gzip`.
2. **whitespace-only output** -- `{ status: 0, stdout: '  \n  ', stderr: '' }` -> `gzip`. This is the
   `.trim()` proof; without the trim a build that prints a bare newline selects zstd.
3. **stderr-ONLY output** -- `{ status: 0, stdout: '', stderr: '*** zstd command line interface
   64-bits v1.5.7 ***' }` -> `zstd-without-long`. A stdout-only implementation reports `gzip` here,
   which is `runHelper`'s first inverted axis.
4. **non-zero exit with output** -- `{ status: 1, stdout: 'zstd 1.5.7', stderr: '' }` ->
   `zstd-without-long`. **THIS IS THE CONTROL.** It is the only case that distinguishes a faithful
   port from a `runHelper`-shaped one, and it is the exact broken-but-present-zstd scenario VER-05
   names. Label it as the control in a comment so a later reader does not "simplify" it away.
5. **spawn fault (ENOENT)** -- `{ error: new Error('spawn zstd ENOENT'), status: null, stdout: null,
   stderr: null }` -> `gzip`. Byte-for-byte the behaviour of upstream's caught throw.
6. **non-semver output** -- `{ status: 0, stdout: 'not a version at all', stderr: '' }` ->
   `zstd-without-long`. This proves the parsed semver is not a condition: upstream computes it and
   discards it, so unparseable output still selects zstd.

Plus a SEVENTH, structural test: `spawnSync` was called exactly ONCE, with file `zstd`, args exactly
`['--quiet', '--version']`, and an options object containing `shell: false`, `windowsHide: true` and
`encoding: 'utf8'`. Assert the args array by equality, so a future reordering or an added flag fails.
Note in a comment that `--quiet` precedes `--version` because upstream PUSHES `--version` onto the
passed `['--quiet']` array, so the real invocation order is fixed by upstream and not by preference.

Also assert, structurally, that there is **no timeout option** in the recorded options object. D-15
declines the timeout deliberately, and a later "hardening" pass that adds one would make the reported
value diverge from what the library computed -- which is the one thing this probe must not do. An
assertion is how a deliberate omission stops being indistinguishable from an oversight.

The header must record, in prose:
- why `spawnSync` and not `spawn`: the exit code is STRUCTURALLY never consulted (`status` is simply
  not read) rather than deliberately ignored, ENOENT yields `''` -> gzip with no listener wiring, and
  there is no promise to leave unsettled;
- that `promisify(execFile)` is ACTIVELY WRONG here and must never be substituted: it REJECTS on
  non-zero exit and the output is only reachable as `error.stdout`/`error.stderr`, so the natural
  `.catch(() => '')` reports `gzip` for a broken-but-present zstd -- the exact inversion VER-05
  exists to forbid, arrived at by writing the idiomatic thing;
- that D-14's wording says "use `spawn` directly" and this is a RECORDED deviation, not a silent one:
  D-14's load-bearing content is (i) do NOT reuse `runHelper` and (ii) borrow `shell: false` +
  `windowsHide: true`, and `spawnSync` preserves both.

Run `npx nx run @op-nx/github-cache:test --skip-nx-cache`. It is EXPECTED to be RED --
`compression-method.ts` does not exist. Record the failure.

**DO NOT COMMIT.** Task 2 makes it green and Task 3 commits.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache || echo "EXPECTED RED"</automated>
  </verify>

  <acceptance_criteria>
- `compression-method.spec.ts` exists and mocks `node:child_process`.
- It contains six behavioural cases matching the table above, each asserting the returned value, with
  case 4 (non-zero exit -> `zstd-without-long`) explicitly labelled as THE control in a comment.
- It contains a structural test asserting `spawnSync` was called once with file `zstd`, args exactly
  `['--quiet', '--version']`, and options containing `shell: false`, `windowsHide: true`,
  `encoding: 'utf8'`.
- It asserts the recorded options object carries NO timeout, with D-15's reason in a comment.
- Its header records the `spawnSync`-over-`spawn` reason, the `promisify(execFile)` prohibition with
  the inversion spelled out, and the D-14 wording deviation as recorded rather than silent.
- `npx nx run @op-nx/github-cache:test --skip-nx-cache` is RED and the failure is recorded.
- Nothing is committed.
  </acceptance_criteria>

  <done>
Every observable clause of upstream's contract has a test, and the one case that separates a faithful
port from the obvious wrong one is named as the control.
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): the leaf, the summary line, and the bump note</name>

  <files>
packages/github-cache/src/lib/compression-method.ts
packages/github-cache/src/action/index.ts
packages/github-cache/src/action/index.spec.ts
packages/github-cache/src/pinned-deps.spec.ts
  </files>

  <read_first>
- Task 1's failing output.
- `packages/github-cache/src/lib/cache-key.ts` `:1-21` -- the leaf doc-block register to copy for
  prose shape (not for content).
- `packages/github-cache/src/action/index.ts` `:150-169` -- `runPublish`'s tail and the
  `writeCountSummary` call, including the `:158-161` comment stating that the shared renderer is NOT
  widened to carry a note row for one caller. That sentence is what forces the append.
- `packages/github-cache/src/lib/summary.ts` in full -- `writeCountSummary` renders a two-column
  table whose second header cell is literally `count`, and takes `[string, number]` pairs. A string
  value cannot go through it.
- `packages/github-cache/src/action/index.spec.ts` `:14-30` (the `core` mock, including the `summary`
  object at `:16-19` with only `addHeading`, `addTable` and `write`) and `:96-124` (the
  `runPublish OBS-01 summary rows` describe, whose `:123` asserts
  `expect(core.summary.write).toHaveBeenCalledOnce()`). BOTH must change: the mock needs whatever
  method the new line calls, and the write count is no longer one.
- `node_modules/@actions/core/lib/summary.js` `:69-77` -- `write()` picks `appendFile` unless
  `options.overwrite` is truthy, which is why a second `write()` appends rather than clobbers.
- `packages/github-cache/src/pinned-deps.spec.ts` in full: the describe-level block at `:4-13` (which
  already names the compression choice as part of the restore key), the `@actions/cache` `it` at
  `:22-26` which has NO preceding comment, and the per-package `//` comments at `:34-38` and `:45-49`
  which are the file's own convention.
- `09-CONTEXT.md` D-13, D-15, D-16, D-17, and C-08(b).
- `09-RESEARCH.md` `## Q6` `### Surfacing it (D-16)` and `### The bump-checklist destination DOES NOT
  EXIST`.
  </read_first>

  <action>
STEP 1 -- `packages/github-cache/src/lib/compression-method.ts` (NEW). Export a `CompressionMethod`
type of the two values this code path can return (`gzip` and `zstd-without-long`) and a synchronous
`resolveCompressionMethod(): CompressionMethod` that:

- calls `spawnSync` with file `zstd`, args `['--quiet', '--version']`, and options
  `{ shell: false, windowsHide: true, encoding: 'utf8' }`;
- concatenates `stdout` and `stderr` into ONE string, coalescing each `null` to `''` so an ENOENT
  result yields `''`;
- `.trim()`s;
- returns `gzip` when the trimmed string is empty and `zstd-without-long` otherwise.

`status` is NEVER read. `error` is NEVER read. That is the point: structurally unconsulted survives a
later tidy in a way "we chose not to read it" does not.

Comment-lock, in the doc block:
- **Pinned to `@actions/cache` 6.2.0**, pointing at `lib/internal/cacheUtils.js:100-136`
  (`getVersion` + `getCompressionMethod`) and `lib/internal/constants.js:6-13` (the enum's string
  values). Name the version, per PARITY-06 discipline.
- **Why this duplicate exists at all:** `getCompressionMethod` is not on `@actions/cache`'s exported
  surface (`ERR_PACKAGE_PATH_NOT_EXPORTED`), and the value is a cache-version component pushed
  UNCONDITIONALLY at `cacheUtils.js:163` -- before and independent of the `enableCrossOsArchive`
  branch -- so the flag cannot rescue a mismatch.
- **The observable contract reduces to ONE bit:** is the combined trimmed output empty? It is empty
  iff `zstd` produced nothing on either stream, including because it does not exist. Interleaving
  order, the parsed semver and the exit code are all unobservable downstream, and the parsed semver
  is computed-and-discarded upstream -- so a broken-but-present zstd still selects zstd. Do NOT
  reintroduce the semver as a condition.
- **Why `runHelper` is NOT reused,** naming both inverted axes with their line citations
  (`local-context.ts:85-86` stdout-only; `:94-104` exit-0-only) and stating that reusing it would
  report `gzip` in precisely the case VER-05 names. This is a deliberate rejection, not an oversight.
- **`ponytail:` NO TIMEOUT, and it is the considered value rather than an omission (D-15).** A timeout
  would make the reported value diverge from what the library computed, which is the one failure this
  probe must not have. The hang risk is not ours to add: `@actions/cache` calls
  `getCompressionMethod()` itself on every restore and save in the same job, so a hanging `zstd` has
  already wedged the publish before this probe runs, and the job's `timeout-minutes` is the backstop.
- **`shell: false` and `windowsHide: true`** borrowed from `local-context.ts:52,65-66` with their
  reasons (injection-safe explicit argv; no console-window flash per spawn on Windows).
- **NOT `serve()`-reachable, and it must stay that way (D-17):** import it ONLY from `runPublish` in
  `action/index.ts`. The bundle has one entry (`start-cache-server/entry.ts`) which does not reach
  `action/index.ts`, so this file stays out of `start-cache-server/index.js` by construction -- and
  `npm run check:action` returning an empty diff is what proves it stayed out.
- **Surfaced, never gated:** no branch anywhere reads this value.

STEP 2 -- `action/index.ts`. Import `resolveCompressionMethod` (this is the ONLY import site in the
repo). After the existing `writeCountSummary` call in `runPublish`, add:

- a `core.info` line naming the resolved method, so the value lands in the log as well as the summary;
- a SECOND `core.summary` write appending one line -- a raw line, not a table row.

Comment-lock three things at the call:
- `core.summary.write()` APPENDS by default (`@actions/core/lib/summary.js:69-77` picks `appendFile`
  unless `options.overwrite`), so a second write after `writeCountSummary` adds to the table rather
  than clobbering it;
- `writeCountSummary` cannot carry this value: it takes `[string, number]` pairs and renders a column
  headed `count`, and `zstd-without-long` under a `count` header is wrong. `summary.ts`'s own comment
  already states the shared renderer is not widened for one caller -- cite it rather than restating
  the rule;
- **surfaced, never gated.** No branch reads the value. If a future requirement wants to gate on it,
  that is a new decision and not an extension of this one.

STEP 3 -- `action/index.spec.ts`. Two required edits, both consequences of STEP 2:
- extend the `summary` mock at `:16-19` with whatever method the new line calls (chainable, returning
  the same object, matching the existing `addHeading`/`addTable` shape);
- update `:123` -- `core.summary.write` is now called TWICE, not once. Change the assertion to the
  new count and add an assertion on the appended line's content, so the surfacing cannot silently
  revert to nothing. Assert on CONTENT, not only on a call count: a bare count of 2 is satisfied by
  two empty writes.

Then check whether `runPublish`'s tests need `resolveCompressionMethod` controlled. If the real
`spawnSync` runs during those tests the assertion becomes machine-dependent -- pin it by mocking the
new module at the module level in this spec (which IS the repo's established seam for a sibling leaf)
so the asserted line is deterministic on every machine. State that reason in a comment; a spec whose
expected text depends on whether the developer has `zstd` installed is a flake waiting to happen.

STEP 4 -- `pinned-deps.spec.ts`. Add a per-`it` `//` comment above the `@actions/cache` assertion at
`:22-26`, matching the file's own per-package convention (`:34-38`, `:45-49`) rather than extending
the describe-level block, so the note sits next to the assertion a bumper must edit. It must say:
**re-read `getCompressionMethod` on any `@actions/cache` bump; VER-05 duplicates it** in
`src/lib/compression-method.ts`, and a change to upstream's branch or to its argv makes this repo's
reported value diverge from the version the library actually computes.

Also record, in the same comment, that VER-05 and D-13 both direct this note to "the `@actions/cache`
bump checklist" and **no such checklist exists** -- zero hits for `checklist` across `docs/`,
`packages/`, `README.md` and `CONTRIBUTING.md`. This file was chosen as the destination because it
already names the same silent-MISS mechanism and is the one file a bumper must touch anyway. Record
the requirement's dangling reference as a correction, do not create a new `docs/` document nobody
reads at the moment it matters.

STEP 5 -- run the full nine-command battery. `npm run check:action` MUST exit 0 with an EMPTY diff:
that is the structural proof of D-17, because a `compression-method.ts` that had leaked into the
`serve()` graph would grow the bundle and show up here.

**DO NOT COMMIT.** Task 3 commits.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache</automated>
    <automated>npm run typecheck &amp;&amp; npm run typecheck:action</automated>
    <automated>npm run check:action</automated>
  </verify>

  <acceptance_criteria>
- `compression-method.ts` exports `resolveCompressionMethod` and a `CompressionMethod` type of exactly
  the two returnable values, and the function is synchronous.
- The implementation calls `spawnSync('zstd', ['--quiet', '--version'], { shell: false, windowsHide:
  true, encoding: 'utf8' })`, concatenates stdout and stderr with `null` coalesced to `''`, trims, and
  branches on `=== ''`.
- `git grep -n "status\|\.error" -- packages/github-cache/src/lib/compression-method.ts` shows no read
  of the `spawnSync` result's `status` or `error`.
- No semver parsing, no timeout option, no `promisify`, no `execFile`, and no import of
  `local-context.js` appears in the file.
- Its doc block records: the pinned `@actions/cache` 6.2.0 with the `cacheUtils.js` and `constants.js`
  citations; why the duplicate exists including the unconditional push at `:163`; the one-bit
  observable contract and the do-not-reintroduce-the-semver instruction; both `runHelper` inversion
  axes with line citations; the `ponytail:` no-timeout decision with D-15's reasoning; the two
  borrowed hardening options with their reasons; the D-17 not-`serve()`-reachable constraint; and
  surfaced-never-gated.
- `action/index.ts` imports `resolveCompressionMethod` exactly once, and that is the only import of
  the module in the repo (`git grep -c "compression-method" -- packages/github-cache/src` shows the
  implementation, its spec, and this one import site).
- `runPublish` emits a `core.info` naming the resolved method and a second `core.summary` write
  appending one raw line, both AFTER `writeCountSummary`.
- The call site's comment records the append mechanism with its `summary.js:69-77` citation, why
  `writeCountSummary` cannot carry the value with a citation to `summary.ts`'s own comment, and
  surfaced-never-gated.
- `action/index.spec.ts`'s `summary` mock carries the new method, `:123`'s assertion reflects TWO
  writes, and there is an assertion on the appended line's CONTENT.
- `action/index.spec.ts` controls `resolveCompressionMethod` so the asserted line is deterministic
  regardless of whether `zstd` is installed on the machine, with that reason in a comment.
- `pinned-deps.spec.ts` carries a `//` comment above the `@actions/cache` `it` containing the
  re-read-`getCompressionMethod` instruction, the path to the duplicate, and the record that the
  checklist VER-05 names does not exist.
- All NINE battery commands exit 0, and `npm run check:action` exits 0 with an EMPTY diff.
- Nothing is committed.
  </acceptance_criteria>

  <done>
The resolved compression method reaches both the log and the job summary, its derivation is pinned to
the upstream code it duplicates, and the bump that would silently invalidate it now has a note on the
assertion a bumper cannot avoid.
  </done>
</task>

<task type="auto">
  <name>Task 3 (prove, then commit): four mutations, then one commit</name>

  <files>
packages/github-cache/src/lib/compression-method.ts
  </files>

  <read_first>
- `.planning/phases/07-lint-toolchain-and-the-ambient-platform-read-ban/07-EVIDENCE.md` -- the
  applied / OBSERVED / reverted record shape.
- Task 1's six-case table -- each mutation must name which case it reddens.
  </read_first>

  <action>
Apply each mutation, OBSERVE it, revert it, and confirm green again. Record the change, the named
case that went red, and the verbatim failure message.

- **M1 -- consult the exit code.** Add an early `return 'gzip'` when the result's `status` is not 0.
  Case 4 must go RED. This is the `runHelper`-shaped implementation, and M1 is the proof that the
  control case earns its place.
- **M2 -- drop stderr.** Concatenate only `stdout`. Case 3 must go RED.
- **M3 -- branch on a parsed version instead of on emptiness.** Case 6 must go RED.
- **M4 -- remove the `.trim()`.** Case 2 must go RED.

If any mutation does NOT redden its named case, the case is vacuous and must be fixed before
proceeding -- that is the whole reason these four are run rather than reasoned about.

Confirm with `git diff` that `compression-method.ts` is back to Task 2's state, then create ONE commit
staging exactly:

- `packages/github-cache/src/lib/compression-method.ts`
- `packages/github-cache/src/lib/compression-method.spec.ts`
- `packages/github-cache/src/action/index.ts`
- `packages/github-cache/src/action/index.spec.ts`
- `packages/github-cache/src/pinned-deps.spec.ts`

Stage each by name. `start-cache-server/index.js` is NOT in this commit and must not be modified --
if `npm run check:action` shows a diff here, something reached the `serve()` graph that should not
have, and that is a finding to investigate rather than a file to stage.

The commit body should state that the compression method is a third cache-version component surfaced
and never gated, that the value is an independent re-implementation pinned to `@actions/cache` 6.2.0,
and that the bump-checklist destination VER-05 names does not exist so the note went into
`pinned-deps.spec.ts`.
  </action>

  <verify>
    <automated>npm run format:check &amp;&amp; npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run typecheck:action</automated>
    <automated>npm run test &amp;&amp; npm run lint &amp;&amp; npm run fallow:ci</automated>
    <automated>npm run check:action &amp;&amp; npm run pack:check</automated>
  </verify>

  <acceptance_criteria>
- The task notes record all four mutations with the case each reddened and the verbatim failure
  message.
- `git diff` shows no mutated state persisted.
- Exactly ONE commit contains the five paths and `start-cache-server/index.js` is NOT among them.
- `npm run check:action` exits 0 with an empty diff at the committed tree.
- All NINE battery commands exit 0 at the committed tree.
- The commit body carries the three recorded notes.
  </acceptance_criteria>

  <done>
Each of the four ways a plausible implementation gets this wrong has been shown to fail a named test,
and the probe is committed without touching the bundle.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `zstd` on the runner image -> the reported cache-version component | An external binary whose presence is a runner-image provisioning choice. MEASURED 2026-07-26: zstd v1.5.7 lives at `C:\tools\zstd` on `windows-11-arm`, NOT bundled by Git for Windows as previously recorded, which makes it more likely to move than assumed. |
| child-process stdout/stderr -> a summary line rendered into a public job summary | Untrusted external output reaching a rendered surface. |
| the duplicated derivation -> `@actions/cache`'s real behaviour | Two copies of one branch. A bump can move upstream's copy silently, and the repo's copy would then report a value the library did not compute. |
| `compression-method.ts` -> the `serve()` import graph | One accidental import from anywhere in the serve graph pulls a child-process spawn into the committed sidecar bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-26 | Spoofing | a probe reporting gzip for a broken-but-present zstd | high | mitigate | `spawnSync` with `status` structurally unread, and case 4 (non-zero exit -> zstd) asserted as THE control with M1 proving it fires. `promisify(execFile)` is named in the doc block as actively wrong so the idiomatic rewrite is closed off in advance. |
| T-09-27 | Tampering | command injection through the spawn | high | mitigate | `shell: false` with an explicit argv array; both the file and the args are hardcoded literals with no interpolation and no caller input. The args array is asserted by equality, so an added or reordered flag fails. |
| T-09-28 | Information disclosure | child-process output rendered into a public job summary | medium | mitigate | Only the DERIVED two-value enum is surfaced, never the raw output. The concatenated version string is used solely for an emptiness test and is not logged, so a localized or verbose `zstd` banner cannot reach the summary. |
| T-09-29 | Tampering | the duplicate silently diverging on a dependency bump | high | mitigate | The doc block pins `@actions/cache` 6.2.0 with file-and-line citations, and `pinned-deps.spec.ts` carries the re-read instruction on the assertion a bumper must edit. `pinned-deps.spec.ts` already fails the build if the specifier widens to a range, so a bump is always a deliberate edit to that file. |
| T-09-30 | Denial of service | a hanging `zstd` wedging the publish | medium | accept | D-15: NO timeout, recorded as the one place hardening was declined, and asserted structurally so the omission cannot be mistaken for an oversight. `@actions/cache` calls `getCompressionMethod()` itself on every restore and save in the same job, so a hang has already wedged the publish before this probe runs; the job's `timeout-minutes` is the backstop. |
| T-09-31 | Elevation of privilege | the value becoming a gate | medium | mitigate | Surfaced, never gated -- stated in the doc block and at the call site. No branch reads the value, and the requirement says so explicitly. |
| T-09-32 | Tampering | the probe leaking into the committed sidecar bundle | high | mitigate | D-17: one import site, from `runPublish`, which the bundle's single entry does not reach. `npm run check:action` exiting 0 with an EMPTY diff is asserted in this plan's acceptance criteria as the structural proof, and a non-empty diff is a finding rather than a file to stage. RESEARCH Q11 recommends this byte-diff over a bundle-content assertion, which needs no new mechanism. |
| T-09-33 | Repudiation | a machine-dependent spec assertion | medium | mitigate | `action/index.spec.ts` controls `resolveCompressionMethod` at the module level so the asserted summary line does not depend on whether the developer has `zstd` installed. `compression-method.spec.ts` mocks `node:child_process`, so neither spec executes a real spawn. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package. `spawnSync` is a `node:child_process` builtin. No `semver` dependency is introduced -- upstream's parsed version is deliberately not reproduced. |
</threat_model>

<verification>
- All NINE battery commands green at this plan's final commit.
- `npm run check:action` exits 0 with an empty diff; `start-cache-server/index.js` is unmodified by
  this plan.
- `packages/github-cache/src/public-surface.spec.ts`, `packages/github-cache/src/index.ts` and
  `packages/github-cache/src/test/consumer-contract.ts` are UNMODIFIED -- no new package export, env
  knob or action input (PARITY-07, D2-02).
- `packages/github-cache/src/lib/compression-method.ts` is imported from exactly one place.
- No file under `packages/github-cache/src/backend/` or `packages/github-cache/src/lib/cache-archive-path.ts`
  is touched by this plan.
- No mutated state is persisted on the branch.
</verification>

<success_criteria>
- The resolved compression method appears in both the publish log and the publish job summary, as a
  line rather than a table row, and nothing branches on it.
- The derivation reproduces upstream's observable contract on all four discriminating axes -- stderr
  captured, exit code unconsulted, spawn fault swallowed, branch on emptiness -- each proven by a
  mutation.
- No timeout, recorded as a deliberate decision and asserted structurally.
- The module stays out of the committed sidecar bundle, proven by an empty `check:action` diff.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

See plan 09-01's identical section for the full phase-wide list. This plan contributes:

**New files**
- `packages/github-cache/src/lib/compression-method.ts`
- `packages/github-cache/src/lib/compression-method.spec.ts`

**New exported symbols**
- `resolveCompressionMethod` -- `packages/github-cache/src/lib/compression-method.ts`
- `CompressionMethod` (type) -- `packages/github-cache/src/lib/compression-method.ts`

**Changed observable behaviour**
- `runPublish` writes the job summary TWICE: `writeCountSummary`'s table, then one appended raw line
  carrying the resolved compression method. `action/index.spec.ts:123`'s call-count assertion moves
  from one to two accordingly.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-os-invariant-actions-cache-version/09-04-SUMMARY.md` when done.
</output>
