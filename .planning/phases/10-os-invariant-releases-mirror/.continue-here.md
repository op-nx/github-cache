---
context: phase
phase: 10-os-invariant-releases-mirror
task: 8
total_tasks: 8
status: paused
last_updated: 2026-07-29T13:47:38.617Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> These are not suggestions. Each was discovered through failure -- two of them in THIS session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **secure-phase self-certification** -- `/gsd:secure-phase` short-circuits past
      `gsd-security-auditor` and writes SECURITY.md inline when `threats_open:0 AND
      register_authored_at_plan_time:true`. That predicate is EXACTLY this phase's shape, so it
      WILL fire. TRUST-13 states the classification must come from the auditor and that the
      proposed classification is INPUT, not conclusion. -- MITIGATION: spawn `gsd-security-auditor`
      regardless of the clean-looking short-circuit; the orchestrator may only route and commit the
      agent's returned verdict, never derive one from its own reading.

- [ ] CONSTRAINT: **never respawn a spend-limit-killed agent** -- three agents were terminated
      mid-task by the org monthly spend limit this session (plan-checker once, verifier twice).
      Each had completed 350K+ tokens of analysis that had NOT reached disk. -- MITIGATION: resume
      with `SendMessage` to the agent id, instructing it to write its file FIRST in one call before
      any refinement. Respawning discards the work and re-derives from cold.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auditor short-circuit | `secure-phase` writes SECURITY.md inline on a clean register, self-certifying TRUST-13 | blocking | Spawn `gsd-security-auditor` unconditionally; orchestrator routes the verdict, never authors it |
| Respawn-after-kill | A spend-limit kill leaves complete analysis in agent context but nothing on disk | blocking | `SendMessage` to resume; brief every agent with a FILE deliverable and "write early and terse, then refine in place" |
| Stale LSP diagnostics | The `new-diagnostics` feed reported phantom main-tree errors after SIX consecutive plans this phase; `npm run typecheck` reproduced NONE of them at HEAD | advisory | Treat the feed as non-authoritative. Run `npm run typecheck --skip-nx-cache` before acting on any diagnostic |
| Bare `cd` in a Bash call | The tool's cwd persists across calls; a bare `cd` into the phase dir made a later `rg` report "path not found", which I misread as an agent having lied about writing its file | advisory | Never `cd`; anchor with `git rev-parse --show-toplevel` or absolute paths |
| Non-ASCII in planning artifacts | I committed `10-VALIDATION.md` with em dashes and emoji status glyphs, violating the repo's ASCII rule, and had to fix it in a follow-up commit | advisory | `rg -c '[^\x00-\x7F]' <file>` before every commit of an authored artifact |
| Revert discards uncommitted GREEN | Executor 10-02 mutated a file to test a guard, then `git checkout --` reverted away its own still-uncommitted GREEN edit | advisory | Commit GREEN before mutating; never mutate against staged-but-uncommitted work |

<current_state>
Phase 10 is CODE-COMPLETE and INDEPENDENTLY VERIFIED. Paused on explicit user instruction at a
clean checkpoint -- nothing is stuck, nothing is half-applied, the working tree is clean.

- 8/8 plans executed, 34 commits from planning base `06019d4`
- `10-VERIFICATION.md` status: **passed** (13/13 code-level must-haves; two independent
  end-to-end mutation tests run by the verifier itself, not ratified from summaries)
- Battery at HEAD: 823 tests; `build`, `typecheck`, `lint`, `format:check`, `check:action`,
  `fallow:ci`, `integration` all exit 0
- Requirements: 10/12 ticked. OBS-05 and TRUST-13 are deliberately OPEN and must stay open until
  their real gates close.
- Branch `gsd/v0.0.2-os-invariant-cross-os-sharing`, 209 ahead of `origin/main`, UNPUSHED, no PR.
- `workflow._auto_chain_active` was set FALSE at pause, so no GSD command will auto-advance.
</current_state>

<completed_work>

- 10-01: capture the two perishable pre-rename measurements -- Done (`f867326`)
- 10-02: the `mirrored-by` label seam through the real Octokit adapter -- Done (`d17cbd2`)
- 10-03: widen `publish`'s `needs:` + the repo's first `needs:` VALUE guard -- Done (`7cb071b`)
- 10-04: `lib/mirror-seed.ts` + the `mirror-seed` operation branch -- Done (`1894b70`)
- 10-05: flip seed and reader atomically + the `mirrored-by` read-back control -- Done (`cfe22d1`)
- 10-06: ADD-only assertion-level RED for the rename wave -- Done (`b37a358`)
- 10-07: THE ONE COMMIT -- OS-free name, two-branch filter, prose sweep, rebuilt bundle -- Done (`6d65459`)
- 10-08: `ref`-scoping pin, TRUST-11/12 to the auditor as INPUT, SC6 record -- Done (`80e27ad`)
- verify_phase_goal: `gsd-verifier` returned **passed** -- Done (`bb3a23a`)
</completed_work>

<remaining_work>

1. `/gsd:secure-phase 10` -- NOT STARTED. This is the pause point. Read the blocking constraint above first.
2. `/gsd:validate-phase 10` -- NOT STARTED. It owns flipping `10-VALIDATION.md`'s `status: draft`,
   `nyquist_compliant: false`, `wave_0_complete: false`. Those fields reading `draft` right now is
   CORRECT SEQUENCING, not an oversight -- the verifier flagged and correctly dismissed it.
3. `/gsd:extract-learnings 10` -- NOT STARTED. Run LAST, after secure and validate are both clean,
   so it captures what those audits surface rather than a half-closed snapshot.
</remaining_work>

<decisions_made>

- **Sequential-on-main, not worktrees.** `AGENTS.md`'s own rule covers it (sequentially-dependent
  plans gain no parallelism -- 7 waves for 8 plans), and decisively, 10-07 requires
  `npm run check:action` in the MAIN tree: a fresh worktree has no `node_modules`, and junctioning
  it makes esbuild rewrite ~689 module paths with zero source edits and report false drift. Set
  per-run only; `workflow.use_worktrees` was NOT persisted to false.
- **U-01 resolved to the additive branch.** Measured 5/5 push runs (ubuntu's `publish` ends 150-190 s
  before the Windows `seed` starts), but GitHub documents NO matrix leg ORDER guarantee. The law is
  narrower than CONTEXT.md guessed: the leg that runs LAST is the only one whose dead publish path
  is detectable. The `mirrored-by` label assertion REPLACES the ordering dependency with a
  non-overlap dependency -- and it is the ONLY mechanism that detects a dead publisher, because
  the payload tightening cannot (the bytes are correct regardless of who uploaded them).
- **D-00/D-01 tagged `[informational]`** rather than cited in a plan -- they are orchestrator
  pre-flight instructions about artifact defects, not implementable behaviour. The other nine
  uncovered decisions got real `D-NN:` citations in `must_haves.truths`.
- **OBS-05 and TRUST-13 left unticked deliberately.** Not an oversight; see remaining gates.
</decisions_made>

<blockers>
None. The pause is a deliberate checkpoint, not a stuck state.
</blockers>

## Required Reading (in order)

1. `.planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md` -- what was proven and by
   what evidence; its `## Not verified` section states its own coverage boundary honestly.
2. `.planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md` -- the INPUT for
   `gsd-security-auditor`. Part B0 places the INPUT framing BEFORE B2's proposed classification;
   B8 records the no-short-circuit constraint. **The auditor must read this, not be handed a verdict.**
3. `.planning/phases/09-os-invariant-actions-cache-version/09-SECURITY.md` section 1 -- "the OS
   partition was never a security boundary". This is the reframing that makes TRUST-12's exposure
   delta assessable as BOUNDED rather than alarming. Required auditor input.
4. `.planning/REQUIREMENTS.md` -- authoritative requirement text. **Never audit against ROADMAP's
   table**: its Coverage Validation says "Phase 10: 11" and its traceability rows omit `RETAIN-05`.
   REQUIREMENTS.md is self-consistent at TWELVE.
5. `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `## U-01 Resolution` and
   `## Corrections to CONTEXT.md` -- RESEARCH wins over CONTEXT wherever it measured.
6. `.planning/THREAT-MODEL.md` -- the C1-C18 ledger. C1/C2/C16 are what TRUST-10 verified unchanged.

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Orchestrator authors an audit verdict a dedicated agent exists to reach
  -> spawn the agent; route and commit its result, never derive one inline.
- [ANTI-PATTERN]: Respawning an agent killed mid-task -> `SendMessage` to resume from its transcript.
- [ANTI-PATTERN]: Acting on the LSP `new-diagnostics` feed -> run the compiler; the feed was wrong
  six times out of six this phase.
- [ANTI-PATTERN]: `git grep` for an absence claim -> it FALSE-ZEROES on untracked and gitignored
  paths and reads exactly like confirmation. Use `rg`. Also `git grep -c` counts LINES, not occurrences.
- [ANTI-PATTERN]: `git commit -m` on this repo -> fails on the `D:` ReFS Dev Drive
  (`.git/COMMIT_EDITMSG` "Invalid argument"). Use `git commit -F <file>`.
- [ANTI-PATTERN]: `git add .` / `-A` / `-u` -> stage by name, always.

## Infrastructure State

- **Working tree:** clean. No background processes, no running sidecar, no leaked ports
  (10-01 confirmed teardown and port closure after its live probe).
- **`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` is set in the environment.** GSD warns its multi-agent
  orchestration is not validated under agent-teams and a subagent's completion can fail to route
  back. It cannot be unset mid-session. Mitigation used throughout: every agent briefed with a FILE
  deliverable. If you want this clean for the remaining audits, restart with the var unset.
- **Branch:** `gsd/v0.0.2-os-invariant-cross-os-sharing`, 209 ahead of `origin/main`, unpushed, no PR.
- **`.planning/config.json`** carries `workflow._auto_chain_active: false` (set at pause) and is
  otherwise unmodified. It was deliberately never staged by any executor.
- **Local Nx cache:** intact. `nx reset` was never run -- 10-01 took its cold measurement via a
  cold-DIRECTORY variant that removes the hazard rather than merely satisfying it.

<context>
The phase collapsed the Releases mirror namespace from two names per hash to one
(`nx-cache-<hash>`), while keeping it prunable (two-branch cleanup filter, same commit) and
attributable (`mirrored-by: <os>` in the free-form Release asset label, outside the lookup name).

The subtle part -- and the thing most likely to be undone by a future reader -- is that the
`mirrored-by` label is the PUBLISHING leg's OS, never the producing OS. Phase 9 is precisely what
broke the publisher-equals-producer identity, so a "producing OS" claim would be wrong in exactly
the cross-OS case the label exists to serve. That retraction is comment-locked at the construction
site and guarded by `docs-same-os-claims.spec.ts`.

Three executors caught real defects the plans had missed: a sixth `releaseAssetName` consumer
research enumerated as five; an unanchored `\bbuild\b` that would have matched the publish job's own
`npm run build` step and made the XOS-07 guard a tautology; and a `read-back.ts` label read that
needed pagination because the live shard holds 122 assets and a single-page read would have reddened
`publish-verify` on CORRECT code. Trust the executors' measurements over the plans' prose.
</context>

<next_action>
Start with: `/gsd:secure-phase 10` -- and force it to spawn `gsd-security-auditor`. Its documented
inline short-circuit (`threats_open:0 AND register_authored_at_plan_time:true`) matches this phase
exactly and will fire; taking it would self-certify TRUST-13, which the requirement forbids in its
own words. Hand the auditor `10-TRUST-EVIDENCE.md` plus `09-SECURITY.md` section 1 and Phase 9's
commit range, and let IT reach the verdict.

Then `/gsd:validate-phase 10`, then `/gsd:extract-learnings 10` LAST.
</next_action>
