---
phase: 10-os-invariant-releases-mirror
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md
autonomous: true
requirements: [RETAIN-05]
must_haves:
  truths:
    - A pre-rename local Windows read of the Releases mirror is recorded with its
      verbatim command transcript and its ACTUAL outcome, whatever that outcome was.
    - The `cache-mirror-202607` asset census is recorded with the release id, the tag,
      per-family counts, and the shard-scoped bounding argument.
    - RETAIN-05(a)'s disposition is stated as a decision with a measured count -- accepted
      dead weight, no third accept branch, manual prune available and explicitly not code.
  artifacts:
    - .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md
  key_links:
    - The artifact cites `.planning/quick/260725-w3s-verify-local-development-environment-nx-/260725-w3s-STEP0-RESULTS.md`
      as the corroborating prior AND states why a fresh capture was still required (the
      2026-07-26 record predates Phase 9's `@actions/cache` version rotation).
    - The artifact names the shard tag beside every count, because the shard rolls over
      2026-08-01 and a bare number is unattributable afterwards.
  prohibitions:
    - MUST NOT edit any file under `packages/`, `.github/`, or `start-cache-server/`.
      This plan is evidence capture only; a single source edit here would consume the
      pre-rename window it exists to preserve.
    - MUST NOT record a HIT that was not observed. A MISS is a finding, recorded as a
      BLOCKER for Phase 11's XOS-02, never smoothed into the prior record's HIT.
    - MUST NOT paste any `gh api` output field that can carry a committer email address.
    - MUST NOT run `nx reset` while the sidecar is running (VER-07/TEST-10: `nx reset`
      deletes `.nx/cache`, which is where the archive lives, and the next PUT 500s).
  human_needed: []
---

<objective>
Capture the two measurements this phase destroys, before it destroys them.

Purpose: D-25's pre-rename XOS-02 baseline is owed to Phase 11 and its window closes
permanently the moment CORR-02 lands or the branch merges to `main` (a merge triggers
`publish`, which republishes under the new name). D-08's shard census is RETAIN-05(a)'s
recorded disposition and is scoped to `cache-mirror-202607`, which rolls over 2026-08-01.
Both are perishable; nothing else in this phase can be planned around recovering them.

Output: `10-EVIDENCE-PRE-RENAME.md` -- one artifact, two sections, both dated and both
citing the exact commands that produced them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md
@.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md
</context>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / path | Note |
|------|---------------|------|
| new file | `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md` | UNTRACKED when first written -- every absence claim about it MUST use `rg`, never `git grep` (Pitfall 1; `git grep` false-zeroes on untracked paths and the zero reads as confirmation) |

No source symbol, no export, no CI input is created by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-rename local Windows mirror read (D-25)</name>

  <files>.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md</files>

  <read_first>
    - `.planning/quick/260725-w3s-verify-local-development-environment-nx-/260725-w3s-STEP0-RESULTS.md`
      -- the corroborating prior AND the recipe of record. Read its "Measurement soundness"
      control table and its "End-to-end Nx probe" section; reuse both shapes verbatim.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `## Sequencing hazards`
      H-5, and `## Corrections to CONTEXT.md`.
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-25.
    - `packages/github-cache/src/lib/select-backend.ts` -- the off-CI branch selection, so
      the transcript can record WHICH backend was selected rather than assuming it.
  </read_first>

  <action>
    Measure, on this Windows arm64 workstation, whether a local `nx run
    @op-nx/github-cache:integration` reads `[remote cache]` from the Releases mirror under
    the CURRENT `<hash>-<os>` asset name, and write the result into a new
    `10-EVIDENCE-PRE-RENAME.md` under a `## D-25 pre-rename XOS-02 baseline` heading.

    Method, in this order. Prefer the COLD-DIRECTORY variant (it is what the cited prior
    record used and it mutates nothing in the repo); the `nx reset` variant is the fallback:

    1. COLD-DIRECTORY variant (preferred): set `NX_CACHE_DIRECTORY` and
       `NX_WORKSPACE_DATA_DIRECTORY` to fresh paths inside the session scratchpad, so the
       run is cold without deleting the repo's `.nx/cache`. Record that this deviates from
       D-25's literal "`nx reset`" wording and why: it reaches the same cold state, it keeps
       `git status` clean, and it sidesteps H-5's ordering hazard entirely.
    2. FALLBACK variant: `npx nx reset` FIRST, then start the sidecar. Never the reverse --
       `nx reset` deletes `.nx/cache`, which is where the archive lives, so resetting under
       a running sidecar 500s the next PUT (VER-07/TEST-10).
    3. Establish the soundness controls BEFORE any read, because the reader degrades every
       fault to a MISS so a HIT is self-evidencing but a MISS is not. Reuse the prior
       record's control table rows: `gh auth token` resolves (length only, never the value);
       `git remote get-url origin` resolves; `GITHUB_ACTIONS` / `GH_TOKEN` / `GITHUB_TOKEN` /
       `GITHUB_REPOSITORY` all UNSET so the off-CI Releases-reader branch is taken; the bound
       URL read from the sidecar's OWN stdout; a wrong bearer returns 401; a valid bearer on a
       known-absent hash returns 404; sidecar stderr byte count; teardown confirmed.
    4. Run `npx nx run @op-nx/github-cache:integration` behind the sidecar and capture the
       verbatim Nx output lines, including the `[remote cache]` marker and the
       `Cache: N/N hit` line if present.
    5. Record the ACTUAL outcome. If it is a HIT, that capture is the baseline of record and
       the 2026-07-26 record is the corroborating prior (D-25 locks both). If it is a MISS,
       write the MISS verbatim, state which control eliminates which cause, and flag it as an
       OPEN BLOCKER for Phase 11's XOS-02 -- do not substitute the prior record's HIT.
    6. Record the reason a fresh capture was required even though ROADMAP sanctions citing
       the prior: the 2026-07-26 record predates Phase 9's `@actions/cache` version rotation,
       so citing it alone would leave Phase 11's non-regression comparison straddling TWO
       changes instead of one.
    7. Record the exact commit SHA, the Nx version, the Node version, and that the run was in
       the MAIN tree (not a worktree with a junctioned `node_modules`).

    ASCII only. Never paste a field that can carry a committer email address.
  </action>

  <acceptance_criteria>
    - `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md` exists and
      `rg -c 'D-25 pre-rename XOS-02 baseline' <path>` returns at least 1.
    - The artifact contains a soundness-control table: `rg -c 'gh auth token' <path>` >= 1 AND
      `rg -c '401' <path>` >= 1 AND `rg -c '404' <path>` >= 1.
    - The artifact contains the verbatim Nx outcome line for the `integration` run:
      `rg -c 'remote cache|Cache: [0-9]+/[0-9]+' <path>` >= 1.
    - The artifact names the corroborating prior by path:
      `rg -c '260725-w3s-STEP0-RESULTS' <path>` >= 1.
    - The artifact states the reason a fresh capture was required:
      `rg -c 'predates|rotation' <path>` >= 1.
    - The artifact records the measurement environment: `rg -c 'MAIN tree' <path>` >= 1.
    - `git status --porcelain packages/ .github/ start-cache-server/` prints NOTHING (this
      plan edits no source). Assert on the printed content, not on an exit code.
    - Every absence or presence check above uses `rg`, not `git grep`: the artifact is
      UNTRACKED at first write, and `git grep` returns zero on an untracked path with no
      error, which is indistinguishable from confirmation.
  </acceptance_criteria>

  <verify>
    <automated>rg -c "D-25 pre-rename XOS-02 baseline" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md && rg -c "260725-w3s-STEP0-RESULTS" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md && rg -c "MAIN tree" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md</automated>
  </verify>

  <done>
    The pre-rename local read is recorded with its controls, its verbatim outcome, its
    environment, and the prior it corroborates or contradicts. No source file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the shard census and RETAIN-05(a)'s disposition (D-08)</name>

  <files>.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` RETAIN-05 (the authoritative text -- read (a), (b) and (c);
      only (a) is this plan's, but the split matters).
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-08 and the
      `<specifics>` note on the per-SHARD cap framing.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `### RETAIN-05` and
      `## Open Questions` item 5.
    - `packages/github-cache/src/lib/retention.ts` -- `shardTag` / `shardTagsForWindow`, so
      the artifact names the shard scheme correctly rather than paraphrasing it.
  </read_first>

  <action>
    Re-take the census live and write it into the SAME artifact under a
    `## RETAIN-05(a) shard census and disposition` heading.

    Commands: `gh api repos/op-nx/github-cache/releases/tags/cache-mirror-202607` for the
    release id, then `gh api repos/op-nx/github-cache/releases/<id>/assets?per_page=100
    --paginate` for the asset list. Bucket every asset name into exactly one of five
    families: `<hash>.tar.gz` (PoC-era), `<hash>-linux`, `<hash>-windows`, `<hash>-macos`,
    anything else. Report the total and each count.

    The research measured 122 / 50 / 46 / 26 / 0 / 0 at release id `354838660` on 2026-07-29.
    If today's numbers differ, record TODAY's numbers as the census of record and note the
    delta and its cause (a default-branch push between the two reads mirrors more assets).
    Do not reconcile by preferring the research figure.

    Also record, because the count alone is not the requirement:
    - The DECISION: accepted dead weight with a measured count. No third accept branch --
      widening a DELETE filter that quick `260721-vdn` deliberately narrowed on security
      grounds is the worst of RETAIN-05(a)'s three sanctioned options, because a
      `<hash>.tar.gz` name is indistinguishable in shape from a foreign asset dropped into a
      genuine shard. No code change.
    - The BOUNDING argument: the 1000-asset cap is per-SHARD, this shard holds the measured
      total of 1000, and it rolls over 2026-08-01, so its cap is now unreachable and the
      PoC-era assets are permanent occupants of a cap nothing will approach. The cap already
      degrades to skip-and-warn, never a hard failure.
    - The remaining option: a one-off manual prune stays available to the maintainer and is
      explicitly operational, NOT code and NOT this phase's work.
    - That `label` was measured EMPTY on every asset in the census, which is what makes
      OBS-03 a genuinely new field rather than a partially populated one.
    - The shard TAG beside every number, because the shard rolls over and a bare count is
      unattributable afterwards.

    ASCII only. Do not paste `gh api` output verbatim -- extract the counts and names you
    need; some fields carry committer identity.
  </action>

  <acceptance_criteria>
    - `rg -c 'RETAIN-05\(a\) shard census' <artifact>` >= 1.
    - The artifact names the shard tag and the release id:
      `rg -c 'cache-mirror-202607' <artifact>` >= 1.
    - The artifact carries a five-family table with a total; every family named:
      `rg -c 'tar\.gz' <artifact>` >= 1 AND `rg -c 'macos' <artifact>` >= 1.
    - The artifact states the disposition in decision form:
      `rg -c 'accepted dead weight' <artifact>` >= 1.
    - The artifact states the rejection of a third accept branch AND its reason:
      `rg -c 'third accept branch' <artifact>` >= 1 AND
      `rg -c '260721-vdn' <artifact>` >= 1.
    - The artifact states the per-shard bounding argument including the rollover date:
      `rg -c '2026-08-01' <artifact>` >= 1.
    - The artifact records that the manual prune is operational and not code:
      `rg -c 'not code' <artifact>` >= 1.
    - The artifact records the empty-label observation, which OBS-03 depends on:
      `rg -c 'label' <artifact>` >= 1.
    - `git status --porcelain packages/ .github/ start-cache-server/` prints NOTHING.
  </acceptance_criteria>

  <verify>
    <automated>rg -c "RETAIN-05\(a\) shard census" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md && rg -c "cache-mirror-202607" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md && rg -c "accepted dead weight" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md && rg -c "2026-08-01" .planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md</automated>
  </verify>

  <done>
    RETAIN-05(a) is a recorded DECISION with a measured, shard-attributed count and a stated
    bounding argument -- not an open question and not a bare number.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workstation -> Releases mirror (anonymous read) | Task 1 reads a public release asset with a locally resolved token; a read fault degrades to a MISS by design |
| local workstation -> Actions cache | NOT crossed by this plan: the off-CI branch selects the Releases reader, and the soundness controls prove which branch was taken |
| `gh api` output -> committed artifact | measured values cross into a PUBLIC repo's tracked planning tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Information disclosure | the committed `10-EVIDENCE-PRE-RENAME.md` | medium | mitigate | Extract counts and names only; never paste raw `gh api` payloads, which carry committer identity fields. Never record a resolved token value -- length only, per the prior record's own discipline |
| T-10-11 | Repudiation | a recorded baseline that was not observed | high | mitigate | Acceptance requires the verbatim Nx outcome line. A MISS is recorded as a MISS and flagged as an OPEN BLOCKER for XOS-02; the prior record is cited as a corroborating prior, never substituted for the fresh capture |
| T-10-08 | Denial of service | 1000-asset per-shard Release cap | low | accept | Unchanged by this plan. Cap degrades to skip-and-warn, never a hard failure; the measured occupancy plus the 2026-08-01 rollover is the bounding argument, and it is recorded rather than acted on |
| T-10-SC | Tampering | npm / pip / cargo installs | low | accept | ZERO new packages in this phase (RESEARCH A-6). This plan runs no installer and edits no `package.json` or lockfile, so there is no supply-chain delta to gate |
</threat_model>

<verification>
- `rg` (never `git grep`) confirms every required heading and value in the new artifact.
- `git status --porcelain packages/ .github/ start-cache-server/` prints nothing.
- The artifact's D-25 section carries a verbatim command transcript, not a summary.
</verification>

<success_criteria>
- Both perishable measurements are on disk and committed BEFORE any plan in wave 2 runs.
- The artifact is readable as evidence by Phase 11 without re-deriving anything.
- No source file, workflow file, or bundle changed.
</success_criteria>

<output>
Create `.planning/phases/10-os-invariant-releases-mirror/10-01-SUMMARY.md` when done.
</output>
