---
phase: 10-os-invariant-releases-mirror
plan: 07
type: execute
wave: 6
depends_on: ["10-06"]
files_modified:
  - packages/github-cache/src/lib/release-asset-name.ts
  - packages/github-cache/src/lib/release-asset-name.spec.ts
  - packages/github-cache/src/lib/cache-key.ts
  - packages/github-cache/src/lib/dogfood-body.ts
  - packages/github-cache/src/backend/releases-backend.ts
  - packages/github-cache/src/backend/releases-backend.spec.ts
  - packages/github-cache/src/cleanup/cleanup.ts
  - packages/github-cache/src/publish/publish-mirror.ts
  - packages/github-cache/src/lint-rules.spec.ts
  - packages/github-cache/src/test/workspace-root-cwd.ts
  - packages/github-cache/src/docs-same-os-claims.spec.ts
  - .github/workflows/ci.yml
  - start-cache-server/index.js
autonomous: true
requirements: [CORR-02, CORR-05, RETAIN-04, RETAIN-05]
must_haves:
  truths:
    - "D-03: `releaseAssetName(hash)` returns `nx-cache-<hash>` -- one asset name per hash, no
      OS component -- with the prefix IMPORTED from its single source and composed locally,
      never re-authored and deliberately NOT aliased to `cacheKeyFor`."
    - "D-04: the `platform` parameter is DELETED, not defaulted away -- that deletion is what
      removes two of the three CORR-05 sites -- while `cachePlatform` and `CACHE_OS_VALUES`
      both SURVIVE with intentionally-kept annotations naming their live consumers."
    - "D-06: the cleanup accept filter admits BOTH the new name and the legacy OS-suffixed
      names, through TWO separately named branch predicates composed in one exported function,
      in the SAME COMMIT as the rename."
    - "D-07: the two branches are asserted mutually exclusive DIRECTLY, over an adversarial
      table, and the MECHANISM is asserted too -- disjointness is a property of the PREFIX, not
      of the last-dash split."
    - "D-05: `CACHE_KEY_PREFIX` is pinned by EXTENDING the existing `cache-key.spec.ts`
      authored-count guard rather than by a new mechanism, and its comment lock is widened to
      name FOUR distinct governed consumers plus the orphaning consequence."
    - "D-17: site 4's unit-spec original leaves in THIS commit together with its
      `eslint-disable-next-line` directive and its `CORR_05_SITES` row -- the DELETE half of
      the two-commit move whose ADD half landed in plan 10-06."
    - CORR-05 is TRUE: zero extant ambient-platform reads in unit specs, and the violation
      table is empty under a positive assertion rather than an enumeration that emits nothing.
    - The committed action bundle matches a fresh build, with the diff CHECKED rather than
      assumed.
  artifacts:
    - packages/github-cache/src/lib/release-asset-name.ts (the rewritten helper and the
      two-branch filter)
    - start-cache-server/index.js (rebuilt and staged in THIS commit)
  key_links:
    - `CACHE_KEY_PREFIX` -> four distinct consumers: the Actions-cache key builder, the
      Actions-cache enumeration filter, the Release asset name, and the cleanup accept filter's
      new branch. Four consumers of ONE prefix, deliberately NOT two aliases of one builder.
    - `releaseAssetName` -> the `serve()` graph -> `start-cache-server/index.js`. This is the
      ONLY edit in the whole phase that reaches the committed bundle, which is why the entire
      ROBUST-04 obligation lands here.
    - `lint-rules.spec.ts`'s `CORR_05_SITES` rows -> their sites -> their
      `eslint-disable-next-line` directives. All three triples leave together or the build
      breaks in two different ways.
  prohibitions:
    - THIS PLAN PRODUCES EXACTLY ONE COMMIT. Tasks 1 and 2 STAGE ONLY -- they must not commit.
      Task 3 performs the single commit. Five independent same-commit rules converge here and
      every one of them is violated by an intermediate commit.
    - MUST run in the MAIN TREE. `npm run check:action` is only meaningful there: a junctioned
      `node_modules` in a git worktree made esbuild rewrite 689 lines with NO source edit at
      all, which is the INVERSE fault with an identical symptom. If this plan is dispatched
      into a worktree, STOP and either re-run in the main tree or explicitly DEFER the bundle
      verdict and say so in the SUMMARY -- never stage a bundle whose diff was produced under a
      junction.
    - MUST NOT alias `releaseAssetName` to `cacheKeyFor`, even though the two return the
      identical string today. Comment-lock the deliberate separation: aliasing would silently
      couple the Actions-cache KEY namespace to the Release ASSET namespace, so a later change
      to either would move both.
    - MUST NOT alias the new-name branch predicate to `isServerProducedKey`, even though its
      body is identical in shape. Same reason, and the comment must say so, because "these two
      are the same, tidy them together" is exactly what a future reader will propose.
    - MUST NOT default the `platform` parameter away. DELETE it. That deletion is what removes
      two of the three CORR-05 sites.
    - MUST NOT delete `cachePlatform` or `CACHE_OS_VALUES`. Both SURVIVE with
      intentionally-kept annotations: the label needs `cachePlatform` and the legacy filter
      branch needs `CACHE_OS_VALUES`. `fallow dead-code --fail-on-issues` gates the whole repo
      in CI, so an unexplained export is a build failure, not a style note.
    - MUST NOT add a third accept branch for the PoC-era `<hash>.tar.gz` family. That widens a
      DELETE filter which was deliberately narrowed on security grounds, and the disposition is
      already recorded as accepted dead weight with a measured count.
    - MUST NOT infer branch disjointness from the last-dash split. Assert it directly, over the
      adversarial table, AND assert the two structural reasons.
    - MUST NOT repoint the third `CORR_05_SITES` row at the new integration path -- delete it.
      At an exempt path the ban does not fire, so the row's caught-when-stripped test would
      return an empty rule list and redden on a CORRECT implementation.
    - MUST NOT delete the `CORR_05_SITES` HISTORICAL block. A removed miscount lock is
      indistinguishable from a miscount that never existed.
    - MUST NOT correct any claim by deletion alone. Every corrected comment in this commit gets
      a NEW reason on a different axis.
    - MUST NOT use `git grep` for any absence claim.
  human_needed:
    - "LIVE CI (push-to-`main` only): a default-branch push must republish the mirror under the
      new name before Phase 11's proofs can run -- the warm-mirror precondition. `publish` is
      `push`-gated to `main`, so no PR run reaches it. A real runner must show: the ubuntu
      `publish` leg's OBS-01 summary reporting a nonzero `mirrored` count with `readMisses` at 0
      and NO all-restore-MISS warning (predicted: every name is new, so `mirrored` is roughly
      the full restorable set); and a post-push shard census showing `nx-cache-*` names present
      and legacy OS-suffixed names no longer growing. There is deliberately NO pre-merge
      acceptance check for this. Note the ROADMAP's Live-CI close says to expect the first such
      push to publish NOTHING if it coincides with Phase 9's rotation window -- that window is
      SPENT (sampled on run `30400231720`), and CORR-02 rotates the ASSET NAME, a different
      mechanism from the cache VERSION the warning is about, so a full republish is the
      predicted signal. If the ubuntu leg instead reports `mirrored: 0` with `readMisses` equal
      to `scanned`, that prediction is FALSIFIED and must be recorded as such rather than
      explained away."
---

<objective>
Collapse the Releases namespace to one asset name per hash, and land every change that
collapse forces in the same commit.

Purpose: CORR-02, plus RETAIN-04 and RETAIN-05 which cannot lag it. A publisher writing the new
name against an unextended filter silently stops pruning. A CORR-05 site whose
`eslint-disable-next-line` outlives it fails the build under
`reportUnusedDisableDirectives: 'error'`. A `CORR_05_SITES` row whose site is gone fails
`lineIndexOf`. `releaseAssetName` is inside the `serve()` graph, so ROBUST-04 binds the bundle
rebuild here too. And D-20's byte-identity justification changes its REASON in the same breath.
Five rules, one commit. This is explicitly NOT a phase split.

Output: the OS-free name, the two-branch filter with its disjointness MECHANISM asserted, the
widened prefix lock, three deleted violation sites with their directives and rows, the corrected
prose sweep, and a rebuilt bundle whose diff was predicted and then checked.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/THREAT-MODEL.md
@.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md
@.planning/phases/10-os-invariant-releases-mirror/10-PATTERNS.md
@.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md
@.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md
@packages/github-cache/src/lib/release-asset-name.ts
@packages/github-cache/src/lib/cache-key.ts
</context>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / signature | Location |
|------|--------------------|----------|
| CHANGED signature | `releaseAssetName(hash: Hash): string` -- the `platform` parameter is DELETED | `lib/release-asset-name.ts` |
| new export | `isCurrentAssetName(name: string): boolean` -- the new-name branch | `lib/release-asset-name.ts` |
| new export | `isLegacyOsSuffixedAssetName(name: string): boolean` -- today's body, preserved verbatim under a new name | `lib/release-asset-name.ts` |
| new import edge | `CACHE_KEY_PREFIX` added to the existing `./cache-key.js` import | `lib/release-asset-name.ts` |
| new spec describe | RETAIN-05(b): the three structural atoms plus the adversarial disjointness table with the both-branch count pinned to 0 | `release-asset-name.spec.ts` |
| new drift row | the shard-growth estimate's THIRD correction | `DOCS_08_SITES` in `docs-same-os-claims.spec.ts` |
| widened constant | `EDITED_FILES` gains `packages/github-cache/src/lib/release-asset-name.integration.spec.ts` (created by plan 10-06, so it now exists) | `docs-same-os-claims.spec.ts` |
| regenerated artifact | `start-cache-server/index.js` -- rebuilt and staged in THIS commit | repo root |

**Both new predicates are EXPORTED, deliberately.** RETAIN-05(b) requires the branches asserted
mutually exclusive DIRECTLY, which needs two named subjects; asserting through the composed
export instead would be strictly weaker. There is no in-repo precedent for a module-private
predicate under test, so this is a recorded decision rather than a default. The surface cost is
two names on an INTERNAL module -- `public-surface.spec.ts` structurally excludes internal
module exports -- and `fallow dead-code` credits exports reached from test imports, so the spec
import is what keeps them reachable.

## Predicted bundle diff -- predict it, then CHECK it

Measured at research time: `start-cache-server/index.js` contains `cachePlatform` (definition
plus one call) and `releaseAssetName` (definition plus one call from the reader), and does NOT
contain `CACHE_OS_VALUES` or `isServerProducedAssetName`. That absence PROVES esbuild
tree-shakes unused exports from this very module. So:

- `releaseAssetName`'s one-line body changes.
- `cachePlatform` is REMOVED (~10 lines): after the rewrite nothing in the `serve()` graph calls
  it. Predict this so a reviewer does not read a legitimate removal as collateral damage.
- The two new predicates do NOT appear: nothing in the `serve()` graph reaches them.
- Roughly a dozen changed lines, and ZERO `__commonJS` registrations changed -- the removed
  symbol claims no common base name, unlike the rename cascade that produced 426 lines of pure
  renaming in Phase 9.
- `cache-key.ts` is in the bundle but comments do not survive bundling, so its comment-only
  edit contributes ZERO lines. Run the check anyway; do not skip it on that reasoning.

Anything materially larger than that -- hundreds of lines, changed `__commonJS` keys, changed
path comments -- is a SIGNAL, not noise. The two known causes are a source edit that reached the
`serve()` graph unexpectedly, and a build run in a worktree with a junctioned `node_modules`.
Diagnose before staging.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The OS-free name, the two-branch filter, and the widened prefix lock (STAGE ONLY)</name>

  <files>
    packages/github-cache/src/lib/release-asset-name.ts,
    packages/github-cache/src/lib/release-asset-name.spec.ts,
    packages/github-cache/src/lib/cache-key.ts
  </files>

  <read_first>
    - `packages/github-cache/src/lib/release-asset-name.ts` IN FULL -- the `CACHE_OS_VALUES`
      tuple and its doc block, `CacheOs`, `cachePlatform` and its doc block, the
      `releaseAssetName` doc block (whose OS-namespacing claim becomes FALSE and which is
      executable-adjacent prose of the class that shipped the Phase 9 regression), and
      `isServerProducedAssetName`'s body including the readonly-tuple cast on the
      membership check.
    - `packages/github-cache/src/lib/cache-key.ts` IN FULL -- the file-level lock, the prefix,
      `HASH_PATTERN`, the `Hash` brand, `parseHash`, `cacheKeyFor`, and `isServerProducedKey`
      with its doc block naming the REJECTED weaker version.
    - `packages/github-cache/src/lib/cache-key.spec.ts` -- the authored-count `files` map plan
      10-06 extended, and the accept/reject pin on `isServerProducedKey`.
    - `packages/github-cache/src/lib/release-asset-name.spec.ts` -- every case, including the
      new post-rename describe plan 10-06 added, both described disables, and the existing
      accept/reject sets to extend.
    - `.planning/phases/10-os-invariant-releases-mirror/10-PATTERNS.md` Tier 2 item 6 and
      shared pattern S-1.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `### CORR-02`,
      `### RETAIN-04` (including the 26-row measured disjointness table, which is the table to
      author, and the two independent MECHANISM reasons), and `### RETAIN-05`.
    - `.planning/REQUIREMENTS.md` CORR-02, RETAIN-04, RETAIN-05.
  </read_first>

  <behavior>
    - `releaseAssetName('abc123' as Hash)` is exactly `'nx-cache-abc123'`, and the function takes
      ONE parameter. Pin the arity structurally too: `releaseAssetName.length` is 1, which is
      what catches a `platform` parameter creeping back with a default (`Function.length` counts
      parameters before the first default, the established precedent in this package).
    - `isCurrentAssetName` accepts the prefix followed by a valid hash and rejects the bare
      prefix, a non-hex suffix, an over-length suffix, and an OS-suffixed name.
    - `isLegacyOsSuffixedAssetName` accepts every name today's filter accepted -- the existing
      accept set passes UNCHANGED, which is what proves the widening is purely additive.
    - `isServerProducedAssetName` accepts the union and nothing else.
    - RETAIN-05(b) MECHANISM atoms: `HASH_PATTERN` does not match a lone dash;
      `CACHE_KEY_PREFIX` CONTAINS a dash; and that dash is its LAST character. Those three
      facts are the proof that for any name branch A accepts, branch B's hash half is exactly
      the prefix minus its trailing dash -- which itself contains a dash and can never match
      `HASH_PATTERN`. Disjointness is therefore a property of the PREFIX, not of the split.
    - RETAIN-05(b) TABLE: for every row of the adversarial table, NOT both branches are true.
      Pin the both-true COUNT across the whole table to 0 -- a count, not a per-row assertion
      only, so a row silently dropped from the table cannot reduce coverage unnoticed. Include
      the hybrids a reader will worry about and the cases the research measured: the new name,
      the new name with each OS appended, a bare legacy name, the minimum-length legacy name,
      a bare hash, the bare prefix, a doubled prefix, a lone OS suffix, a trailing dash, an
      unknown OS, uppercase variants, both `.tar.gz` shapes, the maximum and over-maximum hash
      lengths, a doubled OS suffix, the shipped `cafe`-marker seed under the new name, this
      phase's own seed under the new name, a prefix near-miss, and an unmistakably foreign name.
  </behavior>

  <action>
    Rewrite `releaseAssetName` to take ONE parameter and return the imported prefix followed by
    the hash. Add `CACHE_KEY_PREFIX` to the EXISTING `./cache-key.js` import statement.

    Rewrite its doc block. The old text asserts OS-namespacing and that a read on platform P can
    only resolve an asset produced under platform P -- both FALSE now. Supply the replacement:
    ONE asset name per hash, the prefix single-sourced, and attribution moved to Release
    metadata. Keep the three clauses the house lock always carries -- what it governs, the
    FAILURE MODE (a silent MISS, not a crash), and the tidy-up a future reader will propose.
    Name that tidy-up explicitly: folding this into `cacheKeyFor`, which returns the identical
    string today. State why not: the requirement single-sources the PREFIX across FOUR distinct
    consumers, not two aliases of one builder, and aliasing would couple the Actions-cache key
    namespace to the Release asset namespace so a later change to either would move both.

    Annotate `cachePlatform` AND `CACHE_OS_VALUES` as intentionally kept, each with its live
    consumer named -- the label for `cachePlatform`, the legacy filter branch for
    `CACHE_OS_VALUES`. `fallow dead-code --fail-on-issues` is a real CI gate, so an unexplained
    survivor is a build failure. The requirement names only the tuple; the mapper needs the same
    annotation for the same reason, and nothing in the requirements says so.

    Split the filter into two separately named, EXPORTED module functions composed in one
    exported `isServerProducedAssetName`:
    - the new-name branch: starts with the prefix AND the post-prefix remainder matches
      `HASH_PATTERN`. Two conditions in a single return, both derived from single-sourced
      constants -- never a regex literal re-authoring the prefix or the hex class.
    - the legacy branch: today's body, preserved VERBATIM including the readonly-tuple cast on
      the membership check and the `separator < 0` early return. That early return is one of the
      two independent disjointness reasons, so it is load-bearing, not incidental.
    Each branch's doc block leads with its requirement/control ID, names what it is NOT (the
    house convention, copying the sibling filter's doc block that names its rejected weaker
    version), and the new-name branch additionally states why it is NOT aliased to the
    Actions-cache-side filter despite being identical in shape. Comment-lock the disjointness
    MECHANISM on the branch pair: both reasons, and the measured result (0 collisions over a
    26-case adversarial table plus 1.6M randomised candidates).

    Extend `cache-key.ts`'s comment lock to name all FOUR governed consumers -- the
    Actions-cache key builder, the Actions-cache enumeration filter, the Release asset name, and
    the cleanup accept filter's new branch -- PLUS the consequence: changing the literal orphans
    the entire mirror, and the legacy filter branch does NOT cover the orphans because it only
    knows the OS-suffixed shape.

    In `release-asset-name.spec.ts`: DELETE the two CORR-05 sites in this file together with
    their `eslint-disable-next-line` directives (the default-argument case for the name, whose
    parameter no longer exists, and the default-argument case for the platform mapper, which
    plan 10-06 already moved to the integration spec). Re-author the remaining two-argument
    calls in the `releaseAssetName (CORR-01)` describe to the one-argument form -- four whole
    cases plus the round-trip case must be re-authored rather than edited, because their claims
    change. Add the arity pin. Extend the accept/reject sets so both name families are covered.
    Add the RETAIN-05(b) describe with the three atoms and the table.

    STAGE these three files with `git add <path>` by name. DO NOT COMMIT. `test` and `lint` will
    be RED until task 2 lands the rest of the fallout -- that is expected and is exactly why
    the five same-commit rules exist. Never `git add .`, `-A` or `-u`.
  </action>

  <acceptance_criteria>
    - `releaseAssetName` takes one parameter:
      `rg -n 'export function releaseAssetName' -A2 packages/github-cache/src/lib/release-asset-name.ts`
      shows a single `hash: Hash` parameter, and the spec's arity pin asserts
      `releaseAssetName.length` is 1.
    - The prefix is IMPORTED, not authored:
      `rg -c "'nx-cache-'" packages/github-cache/src/lib/release-asset-name.ts` returns 0, and
      `rg -c 'CACHE_KEY_PREFIX' packages/github-cache/src/lib/release-asset-name.ts` >= 1.
    - Both branch predicates are exported and both are annotated:
      `rg -c '^export function is' packages/github-cache/src/lib/release-asset-name.ts` returns 3
      (the two branches plus the composed export).
    - `cachePlatform` and `CACHE_OS_VALUES` still exist and each carries an intentionally-kept
      annotation naming its live consumer. Verify by reading both doc blocks.
    - The stale two-argument signature is gone from this file's code AND its prose. Build the
      needle with a single-character character class so this criterion does not itself plant the
      string it forbids -- `rg -c 'releaseAssetName\(hash, platfor[m]'
      packages/github-cache/src/lib/release-asset-name.ts` returns 0. The bracket is
      load-bearing, not style; do not "tidy" it.
    - `cache-key.ts`'s lock names four consumers and the orphaning consequence. Assert on
      CONTENT by reading it; then `rg -c 'orphan' packages/github-cache/src/lib/cache-key.ts` >= 1.
    - The RETAIN-05(b) describe exists with a pinned both-true count of 0 and at least 26 table
      rows. Count the rows and state the number in the SUMMARY.
    - The three structural atoms are asserted:
      `rg -c 'HASH_PATTERN' packages/github-cache/src/lib/release-asset-name.spec.ts` >= 1 and
      the describe contains an assertion that the prefix contains a dash and one that the dash
      is its last character.
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache -- release-asset-name cache-key`
      passes for these two files' own suites. Other suites are expected RED until task 2.
    - NO COMMIT was made: `git log --oneline -1` is unchanged from the start of this task.
    - Files were staged BY NAME: the command history shows `git add <explicit path>`, never
      `git add .`, `git add -A` or `git add -u`.
    - MUTATION CHECKS, predicted first and recorded in the SUMMARY:
      1. Drop the prefix from `releaseAssetName` -> the pinned-literal cases redden; the
         reader-derivation EQUALITY clause stays GREEN because it rebuilds from the helper.
         That contrast is exactly why the hand-authored literal is required.
      2. Re-add the OS token to `releaseAssetName` -> the no-platform-token clause reddens
         while the exactly-one and equality clauses stay GREEN.
      3. Make the composed filter branch A only -> the legacy accept cases redden; the
         disjointness table stays GREEN.
      4. Make the composed filter A AND B instead of A OR B -> every accept case reddens.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- release-asset-name cache-key</automated>
  </verify>

  <done>
    One asset name per hash, a two-branch filter whose disjointness is asserted as a property of
    the design, and a prefix lock that names all four things it governs. Staged, not committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The consequent deletions and the corrected justifications (STAGE ONLY)</name>

  <files>
    packages/github-cache/src/backend/releases-backend.ts,
    packages/github-cache/src/backend/releases-backend.spec.ts,
    packages/github-cache/src/lint-rules.spec.ts,
    packages/github-cache/src/cleanup/cleanup.ts,
    packages/github-cache/src/publish/publish-mirror.ts
  </files>

  <read_first>
    - `packages/github-cache/src/backend/releases-backend.spec.ts` -- the `OTHER_PLATFORM`
      constant with its described disable, the cross-OS round-trip describe including the MISS
      case CORR-02 destroys on purpose, the name-derivation describe plan 10-06 extended, and
      the two remaining two-argument helper calls.
    - `packages/github-cache/src/backend/releases-backend.ts` -- its doc blocks. Two claims go
      stale: that the injected read seam sits at an OS-namespaced asset NAME, and that `get`
      resolves the RUNNING PLATFORM's asset. Locate both by content.
    - `packages/github-cache/src/lint-rules.spec.ts` -- the whole `CORR_05_SITES` block: its
      REMOVAL SCHEDULE, its count statement, its HISTORICAL block with the two recorded
      miscounts, the three rows, `lineIndexOf` and its failure message, the enumeration
      describe, the positive assertion plan 10-06 added, and `EVASION_SHAPES` with its drivers.
    - `packages/github-cache/src/cleanup/cleanup.ts` -- the single `isServerProducedAssetName`
      call site and its comment, which names only the OS-suffixed shape and is the only prose a
      cleanup reader sees.
    - `packages/github-cache/src/publish/publish-mirror.ts` -- EVERY occurrence of the
      byte-identity justification. There are TWO, not one: the engine's doc block and the
      first-write-wins branch comment inside the loop. RESEARCH named only the first. Locate
      both by the quoted phrase, never by a line number.
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-18, D-20.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` C-2, C-3, C-5, and
      `### TRUST-11`'s statement of what byte-identity now rests on.
  </read_first>

  <action>
    `releases-backend.spec.ts`:
    - DELETE the `OTHER_PLATFORM` constant together with its `eslint-disable-next-line`
      directive. It is defined as the OS this process is not, and once the name has no platform
      the concept has no meaning.
    - DELETE the cross-OS MISS case that asserts a miss for a hash present only under another
      platform. CORR-02 destroys it ON PURPOSE: with no platform in the name, "here" and "there"
      are the same name and the case is incoherent. Its named REPLACEMENT is already in the
      name-derivation describe (plan 10-06 authored the third clause), so coverage does not
      drop. Record the trade in a comment at the deletion site: what was removed, why it became
      incoherent, and where the replacement lives.
    - Re-author the remaining two-argument helper calls to the one-argument form. Where a case's
      claim depended on two platforms differing, re-author the case rather than editing it.
    - Do NOT add a redundant "exactly one" guard: the existing array-equality assertion already
      pins the length, and three guards for one invariant can share one blind spot.

    `releases-backend.ts`: correct the two stale doc-block claims, each with a supplied
    replacement reason. The read seam now sits at an OS-INVARIANT asset name -- one name per
    hash -- and `get` resolves THAT name rather than the running platform's. Say what the seam
    now proves, so the sentence still earns its place.

    `lint-rules.spec.ts`:
    - DELETE all three `CORR_05_SITES` rows. Each row's site left in this commit (two in task 1,
      one moved by plan 10-06 whose original leaves here), so a row left behind fails
      `lineIndexOf` and a directive left behind fails
      `reportUnusedDisableDirectives: 'error'`. Two different failure modes, one commit.
    - DELETE the now-dead enumeration loop over the empty table. An enumeration over an empty
      array emits ZERO tests, silently -- a coverage cliff. The positive assertion plan 10-06
      added replaces it and goes GREEN here.
    - PRESERVE the HISTORICAL block. Update the count statement to record that the table is now
      EMPTY and that CORR-05 is TRUE, and add that the RULE's non-vacuity did not live in this
      table: `EVASION_SHAPES` proves the ban fires on all six evasion shapes at a unit path and
      is exempt at an integration path, and it survives untouched. Without that sentence a
      reader finds an empty table and concludes the rule is unproven.

    `cleanup.ts`: widen the filter call site's comment to name BOTH families and to say the
    widening is ADDITIVE -- every previously-accepted name is still accepted. Keep the existing
    discipline clauses verbatim in force: a foreign asset dropped into a genuine shard is never
    deleted as ours, and the check is the first statement in the loop so the scanned count means
    what it says.

    `publish-mirror.ts`: rewrite BOTH byte-identity justifications. Byte-identity SURVIVES; its
    REASON changes. Old reason: OS-namespacing. New reason: for a given hash the Actions cache
    holds exactly ONE entry, and both legs restore it and upload it VERBATIM without
    re-executing the task -- so the uploaded bytes are byte-identical regardless of which leg
    uploads. Key the edits on the quoted phrase, never on a line number: this commit's edits
    shift each other's lines and `ci.yml` drifted ~220 lines inside one milestone.

    STAGE these five files by name. DO NOT COMMIT.
  </action>

  <acceptance_criteria>
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache` passes IN FULL. This is the moment
      the wave goes GREEN: every RED plan 10-06 authored is now satisfied. Enumerate in the
      SUMMARY each RED from 10-06 and confirm it is green.
    - `npm run lint` passes. This proves no `eslint-disable` directive outlived its violation --
      the LINT-06 mechanism working as designed.
    - `npm run typecheck` passes.
    - `CORR_05_SITES` is empty and the positive assertion is green:
      `rg -c 'CORR_05_SITES' packages/github-cache/src/lint-rules.spec.ts` >= 2 (the declaration
      and the assertion), and the declaration's array literal has no entries. Read it.
    - The HISTORICAL block survives:
      `rg -c 'HISTORICAL' packages/github-cache/src/lint-rules.spec.ts` >= 1.
    - The `EVASION_SHAPES` proof is untouched:
      `git diff -- packages/github-cache/src/lint-rules.spec.ts` shows no change inside the
      `EVASION_SHAPES` array or its drivers.
    - Zero `eslint-disable-next-line no-restricted-syntax` directives remain in any UNIT spec
      that this phase was responsible for. Enumerate the surviving directives repo-wide with
      `rg -n 'eslint-disable-next-line no-restricted-syntax' packages/github-cache/src` and
      confirm in the SUMMARY that each survivor belongs to a different requirement, not to
      CORR-05. Use `rg`, not `git grep`.
    - BOTH byte-identity justifications were rewritten:
      `rg -c 'byte-identical' packages/github-cache/src/publish/publish-mirror.ts` >= 2, and
      reading both sites confirms each now states the one-entry-per-hash reason. A rewrite that
      caught only the doc block and missed the in-loop comment is the exact
      documentation-scoped-sweep failure Phase 9 shipped.
    - The cleanup call-site comment names both families -- read it.
    - `releases-backend.ts`'s two corrected claims each carry a replacement reason -- read both.
    - NO COMMIT was made yet: `git log --oneline -1` is unchanged.
    - Files staged BY NAME only.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>

  <done>
    Every site, directive and row that had to leave with the rename has left; every corrected
    claim carries a new reason; and the whole unit suite plus `lint` and `typecheck` are green
    with nothing yet committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: The prose sweep, the bundle, and THE SINGLE COMMIT</name>

  <files>
    packages/github-cache/src/lib/dogfood-body.ts,
    packages/github-cache/src/test/workspace-root-cwd.ts,
    packages/github-cache/src/docs-same-os-claims.spec.ts,
    .github/workflows/ci.yml,
    start-cache-server/index.js
  </files>

  <read_first>
    - `packages/github-cache/src/lib/dogfood-body.ts` -- its doc block's CONTRAST case, which
      cites the sibling helper's two-argument defaulted signature as the reason its own
      parameter has no default. Task 1 deleted that signature, so the contrast is gone.
    - `packages/github-cache/src/test/workspace-root-cwd.ts` -- its LOCK 2 prose, which cites
      the same two-argument signature as an example of the injection convention.
    - `.github/workflows/ci.yml`, the `publish` job -- the shard-growth estimate. Locate it by
      the per-push distinct-task-hash phrasing and by the doubled per-push asset figure Phase 9
      authored. This is the estimate's THIRD correction and the first DOWNWARD one.
    - `packages/github-cache/src/docs-same-os-claims.spec.ts` -- the row shape, the self-evading
      `forbidden` regex technique, and `EDITED_FILES`.
    - `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md` -- the census,
      for the corrected estimate's arithmetic.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `## Sequencing hazards`
      H-3 (the per-commit bundle obligation table and the predicted diff) and H-4 (why the check
      is main-tree-only), plus Pitfall 5.
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-02(a), D-21, D-27.
    - `AGENTS.md` -- the worktree decision rule, to confirm this plan is running in the main
      tree before the bundle step.
  </read_first>

  <action>
    First, CONFIRM THE TREE. Run `git rev-parse --show-toplevel` and `git worktree list`, and
    confirm this working directory is the MAIN tree and that `node_modules` is a real directory
    rather than a junction or symlink into another tree. If it is not the main tree, STOP the
    bundle step, complete the prose edits, and record in the SUMMARY that the bundle verdict is
    DEFERRED to a main-tree run -- do not stage a bundle produced under a junction. Phase 9
    measured a junction causing esbuild to rewrite 689 lines with NO source edit, which is the
    inverse of the documented drift rule with an identical symptom.

    Prose sweep. Correct each, supplying a replacement reason rather than deleting:
    - `dogfood-body.ts`: its no-default rule was justified BY CONTRAST with the sibling's
      defaulted platform parameter. That contrast is gone. Supply a replacement justification
      that stands on its own: a default would let the verify leg silently compare against its
      OWN OS, and that silent self-comparison is the vacuity trap the rule exists to close, so
      the ABSENCE of the default is the control. Keep the structural `Function.length` pin
      reference. Do not restate the deleted signature.
    - `test/workspace-root-cwd.ts`: its LOCK 2 prose cites the same deleted signature as an
      example of the injection convention. Replace the example with a live one -- the backend
      selector's env bag is the obvious candidate -- and do not restate the deleted signature.
      This is precisely the stale-prose class the Phase 9 regression came from.
    - `ci.yml` shard-growth estimate: correct it a THIRD time, DOWNWARD. Under one name per
      hash each restorable hash is mirrored ONCE, not once per leg, so the per-push real-asset
      figure returns to roughly the distinct-task-hash count and the pushes-per-month-shard
      figure roughly doubles back. Recompute both numbers from the estimate's own stated inputs
      and show the arithmetic. Record that this is the THIRD correction and name both prior
      ones -- upward in Phase 9 because each leg mirrored every restorable hash under its own
      suffix, downward here because the suffix is gone -- so a fourth reader does not re-derive
      the history. Also record that the Windows leg still mirrors exactly ONE real asset, its
      own seed, which is what keeps the per-leg counts non-equal.
    - `docs-same-os-claims.spec.ts`: add a `DOCS_08_SITES` row for the corrected estimate,
      requiring both the corrected figures and the third-correction record. Add
      `packages/github-cache/src/lib/release-asset-name.integration.spec.ts` to `EDITED_FILES`
      (plan 10-06 created it, so `readFileSync` resolves). Any new `forbidden` regex uses the
      single-character character-class technique with a note explaining the contortion.

    Then the bundle. In this order:
    1. `npm run build:action`.
    2. `git diff --stat -- start-cache-server/index.js` and `git diff -- start-cache-server/index.js`.
       COMPARE against the prediction recorded in this plan's "Predicted bundle diff" section:
       the helper's one-line body changed, the platform mapper removed, no new symbols, roughly
       a dozen lines, zero `__commonJS` registrations changed. Write the OBSERVED shape into the
       SUMMARY beside the prediction. Never assume the diff -- Phase 9 measured a lockfile
       re-resolution drifting it 88 lines with no source edit.
    3. If the observed diff is materially larger or touches `__commonJS` keys or path comments,
       STOP and diagnose. Do not stage it.
    4. `git add start-cache-server/index.js`.
    5. `npm run check:action` -- it rebuilds and then diffs against the index, so it passes once
       the rebuilt bundle is staged.

    Then run the FULL battery: `npm run test`, `npm run typecheck`, `npm run lint`,
    `npm run integration`, `npm run fallow:ci`. `fallow:ci` matters specifically here: two new
    exports reached only from a spec import, plus two intentionally-kept survivors, are exactly
    the shapes `fallow dead-code --fail-on-issues` reports.

    Finally, THE SINGLE COMMIT. Stage every remaining file BY NAME -- never `git add .`, `-A` or
    `-u`. Write the commit message to a file and use `git commit -F <file>`: `git commit -m`
    fails with an EINVAL on `COMMIT_EDITMSG` on this Dev Drive. The message must enumerate what
    the same-commit rules forced together and why, so a reviewer reading the commit alone can
    see the constraint rather than infer a large commit was careless.
  </action>

  <acceptance_criteria>
    - The tree was CONFIRMED main before the bundle step, and the confirmation is recorded in
      the SUMMARY (`git worktree list` output and the `node_modules` link check).
    - `npm run build:action` ran, and the OBSERVED bundle diff is recorded in the SUMMARY beside
      this plan's PREDICTION, with any divergence named and explained. A SUMMARY that says the
      diff was as expected without stating the observed shape does not satisfy this.
    - `npm run check:action` passes AFTER the rebuilt bundle is staged.
    - `npm run test`, `npm run typecheck`, `npm run lint`, `npm run integration` and
      `npm run fallow:ci` ALL pass.
    - `npm run integration` output shows `release-asset-name.integration.spec.ts` being
      collected. A green run that never collected it proves nothing.
    - The corrected shard-growth estimate shows its arithmetic and records the third-correction
      history: `rg -c 'THIRD|third correction' .github/workflows/ci.yml` >= 1 within the located
      comment, and the comment names both prior corrections.
    - The estimate records that the Windows leg still mirrors exactly one real asset:
      read the comment and confirm.
    - Neither `dogfood-body.ts` nor `test/workspace-root-cwd.ts` restates the deleted
      two-argument signature. Build the needle with a single-character character class so this
      criterion does not plant what it forbids: `rg -c 'releaseAssetName\(hash, platfor[m]'
      packages/github-cache/src` returns 0. The bracket is load-bearing; do not tidy it.
    - Each corrected prose site carries a REPLACEMENT reason. Assert on the surviving CONTENT,
      never on the absence of the old text -- an absence check is satisfied by deleting the whole
      paragraph, which is the failure this instruction exists to prevent.
    - `EDITED_FILES` names the new integration spec and every path in it resolves on disk.
    - EXACTLY ONE COMMIT was created by this plan:
      `git rev-list --count <plan-start-sha>..HEAD` returns 1. State the plan-start SHA in the
      SUMMARY.
    - That commit contains ALL thirteen files this plan lists as modified:
      `git show --name-only HEAD` lists each. A missing file means a same-commit rule was
      broken; a file NOT in this plan's list means scope crept.
    - `start-cache-server/index.js` is IN that commit.
    - No `git add .`, `git add -A` or `git add -u` was used; the commit was made with
      `git commit -F <file>`.
    - The commit message enumerates the converging same-commit rules.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:action &amp;&amp; npm run test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run integration &amp;&amp; npm run fallow:ci</automated>
  </verify>

  <done>
    The prose no longer cites a signature that does not exist, the estimate reads correctly for
    the third time, the committed bundle matches a fresh main-tree build with a diff that was
    predicted and then checked, and the whole rename wave is exactly one commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release asset name -> cleanup DELETE path | the accept filter decides what may be deleted; this commit WIDENS it, which is the highest-attention change in the phase |
| `CACHE_KEY_PREFIX` -> four distinct consumers | one literal now governs the Actions-cache key, the enumeration filter, the asset name and the delete filter; changing it orphans the entire mirror |
| `serve()` graph -> the committed action bundle | four `ci.yml` sidecar jobs execute the bundle FROM THE GIT REF, not from a build output, so a stale bundle is a live divergence |
| Actions cache entry -> Release asset (anonymous read) | unchanged in exposure terms by this commit; only the naming collapses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | the widened cleanup accept filter (a validation widening on a DELETE path) | high | mitigate | Two separately named branches, both derived from single-sourced constants and never from a re-authored regex; the legacy branch preserved VERBATIM so the widening is provably additive; disjointness asserted DIRECTLY over an adversarial table with the both-true count pinned to 0, plus the two structural MECHANISM reasons; and a mixed-shard cleanup dry-run (authored in plan 10-06) proving a foreign asset is never deleted as ours. No third branch for the PoC-era family, because that shape is indistinguishable from a foreign asset dropped into a genuine shard |
| T-10-25 | Tampering | a stale `start-cache-server/index.js` after a `serve()`-reachable edit | high | mitigate | `npm run build:action` runs and the bundle is staged in THIS commit; the diff is PREDICTED in the plan and then CHECKED against the prediction, never assumed empty. The main-tree precondition is confirmed first, because a junctioned `node_modules` produced 689 rewritten lines with no source edit -- the inverse fault with an identical symptom. `action-bundle-drift` is the standing CI guard and runs on every PR and push |
| T-10-26 | Tampering | `CACHE_KEY_PREFIX` becoming quadruply load-bearing | high | mitigate | Pinned by the existing authored-occurrence guard, extended in plan 10-06 so the asset-name module is asserted to author it ZERO times, and comment-locked naming all four consumers plus the orphaning consequence -- including that the legacy filter branch does NOT cover the orphans |
| T-10-27 | Repudiation | attribution loss from removing the OS token from the asset name | medium | mitigate | Attribution was moved to Release `label` metadata in plan 10-02, which landed BEFORE this commit, so attribution never lapses. The label names the PUBLISHING leg, and that retraction is comment-locked and scanned repo-wide |
| T-10-05 | Information disclosure | the anonymously-readable Releases mirror | medium | accept | The exposure delta was created by Phase 9 (VER-01/VER-03), not by this commit: the bytes were already crossing. This commit removes the asset-name OS token, so the delta it introduces is an ATTRIBUTION change, not an EXPOSURE change. Bounded by the reframing that the Actions cache boundary is ref scope, not OS. Offered as INPUT to TRUST-12's classification, never as its conclusion |
| T-10-04 | Tampering | first-write-wins arbitrating between NON-identical payloads | medium | accept | NOT REACHABLE here. For a given hash the Actions cache holds exactly ONE entry and both publish legs restore it and upload it VERBATIM without re-executing the task, so the uploaded bytes are byte-identical -- which is why the byte-identity comments SURVIVE with a changed reason rather than being deleted. The real race appears only once a second producer exists on another OS, which moves the residual risk into a later phase's write decision. Offered as INPUT to TRUST-11's classification |
| T-10-16 | Repudiation | a guard that passes vacuously after the rename | high | mitigate | Four mutation checks in task 1, each with a predicted redness split; the destroyed non-vacuity proof has a named replacement authored one commit EARLIER; and the emptied violation table is replaced by a positive assertion so a silent zero-test enumeration cannot masquerade as coverage |
| T-10-SC | Tampering | npm / pip / cargo installs | low | accept | ZERO new packages. No `package.json` or lockfile edit -- which also means the bundle diff cannot be attributed to a dependency re-resolution, so any unexpected diff is a source or tree-shape signal |
</threat_model>

<verification>
- Full battery green: `test`, `typecheck`, `lint`, `integration`, `check:action`, `fallow:ci`.
- The main-tree precondition confirmed and recorded before the bundle step.
- The observed bundle diff recorded beside the prediction, with divergences explained.
- Exactly ONE commit, containing all thirteen listed files including the rebuilt bundle.
- Every RED authored by plan 10-06 enumerated and confirmed green.
- Every surviving `no-restricted-syntax` disable directive enumerated and attributed to a
  requirement other than CORR-05.
</verification>

<success_criteria>
- One asset name per hash, prunable in both families, with disjointness a property of the design
  rather than of a string split.
- CORR-05 is TRUE and says so positively, not by an enumeration that emits nothing.
- Nothing in the tree cites a signature that no longer exists.
- The rename is one commit, and the commit message explains why it had to be.
</success_criteria>

<output>
Create `.planning/phases/10-os-invariant-releases-mirror/10-07-SUMMARY.md` when done.
</output>
