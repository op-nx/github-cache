---
phase: 10-os-invariant-releases-mirror
plan: 08
type: execute
wave: 7
depends_on: ["10-07"]
files_modified:
  - packages/github-cache/src/action/index.ts
  - packages/github-cache/src/action/index.spec.ts
  - .planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md
  - .planning/phases/10-os-invariant-releases-mirror/10-SC6-NOTES.md
autonomous: true
requirements: [TRUST-10, TRUST-11, TRUST-12, TRUST-13]
must_haves:
  truths:
    - C1, C2 and C16's Actions-cache-side filter are proven UNCHANGED by a diff over this
      phase's commit range -- an assertion, not a sentence.
    - `listCacheEntries` is proven `ref`-scoped by a whole-argument-array spec assertion with
      the call count pinned, and comment-locked as the now-SOLE in-repo control keeping
      non-default-branch trusted writes out of the world-readable mirror.
    - TRUST-11 and TRUST-12 are recorded as QUESTIONS with a proposed classification explicitly
      labelled INPUT, with the corrected arbitration point and its code citations, and with the
      auditor's required reading given by path and by concrete commit range.
    - The two SC6 facts are written down with both of the research corrections applied, so
      v0.0.3 does not re-derive them and does not read D-26(a) as a consequence of the rename
      alone.
  artifacts:
    - .planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md
    - .planning/phases/10-os-invariant-releases-mirror/10-SC6-NOTES.md
  key_links:
    - `TRUSTED_EVENTS` includes `push` with NO ref check, and the OS-version barrier is gone --
      which is precisely why the `ref` scoping on the enumeration is now the sole in-repo
      control rather than one of two.
    - `cache-key.ts` DOES change in this phase (its comment lock widened), so C16's
      unchanged-ness must be asserted at FUNCTION scope: the file's diff reviewed to contain
      only comment lines, plus the existing accept/reject pin on the enumeration filter.
  prohibitions:
    - MUST NOT reach the TRUST-13 verdict here, or anywhere in a PLAN.md or SUMMARY.md. The
      classification is authored BY `gsd-security-auditor` in SECURITY.md. This plan supplies
      INPUT and says so in as many words.
    - MUST NOT let `/gsd:secure-phase` take its inline short-circuit. That workflow writes
      SECURITY.md directly when the threat register looks clean and was authored at plan time,
      which is exactly this phase's shape. TRUST-13 forbids self-certification in as many
      words. Spawn the auditor.
    - MUST NOT claim anywhere that the `mirrored-by` label answers which producer's bytes a
      reader received. The exact forbidden phrasing is pinned as the `retracted` regex in
      `packages/github-cache/src/docs-same-os-claims.spec.ts`; read it there and stay clear of
      it. It is deliberately not restated in this plan.
    - MUST NOT argue cross-OS safety from publish-leg ordering anywhere in either artifact.
      Cross-OS sharing rests on platform-agnosticism; ordering carries only a CI guard's
      SENSITIVITY, and that distinction is already comment-locked in two places.
    - MUST NOT record D-26(a) as "the Windows publish leg mirrors ZERO assets" unqualified.
      Both research corrections are mandatory: it is zero REAL TASK assets, and it becomes true
      only once the widened `needs:` lands, not from the rename alone.
    - MUST NOT spend `09-EVIDENCE.md`'s pre-Phase-9 producer-attribution snapshot. Phase 11's
      TEST-08 depends on it; cite it, never supersede it.
    - MUST NOT paste any `gh api` or `git log` output field that can carry a committer email
      address into either committed artifact.
    - MUST NOT use `git grep` for any absence claim -- both artifacts are UNTRACKED at first
      write.
  human_needed:
    - "The TRUST-13 classification itself: `/gsd:secure-phase 10` must spawn
      `gsd-security-auditor`, which authors `10-SECURITY.md`. The verdict is NOT reachable from
      this plan and must not be inferred from its `<threat_model>` block. Do not take
      `secure-phase`'s inline short-circuit even though this phase's register is authored at
      plan time and shows no open high-severity threat -- that short-circuit is precisely what
      TRUST-13 forbids."
    - "The three Live-CI-only items, consolidated in `10-SC6-NOTES.md` and each owned by an
      earlier plan: OBS-05's per-leg own-asset read-back with its `mirrored-by` label (plan
      10-05), the warm-mirror republish under the new name that gates Phase 11 (plan 10-07), and
      the post-widening full-task-set mirror (plan 10-03). All three require a real push to
      `main`; `publish` and `publish-verify` are `push`-gated, so no PR run samples them at any
      rate. No pre-merge acceptance check exists for any of them, deliberately."
---

<objective>
Prove the trust posture rather than assert it, and hand the two classification questions to an
independent auditor with everything it needs.

Purpose: TRUST-10 asks for C1, C2 and C16's enumeration-side filter verified rather than
assumed, and for the `ref` scoping pinned and comment-locked now that it is the only in-repo
control left. TRUST-11 and TRUST-12 ask for two facts RECORDED in the phase threat model.
TRUST-13 forbids self-certifying their classification. And SC6 asks for two facts written down
so the next milestone does not re-derive them -- one of which the research corrected in two
separate ways.

Output: the `ref` pin and its comment lock; `10-TRUST-EVIDENCE.md` carrying the
unchanged-by-diff record and the auditor hand-off; and `10-SC6-NOTES.md` carrying the two
recorded facts plus the consolidated Live-CI close register.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/THREAT-MODEL.md
@.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md
@.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md
@.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md
@packages/github-cache/src/action/index.ts
@packages/github-cache/src/lib/trust.ts
@packages/github-cache/src/lib/sync-gate.ts
</context>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / path | Location |
|------|---------------|----------|
| new spec case | the whole-argument-array `ref`-scoping pin on the enumeration adapter, with its call count pinned | `action/index.spec.ts` |
| widened comment lock | the `ref` scoping annotated as the now-SOLE in-repo control | `action/index.ts` |
| new file | `.planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md` | NEW |
| new file | `.planning/phases/10-os-invariant-releases-mirror/10-SC6-NOTES.md` | NEW |

## The phase base commit, resolved

`ff21b5f` -- `docs(roadmap): unwrap Requirements lines so gsd-tools stops dropping continuation
IDs`. That is the last commit before this phase's planning began, and it is the base for every
commit-range diff in this plan. Record it in `10-TRUST-EVIDENCE.md` with its subject line so a
later reader can confirm the range rather than trust it. If the executor finds a different
base (a rebase, an intervening merge), resolve it by locating the parent of this phase's first
`docs(10):` commit and record BOTH the expected and the observed base.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the ref scoping and lock it as the sole remaining control</name>

  <files>
    packages/github-cache/src/action/index.ts,
    packages/github-cache/src/action/index.spec.ts
  </files>

  <read_first>
    - `packages/github-cache/src/action/index.ts` -- `createPublishClient`, its `ref` parameter,
      the `listCacheEntries` adapter body with its scoping comment and its `octokit.paginate`
      call, and `runPublish`'s supply of `ref` from the environment. Locate the scoping comment
      by its content, not by a line number: the requirement cites one range and the context
      document cites another, and BOTH are stale -- the parameter, the comment and the call are
      three separate places.
    - `packages/github-cache/src/action/index.spec.ts` -- the existing `createPublishClient`
      test that drives `listCacheEntries` and asserts the keyless-row filter, which is the case
      to extend or sit beside.
    - `packages/github-cache/src/lib/trust.ts` -- the trusted-event allowlist. Read it and
      confirm first-hand that the allowlist includes the push event with NO ref check; that
      fact is the whole reason the enumeration's scoping is load-bearing.
    - `packages/github-cache/src/lib/sync-gate.ts` -- the in-process write gate, so the comment
      lock states accurately which control does what.
    - `.planning/THREAT-MODEL.md` -- the C1, C2, C9 and C16 rows.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `### TRUST-10` and C-7.
  </read_first>

  <behavior>
    - Given `createPublishClient(octokit, owner, repo, ref)`, calling `listCacheEntries()`
      invokes `octokit.paginate` EXACTLY ONCE, and the whole recorded argument array deep-equals
      the expected pair: the endpoint reference, and an options object whose `ref` equals the
      constructor's `ref` alongside `owner`, `repo` and the page size.
    - The call COUNT is pinned, so a second, unscoped enumeration cannot be added without
      reddening. A whole-array assertion alone would not catch an ADDITIONAL call.
    - Passing a different `ref` to the constructor changes the recorded `ref` -- so the value is
      genuinely plumbed rather than a coincidence of a default.
  </behavior>

  <action>
    Add the spec case. Assert over the WHOLE `.mock.calls[0]` array with a deep equality, in the
    established shape for this repo, rather than picking out the `ref` property: a
    property-scoped assertion would pass while an adjacent option regressed. Then pin the call
    count separately -- the argument-array assertion says nothing about a SECOND call, and a
    second unscoped enumeration is exactly the regression this control exists to prevent.
    Drive it twice with two different constructor `ref` values so the plumbing is proven, not
    inferred.

    Then widen the adapter's scoping comment into a lock. It must state:
    - what it governs: only default-branch Actions-cache entries are enumerated, therefore only
      those can be mirrored into the world-readable Release;
    - that it is now the SOLE in-repo control for that property, and WHY: the trusted-event
      allowlist admits the push event with no ref check, and the OS-version barrier that
      incidentally partitioned entries is gone since Phase 9. Two facts, both citable in the
      tree;
    - the FAILURE MODE of losing it: a non-default-branch trusted write becomes reachable from
      an anonymously-readable Release. That is an information-disclosure change, not a MISS;
    - that it is pinned by spec, and by which assertion, so a future reader who removes the
      comment still finds the guard.
    Do not restate the C-numbered control ledger; cite it.
  </action>

  <acceptance_criteria>
    - `npx nx run @op-nx/github-cache:test --skip-nx-cache -- action/index` passes.
    - The new case asserts over the whole argument array AND pins the call count. Read both
      assertions; a property-only assertion or a missing count pin does not satisfy this.
    - The case is driven with two distinct constructor `ref` values -- read it.
    - The comment lock names the allowlist fact and the removed barrier:
      `rg -c 'SOLE|sole' packages/github-cache/src/action/index.ts` >= 1 within the located
      adapter comment, and reading it confirms both supporting facts and the failure mode.
    - MUTATION CHECKS, predicted first and recorded in the SUMMARY:
      1. Drop `ref` from the `octokit.paginate` options -> the whole-array assertion reddens.
      2. Add a SECOND `octokit.paginate` call inside the adapter -> the count pin reddens while
         the whole-array assertion stays green. That split is the reason the count pin exists.
      3. Hardcode the `ref` value instead of using the constructor's -> the second-value case
         reddens while the first stays green.
    - `npm run typecheck` and `npm run lint` pass.
    - `git diff --stat -- start-cache-server/index.js` prints nothing. `action/index.ts` is an
      independent entry point and is not reachable from `serve()`; if a bundle diff appears,
      diagnose before staging anything.
  </acceptance_criteria>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- action/index</automated>
  </verify>

  <done>
    Removing or weakening the enumeration's ref scoping now reddens a spec, adding a second
    unscoped enumeration reddens a different one, and the comment says why the control carries
    the whole property on its own.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the unchanged controls by diff, and hand TRUST-11/12 to the auditor</name>

  <files>.planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` TRUST-10, TRUST-11, TRUST-12, TRUST-13 -- the authoritative
      text, in full. TRUST-11's own words carry the corrected arbitration point.
    - `.planning/THREAT-MODEL.md` -- the C1, C2, C9 and C16 rows, cited by number.
    - `.planning/phases/09-os-invariant-actions-cache-version/09-SECURITY.md` section 1 -- the
      reframing that the OS partition was never a security boundary. This is the auditor's
      required reading and the thing that makes TRUST-12's delta assessable as bounded.
    - `.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md` -- cite it, do
      not spend it. Phase 11's TEST-08 depends on it.
    - `packages/github-cache/src/publish/publish-mirror.ts` -- the restore call, the verbatim
      upload of the restored bytes, and the absence of any re-execution. These are the code
      citations TRUST-11's corrected arbitration point rests on; locate them by content and
      record what you read rather than copying a line number.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` `### TRUST-10`,
      `### TRUST-11`, `### TRUST-12`, `### TRUST-13` (whose seven-item list is the checklist for
      this artifact), and `## U-01 Resolution`.
    - All seven earlier `10-*-SUMMARY.md` files -- the threat dispositions actually reached,
      the mutation results, and the Live-CI items each plan left open.
  </read_first>

  <action>
    Write `10-TRUST-EVIDENCE.md` with two parts.

    PART A -- TRUST-10, verified by assertion rather than by sentence. Record the phase base
    commit with its subject line, then for each control record the COMMAND you ran and the
    OBSERVED result, not a verdict:
    - C1, the write-trust allowlist, and C2, the sync gate: a diff of their two files over
      `<base>..HEAD` must be EMPTY. Paste the command and state the observed output. If either
      is non-empty, that is a finding and must be recorded as one.
    - C16's Actions-cache-side filter: `cache-key.ts` DOES change in this phase, because its
      comment lock widened. So the assertion is FUNCTION-scoped, not file-scoped: diff the file
      over the range and confirm every changed line is a comment line, then cite the existing
      accept/reject pin on the enumeration filter as the behavioural half. Record both.
    - C16's Releases-side filter: it DOES change, and the change is ADDITIVE. Record why that is
      provable rather than claimed: the legacy branch is preserved verbatim, so every
      previously-accepted name is still accepted, and the disjointness table is the evidence.
    - C9, the cleanup delete-path discipline: EXTENDED by the new branch, with the mixed-shard
      dry-run as the offline proof. Record it as extended, not unchanged.
    - The `ref` scoping: record the spec assertion that now pins it and the comment lock's
      location, both by content.
    Also record the enumeration of surviving `no-restricted-syntax` disable directives from plan
    10-07, with each one's owning requirement, so a reader can see CORR-05 is closed and the
    survivors belong elsewhere.

    PART B -- the auditor hand-off for TRUST-11, TRUST-12 and TRUST-13. This part is INPUT to an
    independent audit and must say so at the top, in as many words. Cover all seven items:
    1. The two claims stated as QUESTIONS, not verdicts: whether the first-write-wins race
       between two same-hash producers at the cache-save boundary crosses a trust boundary, and
       whether collapsing the asset namespace changes the public-repo exposure surface.
    2. The PROPOSED classification -- that neither crosses a trust boundary, because the Actions
       cache's boundary is ref scope rather than OS -- explicitly labelled as offered as INPUT to
       the audit and NOT as its conclusion.
    3. The CORRECTED arbitration point with its code citations: the race is at the cache SAVE,
       not at the Release upload, because the publish engine restores each hash and uploads the
       restored bytes VERBATIM without re-executing the task, and for a given hash the Actions
       cache holds exactly ONE entry -- so both legs upload byte-identical bytes. State that the
       race is NOT reachable until a second producer exists on another OS, and that this moves
       TRUST-11's residual risk into that later phase's write decision -- a cross-phase
       consequence the next phase inherits.
    4. Required reading, BY PATH: `09-SECURITY.md` section 1; the THREAT-MODEL rows C1, C2, C9,
       C16; and Phase 9's commit range given CONCRETELY as `3327a4f..7d467e8` (from Phase 8's
       last commit, exclusive, through Phase 9's PROJECT.md evolution, inclusive) -- because
       Phase 9 CREATED the delta TRUST-12 records. Verify that range resolves before recording
       it; if it does not, resolve the equivalent boundary commits and record how.
    5. The controls in scope with their verdict-relevant status, drawn from Part A rather than
       restated independently.
    6. The three anti-requirements, so the auditor can check them: no producing-OS claim
       anywhere; no self-certified verdict; no ordering-based cross-OS-safety argument.
    7. The U-01 finding and the LINE THAT WAS DRAWN -- a forbidden wrong-result guarantee
       resting on ordering versus an acceptable-with-record CI guard SENSITIVITY resting on
       non-overlap -- so the auditor sees the reasoning rather than discovering the tension. Note
       that the distinction is comment-locked in two places on purpose and that it is testable:
       removing the serialisation knob costs a guard's sensitivity, not any reader's correctness.

    Add TRUST-12's exposure-delta record: the incidental within-scope OS partitioning is gone,
    leaving the declared discriminator as the sole separation mechanism; and a single-OS publish
    leg can now restore and mirror every OS's entries, so the captured terminal output of every
    CI job on every OS crosses into the anonymously-readable mirror. Record that this is already
    live and measurable rather than hypothetical, citing the measured per-leg mirroring
    fingerprint. Then record what the rename ADDS on top: nothing in exposure terms -- the bytes
    were already crossing -- so the delta this phase introduces is an ATTRIBUTION change, and the
    attribution restored is of the PUBLISHER.

    Close with the PROCESS CONSTRAINT, stated as an instruction to whoever runs the next step:
    `/gsd:secure-phase` writes SECURITY.md inline when the register looks clean and was authored
    at plan time. That is this phase's exact shape. Do NOT take that path. Spawn
    `gsd-security-auditor` and let it author the verdict.

    ASCII only. No committer email fields from any command output.
  </action>

  <acceptance_criteria>
    - The artifact exists and `rg -c 'INPUT' <path>` >= 1, with the input-not-conclusion framing
      appearing BEFORE the proposed classification. Read the order.
    - Part A records a COMMAND and an OBSERVED result for each control, not a verdict:
      `rg -c 'git diff' <path>` >= 2.
    - The phase base commit is recorded with its subject line: `rg -c 'ff21b5f' <path>` >= 1.
    - C16 is recorded at FUNCTION scope with the comment-only-diff reasoning:
      `rg -c 'comment' <path>` >= 1 in that section -- read it to confirm the reasoning is
      present rather than the word.
    - C9 is recorded as EXTENDED, not unchanged: `rg -c 'C9' <path>` >= 1 and the status reads
      extended.
    - Part B covers all seven required items. Enumerate them in the SUMMARY with the artifact
      heading that carries each; a missing item is a gap in the auditor's input, not a
      formatting nit.
    - Phase 9's commit range is recorded concretely and RESOLVES:
      `git rev-list --count 3327a4f..7d467e8` returns a positive number, and that number is
      recorded in the artifact.
    - The corrected arbitration point is recorded with code citations located by CONTENT:
      `rg -c 'verbatim' <path>` >= 1 and `rg -c 'exactly ONE entry|exactly one entry' <path>` >= 1.
    - The three anti-requirements are enumerated: `rg -c 'anti-requirement' <path>` >= 1.
    - The process constraint is recorded: `rg -c 'gsd-security-auditor' <path>` >= 1 AND
      `rg -c 'short-circuit' <path>` >= 1.
    - No verdict is stated. Confirm by reading: every classification sentence is framed as a
      question or as proposed input. The SUMMARY must state this explicitly rather than implying
      it.
    - `rg` used for every check; the artifact is untracked at first write.
  </acceptance_criteria>

  <verify>
    <automated>rg -c "gsd-security-auditor|short-circuit|ff21b5f|anti-requirement" .planning/phases/10-os-invariant-releases-mirror/10-TRUST-EVIDENCE.md</automated>
  </verify>

  <done>
    Every control TRUST-10 names has a recorded command and an observed result rather than a
    claim, and the two classification questions are handed over as INPUT with the corrected
    arbitration point, the concrete Phase 9 range (measured: 51 commits in `3327a4f..7d467e8`),
    the three anti-requirements, and the U-01 line that was drawn.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record SC6's two facts, both corrections applied, and the Live-CI register</name>

  <files>.planning/phases/10-os-invariant-releases-mirror/10-SC6-NOTES.md</files>

  <read_first>
    - `.planning/ROADMAP.md` Phase 10 success criterion 6, the Live-CI close, and the
      pre-condition owed to Phase 11.
    - `.planning/phases/10-os-invariant-releases-mirror/10-CONTEXT.md` D-26 and the
      `<deferred>` block's entry on collapsing the publish matrix to one leg.
    - `.planning/phases/10-os-invariant-releases-mirror/10-RESEARCH.md` C-1 IN FULL -- both
      corrections to D-26(a) -- and `## Sequencing hazards` H-6 with Assumptions Log entry A-3.
    - `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md` -- the census
      that is D-26(b)'s record.
    - `.github/workflows/ci.yml`, the corrected shard-growth comment plan 10-07 wrote -- so the
      note and the comment agree rather than drift.
    - All seven earlier `10-*-SUMMARY.md` files, for the Live-CI items each one left open.
  </read_first>

  <action>
    Write `10-SC6-NOTES.md`. Recorded, not gated: nothing in this artifact is a test and nothing
    in it blocks the phase. Its whole purpose is that the next milestone does not re-derive it or,
    worse, act on the uncorrected version.

    D-26(a), with BOTH research corrections applied and each labelled as a correction:
    - The claim: after this phase the Windows publish leg mirrors zero REAL TASK assets, because
      under one name per hash the ubuntu leg uploads every hash it can restore and the Windows
      leg finds every name already present and takes the benign no-op branch.
    - CORRECTION 1: it depends on the widened `needs:`, not on the rename alone. MEASURED on run
      `30400231720`: the ubuntu publish leg enumerated the Actions cache roughly two minutes
      BEFORE the Windows `integration` leg finished, and the shard carries the fingerprint --
      task hash `8059758544828235640` exists ONLY under a `-windows` suffix, never under
      `-linux`. Under the rename alone the Windows leg would remain the SOLE mirrorer of the
      Windows `integration` hash, i.e. it would mirror ONE real asset, not zero. State this
      plainly, because otherwise v0.0.3 reads D-26(a) as a consequence of the rename and reaches
      the wrong conclusion about collapsing the matrix.
    - CORRECTION 2: "zero assets" is false; "zero REAL TASK assets" is true. After OBS-05 the
      Windows leg mirrors exactly ONE asset -- its own seed -- and that one asset is precisely
      what makes OBS-05 non-vacuous. Two consequences, both recorded: the Windows leg's OBS-01
      summary will report `mirrored: 1`, not `0`, so the all-restore-MISS warning (which requires
      a mirrored count of zero) will NOT fire; and the DEFERRED single-leg matrix collapse would
      DESTROY OBS-05, because the only thing the Windows leg publishes is the artifact OBS-05
      exists to check. Record BOTH halves side by side: D-26(a) was framed as the strongest
      argument FOR that collapse, and it is now also the strongest argument that the collapse
      cannot happen without re-pricing OBS-05. A reader who finds only the first half will
      collapse the matrix.
    - Also record the second standing reason the collapse stays deferred: the Windows leg is
      still the only leg that produces Windows-hash entries at all, because the platform
      discriminator keeps those task hashes distinct. That reason does not expire with a proof.

    D-26(b): the Phase 9-to-10 window DOUBLED shard growth, because every restorable hash was
    mirrored under both OS suffixes until the rename landed. Bounded, NOT a correctness bug. Its
    record is the census in `10-EVIDENCE-PRE-RENAME.md` -- cite it with the shard tag, since the
    shard rolls over and a bare number is unattributable afterwards. Note that the `ci.yml`
    estimate was corrected upward in Phase 9 for this reason and downward in this phase's rename
    commit, and that the note and that comment must agree.

    Then the predicted signal for the first post-rename default-branch push, recorded IN ADVANCE
    so the observation is a test of a prediction rather than a rationalisation: restores still
    HIT and every asset NAME is new, so the ubuntu leg mirrors roughly the full restorable set
    with a read-miss count of zero and no warning. State the FALSIFIER explicitly: if the ubuntu
    leg reports a mirrored count of zero with read misses equal to the scanned count, this
    prediction is WRONG and must be recorded as falsified. Record that this differs from the
    ROADMAP's Live-CI close, which says to expect the first such push to publish nothing if it
    coincides with Phase 9's rotation window -- that window is SPENT, and the rename rotates the
    ASSET NAME, which is a different mechanism from the cache VERSION the warning is about. Say
    which document is being contradicted and why, rather than quietly asserting the new
    prediction.

    Finally, the consolidated Live-CI close register: one table with the three items, each with
    its owning plan, its owning requirement, exactly what a real runner must show, and the fact
    that no pre-merge check exists for it. Add the two items Phase 9 left open at its merge, so
    the register is the single place a reader looks. Note that Phase 9's rotation observation is
    already SPENT and that a later all-HIT merge run must NOT be cited as the rotation
    observation.

    ASCII only.
  </action>

  <acceptance_criteria>
    - The artifact exists and both corrections are present and LABELLED as corrections:
      `rg -c 'CORRECTION' <path>` >= 2.
    - The dependency on the widened `needs:` is stated with its measurement:
      `rg -c '30400231720' <path>` >= 1 AND `rg -c '8059758544828235640' <path>` >= 1.
    - The mirrored-count prediction is `1`, not `0`, and the warning's non-firing is stated:
      read the section and confirm both.
    - Both halves of the collapse argument are recorded:
      `rg -c 'collapse' <path>` >= 2, and reading confirms the second half (that the collapse
      would destroy OBS-05) sits beside the first.
    - The second standing reason for deferral is recorded:
      `rg -c 'only leg that produces' <path>` >= 1.
    - D-26(b) cites the census by shard tag: `rg -c 'cache-mirror-202607' <path>` >= 1.
    - The predicted first-push signal carries an explicit FALSIFIER:
      `rg -c 'falsif' <path>` >= 1.
    - The contradiction with the ROADMAP's Live-CI close is named rather than silently resolved:
      `rg -c 'ROADMAP' <path>` >= 1.
    - The Live-CI register has at least five rows (this phase's three plus Phase 9's two), each
      naming its owning plan and what a runner must show. Count the rows in the SUMMARY.
    - The register records that Phase 9's rotation signal is SPENT:
      `rg -c 'SPENT|spent' <path>` >= 1.
    - No claim in the artifact asserts cross-OS safety from publish-leg ordering. Confirm by
      reading, and state the confirmation in the SUMMARY.
    - `rg` used for every check; the artifact is untracked at first write.
  </acceptance_criteria>

  <verify>
    <automated>rg -c "CORRECTION|30400231720|8059758544828235640|cache-mirror-202607|falsif" .planning/phases/10-os-invariant-releases-mirror/10-SC6-NOTES.md</automated>
  </verify>

  <done>
    SC6's two facts are written down in their CORRECTED form, the first-push prediction carries
    its own falsifier, and the three Live-CI items plus Phase 9's two open items live in one
    register.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> Actions cache (GitHub per-ref scope) | GitHub's own boundary; OS-blind, and it always was |
| non-default-branch trusted write -> the world-readable Releases mirror | the boundary the enumeration's `ref` scoping now holds ON ITS OWN |
| this phase's threat register -> the independent audit | the register is INPUT; the verdict lives in SECURITY.md, authored by the auditor |
| Actions cache save (two same-hash producers) -> which bytes a reader accepts | not reachable in this phase; a later phase's write decision inherits it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Information disclosure | the `ref` scoping on `listCacheEntries`, now the SOLE in-repo control | high | mitigate | Pinned by a whole-argument-array assertion on the pagination call PLUS a call-count pin, so neither dropping the scope nor adding a second unscoped enumeration can land silently, and driven with two constructor values so the plumbing is proven. Comment-locked with both supporting facts -- the trusted-event allowlist admits the push event with no ref check, and the OS-version barrier is gone -- and with the failure mode named as information disclosure, not a MISS |
| T-10-28 | Repudiation | C1, C2 and C16 claimed unchanged rather than shown unchanged | medium | mitigate | Each is recorded with the COMMAND and the OBSERVED result over a named base commit. C16's Actions-cache side is asserted at FUNCTION scope because its file legitimately changes (a widened comment lock), which is the exact trap a file-scoped diff would fall into. C9 is recorded as EXTENDED rather than unchanged, because the new filter branch extends the delete path |
| T-10-04 | Tampering | first-write-wins arbitrating between NON-identical payloads at the cache save | medium | accept | Recorded as a QUESTION for the auditor with the corrected arbitration point and its code citations: for a given hash the Actions cache holds exactly ONE entry and both publish legs restore it and upload it verbatim without re-executing. Not reachable until a second producer exists on another OS, which moves the residual risk into that later phase's write decision. Offered as INPUT, explicitly not as the conclusion |
| T-10-05 | Information disclosure | the public-repo exposure delta from a single-OS leg mirroring every OS's entries | medium | accept | Recorded as a QUESTION for the auditor, bounded by the reframing that the Actions cache boundary is ref scope plus this repo's own write gate, both OS-blind -- two runners in the same scope were ALREADY mutually trusted, so the OS partition was never a security boundary. What changed is which of an already-trusted writer's bytes a reader will accept, not who may write. This phase's own delta is an ATTRIBUTION change, not an exposure change. Offered as INPUT, explicitly not as the conclusion |
| T-10-29 | Repudiation | a self-certified TRUST-13 verdict | high | mitigate | The plan states in as many words that no verdict is reachable here, the acceptance criteria require every classification sentence to be framed as a question or as proposed input, and the artifact records the process constraint: `/gsd:secure-phase` must spawn `gsd-security-auditor` and must NOT take its inline short-circuit, which fires on exactly this phase's shape (a clean register authored at plan time) |
| T-10-30 | Repudiation | an uncorrected D-26(a) driving a later matrix collapse | high | mitigate | Both research corrections are required and must be LABELLED as corrections, and both halves of the collapse argument must sit side by side -- the strongest argument FOR it, and the reason it cannot happen without re-pricing OBS-05. The second, non-expiring reason for deferral is recorded separately |
| T-10-SC | Tampering | npm / pip / cargo installs | low | accept | ZERO new packages across the whole phase. No `package.json` or lockfile edit in any plan, so there is no supply-chain delta to gate and no legitimacy audit is owed |
</threat_model>

<verification>
- `npx nx run @op-nx/github-cache:test --skip-nx-cache` green, with the three `ref`-pin mutation
  checks predicted and recorded.
- `npm run typecheck` and `npm run lint` green.
- Both artifacts present, with every `rg` content check satisfied.
- `git rev-list --count 3327a4f..7d467e8` resolves (measured: 51) and the count is recorded.
- The SUMMARY states explicitly that no TRUST-13 verdict was reached in any artifact.
- `git diff --stat -- start-cache-server/index.js` prints nothing.
</verification>

<success_criteria>
- Removing the enumeration's ref scoping, or adding a second unscoped enumeration, each reddens
  a different assertion.
- Every control TRUST-10 names is backed by a recorded command and an observed result.
- The auditor can classify TRUST-11 and TRUST-12 from the hand-off alone, without re-deriving
  the arbitration point or hunting for Phase 9's range.
- SC6's record cannot be read as an argument for collapsing the publish matrix.
</success_criteria>

<output>
Create `.planning/phases/10-os-invariant-releases-mirror/10-08-SUMMARY.md` when done.

After this plan: run `/gsd:secure-phase 10`, which MUST spawn `gsd-security-auditor` to author
`10-SECURITY.md`. Do not take the inline short-circuit -- TRUST-13 forbids it, and this phase's
register is exactly the shape that triggers it.
</output>
