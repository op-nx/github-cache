---
phase: 11-live-proofs-o1-o2-o3
plan: 04
type: execute
wave: 4
depends_on: ['11-03']
files_modified:
  - .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md
autonomous: false
requirements: [TEST-08, XOS-01, XOS-02]

must_haves:
  truths:
    - "Per hit hash, the Actions-cache entry list and the shard asset list are captured with created_at and the OBS-03 mirrored-by label, and cross-referenced against job windows (TEST-08's own mandated capture)."
    - "Attribution is established by the four independent means, and each hash states WHICH means carried its verdict, with that means' limit stated too."
    - "09-EVIDENCE.md's pre-Phase-9 producer-attribution snapshot is CITED, not re-derived and not spent (D-15)."
    - "No claim that mirrored-by answers the producing OS appears anywhere; the retraction carries a replacement means rather than only a deletion (D-14)."
    - "The maintainer signs off on O1 and O2 BEFORE any hash-rotating edit lands, because after wave 5 three of the four proof hashes rotate and this measurement cannot be retaken at this commit."
  artifacts:
    - ".planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md attribution section"
  key_links:
    - "GET /repos/{owner}/{repo}/actions/caches (paginated) -> per-entry key and created_at"
    - "release cache-mirror-202607 asset list (paginated) -> per-asset name, created_at, label"
    - "GET /repos/{owner}/{repo}/actions/runs/{id}/jobs -> the step windows that bound when an entry could have been written"
---

<objective>
Capture TEST-08's producer attribution at proof time, and gate the phase's first hash-rotating edit
behind an explicit maintainer sign-off on O1 and O2.

TEST-08 requires the O1 proof to additionally capture PRODUCER ATTRIBUTION at proof time, per hit
hash: the Actions-cache entry list and the shard asset list with `created_at`, cross-referenced
against job windows, capturing `created_at` and the OBS-03 label per asset rather than just the
asset list. The requirement's own reason is that enabling O4 permanently destroys the ability to
re-derive it.

TEST-08 also states that the attribution window closed once already, at Phase 9 rather than at
Phase 12: once the version is OS-invariant the ubuntu publish leg starts mirroring the Windows
`integration` entry too, so the shard stops being "everything here is ubuntu's" before Phase 11
runs. `09-EVIDENCE.md` covers that first window. This plan's capture is the SECOND half of a
two-part record, not the first.

Purpose: attribution is the one thing Phase 12 destroys permanently, and `mirrored-by` cannot supply
it. This is the last plan in the phase where nothing has rotated yet.

Output: the attribution section of `11-EVIDENCE.md`, and an explicit recorded authorisation to
proceed into the hash-rotating waves.

There is a tighter clock on this capture than the mirror's. The Releases shard is governed by the
30-day retention floor and the D-03 expiry around 2026-08-28, but GitHub evicts Actions cache
entries after a period of no access, so the Actions-cache half of this capture is the more
perishable of the two. Take it first within this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-CONTEXT.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-RESEARCH.md
@.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-LIVE-CI.md
@.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md
@.planning/phases/11-live-proofs-o1-o2-o3/11-03-SUMMARY.md
</context>

## Decisions implemented

| Decision | Where |
|---|---|
| D-03 (the clock, and the Actions-cache half being the tighter one) | task 1 takes the Actions-cache read first |
| D-10 / D-11 (rotation table; O1/O2 measured first) | task 2's gate is the boundary between the free waves and the rotating ones |
| D-14 (four independent means; `mirrored-by` is unusable for producer attribution) | task 1's per-hash table names which means carried each verdict, with its limit |
| D-15 (`09-EVIDENCE.md`'s snapshot is CITED, not spent, not re-derived) | task 1 cites it by section name and does not re-enumerate its 106 assets |
| D-22 (ONE evidence record) | task 1 appends to the existing `11-EVIDENCE.md` rather than creating a second file |

## The four means, and the one that is unusable (D-14)

Reproduced here because task 1's per-hash table is keyed on it.

| Evidence | What it establishes | Its limit |
|---|---|---|
| The graph premise from plan 11-01 | Windows CI resolves no `build`, `typecheck` or `test` task, so any such hash in the store is Linux-produced | structural, and valid only because it is ASSERTED rather than assumed |
| The replayed artifact's `terminalOutput` runner path | a per-hit, byte-level producer fingerprint inside the served artifact | readable only after a HIT, which is why plan 11-03 captured it |
| The Actions-cache entry list plus the shard asset list with `created_at` and the OBS-03 label, cross-referenced against job windows | the requirement's own mandated capture; bounds when each entry could have been written | timing, not identity |
| Recompute the hash on a known platform and match | established the producer live in `10-EVIDENCE-LIVE-CI.md` | works only for OS-SENSITIVE targets, so it serves O2 and `integration` and never O1 |

`mirrored-by` is NOT on this list and cannot be added to it. `10-EVIDENCE-LIVE-CI.md` records the
Windows-produced `integration` hash `8137422034373911537` in the mirror stamped
`mirrored-by: linux`, because the ubuntu publish leg restored a Windows-produced entry and stamped
its own OS. Any artifact, comment or doc line claiming the label answers whose bytes a developer
received is RETRACTED and must not reappear.

## Artifacts this phase produces

**New CLI surface (plan 11-01)**
- `capture-hashes.mjs` flag `--assert-graph-premise`, plus `resolvedTaskIds`, `assertGraphPremise`,
  `FORBIDDEN_TARGETS`, `CONTROL_TARGET`

**New root-level files (plan 11-01)**
- `read-integration-hash.mjs`

**New `ci.yml` step names in the existing `integration` job (plan 11-06)**
- `Run the integration target and tee its output`
- `Read this leg's integration hash from run.json`
- `Record the remote-cache label occurrence count for this leg`
- `Positive control on this leg's own key, which must return 200`
- `Record whether step debug logging is active`

**New `ci.yml` job and step names (plan 11-06)**
- job `o3-witness`
- `Read H_linux from the ubuntu integration leg's record`
- `Assert the H_linux cache entry existed before the Windows integration step started`

**New CI artifact names (plan 11-06)**
- `integration-hash-${{ matrix.os }}`

**Runtime files, not committed (plans 11-06, 11-07)**
- `integration-hash.txt`, `integration-nx.log`, `o3-witness.log`

**New anchored success literal (plan 11-06)**
- `o3-witness: EXISTENCE OK`

**New spec surface (plan 11-05)**
- `dogfood-cross-os.spec.ts` describe block `ci.yml o3-witness job exists and keeps its shape
  (XOS-03, TEST-09)`
- `docs-same-os-claims.spec.ts` new additive `DOCS_08_SITES` rows keyed on
  `.github/workflows/ci.yml`

**New `.planning` artifacts**
- `11-task-graph-premise.json` (11-01), `11-hashes-warm.json`, `11-hashes-cold.json` (11-02),
  `11-EVIDENCE.md` (11-03, 11-04, 11-07)

<tasks>

<task type="auto">
  <name>Task 1: Capture producer attribution per hit hash and append it to 11-EVIDENCE.md (TEST-08, D-14, D-15)</name>

  <files>.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` TEST-08 in full, which states the capture in its own words,
      including the clause that the window closed at Phase 9 and the instruction to capture
      `created_at` and the OBS-03 label per asset rather than just the asset list.
    - `.planning/phases/09-os-invariant-actions-cache-version/09-EVIDENCE.md`
      `## Producer-attribution snapshot (D-34)`. Read it, cite it, and do NOT re-derive it. Its own
      scope note records that it is evidence PRESERVATION and explicitly not a proof, and forbids the
      next step being the proof.
    - `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-LIVE-CI.md`, for run
      `30471772954`, the 16 new-form assets, the four warm hashes, and the live
      publisher-not-producer instance.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-03-SUMMARY.md`, for the four hit hashes and their
      per-hit `terminalOutput` runner-path fingerprints.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-RESEARCH.md` `## U-01` section (1), which measured
      the entry-created-inside-its-own-leg's-step-window method working in practice across three
      runs, and is the template for the cross-reference below.
    - `.planning/phases/10-os-invariant-releases-mirror/10-EVIDENCE-PRE-RENAME.md` secret-hygiene
      note and its census section, for the no-raw-payload rule.
  </read_first>

  <action>
    Take the Actions-cache read FIRST, because it is the more perishable of the two halves.

    Read the repository's Actions cache entry list, read-only and PAGINATED. The repository held 142
    entries against a `per_page` maximum of 100, so a single page truncates silently. For each of the
    four hit hashes from plan 11-03, record the entry whose key matches the authored `nx-cache-`
    prefix followed by that hash, and record its `created_at`, `last_accessed_at`, `ref` and size.
    Compare the key for EXACT string equality, never by a prefix filter's `total_count`: RESEARCH.md
    measured that `?key=` is a PREFIX match and a shorter hash that is a prefix of a longer one would
    pass a count test. Record ABSENT explicitly for any hash whose entry has already been evicted;
    an eviction is a fact about the clock, not a gap in the proof.

    Then read the `cache-mirror-202607` release asset list, also read-only and PAGINATED, and record
    per matching asset the name, `created_at` and the free-form `label` field, which is where OBS-03
    puts `mirrored-by`. Record the shard's total asset count as the mechanical evidence that
    pagination was necessary.

    Then read the job list for the run that produced those entries and record, per relevant job leg,
    the step window that could have written each entry: the leg name and the `started_at` and
    `completed_at` of the step that ran the target. The method to reuse is the one RESEARCH.md
    measured: an entry created INSIDE a given leg's step window is attributable to that leg. Note
    the recorded artefact so a transcriber does not misread it: the background sidecar step's
    conclusion is `cancelled` in every run, because it is killed by the cancel step, while the job
    conclusion is `success`.

    Build the per-hash attribution table in `11-EVIDENCE.md`, one row per hit hash, with columns for
    the target, the Actions-cache entry `created_at` or ABSENT, the shard asset `created_at`, the
    `mirrored-by` label value, the bounding job window, the `terminalOutput` runner-path fingerprint
    from plan 11-03, the producer OS concluded, and WHICH of the four means carried that verdict.
    Where a means does not apply, say so with its limit rather than leaving the cell blank. For the
    three O1 targets the hash-recomputation means does NOT apply, because it works only for
    OS-sensitive targets; for `integration` it does, and `10-EVIDENCE-LIVE-CI.md` already established
    it live, so cite that rather than re-deriving it.

    Add a citation line for `09-EVIDENCE.md`'s snapshot naming the section, the 106 shard assets, the
    repository Actions-cache entry list and the `2026-07-28T20:30:55Z` capture time, and stating in
    one sentence that Phase 11's capture is the second half of a two-part record. Do not re-enumerate
    its contents.

    Restate the D-14 retraction in this section too, WITH the replacement means, because a bare
    deletion of the wrong claim leaves a future reader holding a documented argument for reinstating
    it.

    Extract every value programmatically and paste NO raw REST payload: payload fields carry
    uploader identity, node ids and signed URLs, and this is a public repository. Write no email
    address into the file. Use the Write or Edit tool, never a heredoc. ASCII only.
  </action>

  <verify>
    <automated>node -e "const t=require('node:fs').readFileSync('.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md','utf8');const need=['created_at','mirrored-by','09-EVIDENCE.md','Producer-attribution snapshot','cache-mirror-202607','terminalOutput'];const missing=need.filter((s)=>!t.includes(s));if(missing.length)throw new Error('attribution section missing: '+missing.join(' | '));if(/\"node_id\"|\"uploader\"|browser_download_url/.test(t))throw new Error('a raw REST payload field leaked into the evidence record');const nonAscii=t.split('\n').map((l,i)=>[i+1,l]).filter(([,l])=>/[^\x00-\x7F]/.test(l));if(nonAscii.length)throw new Error('non-ASCII on lines: '+nonAscii.map(([n])=>n).join(', '));if(/@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/.test(t.replace(/@op-nx\/[a-z-]+/g,'')))throw new Error('an email-shaped token appears in the evidence record');console.log('attribution section OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `11-EVIDENCE.md` contains a per-hash attribution table with one row per hit hash from plan
      11-03, and every row states the producer OS concluded and WHICH of the four means carried the
      verdict.
    - Each row carries the Actions-cache entry `created_at` or the literal ABSENT, the shard asset
      `created_at`, and the `mirrored-by` label value.
    - Each row carries a bounding job window with a leg name and a step `started_at` and
      `completed_at`.
    - Both listings were paginated, and the SUMMARY records the total counts (Actions-cache entries
      and shard assets) as the mechanical evidence that a single page would have truncated.
    - Every key comparison used exact string equality; the SUMMARY states so and names the prefix
      trap it avoids.
    - `09-EVIDENCE.md`'s snapshot is cited by section name with its capture time, and its contents
      are NOT re-enumerated.
    - The D-14 retraction appears with its replacement means, and no line in the file claims
      `mirrored-by` answers the producing OS.
    - No raw REST payload field, no email-shaped token, and no non-ASCII character appears in the
      file.
  </acceptance_criteria>

  <done>
    Producer attribution is captured per hit hash by TEST-08's own mandated means, each verdict names
    which means carried it and that means' limit, the inherited Phase 9 snapshot is cited rather than
    spent, and the retraction carries its replacement.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Sign off O1 and O2 before any hash-rotating edit lands (D-10, D-11)</name>

  <files>none</files>

  <read_first>
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md` as it now stands: the O1 section, the
      O2 section, and the attribution table from task 1.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-03-SUMMARY.md`, for the per-target MET or NOT MET
      verdicts against the pre-registered counts.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-PATTERNS.md` `## Rotation Cost per File`, which is
      what makes this gate load-bearing rather than ceremonial.
  </read_first>

  <decision>
    Whether the O1 and O2 record is complete enough to proceed into the hash-rotating waves (plans
    11-05, 11-06 and 11-07), or whether something must be re-opened first.
  </decision>

  <context>
    This is the last moment at which nothing has rotated. Plan 11-05 edits two files under
    `packages/github-cache/src/`, which rotates `test`, `typecheck`, `integration` and `lint`; plan
    11-06 edits `ci.yml`, which rotates `test`. After either lands, three of the four proof hashes
    are different values and O1 cannot be retaken at this commit. The mirror still holds the OLD
    hashes, so a re-measurement after the rotation would MISS for a reason that has nothing to do
    with cross-OS sharing.

    That asymmetry is the whole reason this phase exists separately from Phase 12, and it is why the
    ordering is encoded as a `depends_on` graph rather than as a comment asking an executor to
    preserve it.
  </context>

  <options>
    <option id="proceed">
      <name>Proceed into waves 5 to 7</name>
      <pros>O1 and O2 are recorded with their pre-registered counts met, the attribution table is
      complete, and the phase can close its live-CI half.</pros>
      <cons>Irrevocably rotates three of the four proof hashes. Any O1 or O2 detail noticed as
      missing afterwards cannot be captured at this commit.</cons>
    </option>
    <option id="reopen-local-proof">
      <name>Re-open the local proof before rotating anything</name>
      <pros>The window is open until around 2026-08-28, so a gap found now is still fixable. Correct
      choice if any target reported NOT MET, if a `terminalOutput` fingerprint is missing, or if
      plan 11-02's D-07 contingency fired.</pros>
      <cons>Costs another measurement session and more of the window.</cons>
    </option>
    <option id="defer">
      <name>Defer the decision</name>
      <pros>Nothing rotates; the record is already banked and committed.</pros>
      <cons>Blocks the entire live-CI half of the phase and spends window.</cons>
    </option>
  </options>

  <resume-signal>Select: proceed, reopen-local-proof, or defer</resume-signal>

  <action>
    Present, in this order: the per-target MET or NOT MET verdicts against the pre-registered counts;
    the `run.json` `cacheStatus` set; the four attribution verdicts with the means that carried each;
    whether plan 11-02's D-07 contingency fired; and the days remaining before the D-03 clock. Then
    state plainly that selecting `proceed` rotates `test`, `typecheck`, `integration` and `lint` and
    that O1 becomes unrepeatable at this commit. Wait for an explicit selection. Do not begin plan
    11-05 without one.
  </action>

  <verify>
    <human-check>The maintainer has selected one of the three options explicitly, and the selection
    is recorded in the plan SUMMARY beside the numbers it was taken against.</human-check>
  </verify>

  <acceptance_criteria>
    - The SUMMARY records the selected option id verbatim, one of `proceed`, `reopen-local-proof` or
      `defer`.
    - The SUMMARY records the per-target MET or NOT MET verdicts and the four attribution verdicts
      that the decision was taken against.
    - The SUMMARY states explicitly that selecting `proceed` was understood to rotate `test`,
      `typecheck`, `integration` and `lint`.
    - If any option other than `proceed` was selected, plans 11-05 through 11-07 are reported as
      BLOCKED and no file under `packages/` or `.github/` is touched.
  </acceptance_criteria>

  <done>
    The maintainer has explicitly authorised, or explicitly withheld, the phase's first
    hash-rotating edit, and the authorisation is on record next to the evidence it was taken
    against.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub REST API to the evidence record | untrusted JSON is read, and its fields carry uploader identity and signed URLs into a PUBLIC repository if pasted |
| attribution capture to the milestone claim | a wrong attribution is unrecoverable once Phase 12 enables O4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information disclosure | the attribution section of `11-EVIDENCE.md` | high | mitigate | Every value is extracted programmatically by field name. No raw REST payload is pasted; an automated check fails the task if `node_id`, `uploader` or `browser_download_url` appears. No email address, ASCII only |
| T-11-02 | Spoofing | matching a cache entry by key | high | mitigate | Exact `.key` string equality. RESEARCH.md measured `?key=` as a PREFIX match returning 40 entries for a short prefix, so a count test would accept a different entry |
| T-11-12 | Repudiation | the paginated listings | high | mitigate | Both listings paginate; the recorded totals (142 Actions-cache entries, a shard asset count above 100) are themselves the proof that a single page would have truncated and reported a present item as ABSENT |
| T-11-16 | Repudiation | claiming `mirrored-by` attributes a producer | high | mitigate | The retraction is restated WITH its replacement means, and the four-means table names which means carried each verdict and that means' limit. The shipped `docs-same-os-claims.spec.ts` guard independently sweeps every edited file for the retracted claim |
| T-11-17 | Tampering | rotating a proof hash before the proof is signed off | high | mitigate | Task 2's blocking decision checkpoint is the control, and the `depends_on` graph is what enforces it rather than prose |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this phase; RESEARCH.md's Package Legitimacy Audit is N/A |
</threat_model>

<verification>
- `11-EVIDENCE.md`'s attribution section carries a per-hash row with `created_at`, the `mirrored-by`
  label, a bounding job window, a `terminalOutput` fingerprint, and the means that carried the
  verdict.
- `09-EVIDENCE.md`'s snapshot is cited by section name and not re-derived.
- The automated payload-hygiene, ASCII and email checks pass over the whole evidence record.
- The maintainer's selection from task 2 is recorded verbatim in the SUMMARY.
- `git status --porcelain packages/ .github/ start-cache-server/` prints nothing. This plan writes
  only under `.planning/`, so it rotates no hash and the gate it opens is the first rotation.
</verification>

<success_criteria>
- Producer attribution is captured per hit hash by TEST-08's own mandated means, at proof time,
  before anything rotates.
- Each attribution verdict names WHICH of the four means carried it and that means' limit.
- The Phase 9 snapshot is cited rather than spent, and the `mirrored-by` retraction carries a
  replacement reason.
- No raw REST payload, no email-shaped token and no non-ASCII character reaches the record.
- The phase's first hash-rotating edit is explicitly authorised or explicitly withheld, on record.
</success_criteria>

<output>
Create `.planning/phases/11-live-proofs-o1-o2-o3/11-04-SUMMARY.md` when done. It MUST carry: the two
paginated totals, the per-hash attribution verdicts with their means, the selected option id from
task 2, and the statement that selecting `proceed` was understood to rotate four target hashes.
</output>
