---
phase: 12-windows-ci-reuse-o4-consumer-recipe
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - packages/github-cache/src/docs-cross-os.spec.ts
  - packages/github-cache/src/nx-target-inputs.spec.ts
  - docs/cross-os.md
  - nx.json
  - README.md
  - docs/advanced.md
autonomous: true
requirements: [DOCS-07]

must_haves:
  truths:
    - "A consumer reading only the first section of docs/cross-os.md lands on the SAFE configuration: declare the discriminator on all cacheable targets (D-11)"
    - "The portability checklist is the SECOND section and is framed as how to EARN a removal, never as a menu of reasons to skip the default (D-11)"
    - "The doc names architecture AND libc as axes process.platform does not cover, and states in the same breath that this repo cannot exercise either (D-13)"
    - "The doc renders the EXACT discriminator literal nx.json declares -- widening or re-spelling the config trips the build until the doc is updated (D-15)"
    - "The drift guard cannot replay a stale cached PASS, because docs/cross-os.md is an nx.json test input from the same commit as the doc (DOCS-07, PARITY-08)"
    - "The doc is discoverable: it is linked from README's Documentation nav and cross-linked from docs/advanced.md"
    - "Every pinned phrase was verified to fit on ONE line of the written file, so no assertion is a silent false PASS in the additive direction"
  artifacts:
    - path: "docs/cross-os.md"
      provides: "the DOCS-07 consumer cross-OS adoption recipe: safe default first, checklist second"
      contains: "--no-warnings"
    - path: "packages/github-cache/src/docs-cross-os.spec.ts"
      provides: "single-sourced equality on the discriminator plus phrase-keyed section-order and arch/libc locks"
      contains: "targetDefaults"
    - path: "nx.json"
      provides: "test-input registration for docs/cross-os.md"
      contains: "docs/cross-os.md"
  key_links:
    - from: "packages/github-cache/src/docs-cross-os.spec.ts"
      to: "nx.json targetDefaults.integration.inputs"
      via: "reads nx.json as JSON and extracts the runtime entry, never re-spelling the literal in the spec"
      pattern: "targetDefaults"
    - from: "packages/github-cache/src/docs-cross-os.spec.ts"
      to: "docs/cross-os.md"
      via: "new URL('../../../docs/cross-os.md', import.meta.url) plus an occurrence COUNT, never a bare toContain"
      pattern: "docs/cross-os\\.md"
    - from: "README.md"
      to: "docs/cross-os.md"
      via: "the ## Documentation nav list, in the shipped bullet form"
      pattern: "\\(docs/cross-os\\.md\\)"
---

<objective>
Ship DOCS-07: a consumer-facing cross-OS adoption recipe that leads with the safe default, frames the
portability checklist as how to EARN a removal, states the limits of `process.platform` honestly, and
renders the exact discriminator this repo itself runs -- guarded so it cannot rot silently.

Purpose: this is the milestone's headline consumer deliverable and the only consumer-facing artifact
in the phase. Everything else here is project-local. The section ORDER is load-bearing rather than
editorial: a reader who stops after section one must land on the safe configuration, because the
unsafe configuration (no discriminator on a non-portable target) is a WRONG-RESULT risk, not a
performance one. And the documented string must be the string the repo runs -- documenting a
configuration the repo does not itself run is the exact defect class DOCS-08 spent a phase correcting.

Output: one new doc, one registration, two nav links, one new drift guard, and two registration pins.
</objective>

## Phase Goal

**Verbatim from `.planning/ROADMAP.md:523-524`:**

> Windows CI reuses Linux CI's portable artifacts, and an outside project can copy the recipe
> without inheriting a wrong-result risk.

> MVP_MODE discrepancy (ROADMAP's `**Goal**` is not in user-story form) is recorded once in
> `12-01-PLAN.md` `## Phase Goal`. Operator item: `/gsd:mvp-phase 12`.

## Vertical slice

Completes *a consumer can copy the recipe safely*, end to end: the guard, the doc, the registration
that makes the guard non-stale, and the two links that make the doc reachable. After Task 2 an
outside reader can find the recipe from the README and copy a string the repo itself runs. Nothing
here is a layer waiting for a later plan.

## Artifacts this phase produces

See `12-01-PLAN.md` `## Artifacts this phase produces`. Created by THIS plan: `docs/cross-os.md`,
`packages/github-cache/src/docs-cross-os.spec.ts`, and the `nx.json` `test` input literal
`{workspaceRoot}/docs/cross-os.md`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-CONTEXT.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-RESEARCH.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- the docs-cross-os drift guard and the two registration pins</name>

  <files>packages/github-cache/src/docs-cross-os.spec.ts, packages/github-cache/src/nx-target-inputs.spec.ts</files>

  <read_first>
    - `packages/github-cache/src/docs-trust.spec.ts` IN FULL (86 lines). It is the STRONGEST shape and
      the one D-15's equality clause needs: import/read the single source, assert the doc renders it
      verbatim, with a named existence `it` first and an `existsSync`-guarded read.
    - `packages/github-cache/src/docs-same-os-claims.spec.ts` -- the phrase-keyed table, its
      `WORKSPACE_ROOT_URL` + `read(file)` helper, the failure messages that say "update the ROW in the
      SAME commit; do not delete the assertion to make the suite green", the occurrence-COUNT
      assertion that fixes `toContain`'s half-lock, and the HARD CONSTRAINT in its header that every
      phrase in the table must fit on ONE LINE of its file.
    - `packages/github-cache/src/docs-adoption.spec.ts` -- its docstring paragraph naming the
      `nx.json` registration dependency, and its anchored same-sentence idiom
      (`[^.!?]{0,80}` between two tokens), which is the right tool for D-13's three related tokens.
    - `packages/github-cache/src/nx-target-inputs.spec.ts` -- the `{workspaceRoot}` literal pin shape
      and the comment lock explaining why an explicit path beats a glob.
    - `nx.json` after plan 12-04, to see the exact runtime literal the guard must read rather than
      re-spell.
    - `12-PATTERNS.md` `### packages/github-cache/src/docs-cross-os.spec.ts (NEW)` (all three shapes
      quoted side by side) and `12-RESEARCH.md` `## Code Examples` (the JSON extraction, whose
      `flatMap` matches `nx-target-inputs.spec.ts` character for character).
  </read_first>

  <behavior>
    A new spec file `packages/github-cache/src/docs-cross-os.spec.ts` resolving the doc via
    `new URL('../../../docs/cross-os.md', import.meta.url)` -- THREE `../`, since it lives in flat
    `src/` -- with an `existsSync`-guarded read so a missing doc yields `''` rather than a module-load
    crash. Every describe name contains the token `cross-os`.

    - Test 1 (existence, FIRST, and it is the control that makes every later clause non-vacuous):
      `existsSync(docUrl)` is `true`.
    - Test 2 (D-15, single-sourced equality): extract every `runtime` entry from
      `nxJson.targetDefaults.integration.inputs` by the same `flatMap` shape
      `nx-target-inputs.spec.ts` uses. Assert the extracted list has LENGTH 1 -- a second runtime
      entry is as much a CORR-04 event as the string changing -- then assert the doc contains that
      extracted string with an occurrence COUNT (`doc.split(cmd).length - 1`) of at least 1. NEVER
      re-spell the literal in this spec: reading it from `nx.json` is the entire mechanism.
    - Test 3 (D-11, section ORDER): the index of the safe-default section heading is LESS than the
      index of the portability-checklist section heading, and BOTH are present. Phrase-keyed on the
      heading text, never on a line number.
    - Test 4 (D-11, order is load-bearing): the safe-default heading is the FIRST numbered `##`
      section in the file -- no numbered section precedes it.
    - Test 5 (D-13, anchored same-sentence): the doc relates architecture, libc and the
      cannot-exercise limit, asserted with an anchored same-sentence regex rather than three separate
      `toContain` calls. Three tokens that must be RELATED, not merely co-present.
    - Test 6 (D-12): the portability checklist contains exactly FIVE numbered items.
    - Test 7 (nav): `README.md` links `docs/cross-os.md`, and `docs/advanced.md` links it too.
    - Test 8 (registration, and it belongs in `nx-target-inputs.spec.ts` rather than here):
      `nx.json`'s `targetDefaults.test.inputs` contains `{workspaceRoot}/docs/cross-os.md`.

    Expected RED: Test 1 fails with `expected false to be true`. Tests 2-6 fail against the empty
    string. Test 7 fails on both files. Test 8 fails on the absent `nx.json` entry.
  </behavior>

  <action>
    Create `packages/github-cache/src/docs-cross-os.spec.ts` implementing the `<behavior>` above.
    Recommended over extending `docs-adoption.spec.ts`: the single-sourced `nx.json` read is a
    mechanism that file does not have, and its own header scopes it to adoption-doc presence tokens.

    Carry a docstring paragraph -- every docs guard in this repo has one, and without it the guard
    replays a stale pass -- stating that `docs/` lives OUTSIDE this project's graph, that
    `docs/cross-os.md` is therefore wired into `nx.json`'s `test` inputs, and that the registration
    lands in the SAME COMMIT as the doc so there is no stale-pass window.

    Use the `docs-same-os-claims.spec.ts` register for every failure message: name the phrase, say the
    table is keyed on FILE plus PHRASE on purpose because edits in one commit shift each other's
    lines, and say "if the site was legitimately reworded, update its ROW here in the SAME commit; do
    not delete the assertion to make the suite green".

    Do NOT use a bare `toContain` for the discriminator. A phrase occurring TWICE is only HALF locked
    -- `toContain` is satisfied by the first occurrence, so deleting the other leaves the guard green.
    Use the occurrence COUNT. Do NOT copy the single-character character-class trick from
    `docs-same-os-claims.spec.ts` unless a clause is a genuine ABSENCE; DOCS-07's clauses are
    presence, so an empty forbidden list is the honest default.

    Add TWO `it`s to `packages/github-cache/src/nx-target-inputs.spec.ts` in its existing
    registration-pin shape: one asserting `docs/cross-os.md` is a declared `test` input, and -- if not
    already present from plan 12-01 -- the detector-workflow pin. Use explicit `{workspaceRoot}`
    literals, never a `docs/**` glob: the reason is comment-locked in that file, a glob "would
    silently adopt whatever lands there next".

    Every phrase you intend to pin MUST be short enough to fit on one line of an ~80-column wrapped
    markdown file. `read()` returns raw text, so a phrase spanning a hard wrap matches NOTHING and the
    row is a silent false PASS in the additive direction. Prefer heading text and short noun phrases.
    Task 3 verifies each one against the written file; do not defer that check to a reviewer.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "cross-os" 2>&1 | rg -q -F "cross-os.md"</automated>
  </verify>

  <acceptance_criteria>
    - `packages/github-cache/src/docs-cross-os.spec.ts` exists and resolves the doc with THREE `../`:
      `rg -c -F "'../../../docs/cross-os.md'" packages/github-cache/src/docs-cross-os.spec.ts` >= 1
      and `rg -c -F "../../../../" packages/github-cache/src/docs-cross-os.spec.ts` equals 0.
    - The discriminator literal is NOT re-spelled in the spec:
      `rg -c -F "process.platform" packages/github-cache/src/docs-cross-os.spec.ts` equals 0. The
      spec must read it out of `nx.json`; a hard-coded copy defeats the whole guard.
    - `rg -c -F "targetDefaults.integration.inputs" packages/github-cache/src/docs-cross-os.spec.ts`
      >= 1, and `rg -c -F "toHaveLength(1)" packages/github-cache/src/docs-cross-os.spec.ts` >= 1.
    - The occurrence COUNT is used, not a bare `toContain`, for the discriminator clause:
      `rg -c -F ".split(" packages/github-cache/src/docs-cross-os.spec.ts` >= 1.
    - Neither `__dirname` nor `process.cwd()` appears:
      `rg -c -e "__dirname" -e "process\.cwd\(\)" packages/github-cache/src/docs-cross-os.spec.ts`
      equals 0.
    - The docstring names the registration dependency:
      `rg -c -F "nx.json" packages/github-cache/src/docs-cross-os.spec.ts` >= 1 and
      `rg -c -iF "stale" packages/github-cache/src/docs-cross-os.spec.ts` >= 1.
    - `rg -o -F "docs/cross-os.md" packages/github-cache/src/nx-target-inputs.spec.ts | wc -l` >= 1.
    - OBSERVED RED, transcribed in the SUMMARY: `npx nx run @op-nx/github-cache:test --skip-nx-cache
      -- -t "cross-os"` exits non-zero, and the FIRST failing test is the named existence control
      (Test 1), not a module-load ENOENT. Record which of Tests 2-8 also failed and which passed
      trivially against the empty string -- the trivially-passing ones are exactly why Test 1 is
      asserted first.
    - `npx nx format:check --all` passes.
  </acceptance_criteria>

  <done>
    The drift guard exists with a named existence control first, a single-sourced discriminator
    equality read from `nx.json`, phrase-keyed order and arch/libc clauses, and an occurrence count
    instead of a half-locking `toContain`; both registration pins exist; and the RED was observed as a
    named assertion failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- write docs/cross-os.md, register it, and link it, in ONE commit</name>

  <files>docs/cross-os.md, nx.json, README.md, docs/advanced.md</files>

  <read_first>
    - `docs/trust-and-security.md`, its first ~22 lines. That intro is the form to copy: it names its
      guard spec BY PATH, states "if this document and the code disagree, the code wins", and carries a
      single-sources-of-truth table. Its `##` headings are NUMBERED.
    - `docs/advanced.md` IN FULL for voice, and specifically its fenced YAML blocks that carry trap
      comments INSIDE the fence (the `shell: bash is REQUIRED, not stylistic` block is the model).
      Also note its heading list, so you place the cross-link inside the publish/mirror section where a
      reader actually hits the cross-OS question.
    - `README.md`'s `## Documentation` list. Every entry is
      `- [Title](docs/x.md) -- <one-line what-it-covers>` wrapped at ~80 columns. Mirror it exactly.
    - `.planning/phases/08-nx-task-hash-parity/08-ROOT-CAUSE.md` `## Hand-off to Phase 12 (DOCS-07)`.
      Items 1 through 5 are the checklist, WRITTEN OUT so this phase inherits a list rather than a
      re-read. Item 3's second half is the `## SURFACED, NOT FIXED` section immediately below it.
    - `nx.json`'s `targetDefaults.test.inputs`, to place the new entry.
    - `12-PATTERNS.md` `### docs/cross-os.md (NEW)` -- both analogs, the content-provenance list, and
      the ASCII rule.
  </read_first>

  <action>
    A NEW `docs/cross-os.md` (D-10), never a section inside `docs/advanced.md`: the repo's docs
    convention is one topic per file, and burying a safe-by-default adoption recipe inside a page about
    the opt-in Releases reader would misfile the milestone's headline consumer deliverable. The lazier
    alternative was weighed and declined -- `docs/advanced.md` is ALREADY a `test` input and already
    covered by a shipped guard, so it would satisfy the registration clause for free, but the cost of
    the new file is one `nx.json` line and one nav link and this phase pays a `test` rotation regardless.

    ONE commit: the doc, its registration, and both links. The registration must not lag the doc --
    `{workspaceRoot}/nx.json` is itself a `test` input, so the entry is effective on its OWN commit and
    there is no ordering instruction for anyone to remember. A doc without its registration is a guard
    that replays a stale cached PASS, which is PARITY-08's whole lesson and a defect this repo has
    already shipped once with `typecheck`.

    **`docs/cross-os.md`.** Title it `# Cross-OS caching`. Open with an intro paragraph in
    `docs/trust-and-security.md`'s register: name `packages/github-cache/src/docs-cross-os.spec.ts` by
    path as the guard that fails the build until this file is back in sync, and state that if this
    document and the config disagree, the config wins. Then a single-sources-of-truth table whose one
    row is the platform discriminator, sourced to `nx.json`'s `targetDefaults.integration.inputs`.
    Then NUMBERED `##` sections, so D-11's order is visible to a reader and assertable by an index
    comparison.

    **Section 1 -- the safe default, and it comes FIRST.** The instruction is: declare the platform
    discriminator on ALL cacheable targets, then remove it PER TARGET only after proving that target's
    output is portable. Show the discriminator as a fenced snippet, taking the literal from `nx.json`
    (plan 12-04 already set it) rather than retyping it from memory. Inside the fence, carry the trap
    comments `docs/advanced.md` habitually puts inside its fences:
    (a) why `--no-warnings` and not a shell redirect -- Nx runs a `runtime` input through exactly one
    shell per OS (`%COMSPEC% /C` on Windows, `sh -c` elsewhere), so a redirect breaks the command on
    one OS rather than merely differing, whereas the flag is node's and is identical on both;
    (b) why stderr matters at all -- Nx hashes the command's trimmed stdout AND trimmed stderr,
    concatenated with no separator, so any non-empty stderr silently extends the hashed token, and a
    node warning's text carries the process PID, which would vary the hash on EVERY invocation and
    produce a permanent 100% MISS rather than a one-time rotation;
    (c) what the adopter must VERIFY rather than paste -- that the command emits a NON-EMPTY token and
    that the token DIFFERS across their OSes. State plainly that this repo catches a collapsed
    discriminator with a build-gating two-leg comparison and an adopter has NO such gate, so a silently
    collapsed discriminator would make their target OS-invariant without any signal.
    Also state that `targetDefaults.<target>.inputs` REPLACES the inferred list rather than merging
    into it, so declaring an input means ADDING A LINE, and that a key declared in a project's own
    config sits ABOVE `targetDefaults` and replaces the default's key wholesale.

    **Section 2 -- the portability checklist, framed as how to EARN a removal.** Not a menu of reasons
    to skip the default. FIVE items, and they are INHERITED from
    `08-ROOT-CAUSE.md`'s hand-off section, not re-derived:
    1. A warm local box does NOT compute the hash cold CI published -- measured on all five targets at
       one commit with graph state the only variable; the mitigation is a FULL `nx reset`.
    2. The reset is needed AFTER pulling a fix, not only before: a persisted plugin cache does not
       self-heal, because the file hash still validates so nothing re-derives what it implies. This is
       the single most likely way for a correct fix to look broken.
    3. Deleting the repository's `.nx/` directory does NOT produce a cold state -- the native file
       cache resolves under the OS temp directory, outside the repository. And at Nx 23.1.0 the shared
       cache directory resolves the MAIN worktree root, so a git worktree's "cold" reading is not cold
       either. Both halves ship; the second is item 3's other half and is recorded in the same source.
    4. Document the FULL `nx reset`, never the workspace-data-only form: the narrow form does not stop
       the daemon, so on Windows it can fail with a permission error while the daemon holds the
       workspace-data file open, leaving a WARM graph behind a claim of cold.
    5. To REPRODUCE the measurement rather than repair a box, point the two Nx cache-directory
       environment variables at a temporary directory instead of destroying state. That is cold BY
       CONSTRUCTION -- an empty directory's emptiness is readable BEFORE the measurement -- and it is
       non-destructive, which matters because the stale graph is itself a measurement subject.

    There is NO sixth item. The sixth entry in the source hand-off is STRUCK, was measured FALSE, and
    its own text says what to document instead: nothing. Do not reconstruct it, do not add a
    reassurance about it, and do not mention it -- adding a reassurance about a non-problem is worse
    than silence.

    **Section 3 -- what `process.platform` does not cover.** Name ARCHITECTURE and LIBC, and in the
    same breath state the honest limit: this project cannot exercise either, because every machine here
    is arm64. Keep the three facts in one sentence or two adjacent ones, since the guard asserts they
    are RELATED rather than merely co-present. Do not soften this into a "consider also" bullet -- it
    is the requirement's own wording and it tells the reader where this project's evidence ends.

    **Section 4 -- the one cross-OS hazard Nx already closes for you.** Both streams are trimmed before
    concatenation, so a trailing-newline or CRLF-versus-LF difference in the discriminator's own output
    cannot skew the hash. Worth stating, because it is the one newline hazard an adopter does not have
    to handle.

    Pure ASCII throughout: `--` never an em dash, `->` never an arrow glyph, `*` never a bullet glyph,
    straight quotes only. No contact address of any kind -- the repo's email hygiene is guarded by an
    allowlist-inversion spec and a doc is maintainer-authored content.

    **`nx.json`.** Add `"{workspaceRoot}/docs/cross-os.md"` to `targetDefaults.test.inputs`,
    positioned immediately AFTER the existing `"{workspaceRoot}/docs/configuration.md"` entry. Do not
    reorder or reformat any existing entry -- `nx.json` is a `test` input, so an unrelated reorder is
    diff noise on a hash-rotating file. Use the explicit path, never a `docs/**` glob.

    **`README.md`.** Add one bullet to the `## Documentation` list, in the shipped
    `- [Title](docs/x.md) -- <what it covers>` form wrapped at ~80 columns, placed immediately after
    the Advanced usage bullet and before the Trust and security bullet.

    **`docs/advanced.md`.** Add ONE sentence linking `docs/cross-os.md`, inside the publish / sync and
    cleanup section, where a reader hits the cross-OS question. Additive only: do not reword any
    existing sentence in that file. It carries phrase-keyed assertions from a shipped guard and it must
    not reacquire a same-OS-restore claim.
  </action>

  <verify>
    <automated>npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "cross-os"</automated>
  </verify>

  <acceptance_criteria>
    - Every clause of `docs-cross-os.spec.ts` is GREEN, including the existence control, the
      single-sourced discriminator equality, the section-order comparison, the arch/libc anchored
      clause, the five-item count and both nav clauses:
      `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "cross-os"` exits 0.
    - The doc renders the literal `nx.json` declares, taken from the config and not retyped:
      `node -e "const fs=require('fs');const j=require('./nx.json');const c=j.targetDefaults.integration.inputs.filter(i=>i&&typeof i==='object'&&'runtime' in i)[0].runtime;const d=fs.readFileSync('docs/cross-os.md','utf8');const n=d.split(c).length-1;if(n<1)throw new Error('doc does not render the declared command');console.log('occurrences',n)"`
      exits 0.
    - Section order: `node -e "const d=require('fs').readFileSync('docs/cross-os.md','utf8');const h=[...d.matchAll(/^## \d+\. .*$/gm)].map(m=>m[0]);if(h.length<2)throw new Error('fewer than two numbered sections');console.log(h.join(' | '))"`
      prints the safe-default heading FIRST and the checklist heading SECOND.
    - The checklist has exactly FIVE numbered items and no sixth:
      `node -e "const d=require('fs').readFileSync('docs/cross-os.md','utf8');const s=d.split(/^## /m).find(x=>/^\d+\..*(checklist|earn)/i.test(x));const n=(s.match(/^\d+\. /gm)||[]).length;if(n!==5)throw new Error('checklist items: '+n);console.log('five items')"`
      exits 0.
    - `nx.json` places the new entry adjacent to `docs/configuration.md`:
      `node -e "const i=require('./nx.json').targetDefaults.test.inputs;const a=i.indexOf('{workspaceRoot}/docs/configuration.md');const b=i.indexOf('{workspaceRoot}/docs/cross-os.md');if(b!==a+1)throw new Error('not adjacent '+a+' '+b);console.log('ok')"`
      exits 0, and `rg -c -F "{workspaceRoot}/docs/**" nx.json` equals 0.
    - `rg -c -F "(docs/cross-os.md)" README.md` equals 1 and the bullet matches the shipped form:
      `rg -c "^- \[.+\]\(docs/cross-os\.md\) -- " README.md` equals 1.
    - `rg -c -F "cross-os.md" docs/advanced.md` >= 1, and `git diff HEAD -- docs/advanced.md` shows
      ONLY additions (no `^-` lines other than the diff header).
    - `docs/cross-os.md` is pure ASCII: `rg -c '[^\x00-\x7F]' docs/cross-os.md` returns no matches
      (exit 1).
    - No contact address: `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "governance"`
      exits 0 (the allowlist-inversion email guard).
    - The pre-existing docs guards stay green:
      `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "docs"` exits 0, including
      `docs-same-os-claims` and `docs-adoption`.
    - ONE commit contains the doc, the `nx.json` line and both nav edits:
      `git show --stat HEAD` lists exactly `docs/cross-os.md`, `nx.json`, `README.md`,
      `docs/advanced.md`.
    - `npx nx format:check --all` passes (the doc is Prettier-formatted markdown).
  </acceptance_criteria>

  <done>
    `docs/cross-os.md` exists with the safe default as section 1 and the five-item earn-a-removal
    checklist as section 2, names architecture and libc with the arm64-only limit in the same breath,
    renders the exact declared discriminator with its three in-fence trap comments, is a declared
    `nx.json` `test` input, and is reachable from both README and `docs/advanced.md` -- all in one
    commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: verify every pinned phrase is one-line and MEASURE each clause's red</name>

  <files>packages/github-cache/src/docs-cross-os.spec.ts, docs/cross-os.md</files>

  <read_first>
    - `packages/github-cache/src/docs-cross-os.spec.ts` as written, and `docs/cross-os.md` as written.
    - `12-PATTERNS.md` `## Shared Patterns` `### S-6: A phrase occurring twice is only HALF locked`
      and `### S-7: Observe the RED, never predict it`.
  </read_first>

  <action>
    Two checks, both of which have caught a real defect in this repo before.

    **Check A -- one-line-ness and uniqueness of every pinned phrase.** For each phrase the guard
    pins, run `rg -o -F "<phrase>" docs/cross-os.md | wc -l` and record the count. A count of 0 means
    the phrase spans a hard wrap and the assertion is a SILENT FALSE PASS in the additive direction --
    the guard would stay green while the doc lost the claim. A count of 2 or more means the clause is
    only HALF locked if it uses `toContain`, because `toContain` stops at the first occurrence;
    convert that clause to an occurrence COUNT or re-key it on a phrase measured unique. Use
    `rg -o | wc -l`, never `rg -c`, which counts LINES rather than occurrences. Record every phrase and
    its measured count in the SUMMARY as a table, so a later reviewer can re-run the same commands.

    **Check B -- one MEASURED red per clause.** For each of the guard's substantive clauses, mutate the
    thing it reads, confirm the NAMED assertion fails with the RIGHT cause, and revert. Specifically:
    swap the order of sections 1 and 2 and confirm the ORDER clause fails (not merely some clause);
    delete the architecture token and confirm the D-13 clause fails; delete one checklist item and
    confirm the five-item clause fails; alter one character of the rendered discriminator and confirm
    the equality clause fails naming the value `nx.json` declares; and delete the README bullet and
    confirm the nav clause fails. Five mutations, five named failures, five reverts. Do not predict
    any of them -- run each and transcribe the failure message.

    After every revert, confirm `git status --porcelain` is clean and
    `npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "cross-os"` is green again. A mutation
    left in the tree is worse than no mutation at all.
  </action>

  <verify>
    <automated>git status --porcelain | rg -q '^$' ; npx nx run @op-nx/github-cache:test --skip-nx-cache -- -t "cross-os"</automated>
  </verify>

  <acceptance_criteria>
    - The SUMMARY carries a table with one row per pinned phrase: the phrase, its measured
      `rg -o -F ... | wc -l` count against `docs/cross-os.md`, and whether the clause uses `toContain`
      or an occurrence COUNT. No row may have a count of 0.
    - Any phrase measured at 2 or more occurrences is asserted by COUNT, not by `toContain`. Record the
      conversion if one was needed.
    - Five MEASURED reds are transcribed, each naming WHICH assertion failed and with what message:
      section order, architecture/libc, the five-item count, the discriminator equality, and the README
      nav bullet.
    - After all reverts: `git status --porcelain` is empty, and
      `npx nx run @op-nx/github-cache:test --skip-nx-cache` is green across the whole suite.
    - `git log --oneline -1` shows this task's commit contains at most a spec adjustment (a
      `toContain`-to-COUNT conversion or a re-keyed phrase) -- never a doc edit made to satisfy a
      poorly-chosen phrase. If a phrase could not be measured one-line, the SPEC's phrase changes, not
      the doc's prose.
  </acceptance_criteria>

  <done>
    Every pinned phrase is measured present on exactly one line, every twice-occurring phrase is
    count-asserted rather than half-locked, and five named reds are on record -- observed, not
    predicted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo's docs -> an outside adopter's cache configuration | the only consumer-facing artifact in the phase; a wrong instruction here produces a wrong result in someone else's build, with no gate of ours to catch it |
| `nx.json` -> the drift guard's validity | a doc outside `{projectRoot}` that is not a declared `test` input makes its guard replay a stale cached PASS |

**Delta: one consumer-facing document. No credential, no write path, no code path, no package, no
network egress.** PROJECT.md's constraint holds: this phase's CI and hashing changes are project-local
and the recipe is the ONLY consumer-facing artifact.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Tampering (by proxy) | `docs/cross-os.md` telling an adopter to do something unsafe | medium | mitigate by DESIGN | D-11's section ORDER *is* the control: safe-by-default first, removal-by-proof second, so a reader who stops after section one lands on the safe configuration. The drift guard pins that order by index comparison, and C14/C15 already forbid the doc from recommending fork-PR write tokens or framing retention as poison-containment. Task 3 MEASURES the order clause's red |
| T-12-09 | Tampering | an adopter's discriminator collapsing to one value across OSes | high | mitigate | The doc must tell the adopter to VERIFY a non-empty token that DIFFERS across their OSes, not merely to paste the string. This repo catches a collapsed discriminator with a build-gating two-leg comparison; an adopter has NO such gate, so the instruction is the only control on their side. Asserted by the arch/libc and discriminator clauses plus review |
| T-12-18 | Repudiation | a doc guard that replays a stale cached PASS | medium | mitigate | `docs/cross-os.md` is registered as an `nx.json` `test` input in the SAME COMMIT as the doc, pinned by a literal assertion in `nx-target-inputs.spec.ts`, with the dependency stated in the guard's own docstring. This is PARITY-08's lesson and a defect this repo already shipped once with `typecheck` |
| T-12-19 | Information disclosure | a contact address entering committed content | low | mitigate | No contact address of any kind in the new doc. The repo's allowlist-inversion email guard is asserted green as an acceptance criterion, and it is maintainer-content-scoped by design |
| T-12-04 | Elevation of privilege | `$GITHUB_ENV` writes | critical **if present** | mitigate | CHECKED: this plan writes no workflow YAML. **BLOCKER if any task adds a `$GITHUB_ENV` write of a record- or artifact-derived value** (the T-11-27 shape). A doc snippet that TELLS an adopter to do so would also qualify -- the doc must not contain one |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package installed by this plan.** VERIFIED: `files_modified` contains no `package.json` and no `package-lock.json`. `12-RESEARCH.md`'s Package Legitimacy Audit records the phase installs nothing. Any dependency reached for here is a scope escape and the gate applies from scratch, including a blocking human checkpoint for `[ASSUMED]`/`[SUS]` |
</threat_model>

<verification>
- `npx nx run @op-nx/github-cache:test --skip-nx-cache` is GREEN across the whole suite. This is the
  first plan in the phase after which no guard is intentionally red.
- `npm run typecheck`, `npm run lint`, `npx nx format:check --all` pass.
- ROBUST-04: this plan edits no `serve()`-reachable source (a doc, a config list, two markdown files
  and two spec files). Confirm rather than assume with `npm run check:action` FROM THE MAIN WORKING
  TREE, never a junctioned worktree.
- The five measured reds and the phrase-count table are in the SUMMARY. A predicted-only redness is
  not evidence.

## What this plan explicitly does NOT close

- **Whether the recipe is CORRECT and safe for a consumer to copy.** That is a review judgement about
  prose, not an assertion. The guard proves the doc SAYS the right things; only `/gsd:code-review`
  proves it MEANS them. Surface it as a review item, not as covered.
</verification>

<success_criteria>
- `docs/cross-os.md` exists, safe default first, five-item earn-a-removal checklist second,
  architecture and libc named with the arm64-only limit related in the same breath, the exact declared
  discriminator rendered with its three in-fence trap comments, pure ASCII, no contact address.
- It is an `nx.json` `test` input, adjacent to `docs/configuration.md`, explicit path.
- It is linked from README's Documentation nav and cross-linked from `docs/advanced.md` additively.
- `docs-cross-os.spec.ts` reads the discriminator from `nx.json` and never re-spells it; every clause
  is measured non-vacuous; every pinned phrase is measured one-line.
- Five named reds and one phrase-count table are on record.
- Three commits: `test(12-05): ...`, `feat(12-05): ...`, and a `test(12-05): ...` or
  `docs(12-05): ...` for Task 3.
</success_criteria>

<output>
Create `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-05-SUMMARY.md` when done.
Record: the Task 1 RED output and which clauses passed trivially against the empty string; the exact
section headings written, in order; the phrase-count table; the five mutation reds with their failure
messages; and the `nx.json` insertion index.
</output>
