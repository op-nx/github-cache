---
phase: 12-windows-ci-reuse-o4-consumer-recipe
plan: 06
type: execute
wave: 5
depends_on: ["12-05"]
files_modified:
  - .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md
  - .planning/phases/08-nx-task-hash-parity/deferred-items.md
autonomous: true
requirements: [XOS-05, XOS-04, XOS-08]

must_haves:
  truths:
    - "The O4 claim, its per-target pre-registered counts, and its anti-requirements are on record BEFORE the run, not written after it (D-19, TEST-08)"
    - "D-18: the proof is taken from the FIRST run of a SAME-REPO pull request and from no other vehicle -- not a workflow RE-RUN (on a re-run the ubuntu leg restores the merge-ref entry the first run saved, so it HITs instead of MISS-and-saving and the in-run producer attribution evaporates), and not a temporary `main` push (unnecessary here: `ci.yml` is `on: push` restricted to `main` plus `pull_request`, so a phase-branch push does not trigger CI at all, which makes the PR the ONLY vehicle rather than the preferred one). A record naming any other vehicle, or not naming one, does not close XOS-05."
    - "11-EVIDENCE.md's RESERVED O4 section is filled IN PLACE and the four-row status table at the top of the file is updated in the same edit (D-22)"
    - "The two other O4 references in the same file are reconciled, so the record does not contradict itself"
    - "Every `Cache: n/m hit` line recorded is marked NON-DISCRIMINATING IN BOTH DIRECTIONS beside the line (D-20, OBS-02)"
    - "The record states what this proof does NOT need -- no temporary main push, no mirror row -- so a reader does not look for evidence that never appears"
    - "No verdict is written that the run did not produce: an unobserved O4 stays PENDING rather than becoming a claim"
  artifacts:
    - path: ".planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md"
      provides: "the filled O4 section plus the updated status row"
      contains: "O4 (XOS-04, XOS-05)"
  key_links:
    - from: ".planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md O4 section"
      to: "the FIRST run of the proving pull request"
      via: "per-target `[remote cache]` occurrence counts on each Windows leg, reconciled against the pre-registered numbers below"
      pattern: "\\[remote cache\\]"
---

<objective>
Close O4: pre-register the counts, fill `11-EVIDENCE.md`'s RESERVED O4 section in place, and record
the live observation from the FIRST run of the proving pull request.

Purpose: TEST-08's rule is that every "the job was green" claim is paired with a COUNT that would
differ under the failure hypothesis, named in the PLAN rather than after the run. This phase's
observation is unusually easy to fake to yourself, because XOS-05 says so in its own words: the
success signal for O4 -- every target `[remote cache]`, wall time collapsing to sidecar overhead --
is the IDENTICAL observation to a Windows-only regression being invisible forever. The counts below
are what separate the two.

Output: `11-EVIDENCE.md`'s O4 section filled in place, its four-row status table updated, its two
other O4 references reconciled, and either a recorded verdict or an explicit PENDING.
</objective>

## Phase Goal

**Verbatim from `.planning/ROADMAP.md:523-524`:**

> Windows CI reuses Linux CI's portable artifacts, and an outside project can copy the recipe
> without inheriting a wrong-result risk.

> MVP_MODE discrepancy (ROADMAP's `**Goal**` is not in user-story form) is recorded once in
> `12-01-PLAN.md` `## Phase Goal`. Operator item: `/gsd:mvp-phase 12`.

## Vertical slice

The observation slice. After Task 2 the record states what is claimed and what would falsify it;
after Task 3 it states what was seen. Nothing about the repo's behaviour changes here -- this plan
edits only planning artifacts.

## Artifacts this phase produces

See `12-01-PLAN.md` `## Artifacts this phase produces`. Created by THIS plan: the filled
`## O4 (XOS-04, XOS-05)` section in `11-EVIDENCE.md`.

## PRE-REGISTERED COUNTS (D-19) -- read this before the run, not after

**Derivation, so a mismatch is investigated rather than explained away.** The resolved task set per
target was MEASURED on this workstation at planning time with `nx run-many -t <target> --graph`:

| Command the leg runs | Resolved tasks | Expected `[remote cache]` OCCURRENCES on the Windows leg |
|---|---|---|
| `npm run build` (`nx run-many -t build`) | 1 -- `build` | **1** |
| `npm run typecheck` (`nx run-many -t typecheck`) | 2 -- `typecheck` AND `build` (an inferred `dependsOn`) | **2** |
| `npm run test` (`nx run-many -t test`) | 1 -- `test` (`dependsOn: ["^build"]` resolves to zero tasks in this single-project workspace) | **1** |

**Total expected non-zero `[remote cache]` occurrences across the three Windows legs: 4.**

**Expected ubuntu producer verdict, per target. All three MISS-and-save, and the reason is an edit,
not a guess:**

| Target | ubuntu leg | Why |
|---|---|---|
| `build` | **MISS-and-save** | plan 12-04 edits `packages/github-cache/src/hash-parity/compare.ts`, a NON-spec `src/` file, which is a `build` input. CONTEXT D-19 allowed for a possible `build` HIT on the grounds that a `ci.yml`-only edit does not rotate `build`; that condition does not hold in this phase |
| `typecheck` | **MISS-and-save** | rotated by the new spec files (`typecheck`'s inputs start from `default`, which includes spec files) and by `build`'s rotated declaration outputs |
| `test` | **MISS-and-save** | rotated many times over: `nx.json`, `ci.yml`, `docs/cross-os.md`, the detector workflow registration, the new spec files, `capture-hashes.mjs` and `eslint.config.mjs` are all explicit or implicit `test` inputs |

**One anticipated ambiguity, named in advance so it is not read as a failure.** The ubuntu `typecheck`
job also resolves a `build` task, and the ubuntu `build` job has no `needs:` relationship to it, so
the two race for `nx-cache-<H_build>`. Whichever finishes first executes and saves; the other HITs.
That race predates this phase, is covered by T-12-02, and NEITHER outcome invalidates O4. Record which
one happened; do not treat the `typecheck` job's `build` task HITting as a deviation.

**How to count.** `rg -o -F "[remote cache]" <log> | wc -l`. NEVER `rg -c`, which counts LINES rather
than occurrences. Check the exit code: 0 means hits, 1 means a genuine no-match, and **2 means the
command FAILED** while printing zero lines -- indistinguishable from absence if you read only the
output.

## ANTI-REQUIREMENTS -- each of these would produce a FALSE PASS

Restated here rather than in a distant rationale, because every one of them is a plausible mistake at
the moment of reading the log:

1. **A run that MISSes everything is not a valid O4 proof.** TEST-09's rule, applied to O4. A MISS is
   never self-evidencing in this system: every read fault degrades to a MISS, which is exactly why the
   gate is the literal `[remote cache]` LABEL and not the absence of an error.
2. **A workflow RE-RUN is not the proof.** On a re-run the ubuntu leg restores the merge-ref entry the
   FIRST run saved, so it HITs instead of MISS-and-saving. The Windows HIT would still be against
   Linux-produced bytes, but the producer attribution WITHIN the run evaporates. Phase 11 applied this
   same discipline as T-11-26 and it transfers verbatim: **the FIRST run of the PR, or nothing.**
3. **`Cache: n/m hit` is NOT discriminating, in EITHER direction.** A `0%` prints identically for a run
   with no sidecar at all (observed on run `30169158892`), and a non-zero count includes local hits.
   If the line is recorded at all, it is marked non-discriminating BESIDE the line.
4. **"The job was green" with no paired count is not evidence.** TEST-08.
5. **A green O4 CI is NOT evidence the targets are PORTABLE.** That argument is CIRCULAR and
   `REQUIREMENTS.md`'s Out of Scope table names it as such: a restored task does not execute. It is
   the entire reason the plan 12-03 detector exists. Do not let a green O4 leg be written up as a
   portability finding.

## The vehicle, and why it is the ONLY one (D-18, as corrected)

`ci.yml` is `on: push` restricted to `main`, plus `pull_request`. **A push to the phase branch does
not trigger CI at all.** So a same-repo pull request is not the PREFERRED vehicle -- it is the ONLY
vehicle short of pushing to `main`, which this decision declines.

The original reason recorded in CONTEXT was FALSE and must not be repeated: it claimed a fork PR gets a
read-only Actions cache so both legs would MISS. GitHub's 2026-06-26 changelog says the opposite
verbatim -- any trigger using a non-default-branch scope, `pull_request` included, keeps read-write
caching permission. The read-only rule fires only when an untrusted trigger ALSO runs at the shared
default-branch scope. This repo already measured the scope: both entries of a PR run were written under
`refs/pull/11/merge`.

Two consequences to WRITE DOWN, or a reader looks for evidence that never appears:

- **The PR-scope entry is ephemeral and isolated.** The proof seeds neither `main`'s cache scope nor the
  Releases mirror. That is fine -- the proof is a SAME-RUN property -- but say so.
- **Do not expect a mirror row.** `publish`, `publish-verify`, `dogfood-seed`, `dogfood-verify` and
  `consumer-smoke` are all push-gated and are SKIPPED on a PR run. `11-EVIDENCE.md` already records
  observing exactly that on a rehearsal. O4 does not need mirror warming.

**Opening the PR is an OPERATOR decision, not this plan's work.** CONTEXT records the decision on
opening a PR for the phase branch as a carried operator item. This plan prepares the observation and
records it; it does not open the pull request.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-CONTEXT.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-RESEARCH.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-PATTERNS.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-02-SUMMARY.md
@.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-flight -- re-measure the resolved task sets and close RESEARCH assumption A1</name>

  <files>(no file changes -- measurements recorded in the SUMMARY)</files>

  <read_first>
    - `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-04-SUMMARY.md`, for the
      `check:action` verdict and both sweep counts.
    - `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-RESEARCH.md`
      `## Assumptions Log` rows A1 and A6, and `## Validation Architecture` `### Sampling rate`.
    - `capture-hashes.mjs`'s `readDiscriminatorCommand`, so you can see WHY the per-leg verification
      of the new discriminator is free rather than needing a new instrument.
  </read_first>

  <action>
    Four pre-flight measurements. All are cheap, and each closes something the record would otherwise
    carry as an assumption.

    **A -- the full local battery.** Run `npm run test`, `npm run typecheck`, `npm run lint` and
    `npx nx format:check --all`. All four must pass. This is the wave gate for the whole phase; a red
    here means the proving run would be reddened by something unrelated to O4.

    **B -- the action bundle, from the MAIN working tree.** Run `npm run check:action` and record the
    verdict. NEVER run it from a junctioned worktree: esbuild rewrites 689 module paths there and
    reports a false drift with no source edit. State the outcome explicitly rather than omitting it --
    plan 12-04 touched a non-spec `src/` file, so this is the check that confirms ROBUST-04 was honoured
    rather than assumed.

    **C -- re-measure the resolved task sets, and reconcile them against the pre-registered counts
    above.** For each of `build`, `typecheck` and `test`, run
    `npx nx run-many -t <target> --graph <file>` into a scratch path and count the resolved task ids.
    Confirm 1, 2 and 1 respectively. If any count differs from the pre-registration, the expected
    `[remote cache]` occurrence count for that leg changes with it: record the CORRECTED number in the
    SUMMARY and in the `11-EVIDENCE.md` section BEFORE the run, never after. A pre-registration
    adjusted after the observation is not a pre-registration. Do not write the scratch graph files into
    the repository.

    **D -- close assumption A1 by READING, not by assuming.** A1 is that `--no-warnings` behaves
    identically on `linux/arm64`; it was measured only on `win32/arm64`. It is closed for free by the
    `hash-parity` job, which runs `capture-hashes.mjs` on BOTH legs and whose `readDiscriminatorCommand`
    reads the command out of `nx.json` rather than re-spelling it, so the artifact records the new
    command's raw stdout AND stderr per OS with no new instrument. Once a CI run exists on this tree,
    download `hash-parity-<os>.json` for both legs and record the two `stdout`/`stderr` pairs verbatim.
    Expected: stdout `linux` and `win32` respectively, and stderr EMPTY on both. If stderr is non-empty
    on either leg, that is a FINDING -- record it and stop, because it means the hardening did not close
    the channel it was chosen for. If no CI run exists yet, mark A1 explicitly OPEN in the SUMMARY and
    carry it as a human-verify item; do NOT mark it closed by inference.
  </action>

  <verify>
    <automated>npm run test && npm run typecheck && npm run lint && npx nx format:check --all</automated>
  </verify>

  <acceptance_criteria>
    - All four battery commands exit 0, and the SUMMARY records the test file/test counts so a later
      reader can compare against the 40 files / 856 tests baseline plus this phase's additions.
    - `npm run check:action` was run FROM THE MAIN WORKING TREE and its verdict is recorded verbatim. If
      it drifted, that is a BLOCKER for this plan: `start-cache-server/index.js` had to be regenerated
      in the commit that introduced the drift, so a drift detected here means an earlier plan's commit
      is wrong and must be fixed before the proving run.
    - The three resolved-task-set counts are recorded and reconciled:
      `build` = 1, `typecheck` = 2, `test` = 1. Any deviation is recorded together with the corrected
      expected occurrence count, BEFORE the run.
    - `git status --porcelain` shows no scratch graph JSON and no stray log file.
    - A1 is recorded as CLOSED with both legs' verbatim `stdout`/`stderr` pairs, or explicitly OPEN
      with the reason. It is never recorded as closed by inference.
  </acceptance_criteria>

  <done>
    The tree is green on all four gates, the action bundle's state is recorded rather than assumed, the
    pre-registered counts are reconciled against a fresh measurement, and A1 is either closed by a read
    artifact or explicitly left open.
  </done>
</task>

<task type="auto">
  <name>Task 2: fill 11-EVIDENCE.md's RESERVED O4 section IN PLACE, with the pre-registration</name>

  <files>.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md`. You need FOUR anchors, all located by
      quoted text rather than line number: the four-row `## Headline` status table and its O4 row (which
      currently reads RESERVED); the paragraph immediately below that table, which states explicitly
      what the proved halves did NOT need and is the habit to mirror; the
      `## O4 (XOS-04, XOS-05) -- RESERVED` section itself, which carries its own do-not-fill-and-do-not-
      delete instruction that THIS plan is the authorised discharge of; and the two other places in the
      file that reference O4 -- the `## What remains unobservable` bullet, and the
      calibrated-instruments row for the graph-premise flag.
    - The O1 row of the `## Headline` table, as the register to mirror: the left cell states the CLAIM in
      full, the right cell opens with a bold verdict, then the per-target OCCURRENCE COUNT, then whether
      the pre-registered counts were MET.
    - `12-PATTERNS.md` `### 11-EVIDENCE.md -- fill the RESERVED O4 section (doc artifact)`.
    - This plan's `## PRE-REGISTERED COUNTS` and `## ANTI-REQUIREMENTS` sections above -- they are the
      content of the section you are writing.
  </read_first>

  <action>
    Fill the RESERVED section IN PLACE. Do not create `12-EVIDENCE.md`, do not relocate the section, do
    not duplicate it, and do not delete the reservation text -- convert it, so a reader can see what
    the slot was and what discharged it. TEST-08's own words are that Phase 12 appends the O4 row to
    THIS record.

    The section must carry, in this order:

    1. **The CLAIM, stated in full**, in the same shape as the O1 row: the three `windows-11-arm` legs
       log the literal `[remote cache]` per target against entries the ubuntu legs saved in the same
       run, with the `needs:` edges ordering producer before consumer.
    2. **The PRE-REGISTERED counts**, copied from this plan: the per-target expected occurrence counts
       (1 / 2 / 1, total 4) with the resolved-task-set derivation that produces them, and the per-target
       expected ubuntu verdict (MISS-and-save on all three) with the EDIT that causes each rotation.
       Include the named ambiguity: the ubuntu `typecheck` job's own `build` task races the ubuntu
       `build` job and either outcome is legitimate.
    3. **The ANTI-REQUIREMENTS**, all five, verbatim in substance from this plan.
    4. **The vehicle and its corrected reason**: `ci.yml` is `on: push` for `main` plus
       `pull_request`, so a phase-branch push does not trigger CI at all and a same-repo PR is the only
       vehicle short of pushing to `main`. State that the previously-recorded fork-PR read-only-cache
       reason is FALSE, with the changelog's own wording as the replacement, so the corrected claim
       carries a replacement reason rather than a bare deletion.
    5. **What this proof does NOT need**, mirroring the file's existing habit directly under its status
       table: no temporary `main` push; no warm Releases mirror; no mirror row, because the push-gated
       publish and dogfood jobs are SKIPPED on a PR run and the PR-scope entry is ephemeral and
       isolated to `refs/pull/N/merge`.
    6. **The write decision**, recorded as FORCED rather than chosen, cross-referencing the
       `10-SECURITY.md` Q1 append that plan 12-02 made, and noting the sharpening: on a run where every
       Windows task RESTORES, the Windows legs write NOTHING, so the second-producer fact is a
       CAPABILITY that materialises on a Windows MISS.
    7. **The VERDICT slot**, left as `PENDING -- live-CI, first run of the proving PR` until Task 3
       records an observation. Do not pre-write a verdict.

    Update the `## Headline` status table's O4 row in the SAME edit (D-22), from RESERVED to
    `PENDING -- live-CI` (or to the observed verdict if Task 3 has already run). Then reconcile the two
    other O4 references in the file so the record does not contradict itself: the
    `## What remains unobservable` bullet that says O4 is RESERVED and owned by Phase 12, and the
    calibrated-instruments row that says the graph-premise flag's subject moves under XOS-04 -- the
    latter should now point at the correction plan 12-02 actually made, in `ci.yml` and at both
    `capture-hashes.mjs` sites.

    Note in the section, as the file itself notes elsewhere, that this file is NOT an Nx input, so
    nothing about it can serve a stale cached PASS -- and equally, no spec can usefully guard it, which
    is why none is written. Phase 11's Nyquist audit already declined that.
  </action>

  <verify>
    <automated>rg -q -F "PRE-REGISTERED" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md && rg -q -F "refs/pull" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md && rg -c -F "RESERVED -- Phase 12" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md | rg -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - The section is filled IN PLACE: `rg -n -F "## O4 (XOS-04, XOS-05)" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md`
      returns exactly ONE line, and no file named `12-EVIDENCE.md` exists anywhere under
      `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/`.
    - The status table row no longer reads RESERVED:
      `rg -c -F "RESERVED -- Phase 12" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md` equals 0,
      and the O4 row of the `## Headline` table contains either `PENDING` or a bold verdict.
    - The pre-registration is present with all three per-target expectations:
      `rg -c -F "PRE-REGISTERED" ...11-EVIDENCE.md` >= 1, and the section contains the tokens
      `MISS-and-save`, `[remote cache]` and `compare.ts`.
    - All five anti-requirements are present, each separately greppable: the section contains
      `re-run`, `Cache:`, `CIRCULAR` (or the executor's recorded equivalent, transcribed in the
      SUMMARY), `paired with a COUNT`, and `MISSes everything`.
    - The corrected vehicle reason is present with its replacement, not a bare deletion:
      `rg -c -F "refs/pull" ...11-EVIDENCE.md` >= 1 and the section states that a phase-branch push does
      not trigger CI.
    - The two other O4 references are reconciled: `rg -n -F "O4" ...11-EVIDENCE.md` shows no remaining
      line asserting the section is unfilled or reserved.
    - No verdict is claimed that Task 3 has not recorded: if Task 3 has not run, the section's verdict
      slot reads `PENDING`.
    - `git show --stat HEAD` lists only `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md` for
      this task's commit.
  </acceptance_criteria>

  <done>
    The RESERVED O4 section is discharged in place with the claim, the pre-registered counts and their
    derivation, all five anti-requirements, the corrected vehicle reason, the does-not-need list, the
    forced write decision, and a PENDING verdict slot -- and the file's three other O4 references agree
    with it.
  </done>
</task>

<task type="auto">
  <name>Task 3: record the live O4 observation from the FIRST run of the proving PR</name>

  <files>.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md, .planning/phases/08-nx-task-hash-parity/deferred-items.md</files>

  <read_first>
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md`'s O4 section as written by Task 2 --
      the pre-registered counts you are reconciling against.
    - `.planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md`'s O1 and O3 sections, for the shape an
      observation record takes in this file: per-target counts, the run identifier, and an explicit
      statement of which pre-registered numbers were MET.
    - `.planning/phases/08-nx-task-hash-parity/deferred-items.md` item 1 -- the unattributed `test`
      failure at `69bd1b7`.
  </read_first>

  <action>
    Record what the run showed. If no run exists yet, record that and stop -- do NOT infer.

    **If the FIRST run of a same-repo pull request on this tree is available:**

    For each of the three Windows legs, download or open the leg's log and count the literal
    `[remote cache]` OCCURRENCES with `rg -o -F "[remote cache]" <log> | wc -l`. Check `rg`'s exit code
    explicitly -- 2 is a FAILED command printing zero lines. Record, per leg: the occurrence count, the
    target names each occurrence was attached to (named individually, never as an aggregate), the Nx
    hash observed, and whether the pre-registered number was MET.

    For each of the three ubuntu legs, record whether it MISSED-and-saved or HIT, and note which
    outcome the ubuntu `typecheck` job's own `build` task took. Record the run identifier and the run
    URL.

    If you record the `Cache: n/m hit` line at all, mark it NON-DISCRIMINATING IN BOTH DIRECTIONS
    BESIDE the line -- a `0%` prints identically for a run with no sidecar at all, and a non-zero count
    includes local hits. It is context, never the gate.

    Then write the verdict into the section and into the `## Headline` table's O4 row, in the register
    the O1 row uses: a bold verdict, then the per-target occurrence counts, then whether all
    pre-registered counts were MET. If any count differs from the pre-registration, record the
    DIFFERENCE and its investigation -- do not adjust the pre-registration to match the observation, and
    do not round a partial result up to PROVEN.

    **Refuse these four shortcuts explicitly**, and say in the record which vehicle produced the
    numbers: a workflow RE-RUN is not the proof (on a re-run the ubuntu leg HITs the entry the first
    run saved and the in-run producer attribution evaporates); an all-MISS run is not a proof; a green
    job with no paired count is not a proof; and a green O4 leg is NOT evidence the targets are
    portable.

    **If the `test` target FAILS on any leg**, capture the full output BEFORE any re-run -- the
    `69bd1b7` `test` failure has never been attributed and its output was discarded once already, after
    which Nx's flaky-task detection recorded a FAILURE and a SUCCESS at the same hash. Append the
    observation to `.planning/phases/08-nx-task-hash-parity/deferred-items.md` item 1 with the run URL,
    the Nx version, the Node version and the leg's OS. Do not re-run to obtain a green.

    **If no run exists yet:** leave the verdict as `PENDING -- live-CI, first run of the proving PR`,
    write nothing that resembles an observation, and surface the item for human verification with the
    exact commands above so whoever opens the PR can execute them. Opening the pull request is a carried
    OPERATOR decision, not this plan's work. Fabricating or inferring counts here is the single worst
    outcome available in this plan.
  </action>

  <verify>
    <human-check>
      Open the FIRST run of the proving pull request (never a re-run). For each of `build-windows`,
      `typecheck-windows` and `test-windows`, confirm the leg's log carries the literal
      `[remote cache]` the pre-registered number of times: 1, 2 and 1 respectively, total 4. Confirm
      each of the three ubuntu legs MISSED and SAVED. Confirm the recorded numbers in
      `11-EVIDENCE.md`'s O4 section match what the logs show, and that any `Cache: n/m hit` line is
      marked non-discriminating beside it. If the counts differ, confirm the difference is recorded as a
      difference rather than absorbed into the pre-registration.
    </human-check>
    <automated>rg -q -F "O4" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md && rg -q -e "PENDING -- live-CI" -e "PROVEN" .planning/phases/11-live-proofs-o1-o2-o3/11-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - The O4 section's verdict is EITHER a recorded observation with per-target occurrence counts and a
      run identifier, OR the literal `PENDING -- live-CI, first run of the proving PR`. There is no
      third state, and no sentence in the section asserts a HIT that no log showed.
    - If observed: each of the three Windows legs has its own recorded occurrence count, with the
      target names NAMED INDIVIDUALLY rather than aggregated, and an explicit MET / NOT MET against the
      pre-registered 1 / 2 / 1.
    - If observed: the record names the vehicle and states it was the FIRST run of the PR, not a re-run.
    - If observed: every `Cache: n/m hit` line quoted is followed by an explicit non-discriminating
      marking on the same line or the line immediately after.
    - If `test` failed anywhere: `.planning/phases/08-nx-task-hash-parity/deferred-items.md` item 1
      carries the appended observation with the run URL, Nx version, Node version and leg OS, and the
      SUMMARY records that no re-run was performed first.
    - If not observed: the SUMMARY carries a human-verify item with the exact `rg -o -F "[remote cache]"
      <log> | wc -l` commands and the pre-registered numbers, so the operator does not have to
      reconstruct them.
    - No file outside `.planning/**` is modified by this task.
  </acceptance_criteria>

  <done>
    The O4 record states either what was observed, with per-target counts reconciled against the
    pre-registration and the vehicle named, or an explicit PENDING with the observation procedure
    handed to the operator. Nothing in between.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pull-request run -> the `refs/pull/N/merge` Actions-cache scope | the scope both legs of the proving run share; isolated from the base branch and from other PRs |
| the observation -> the permanent record | the risk here is epistemic rather than technical: a claim entering `11-EVIDENCE.md` that no log supports cannot be re-derived later, because XOS-04 permanently destroys the attribution it would rest on |

**Delta: none on any code or credential path.** This plan edits two `.planning/**` documents. Neither
is an Nx input; neither ships to a consumer.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-20 | Repudiation | the O4 record in `11-EVIDENCE.md` | high | mitigate | An unsupported verdict here is PERMANENT: XOS-04 destroys the producer attribution a later session would need to re-derive it, which is exactly why Phase 11 captured the attribution at proof time. Mitigated by pre-registering the counts in the PLAN before the run, by the five anti-requirements, by a PENDING state that is an allowed terminal outcome, and by a `<human-check>` that re-counts from the logs independently |
| T-12-06 | Elevation of privilege | the proving run being a `pull_request` event with read-write cache | low | accept | Unchanged from C1's recorded posture. `pull_request` keeps read-write caching per the 2026-06-26 changelog, and the load-bearing control is Actions-cache SCOPE isolation: a PR run writes only to `refs/pull/N/merge`, unreadable from the base branch or from other PRs. The proof therefore cannot poison `main`'s scope even in principle |
| T-12-01 | Tampering | the second-producer fact being recorded as resolved rather than moved | medium | accept and RECORD | The record must state BOTH halves -- the `needs:` edge removes the RACE, not the second producer -- and must carry the sharpening that a fully-restored Windows leg writes nothing. Plan 12-02 made the `10-SECURITY.md` append; this plan cross-references it rather than restating it differently |
| T-12-03 | Information disclosure | run logs quoted into a committed planning document | low | mitigate | Quote only occurrence counts, target names, hashes and run URLs. Do NOT paste raw runner log excerpts containing absolute runner paths or token-adjacent lines. Never quote a `Bearer` header or an `NX_SELF_HOSTED_*` value, even masked |
| T-12-04 | Elevation of privilege | `$GITHUB_ENV` writes | critical **if present** | mitigate | CHECKED: this plan writes no workflow YAML and adds no CI step. **BLOCKER if any task adds a `$GITHUB_ENV` write of a record- or artifact-derived value** (the T-11-27 shape, this repo's most recent live Critical finding) |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package installed by this plan.** VERIFIED: `files_modified` is two `.planning/**` documents; no `package.json`, no `package-lock.json`. `12-RESEARCH.md`'s Package Legitimacy Audit records the phase installs nothing. Any dependency reached for here is a scope escape and the gate applies from scratch, including a blocking human checkpoint for `[ASSUMED]`/`[SUS]` |
</threat_model>

<verification>
- `npm run test && npm run typecheck && npm run lint && npx nx format:check --all` all pass (Task 1's
  battery, and the phase's wave gate).
- `npm run check:action` from the MAIN working tree reports no drift, verdict recorded.
- `11-EVIDENCE.md`'s O4 section is filled in place, the status table row is updated, and the file's
  other O4 references agree with it.
- The recorded verdict is either an observation with per-target counts or an explicit PENDING.

## Human-verify items this plan surfaces (end-of-phase)

`workflow.human_verify_mode` is `end-of-phase`, so these surface as verification items rather than as
blocking checkpoints:

1. **The O4 observation itself** -- the FIRST run of a same-repo pull request, per-target
   `[remote cache]` occurrence counts reconciled against 1 / 2 / 1. Opening the PR is a carried
   operator decision.
2. **The detector going green on a real `windows-11-arm` runner** -- a POST-MERGE first-run close,
   because `workflow_dispatch` only fires for a workflow file present on the default branch. This repo
   already uses that pattern by name for `ppe`, `dogfood-*` and `consumer-smoke`.
3. **RESEARCH assumption A1** -- `--no-warnings` on `linux/arm64`, closed by READING both legs'
   `hash-parity-<os>.json` once a CI run exists, not by inference.
4. **Whether the recipe is correct and safe for a consumer to copy** -- a review judgement, handled by
   `/gsd:code-review`, not by any assertion.
</verification>

<success_criteria>
- The pre-registered counts are in the PLAN and in `11-EVIDENCE.md` BEFORE any observation.
- `11-EVIDENCE.md`'s RESERVED O4 section is discharged in place; no `12-EVIDENCE.md` exists; the
  four-row status table and both other O4 references are reconciled in the same work.
- All five anti-requirements are on record at the point the observation is made.
- The verdict is an observation with counts, or an explicit PENDING with the procedure handed over.
- Two or three commits: `docs(12-06): ...` for Task 2, `docs(12-06): ...` for Task 3, and Task 1's
  measurements recorded in the SUMMARY (no commit needed if it changed no file).
</success_criteria>

<output>
Create `.planning/phases/12-windows-ci-reuse-o4-consumer-recipe/12-06-SUMMARY.md` when done.
Record: the four pre-flight results including the `check:action` verdict and the three resolved
task-set counts; A1's status with both legs' stream pairs if available; the O4 verdict as written; and
every human-verify item still open, with its exact reproduction command.
</output>

