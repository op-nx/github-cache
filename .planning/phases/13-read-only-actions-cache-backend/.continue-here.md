---
context: phase
phase: 13-read-only-actions-cache-backend
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-08-02T11:29:15.965Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure in the session that
> produced this handoff. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GSD state handlers corrupt tracked files -- `state.advance-plan`,
      `state.add-decision`, `state.update-progress` and `state.record-metric` each damaged
      `.planning/STATE.md` on EVERY plan this phase -- after ANY `state.*` call, re-read STATE.md
      and repair before staging it.
- [ ] CONSTRAINT: Audit gates must be reached by their dedicated agent, never self-certified --
      `secure-phase` and `validate-phase` both short-circuit past their auditor on a clean-looking
      path and have the ORCHESTRATOR write the artifact -- spawn `gsd-security-auditor` and
      `gsd-nyquist-auditor` REGARDLESS of the short-circuit.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GSD handler corruption of STATE.md | Fired on all six plans. `state.advance-plan` regressed `last_activity_desc` to a stale plan id; `state.update-progress` wrote `completed_phases`/`percent` as 2/29 against a body saying 6/7 and 86%, and emits a Unicode block-character bar; `state.add-decision` injects U+2014 into a repo that bans non-ASCII and needs `--summary` (it drops the `--decision` body); `state.record-metric` and `state.add-decision` both REJECT positional args. | blocking | After every `state.*` call, re-read STATE.md and repair before staging. Use named flags only. Never pipe handler JSON into a file. |
| Self-certifying an audit gate | `secure-phase` skips its auditor and writes SECURITY.md directly when `threats_open:0 AND register_authored_at_plan_time:true`; `validate-phase` writes VALIDATION.md in the orchestrator when its gap analysis finds nothing, spawning the auditor only to FILL gaps. Both paths look clean and both are self-certification. | blocking | Spawn `gsd-security-auditor` / `gsd-nyquist-auditor` and let IT reach the verdict. The orchestrator only routes and commits. |
| Prose in a scanned file is input to its own criterion | Hit FOUR times: 13-02 once, 13-03 twice (a JSDoc line put the knob's first occurrence above the branch check so an `indexOf` order test failed on correct code; a comment containing `never === 'true'` tripped a zero-count grep banning that string), 13-04 once (a criterion whose only match was the comment documenting the ban -- unsatisfiable). | advisory | After writing ANY comment on a file a mechanical criterion scans, re-run that criterion. Prefer criteria scoped to assertion arguments, not raw file text. |
| A red CI job is not proof the gate fired | `ci.yml:527` is a pre-existing bare `exit 1` (sidecar readiness poll) inside every Windows job block. A job can redden for that reason with the gate never reached -- and a spec clause matching `/exit 1/` was green BEFORE this phase existed. | advisory | Assert the count COMPARISON line, never `exit 1`. When observing a red job, confirm WHICH STEP failed via the jobs API before claiming the gate fired. |
| Nx serves a stale cache PASS | `nx test github-cache` returned a 1/1 cache hit rather than executing. This repo already has a documented `typecheck` stale-cache false pass (LINT-04 exists because of it). | advisory | Run gating checks with `--skip-nx-cache` when the result is load-bearing evidence. |
| IDE TypeScript LSP is not authoritative | Emitted false errors after every single plan, including two mutually contradictory diagnostics on adjacent lines (each naming the other symbol as the correct one). All were stale; `tsc --build` was clean. | advisory | Verify with `npx nx test github-cache` and `npx nx typecheck github-cache --skip-nx-cache`. Never act on the diagnostics feed. |
| Decision bullets must close bold on the colon line | `check.decision-coverage-plan` is BLOCKING and returned `could-not-parse` with 2 of 11 decisions read, because wrapped bullets closed `**` on a later line. It reads as a coverage gap; it is a format artifact. | advisory | Write `- **D-NN (LABEL):** text` -- the closing `**` on the SAME line as the colon. Letter suffixes (`D-02a`) are accepted. |
| `rg -h` is HELP, not `--no-filename` | A pipeline silently dumped ripgrep's usage text instead of results. Flag parity with grep does NOT hold for `-h`. | advisory | Use `--no-filename`. Check `rg` exit codes: 0 hits, 1 no-match, 2 command FAILED. |
| `git commit -m` fails on this Dev Drive | ReFS on D: -- `COMMIT_EDITMSG: Invalid argument`. | advisory | Write the message with the Write tool, then `git commit -F <path>`. |

<current_state>
Phase 13 execution and verification are COMPLETE. This pause is deliberate -- it exists only so the
three post-completion gates run in a fresh session with clean context, not because anything is stuck.

All six plans executed. `gsd-verifier` confirmed 7/7 requirements against the codebase (not against
the SUMMARYs) and returned `human_needed` on exactly one point: the gate's FAIL path had never run on
a real runner. **That item is now CLOSED by live observation** -- the maintainer chose proof over
precedent.

The phase goal is proven live TWICE:
- **Pass path** -- run `30744366870`, counts **1/2/1** against a pre-registered 1/2/1, and the run's
  `headSha` (`631a2e7`) IS the pre-registration commit, so the prediction was provably in the tree
  the run measured.
- **Fail path** -- run `30745558383`, `build-windows` red **at the gate step** at count 0 carrying the
  gate's own `::error::`, with `typecheck-windows` and `test-windows` green at 2 and 1 as a same-run
  positive control. Throwaway branch and draft PR #13 already closed and deleted; verified
  `--skip-nx-cache` has zero occurrences in the phase branch's `ci.yml`.

Local battery green: 975/975 tests, `check:action` clean from the main tree, typecheck + lint clean.
</current_state>

<completed_work>

- 13-01: seven requirement IDs registered (VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09,
  DOCS-10); ROADMAP 44 -> 51, REQUIREMENTS 50 -> 57 -- Done (`c35ad00`)
- 13-02: composed read-only Actions-cache factory; the file-scoped drift guard passed with ZERO
  edits -- Done (`658301e`)
- 13-03: `CACHE_READ_ONLY` as the LAST branch in `selectBackend` -- Done (`3a83e01`)
- 13-04: consumer contract 8 -> 9 knobs, fifth selection outcome documented; the intended
  `public-surface.spec.ts` RED landed rather than being pre-empted -- Done (`79aea97`)
- 13-05: three legs converted, counts gated at a floor of 1, seven stale sites corrected, exactly two
  survivors kept -- Done (`6a6370b`)
- 13-06: counts pre-registered BEFORE the proving run; `13-EVIDENCE.md` written -- Done (`a78365f`)
- verify_phase_goal: `13-VERIFICATION.md`, 7/7 -- Done (`4d18acb`)
- Gate FAIL path observed live; `13-EVIDENCE.md` ADDENDUM -- Done (`ee67055`)
</completed_work>

<remaining_work>

1. `/gsd:secure-phase 13` -- spawn `gsd-security-auditor`; do NOT take the inline short-circuit.
2. `/gsd:validate-phase 13` -- spawn `gsd-nyquist-auditor`; do NOT take the no-gap inline write.
3. `/gsd:extract-learnings 13` -- LAST, so it captures what the two audits surface.
4. Global-learnings bridge, only if `features.global_learnings` is true: `learnings.copy` is a silent
   no-op because it reads ONE top-level `.planning/LEARNINGS.md` that extraction never creates. Build
   a TEMP one from `13-LEARNINGS.md` (code-fence-aware transform, script in the scratchpad), run the
   copy, `rm` it -- one uninterrupted sequence. `.planning/` is tracked and this path is NOT
   gitignored, so NEVER `git add` it.
5. Optional: push the four unpushed commits (`ee67055`, `4d18acb`, `a78365f`, `440b824`). 13-06 left
   them local on purpose -- pushing fires another CI run on PR #12.
</remaining_work>

<decisions_made>

- **ROLE, not EVENT, selects the read-only backend.** The event-derived alternative and the
  "event band + role signal" hybrid were both rejected: TRUST-05's asymmetry is a ONE-WAY RATCHET, so
  a read-only `pull_request` base can never be widened back for `dogfood-seed` (which asserts a hard
  PUT 200). Event-derived would have reverted quick task `260801-vyy`'s CR-18 fix unfixably.
- **The gate's soundness is INDUCTIVE, not per-run.** A leg that cannot write can only get a
  `[remote cache]` label from a genuine restore of the ubuntu producer's entry -- independent of job
  ordering. XOS-08's `needs:` edge is LIVENESS only. Do not let a comment rest correctness on it.
- **Both factories in ONE file**, because the drift guard is file-scoped and a sibling split blinds it.
- **The knob is checked LAST**, because branch order is the narrowing guarantee.
- **Stale-site scope bounded to seven**, keeping two true survivors -- the research's proposed blanket
  `git grep ... returns nothing` sweep would have deleted correct claims.
- **Sequential-on-main instead of worktree isolation** for this phase: 13-02/13-03 need a TRUE
  `check:action` verdict, and a worktree has no `node_modules` (junctioning it makes esbuild report
  false drift). Run-scoped only -- `workflow.use_worktrees` was NOT changed.
</decisions_made>

<blockers>
- None. Nothing is stuck.
</blockers>

## Required Reading (in order)

1. `.planning/phases/13-read-only-actions-cache-backend/13-CONTEXT.md` -- locked decisions D-01..D-09,
   including the D-02a rejection of event-derived selection.
2. `.planning/phases/13-read-only-actions-cache-backend/13-EVIDENCE.md` -- what is proven, what is
   pre-registered, what is deferred. Read the ADDENDUM for the fail-path observation.
3. `.planning/phases/13-read-only-actions-cache-backend/13-VERIFICATION.md` -- the 7/7 findings and
   the one item this session closed.
4. `.planning/phases/13-read-only-actions-cache-backend/13-VALIDATION.md` -- the requirement-to-test
   map `validate-phase` audits against.
5. `.planning/THREAT-MODEL.md` -- the C1-C18 CREEP ledger `secure-phase` verifies against.

## Infrastructure State

- Branch `gsd/v0.0.2-os-invariant-cross-os-sharing`, 4 commits ahead of origin, 375 ahead of
  `origin/main`. PR #12 is open for it.
- Working tree clean except an untracked `.gitkeep` in the phase dir (predates this work; logged in
  `deferred-items.md`, suggested resolution is deletion -- the directory now holds many tracked files).
- Draft PR #13 and branch `gsd/13-gate-failpath-proof` are CLOSED and DELETED. No perturbation
  reached the phase branch.
- No async jobs, no background servers.

<context>
The phase removed a launderable gate rather than mitigating it. The interesting part is that the same
defect class the phase exists to eliminate -- a guard that reads as coverage without being it -- kept
appearing in the guards written to BUILD that gate. Four separate times a mechanical criterion was
satisfied by prose rather than by the property it meant to check, and once a clause stayed green when
the row it tested was deleted. Every one was caught by mutation measurement, not by reading. That is
the habit to carry into the audits: mutate, then believe.

Two items stay open BY DESIGN and are recorded rather than hidden -- Case B (a PR restoring from the
base scope; unprovable by a landing commit that rotates all three hashes) and assumption A1 (403 log
noise; unexercised because every task hit). Both are in ROADMAP's Phase 13 Live-CI block with stated
observation conditions. Do not let an audit mark them closed.

One follow-up sits outside this phase: `packages/github-cache/src/dogfood-cross-os.spec.ts:349-352`
conflates OBS-04 (the publish-leg all-restore-MISS tripwire) with what is actually OBS-02's subject.
Two executors and the verifier independently declined to edit it because the referent of "its" is
ambiguous and one reading would delete a true claim. The verifier traced it to Phase 12's `fee5fbe`,
so it has a diagnosis now, not an ambiguity.
</context>

<next_action>
Start with: `/gsd:secure-phase 13` -- and spawn `gsd-security-auditor` to reach the verdict rather
than taking the workflow's inline short-circuit, even though `threats_open` looks clean.
</next_action>
