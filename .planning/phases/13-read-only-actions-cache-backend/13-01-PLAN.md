---
phase: 13-read-only-actions-cache-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/THREAT-MODEL.md
autonomous: true
requirements: [VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09, DOCS-10]

must_haves:
  truths:
    - Every one of the seven Phase 13 requirement IDs has a body in REQUIREMENTS.md under its own category section, a traceability row in REQUIREMENTS.md, and a traceability row in ROADMAP.md.
    - ROADMAP.md's `**Requirements**:` line for Phase 13 names all seven IDs on ONE physical line, so `init.plan-phase 13` returns them all.
    - ROADMAP.md's coverage assertion, per-phase counts and source-category tally all agree with each other after the addition.
    - THREAT-MODEL.md records the knob's narrowing asymmetry as a Residual note and records that NO new control row was added, with the reason.
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/THREAT-MODEL.md
  key_links:
    - "ROADMAP `**Requirements**:` line -> `init.plan-phase 13` -> every downstream GSD gate that reads phase_req_ids"
    - "REQUIREMENTS.md Coverage tally -> ROADMAP.md Coverage Validation assertion (the two must not contradict)"
---

<objective>
Register the seven requirement IDs this phase creates, in both authoritative files, before any code
claims to satisfy them. Record the CONTEXT.md stances research discharged (D-02c, D-03, D-08, D-09)
where they have a home, and carry the Q4 Case-B observation as a named live-CI item per the Phase 12
precedent.

Purpose: Phase 13 has NO requirement IDs today. ROADMAP.md says `TBD (run /gsd-plan-phase 13)` and
neither traceability table has a Phase 13 row, while ROADMAP.md asserts "44/44 v0.0.2 requirements
map to exactly one phase". Every other plan in this phase cites these IDs; if they are never
registered, all seven are orphaned and the milestone audit's three-source cross-reference cannot
close them.

Output: seven requirement bodies, fourteen traceability rows, reconciled tallies in both files, one
THREAT-MODEL.md Residual-notes bullet, and the Case-B live-CI item.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-read-only-actions-cache-backend/13-CONTEXT.md
@.planning/phases/13-read-only-actions-cache-backend/13-RESEARCH.md
</context>

## Artifacts this phase produces

Newly-created symbols across all Phase 13 plans. Drift verification must not treat these as
pre-existing:

| Symbol | Kind | Home |
|---|---|---|
| `createReadOnlyActionsCacheBackend` | exported factory | `packages/github-cache/src/backend/actions-cache-backend.ts` |
| `CACHE_READ_ONLY` | consumer env knob | `packages/github-cache/src/lib/select-backend.ts`, `src/test/consumer-contract.ts`, `.github/workflows/ci.yml`, `docs/*` |
| `VER-08`, `VER-09`, `TRUST-14`, `XOS-09`, `TEST-11`, `DOCS-09`, `DOCS-10` | requirement IDs | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` |
| `Gate on the cross-OS remote-cache label count for this leg` | `ci.yml` step name (replaces the Record step) | `.github/workflows/ci.yml` x3 |
| `13-EVIDENCE.md` | phase evidence record | `.planning/phases/13-read-only-actions-cache-backend/` |

**ZERO new files under `packages/github-cache/src/`.** A sibling `*actions-cache*.ts` module is an
automatic reject (RESEARCH.md Q1).

<tasks>

<task type="auto">
  <name>Task 1: Register the seven requirement bodies and traceability rows in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:258-340` — the `### Cache-version hardening (VER)` section, ending at VER-07/ROBUST-04. Copy the body shape: `- [ ] **ID**: <claim>. <mechanism>. <what breaks silently without it>.`
    - `.planning/REQUIREMENTS.md:341-388` — `### Cross-OS outcomes (XOS)`, ending at XOS-07.
    - `.planning/REQUIREMENTS.md:411-449` — `### CREEP trust posture (TRUST)`, ending at TRUST-13.
    - `.planning/REQUIREMENTS.md:450-470` — `### Documentation (DOCS)`, ending at DOCS-08.
    - `.planning/REQUIREMENTS.md:471-523` — `### Verification (TEST)`, ending at TEST-10.
    - `.planning/REQUIREMENTS.md:604-676` — the Traceability table and the `**Coverage:**` tally paragraph.
    - `.planning/phases/13-read-only-actions-cache-backend/13-RESEARCH.md:164-188` — the proposed ID table and its "Propose only, do NOT edit" note (this task is what discharges that note).
  </read_first>
  <action>
Append one requirement body per ID into its own category section, each as an UNCHECKED `- [ ]` item
placed after that section's current last item. Use the existing body voice: state the claim, then
the mechanism, then the silent failure the requirement exists to prevent. Cite the deciding
CONTEXT.md decision ID in the body text.

- **VER-08** into `### Cache-version hardening (VER)`. Per D-01: the read-only Actions-cache backend
  is the SAME implementation as the writable one's read path. Exactly ONE `cache.restoreCache(...)`
  READ call site survives in the package, and `createActionsCacheBackend` composes
  `createReadOnlyActionsCacheBackend` rather than duplicating it. Both factories live in
  `packages/github-cache/src/backend/actions-cache-backend.ts` — a sibling module is invisible to
  the file-scoped ordered-member guard at `actions-cache-backend.spec.ts:517-531`, so a second
  version computation would ship with the whole suite green. The VER-04 cwd/`GITHUB_WORKSPACE`
  guards and the VER-07 construction-time `mkdirSync` live in the shared core; `put`'s second
  `mkdirSync` and its `lookupOnly` probe (the SECOND `restoreCache`, write path only) stay on the
  write path unchanged. Record that D-09's escape hatch was NOT taken and why it was live: the
  roadmap allowed the phase to SHRINK to a documented decision if no shape satisfied D-01's
  criterion. One does, and it needs zero changes to the guard that enforces it — the ordered
  `cache.*` member array is byte-identical under the composed shape, so the criterion is not
  something this phase establishes, it is something this phase must not break.
- **VER-09** into the same section. The `@actions/cache` drift guard widens from FILE scope to
  PACKAGE scope: exactly one non-spec module under `packages/github-cache/src/` imports
  `@actions/cache`. Closes the future-sibling evasion the file-scoped scan structurally cannot see.
- **TRUST-14** into `### CREEP trust posture (TRUST)`. Per D-02a and D-02b: the producer-vs-consumer
  ROLE signal is a strictly-narrowing env knob read by `selectBackend` as its LAST branch.
  `selectBackend.length` stays 0. The guarantee is BRANCH ORDER, not validation — every branch above
  has already returned a read-only backend or thrown, so the knob's only reachable effect is
  writable -> read-only. Proven behaviourally over an enumerated env table, not by comment.
  Record that a construction-time `readOnly` factory argument stays REJECTED (D-03) and that the
  witness / `created_at` variant is dead at the API level (D-02c: `@actions/cache@6.2.0`'s
  `restoreCache` returns only the matched key string).
- **XOS-09** into `### Cross-OS outcomes (XOS)`. Per D-04 and D-05: all three Windows legs
  (`build-windows`, `typecheck-windows`, `test-windows`) construct the read-only backend, and their
  `[remote cache]` counts are GATED at `>= 1` per leg — a FLOOR, not an exact pin, because these
  counts are emitted by Nx's task graph and legitimately vary with it. The soundness argument is
  INDUCTIVE, not per-run: once the consumer legs cannot write, no Windows-produced entry for those
  hashes can ever exist, so any HIT is NECESSARILY Linux-produced. Keep that SEPARATE from liveness
  — XOS-08's `needs:` edge is why the entry is present at all. The gate's failure message names both
  causes a zero can have so a red gate is actionable.
- **TEST-11** into `### Verification (TEST)`. Per D-07: `dogfood-cross-os.spec.ts` pins the SEMANTIC
  change per leg — the knob write is present, the count COMPARISON is present, and the ungated-count
  revert marker is absent. Asserting a bare `exit 1` is forbidden and the reason is recorded:
  `ci.yml:527` is already `exit 1` (the sidecar readiness poll) inside `jobBlock('build-windows')`,
  so an `/exit 1/` clause is GREEN before the phase changes anything. Non-vacuity is MUTATION-proven
  and the measurement recorded in the clause comment, matching the `cacheClient` precedent at
  `dogfood-cross-os.spec.ts:886-893`.
- **DOCS-09** into `### Documentation (DOCS)`. Per D-06: every site justifying the ungated counts is
  corrected in the SAME commit that gates them. The list is NINE, not four — three `ci.yml` per-leg
  rationale comment blocks, three `ci.yml` printed `echo` strings (which are CODE and survive the
  spec's `#`-strip, and are what an operator actually reads in the job log), one
  `dogfood-cross-os.spec.ts` reason string, plus the two stale outcome-count sites at
  `docs/configuration.md:92` and `docs/versioning.md`. Record the SCOPE LIMIT: two further
  `RECORDED, never gated` sites in `ci.yml` (the `runner.debug` record and the `integration` matrix
  leg's count) remain factually TRUE and MUST NOT be swept.
- **DOCS-10** into `### Documentation (DOCS)`. The new knob is documented in `docs/configuration.md`
  (table row AND a `### <KNOB>` resolution section) and listed in `docs/versioning.md`'s consumer
  env-knob group; `docs/advanced.md`'s "How the backend is selected" grows from four outcomes to
  five INCLUDING its hardcoded prose count; and every remaining "four backend-selection outcomes"
  site is corrected. The contract change lands as a reviewable diff in the explicit
  `public-surface.spec.ts` literal, never a snapshot.

Then add seven rows to the Traceability table (`:610-661`) in the established
`| ID | Phase 13 | Pending |` shape, appended after the `DOCS-07 | Phase 12` row.

Then update the `**Coverage:**` paragraph at `:663-665`: 50 -> 57 requirements, 50 -> 57 mapped, and
append `Phase 13 = 7` to the distribution list. Verify the arithmetic: 7 + 9 + 11 + 12 + 7 + 4 + 7 =
57. Leave the historical "43/43 mapped" sentence at `:606-608` alone — it is a dated statement about
roadmap creation, not the live count — and instead append a dated amendment line to the file tail
(after `:676`) recording the Phase 13 addition, matching the existing amendment-note voice.
  </action>
  <acceptance_criteria>
    - `git grep -c -E "^- \[ \] \*\*(VER-08|VER-09|TRUST-14|XOS-09|TEST-11|DOCS-09|DOCS-10)\*\*" -- .planning/REQUIREMENTS.md` prints `7`.
    - `git grep -c "| Phase 13 |" -- .planning/REQUIREMENTS.md` prints `7`.
    - `git grep -n "57 requirements, 57 mapped" -- .planning/REQUIREMENTS.md` returns exactly one line.
    - `git grep -n "Phase 13 = 7" -- .planning/REQUIREMENTS.md` returns exactly one line.
    - Every one of the seven bodies names at least one CONTEXT.md decision ID (`D-01`, `D-02a`, `D-02b`, `D-02c`, `D-03`, `D-04`, `D-05`, `D-06` or `D-07`).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const ids=['VER-08','VER-09','TRUST-14','XOS-09','TEST-11','DOCS-09','DOCS-10'];const missing=ids.filter(i=>!new RegExp('^- \\\\[ \\\\] \\\\*\\\\*'+i+'\\\\*\\\\*','m').test(s)||!new RegExp('\\\\| '+i+' \\\\| Phase 13 \\\\|').test(s));if(missing.length)throw new Error('unregistered: '+missing.join(','));if(!/57 requirements, 57 mapped/.test(s))throw new Error('coverage tally not reconciled');console.log('REQUIREMENTS.md OK')"</automated>
  </verify>
  <done>All seven IDs have a body and a traceability row; the coverage tally reads 57/57 with `Phase 13 = 7` in the distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the ROADMAP Phase 13 requirements line, traceability rows and tallies, and register the Case-B live-CI item</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md:574-630` — the whole Phase 13 section, including the `**Requirements**: TBD` line at `:579` and the `Plans:` list at `:628-630`.
    - `.planning/ROADMAP.md:516-517` and `:571-572` — the Phase 11 and Phase 12 `**Live-CI close**:` blocks. This is the precedent shape for the Case-B item.
    - `.planning/ROADMAP.md:636-681` — the Traceability table, ending at the `DOCS-07 | Phase 12` row.
    - `.planning/ROADMAP.md:683-705` — the Coverage Validation assertion, the per-phase counts, the `Total mapped` arithmetic line and the source-category tally.
    - `.planning/phases/13-read-only-actions-cache-backend/13-RESEARCH.md:642-722` — Q4's verdict, the Case A/B table, and the four-step Case-B reproduction procedure with its Assumption A2 caveat.
  </read_first>
  <action>
The planner already filled three placeholders in this section: the `**Requirements**:` line, the
`**Plans:**` count, the per-plan list, and a SHORT `**Live-CI close**:` block. **VERIFY them, do not
re-author them.** In particular re-run `gsd-tools query init.plan-phase 13` and confirm
`phase_req_ids` echoes all seven IDs — `init.plan-phase` reads that line and silently drops every ID
past a newline, so a line that got wrapped by a later edit truncates the phase's requirement set
with no error. If it wrapped, unwrap it.

EXPAND the existing `**Live-CI close**:` block. It currently names both items in summary; give the
Case-B item its full procedure. State plainly that
     this phase's own landing commit CANNOT reproduce it: the commit edits
     `packages/github-cache/src/**/*.ts` (a declared `build` input at `nx.json:114`, reaching
     `typecheck` and `test` the same way) and `.github/workflows/ci.yml` plus two `docs/` files (all
     declared `test` inputs at `nx.json:70,62,64`), so all three hashes rotate and every leg takes
     the intra-run merge-ref path — Case A, which proves liveness and nothing about the base-scope
     read. The procedure: after Phase 13 lands on `main`, open a PR whose diff touches no declared
     input of `build` or `typecheck` (a `.planning/`-only diff qualifies), then observe that
     `build-windows` and `typecheck-windows` still report `count >= 1` while their ubuntu producers
     logged a HIT and wrote nothing. Record Assumption A2 as the thing to verify first — confirm
     with `npx nx show project github-cache --json` that `.planning/**` is in no target's input set
     before opening that PR, rather than assuming it. Note the fallback if the read half does NOT
     hold: gate on `push` only and keep the counts as diagnostics on `pull_request`; the BACKEND
     SHAPE is unaffected either way.

Append seven rows to the Traceability table after the `DOCS-07 | Phase 12` row at `:681`, in the
established `| ID | Phase 13 | <one-line note> |` shape.

Reconcile the Coverage Validation block:
  - `:685` assertion `44/44` -> `51/51`.
  - `:690-697` per-phase counts: append `- Phase 13: 7 (VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09, DOCS-10)`.
  - `:699-700` `Total mapped: 7 + 7 + 8 + 11 + 7 + 4 = 44` -> `7 + 7 + 8 + 11 + 7 + 4 + 7 = 51`.
  - `:699-700` source-category tally: VER 6 -> 8, XOS 8 -> 9, TRUST 4 -> 5, DOCS 2 -> 4, TEST 3 -> 4,
    total 44 -> 51. Verify: CORR 5 + LINT 6 + PARITY 5 + VER 8 + XOS 9 + RETAIN 1 + TRUST 5 + DOCS 4
    + TEST 4 + OBS 4 = 51.
  - Append a dated note under the existing `**Count corrected 2026-07-30.**` paragraph recording the
    Phase 13 addition, so the two prior corrections and this one read as one history.

Use scoped `Edit` calls only. Never `Write` this file — a whole-file write destroys every phase
entry outside the diff window.
  </action>
  <acceptance_criteria>
    - `git grep -n -E "^\*\*Requirements\*\*: VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09, DOCS-10\.$" -- .planning/ROADMAP.md` returns exactly one line.
    - `gsd-tools query init.plan-phase 13` returns a `phase_req_ids` value containing all seven IDs.
    - `git grep -c "| Phase 13 |" -- .planning/ROADMAP.md` prints `7`.
    - `git grep -n "51/51 v0.0.2 requirements" -- .planning/ROADMAP.md` returns exactly one line, and `git grep -n "= 51" -- .planning/ROADMAP.md` returns the two reconciled tally lines.
    - The Phase 13 section contains a `**Live-CI close**:` block naming both the gate observation and the Case-B deferral, and the Case-B text states that the landing commit rotates all three hashes.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');const line=s.split('\n').find(l=>l.startsWith('**Requirements**: VER-08'));if(!line)throw new Error('Phase 13 requirements line missing');['VER-08','VER-09','TRUST-14','XOS-09','TEST-11','DOCS-09','DOCS-10'].forEach(i=>{if(!line.includes(i))throw new Error('ID off the single line: '+i);if(!new RegExp('\\\\| '+i+' \\\\| Phase 13 \\\\|').test(s))throw new Error('no traceability row: '+i)});if(!/51\/51 v0\.0\.2 requirements/.test(s))throw new Error('coverage assertion not reconciled');if(!/Live-CI close/.test(s.slice(s.indexOf('### Phase 13'))))throw new Error('Case-B live-CI item missing');console.log('ROADMAP.md OK')"</automated>
  </verify>
  <done>Phase 13's requirements line is one physical line carrying all seven IDs and `init.plan-phase 13` echoes them; seven traceability rows exist; every tally reads 51; the Case-B live-CI item is registered with its procedure and its A2 caveat.</done>
</task>

<task type="auto">
  <name>Task 3: Record the knob's narrowing asymmetry in THREAT-MODEL.md, and record that no control row was added</name>
  <files>.planning/THREAT-MODEL.md</files>
  <read_first>
    - `.planning/THREAT-MODEL.md:84-117` — the `## Residual notes` section, its stated criterion ("keep only what has no canonical home") and the bullet shape: `- **<Bold claim + parenthetical status>.** <2-4 sentences of mechanism.>`
    - `.planning/THREAT-MODEL.md:60-83` — the C1-C18 control table, so the no-new-row decision is made against the actual ledger rather than from memory. C1 in particular ("a blocked write is a benign 409/no-op").
    - `.planning/phases/13-read-only-actions-cache-backend/13-RESEARCH.md:1124-1146` — the per-control assessment and the recommended action.
  </read_first>
  <action>
Add exactly ONE bullet to `## Residual notes`. Add NO new control row: this phase strictly REDUCES
capability, opens no attack surface and introduces no new trust boundary, so the ledger's own
criterion ("keep only what has no canonical home") is not met by a control row. Record that
no-row decision IN the bullet, explicitly, so a security auditor does not read the ledger's silence
as an omission.

The bullet's content, in the file's voice:
  - `CACHE_READ_ONLY` is a strictly-narrowing consumer knob whose only reachable effect is to remove
    `put` from the constructed backend.
  - The guarantee comes from BRANCH ORDER in `selectBackend` — the check is LAST, after every
    existing narrowing branch, so every path that could narrow has already returned and the knob
    cannot resurrect the Releases branch, the fail-closed throw, or the memory-degrade branch. It is
    control flow, not validation, and moving the check earlier would break it.
  - Truthiness (not `=== 'true'`) is the FAIL-SAFE direction for a one-way ratchet: a typo'd value
    still narrows, whereas an exact-string comparison would let a typo silently restore the writable
    backend.
  - C1 is ADJACENT but not contradicted: C1's 409 describes a write blocked AT THE STORE; this
    blocks one layer earlier, so `server.ts:124-129` answers the contract's 403 at the protocol
    boundary and `saveCache` is never attempted. That 403 is the ESTABLISHED behaviour of the two
    existing read-only outcomes, not a new one, and C1's 409 path on the writable backend is
    untouched.
  - Per D-08, record that read-only against the SAME store CI writes contradicts none of the three
    stances it appears to touch: PROJECT.md's "Local read-write mode" Out of Scope line bans LOCAL
    WRITE, while this adds a CI-side READ-ONLY position — strictly less capability, in the same
    direction that stance points; TRUST-05 is scoped to RW-vs-RO-by-construction and is satisfied as
    long as the signal can only narrow; and CORR-01 is about OS-namespacing and is already
    superseded by the v0.0.2 OS-invariant decision, untouched here. This is a RECORD, not a
    re-litigation.
  - C8 runs backwards from the intuition and is worth one sentence so nobody "fixes" a non-problem:
    a read-only leg no longer refreshes an entry's clock by WRITING, but GitHub's eviction policy is
    keyed on ACCESS ("removed if not accessed in over 7 days") and a restore IS an access — so there
    is no retention regression.
  </action>
  <acceptance_criteria>
    - `git grep -c "CACHE_READ_ONLY" -- .planning/THREAT-MODEL.md` prints `1` or more, and every match sits below the `## Residual notes` heading.
    - `git grep -c -E "^\| C19 " -- .planning/THREAT-MODEL.md` prints `0` — no new control row was added.
    - The bullet names branch ORDER as the guarantee and states the no-new-control-row decision with its reason.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/THREAT-MODEL.md','utf8');const i=s.indexOf('## Residual notes');if(i<0)throw new Error('Residual notes section missing');const head=s.slice(0,i),tail=s.slice(i);if(head.includes('CACHE_READ_ONLY'))throw new Error('knob recorded outside Residual notes');if(!tail.includes('CACHE_READ_ONLY'))throw new Error('knob bullet missing');if(/^\| C19 /m.test(s))throw new Error('a new control row was added; the decision was NO row');console.log('THREAT-MODEL.md OK')"</automated>
  </verify>
  <done>One Residual-notes bullet records the knob's asymmetry, names branch order as the guarantee, and records the deliberate no-new-control-row decision. The C1-C18 table is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> downstream GSD gates | `init.plan-phase` parses `**Requirements**:`; a truncated or absent line silently orphans requirements from every later audit |
| planning record -> security auditor | THREAT-MODEL.md's silence is read as "assessed and empty"; an unrecorded no-row decision is indistinguishable from an omission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-R1 | Repudiation | ROADMAP `**Requirements**:` line | medium | mitigate | Single physical line; acceptance criterion re-runs `init.plan-phase 13` and asserts all seven IDs survive the parse |
| T-13-01-R2 | Repudiation | THREAT-MODEL.md ledger silence | low | mitigate | The no-new-control-row decision is written INTO the Residual-notes bullet with its reason, not left implicit |
| T-13-01-I1 | Information Disclosure | planning-doc edits | low | accept | These three files carry no credential and no secret; they are already tracked and public |
| T-13-SC | Tampering | npm/pip/cargo installs | high | accept | This phase performs NO package-manager install. `package.json` and `package-lock.json` are untouched and that is load-bearing — a dependency change would rotate task hashes and breach the phase's own OUT OF SCOPE line. RESEARCH.md's Package Legitimacy Audit records "Not applicable" for the same reason |
</threat_model>

<verification>
- `npx nx test github-cache` stays green — this plan edits only `.planning/**`, which is in no
  target's declared Nx input set, so no task hash rotates and no spec observes these files.
- `gsd-tools query init.plan-phase 13` returns `phase_req_ids` containing all seven IDs.
- The two coverage tallies do not contradict each other: ROADMAP asserts 51 (roadmapped subset),
  REQUIREMENTS.md asserts 57 (full defined set). Both are correct and both were reconciled here; the
  difference is the seven non-roadmapped IDs (PARITY-06/07/08, ROBUST-04, RETAIN-05 and friends)
  that predate this phase.
</verification>

<success_criteria>
- Seven requirement bodies exist in REQUIREMENTS.md, unchecked, each in its own category section,
  each citing at least one CONTEXT.md decision ID.
- Fourteen traceability rows exist (seven per file).
- ROADMAP's Phase 13 `**Requirements**:` line is one physical line and survives `init.plan-phase`.
- ROADMAP tallies read 51/51 and are internally consistent; REQUIREMENTS.md reads 57/57.
- The Case-B live-CI item is registered with its procedure, its A2 verification step, and its
  fallback.
- THREAT-MODEL.md gains one Residual-notes bullet and zero control rows.
</success_criteria>

<output>
Create `.planning/phases/13-read-only-actions-cache-backend/13-01-SUMMARY.md` when done
</output>
