---
quick_id: 260719-3el
title: Add worktree strategy to AGENTS.md
date: 2026-07-19
mode: quick-full
status: ready
must_haves:
  truths:
    - AGENTS.md documents when a worktree node_modules can safely be a junction/symlink to the main tree (deps-unchanged + independent plans).
    - AGENTS.md documents when to just npm ci / run sequential-on-main (deps changed, or sequentially-dependent plans, or small-workspace default).
    - The strategy covers node_modules/.cache, node_modules/.vite, and .nx cache race behavior.
    - The strategy states GSD isolation="worktree" does not create the junction (custom pre-dispatch step).
    - The correction is present: there is no VITE_CACHE_DIR env var; cache isolation is the Vite cacheDir config option.
  artifacts:
    - AGENTS.md worktree strategy section, placed OUTSIDE the nx-managed <!-- nx configuration start/end --> block.
  key_links:
    - AGENTS.md
    - .gitignore
    - package.json
---

# Plan: Add worktree strategy to AGENTS.md

## Context

Prior session gave a (partly garbled) correction that worktrees CAN share the main
tree's node_modules via a junction. Research (260719-3el-RESEARCH.md) verified the
claims against the live repo and found one correction (no VITE_CACHE_DIR env var).
This plan writes the verified strategy into AGENTS.md.

## Task 1: Append worktree strategy section to AGENTS.md

- **files:** AGENTS.md
- **action:** Add a `## Git worktree strategy` section AFTER the
  `<!-- nx configuration end-->` marker (line 23) so Nx's auto-update of the managed
  block never clobbers it. Content: decision rule (deps-changed / deps-unchanged+independent /
  sequentially-dependent), junction commands (Windows `New-Item -ItemType Junction`,
  unix `ln -s`), shared/race-prone resources (`node_modules/.vite`, `node_modules/.cache`,
  `.nx/`), the cacheDir correction, and the small-workspace default (npm ci / sequential).
- **verify:** `git grep -n "Git worktree strategy" AGENTS.md` returns a hit outside the
  nx-managed block; section mentions `node_modules/.vite`, `node_modules/.cache`, junction,
  and `npm ci`.
- **done:** AGENTS.md contains an accurate, self-contained worktree strategy covering all
  requested resources.

## Execution notes

- Run sequential-on-main (single plan, no deps change, no parallelism to gain) - the
  exact case the documented strategy says NOT to worktree-isolate.
