---
quick_id: 260719-in3
type: quick
mode: quick-full
files_modified:
  - package-lock.json
must_haves:
  truths:
    - "package-lock.json is in sync with package.json for a strict `npm ci` on BOTH ubuntu-24.04-arm and windows-11-arm"
    - "The Linux-side WASM-fallback optional deps (@emnapi/core + @emnapi/runtime at 1.11.1 under @rolldown and 1.11.2 under @oxc-resolver) are present in the lockfile"
    - "No win32 optional bindings were dropped; @actions/cache@6.2.0 and @actions/core@3.0.1 pins are intact"
    - "The main-push CI run is fully green, including the dogfood-seed PUT and the dogfood-verify cross-job cache HIT"
  artifacts:
    - path: "package-lock.json"
      provides: "cross-OS-complete, npm-regenerated lockfile"
      contains: "@emnapi/core"
---

## Objective

Repair the cross-OS `package-lock.json` drift (missing `@emnapi` WASM-fallback optional
deps) that fails `npm ci` on every CI job and blocks the Phase 2 dogfood cache-HIT canary.
Regenerate via a clean Linux resolve; prove both platforms; confirm the live HIT.

## Tasks

<task type="auto">
  <name>Task 1: Regenerate the lockfile from a clean Linux resolve and verify both platforms</name>
  <files>package-lock.json</files>
  <action>
    Regenerate in a Linux arm64 node:24 container (node_modules masked):
    `docker run --rm -v <repo>:/app -v /app/node_modules -w /app node:24 sh -c "rm -f package-lock.json && npm install --package-lock-only"`.
    Confirm @emnapi/core+runtime 1.11.1 (@rolldown wasm32-wasi) and 1.11.2 (@oxc-resolver
    wasm32-wasi) are present, win32 bindings retained, @actions pins exact. Then prove
    installability WITHOUT rewriting the lockfile: `npm ci` in Docker (Linux) AND `npm ci`
    locally (Windows) -- both exit 0. Run the full local gate (nx test/typecheck/build,
    fallow:ci, format:check, test:act). Do NOT run `npm install` on Windows afterwards
    (it re-prunes). Commit only package-lock.json.
  </action>
  <verify>
    <automated>npm ci (Linux Docker + Windows) exit 0; nx run-many -t test typecheck build exit 0; fallow:ci exit 0; format:check exit 0</automated>
    <human-check>main-push CI run: dogfood-verify reports a cache HIT with matching bytes</human-check>
  </verify>
  <done>The lockfile installs cleanly on both CI platforms, the full gate is green, and the live main CI run reports a real cross-job cache HIT.</done>
</task>

## Success Criteria

CI `npm ci` passes on both arm64 platforms, the dogfood canary reports a live cross-job
cache HIT, and no pinned dep or platform binding regressed.
