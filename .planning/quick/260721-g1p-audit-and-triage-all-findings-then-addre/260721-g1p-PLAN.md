---
quick_id: 260721-g1p
title: Address v0.0.1 PR #3 review findings (all tiers) + wire integration tests + green CI
status: in-progress
scope: everything (all Important + all Suggestions from the 6-aspect review)
branch: gsd/v0.0.1-greenfield-rebuild
pr: 3
---

# Quick Task 260721-g1p - Address PR #3 review findings

Triage input: 6 verified review reports (code, silent-failure, tests, types, comments, simplify).
User triage decision: fix EVERYTHING; wire real integration tests (do not delete the job).

## Waves (atomic commits; run `nx test github-cache` between waves)

### Wave 1 - Verified bugs
- C1 (Critical) `backend/actions-cache-backend.ts` put(): capture `saveCache` return; on `-1` do a `restoreCache` existence probe to disambiguate benign already-exists from a swallowed fault; surface non-'stored' so the server returns 500. @actions/cache 6.2.0 swallows 5xx/network/denied/finalize and returns -1.
- I3 `lib/local-context.ts`: add `child.stdin?.on('error', () => {})` before `.end()` (never-crash contract).
- I4 operator mismatch: align `server.ts` (`>`) and `publish-mirror.ts` (`>=`) on the 2 GiB cap (make both `>`).

### Wave 2 - Silent-degradation guards
- I2 `backend/releases-backend.ts`: 5s absolute deadline aborts real asset downloads -> use a larger/inactivity bound for the download leg; keep 5s for metadata/list.
- mirror all-MISS: `publish/publish-mirror.ts` warn/fail when `mirrored==0 && skipped==all && total>0`.
- `releases-backend.ts` warnOnce: include HTTP status class so persistent 401/403 is distinguishable.
- `serve.ts` resolvePort: hard-fail an explicitly-supplied-but-unparseable port (keep 0 fallback for omitted only).
- `cleanup/cleanup.ts`: guard `NaN` created_at (treat as expired / warn) so a malformed timestamp can't defeat retention.

### Wave 3 - Dedup + simplify (I7, I8, simplifier)
- I7 dedup `GetResult` (publish-mirror re-declares backend/types).
- I8 `SHARD_TAG_PREFIX` const in retention.ts; cleanup imports it.
- extract `statusOf` to `lib/octokit-status.ts` (cleanup + publish-mirror).
- `releases-backend.ts` `assertOkOrAbsent` helper for the 3x 404/throw block.
- `serve.ts` spread instead of manual get-forward; lazy-shard single sentinel; drop select-backend re-export shim.

### Wave 4 - Type design
- brand `Hash` (thread through backend + naming helpers); `CacheOs` union for cachePlatform.
- trust/sync gates return `{ trusted, reason }` discriminated union; update callers + tests.

### Wave 5 - Coverage (I5, I6)
- I5 make `action/index.ts` importable (extract runPublish/createPublishClient, guard auto-run with import.meta.url) + negative tests: sync-gate-first, keyless-row filter, dogfood fail-loud, setSecret.
- I6 with-hash-lock re-lock/eviction-identity test; read-back + cleanup throw tests.

### Wave 6 - Integration tests (I1)
- Add a real `integration` target + cross-OS `*.integration.spec.ts` so the ci.yml integration matrix exercises something; fix the misleading comment.

### Wave 7 - Hygiene + comments
- package.json deps: SAFE handling only (keep runtime resolution; adjust only if public-surface + docs-adoption gates stay green).
- ppe/action.yml actionlint: pin SHA/checksum instead of curl|bash on mutable tag.
- serve.ts SIGTERM single handler.
- comment drift: action.yml "that is Phase 6" -> point at start-cache-server/action.yml; github-identity line-ref; .fallowrc mirror count (3 not 2); stale future-tense phase comments; memory-backend test-only note.

## Exit
- `nx run-many -t build typecheck test lint` green locally.
- Push; all 9 pull_request CI jobs green.
- Update PR #3 body per ship (verification + tech-debt sections).
- SUMMARY.md + STATE.md quick-tasks row + docs commit.
