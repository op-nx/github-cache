---
context: quick
quick_id: 260726-4cc
slug: audit-and-triage-proposals-1-4-then-appl
task: 0
total_tasks: 4
status: planned_not_executed
last_updated: 2026-07-26T01:26:00.000Z
branch: gsd/debug-windows-publish-one-asset
head: 24a2597
---

# Resume: EXECUTE quick 260726-4cc. Do not re-plan.

The plan is written and **passed independent plan-check with zero issues**. The triage half is
finished: all four proposals APPLY at HIGH confidence, nothing dropped. **Nothing has been
executed** -- no source, doc, or test file is modified.

Resume by executing `260726-4cc-PLAN.md` tasks 1-4 in order. Do NOT re-triage and do NOT re-plan.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Hardcoding `scanned = 37` | 37 is the ROW count. After Task 2 the same measured run scans 25 DISTINCT hashes. A 37 assertion breaks AT the Task 2 commit and destroys the bisect-safety the user explicitly asked for. | blocking | Never assert 37 in a test or doc. Derive expected `scanned` from the fixture's distinct-hash count. The plan's must_haves state this. |
| Trusting the debug file's E4 counts | E4's mirrored/skipped pairs (ubuntu 6/32, windows 2/24) do NOT close arithmetically, because `skipped` INCLUDES `readMisses` (`publish-mirror.ts:185-186` increments both). CONTEXT.md's illustrative summary line has the same defect. | blocking | Use ROW counts only: 37 rows/leg, ubuntu 36 hit + 1 miss, windows 25 hit + 12 miss, 25 distinct keys, 12 dual-version. Research section "Data-quality warning" is authoritative. |
| Presenting `readMisses` as a sibling of `skipped` | It is a strict SUBSET. Sibling rows make every reader double-count. Research called this the single design decision Proposal 1 most needs to get right. | blocking | The label carries the relation: `restore-MISS (of skipped)`. |
| "Improving" the all-miss gate | The predicate at `publish-mirror.ts:264` is the ONLY automated detector for a regressed Actions-cache read scope -- the failure the debug investigation proved is otherwise invisible. Research proved it is already equivalent under dedup. | blocking | Leave it BYTE-IDENTICAL. Any edit contradicts the research and is a plan violation. |
| Reordering tasks 1 and 2 | Task 2 redefines `hashes.length` from rows to distinct hashes. `scanned` must exist and be tested before its denominator moves. | blocking | Task 1 first, Task 2 second. Non-negotiable. |
| Committing a drifted action bundle separately | `check:action` regenerates `start-cache-server/index.js`. A drifted bundle in its own commit breaks bisect. | advisory | No drift is expected (`entry.ts` imports `serve()` only; `publish-mirror.ts` is unreachable from it, verified twice). If `check:action` fails, stage the regenerated bundle in the SAME commit. |
| `git commit -m` on this repo | Fails with COMMIT_EDITMSG "Invalid argument" -- D: Dev Drive (ReFS). | advisory | Write the message to the session scratchpad, use `git commit -F <path>`. |
| Briefing an agent to return TEXT only | Seven agents this session did real work then went idle without returning anything. Every one whose output survived had been told to write a FILE. | advisory | Name an explicit output path in every agent brief; check the filesystem before concluding an agent failed. |

<current_state>
Branch `gsd/debug-windows-publish-one-asset`, HEAD `24a2597`, 1 commit ahead of `origin/main`,
UNPUSHED, no PR. Working tree clean after this wip commit.

Quick 260726-4cc artifacts, all committed in the wip commit: `CONTEXT.md`, `RESEARCH.md`,
`PLAN.md`. No `SUMMARY.md` or `VERIFICATION.md` yet -- correct, execution has not run.
</current_state>

<completed_work>
- Init (`--full --auto`), quick_id `260726-4cc`.
- Discuss (`--auto`): CONTEXT.md. Safe gray areas auto-locked; TWO recorded UNRESOLVED per the
  trap-quadrant rule (HIGH impact + NOT-HIGH confidence is never auto-locked) and routed to
  research with a PRE-COMMITTED drop condition for Proposal 2.
- Research: RESEARCH.md. Both UNRESOLVED items closed. Q1 settled at HIGH confidence. Also caught
  an arithmetic error in the debug file's E4 and in CONTEXT.md's illustrative numbers.
- Plan: PLAN.md -- 4 tasks, must_haves, per-task 8-command battery gate, bisect-safety section.
- Plan-check: **PASSED, zero issues.** The checker verified all 8 spec line numbers byte-for-byte,
  `docs/advanced.md` at `nx.json:54`, the bundle's freedom from publish-mirror refs, and that
  `Hash` is a branded string so `[...new Set()]` dedups by value.
</completed_work>

<remaining_work>
1. **TASK 1** (lands FIRST) -- `PublishResult` gains `scanned` + `readMisses`; `action/index.ts`
   emits 5 summary rows with the miss row labelled `restore-MISS (of skipped)`. RED = the 8
   existing `toEqual` literals in `publish-mirror.spec.ts`. Also adds the first-ever assertion on
   the publish summary's row set in `action/index.spec.ts` (`cleanup.spec.ts` precedent).
2. **TASK 2** (lands SECOND) -- dedup the hash enumeration to distinct hashes, with a WHY comment.
   Gate stays byte-identical. RED = two new duplicate-key specs.
3. **TASK 3** -- `docs/advanced.md`: the expected per-OS publish asymmetry, scoped to task-hash
   assets.
4. **TASK 4** -- `.planning/research/PITFALLS.md` Pitfall 7: correct the stale zstd clause and the
   dead `uploadHash` symbol. Soften no MUST-NOT.
5. Verify (`gsd-verifier`) -> `260726-4cc-VERIFICATION.md`.
6. STATE.md Quick Tasks row + final docs commit.
</remaining_work>

<decisions_made>
- All four proposals APPLY; nothing dropped. The triage was real -- Proposal 2 had a pre-committed
  DROP condition and survived it on evidence.
- Proposal 2's dedup is safe and the gate needs NO adjustment. `actionsCache.get(hash)` derives
  both arguments from `hash` alone and `version` is discarded by `listCacheEntries` before the
  engine sees it, so every row sharing a key returns an identical outcome. `readMisses` increments
  0 or m_h times for a hash, never partially, so `readMisses` and `hashes.length` are weighted sums
  over the SAME multiplicity vector and multiplicity cancels. Today's gate already means "every
  DISTINCT hash missed". The dangerous direction was hunted and proven impossible.
- Proposal 1 first, Proposal 2 second.
- `readMisses` is a strict SUBSET of `skipped`; the label carries the relation.
- One atomic commit per proposal; the full battery green at EVERY commit, not just the last.
- Quick 260726-4cc commits to the debug branch on purpose -- report and fixes in one unit.
- Out of scope: cross-OS mirroring of another leg's entries, and U2.
</decisions_made>

<blockers>
None technical. One open user decision, non-blocking: the PR shape for this branch (see below).
</blockers>

## Required Reading (in order)

1. `.planning/quick/260726-4cc-.../260726-4cc-PLAN.md` -- the contract. Read `must_haves`, the
   per-task gate, and the ordering/bisect-safety section before touching code.
2. `.planning/quick/260726-4cc-.../260726-4cc-RESEARCH.md` -- AUTHORITATIVE. Q1's proof, Q3's
   assertion inventory (the 8 line numbers), and the "Data-quality warning" section.
3. `.planning/quick/260726-4cc-.../260726-4cc-CONTEXT.md` -- locked decisions. Its UNRESOLVED
   section is now settled by research and its illustrative numbers are SUPERSEDED.
4. `.planning/debug/windows-publish-one-asset.md` -- why any of this exists. Its E4 `skipped`
   figures are NOT ground truth.

## Infrastructure State

- No sidecar, watcher, or background process left running. Port 41999 confirmed clear earlier.
- `main` is at `2d3cfc5` (PR #5 merged). Two push runs have completed green since.
- The live mirror shard `cache-mirror-202607` (release 354838660) now holds 5 `<taskhash>-<os>`
  assets. Do NOT delete or modify mirror assets -- they are live evidence for the debug report.
- Local auth: `GH_TOKEN` / `GITHUB_TOKEN` UNSET, `gh` logged in.
- Backup refs, both unspent: `refs/backups/main-pre-rk4-live-close` (dd3fd85),
  `refs/backups/w3s-pre-tidy` (cf95f9a).

<context>
This quick task exists because the debug investigation into the Windows publish leg
(`24a2597`) ended `correct_by_design` but surfaced that the real problem was OBSERVABILITY:
three compounding blind spots made a benign 4-linux/1-windows asset split indistinguishable from
a leg whose Actions-cache read scope had regressed. `@actions/cache` logs restore MISSes at
`core.debug` (`cache.js:294`); the OBS-01 summary had no `scanned` row and folded misses into
`skipped`; and the all-miss warning only fires on a TOTAL miss. Task 1 closes the second of those
three, which is the one this project controls.

The user framed this as "audit and triage THEN apply what remains", so dropping a proposal with a
reason was always a legitimate outcome. It just did not happen -- all four earned their keep.
</context>

<next_action>
Spawn `gsd-executor` against `260726-4cc-PLAN.md`. Execute TASK 1 -> TASK 4 in order, one atomic
commit each, full 8-command battery green before every commit, real RED before GREEN on tasks 1
and 2. Then `gsd-verifier`, then the STATE.md row + docs commit.

Then ask the user the one open question: does branch `gsd/debug-windows-publish-one-asset` get ONE
PR covering the root-cause report plus these four fixes (the intended shape -- it is why the quick
task stayed on this branch), or a separate PR for the report?
</next_action>
