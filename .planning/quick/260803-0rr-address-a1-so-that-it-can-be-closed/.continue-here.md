---
context: default
phase: quick-260803-0rr-address-a1-so-that-it-can-be-closed
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-08-02T22:51:18.622Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through actual failure in the session that produced this handoff.
> Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: **The experiment is ALREADY DONE. Do not re-run it.** A1 is answered
      affirmatively and the evidence is written. Re-running costs nothing but time, but
      RE-DERIVING it and reaching a different conclusion would be a regression against a
      measurement that had proper controls.
- [ ] CONSTRAINT: **A silence is not evidence until you have counted the requests.** One run this
      session reported a clean "Nx said nothing about the refused stores" while adding ZERO tap
      lines -- it was served entirely from local cache and never contacted the sidecar. Always
      compare the tap log's line count before and after a run and require a non-zero delta.
- [ ] CONSTRAINT: **Never add `@actions/core` to `packages/github-cache/src/server/server.ts`.**
      It is deliberately GitHub-agnostic (zero `@actions/*` imports) and it backs
      `createCacheServer`, which is the ENTIRE `EXPECTED_VALUE_EXPORTS`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Vacuous silence | A run that never contacts the instrument reports the same "nothing happened" as a run that contacts it and observes nothing. Fired once this session and was caught only by counting tap lines. | blocking | Record the tap log's line count before and after; require a non-zero delta before interpreting ANY null result. |
| `NX_CACHE_DIRECTORY` is not honoured | Nx 23.1.0 in this workspace ignores it. A verifiably empty directory (`entries: 0`) still produced `[local cache]` 2/2, so Nx read the workspace's real `.nx/cache`. The intended isolation never happened. | blocking | To force a cold task, ROTATE THE HASH (append a comment to a source file, run, then `git checkout --` it and prove byte-exact with `git diff --quiet`). Do not rely on the env var. |
| Instrumenting the wrong layer | The orchestrator's original design put `core.debug()` in `server.ts`, which would have made the package's sole public value export transitively depend on an Actions-only package. | blocking | Check `EXPECTED_VALUE_EXPORTS` and the module's existing import set before adding any dependency edge. |
| Searching for a string the code path never emits | An earlier turn concluded background-step output was invisible in CI. It was a FALSE NEGATIVE -- the needle was `serve.ts`'s CLI string, which sits behind an `isEntrypoint()` guard and never runs on the action path. The shipped entry emits a different string. | advisory | Before treating a zero-hit search as absence, confirm the string is actually emitted on the code path under test, and run a positive control. |
| `git commit -m` fails on this Dev Drive | ReFS on D: -- `COMMIT_EDITMSG: Invalid argument`. | advisory | Write the message with the Write tool, then `git commit -F <path>`. |
| GSD `state.*` handlers corrupt STATE.md | Fired on every plan of Phase 13. | advisory | Hand-edit STATE.md. If a handler is called anyway, re-read and repair before staging. |

<current_state>
Quick task `260803-0rr`. The **experiment half is COMPLETE and A1 IS ANSWERED**; what remains is
purely recording the closure into the phase artifacts.

Paused at maintainer request before that propagation half.

**A1's verdict: ANSWERED AFFIRMATIVELY on both halves.** Nx DOES attempt a store after a MISS, the
read-only backend refuses it with 403, and Nx swallows it in complete silence.

Working tree is clean apart from the untracked quick-task directory. The harness is fully torn
down: port 3111 clear, no stray process, `cleanup.ts` restored byte-exact.
</current_state>

<completed_work>

- Task 1: `gsd-planner` authored `260803-0rr-PLAN.md`. It REPLACED the orchestrator's proposed
  design (instrument `server.ts` + throwaway CI branch + `gh run rerun --debug`) with a purely
  local harness needing no source change and no CI cycle. All four of its load-bearing factual
  claims were independently verified by the orchestrator and all four hold.
- Task 2: Local harness run, A1 answered. Evidence written to `260803-0rr-EVIDENCE.md`.
- Task 3: `gsd-plan-checker` verdict COLLECTED (5 blockers, 8 warnings) and triaged into the
  evidence. It arrived AFTER the measurement and does NOT invalidate it. Its central point is that
  the plan's row 1 (a PUT arrives) closes A1 soundly while row 2 (no PUT) was unearned -- and the
  observation IS row 1. All three row-2 hazards are excluded by what was run: no operator probes
  were fired so every tap entry is Nx-attributable at a REAL task hash (B1); the run exited 0 with
  four PUTs, and a PUT only follows a successful execution (B2); and the local-cache-HIT hazard was
  independently discovered and gated by the tap-line delta (B3). W1 and I1 were accepted and
  applied. B4, B5 and W3 are carried into the propagation half above.
</completed_work>

<remaining_work>

1. **Propagate the closure to FIVE surfaces, not two.** Append to `13-EVIDENCE.md` (APPEND-ONLY --
   forward references, never back-edit). Update `13-VALIDATION.md`'s Manual-Only A1 row with the
   closure AND its scope caveat. PLUS the two the plan-checker found and the plan had MISSED:
   `13-SECURITY.md:296` ("RESEARCH assumption A1 ... OPEN") and `:369` (a CHECKED box asserting A1
   remains known-open), and `13-RESEARCH.md`'s Assumptions Log A1 row ("Confirm on the landing
   run"). `13-SECURITY.md` is a dated snapshot at `80f3066` and may legitimately be FROZEN -- but
   say so explicitly, or the repo asserts both CLOSED and OPEN across two audit artifacts of the
   same phase. Do NOT touch `13-VERIFICATION.md`.
2. **Use a revision RANGE in the append-only check.** `git diff --numstat -- <file>` with no range
   compares worktree to index and is VACUOUS once committed. Use `<base>..HEAD`, per
   `13-VALIDATION.md:90`.
3. **Fix `ROADMAP.md:651`** -- it still carries Case B as an open LATER item although quick
   `260802-toz` proved it on run `30768540898`. Note A1 is NOT in ROADMAP.md at all (`rg` exit 1
   with a positive control), so there is no A1 row there; do not invent one.
4. **Close out:** `260803-0rr-SUMMARY.md`, the STATE.md Quick Tasks row, commit, push.
</remaining_work>

<decisions_made>

- **A1 answered locally, not in CI.** `server.ts:128-133` answers a read-only PUT with 403 and
  RETURNS BEFORE `handlePut`, so no backend method runs on a refused PUT -- the backend's identity
  provably cannot affect the PUT path. That makes the existing `createReadOnlyMemoryBackend`
  exactly equivalent, and it is PUT-MAXIMAL because it is permanently empty, so every task must
  execute and every execution is a store opportunity.
- **Rejected instrumenting `server.ts`** -- see the blocking constraint above.
- **No permanent test added, deliberately.** The durable half (PUT -> 403) is already covered at
  `server.spec.ts:359`. The new half is third-party Nx behaviour; guarding it would test Nx and
  redden on an unrelated bump.
- **Forced coldness by hash rotation** rather than by `NX_CACHE_DIRECTORY`, which does not work here.
</decisions_made>

<blockers>
- None. Nothing is stuck.
</blockers>

## Required Reading (in order)

1. `.planning/quick/260803-0rr-address-a1-so-that-it-can-be-closed/260803-0rr-EVIDENCE.md` -- the
   measurement, its three controls, and the run that was REJECTED as vacuous.
2. `.planning/quick/260803-0rr-address-a1-so-that-it-can-be-closed/260803-0rr-PLAN.md` -- the plan,
   including why the CI route was dissolved.
3. `.planning/quick/260802-toz-close-case-b-and-research-assumption-a1-/260802-toz-EVIDENCE.md` --
   OBSERVATION 2 states the residual this task closes; Case B's proof is in the same file.
4. `.planning/phases/13-read-only-actions-cache-backend/13-VALIDATION.md` -- the Manual-Only table
   that needs updating.

## Infrastructure State

- Harness: **torn down.** Port 3111 clear, no stray `a1-tap` process, `cleanup.ts` restored
  byte-exact (`git diff --quiet` passed).
- Scratchpad artifacts (never committed): `a1-tap.mjs`, `a1-requests.log` (the 8-request tap log),
  `a1-nx-definitive.out`.
- `main` is at `fe25a3f` -- Phase 13 is NOT on it. The temporary-push window from `260802-toz` is
  closed and `main` was restored; backup ref `refs/backups/main-pre-phase13-verify` still exists on
  the remote.
- **PR #16 is OPEN and BLOCKED** on the `publish-verify` regression (fails on both legs at the
  Phase 13 tip, succeeded on all five prior `main` pushes, push-gated so invisible to every PR run).
  Mechanism NOT established. That is the highest-value open thread in the project and it is
  independent of this quick task.
- Branch `gsd/v0.0.2-os-invariant-cross-os-sharing` is in sync with origin at `b293432`.

<context>
The interesting part of this task is that the planner dissolved the orchestrator's design rather
than executing it, and was right on both counts -- the instrumentation would have been an
architectural defect, and the CI cycle was never necessary because the 403 fires before the backend
is ever consulted.

The second interesting part is that A1's residual turned out to have been latent since Phase 10.
`10-EVIDENCE-PRE-RENAME.md:88` records a local sidecar plus real Nx runs against a read-only
backend, with a control row reading "no PUT-to-read-only-backend crash" -- it stood on this exact
seam and recorded only that nothing crashed, never whether a PUT arrived. That row read as coverage
for three phases.

Carry the habit that caught the vacuous run: before believing any silence, prove the instrument was
exercised.
</context>

<next_action>
Read `260803-0rr-EVIDENCE.md`. The experiment is DONE -- do not re-run it. Then decide the
plan-checker question, and propagate the A1 closure into `13-EVIDENCE.md` and `13-VALIDATION.md`.
</next_action>
