---
phase: 260804-lc3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - packages/github-cache/src/docs-same-os-claims.spec.ts
autonomous: true
requirements: [D-1, D-2, D-3, D-4]

must_haves:
  truths:
    - "D-1: all SEVEN `ci.yml` sites are corrected in ONE commit -- five carrying the presence-mechanism defect (`:210`, `:1004`, `:1022`, `:1033`, `:1040`) and two carrying the independent failure-implication defect (`:1005-1006`, `:1030-1031`). Correcting a subset leaves the repo asserting the narrow premise, which is the shape DOCS-09 precedent forbids."
    - "The EIGHTH site -- `docs-same-os-claims.spec.ts:521-522`, row D's own docstring -- is corrected in the SAME commit. It is a LIVE guard file, not a sealed phase record, and it repeats BOTH defects."
    - "The comment lock lands in the SAME commit as the prose it pins. A lock arriving later would not have caught this drift, and row D pinned `:1001-1002` while leaving the REASONS unguarded, which is precisely how the drift survived."
    - "MEASURED and REASONED are visibly distinguished in the shipped prose. The presence disjunction is measured (four leg-observations across runs `30907575624` and `30910935382`); the failure-implication correction is derived from the code and its FAIL direction has NEVER been observed. The prose says so in its own words."
    - "D-2: zero behaviour change. The probe's acceptance set stays 200 ALONE, the no-retry-loop decision stays, the AFTER-the-Nx-run / BEFORE-`cancel:` placement stays, and no `run:` body, `if:`, job, step or `needs:` edge is touched. Only REASONS change."
    - "`ci.yml:1001-1002` stay byte-identical -- they are row D's existing pinned phrases and they sit immediately above the edit zone."
    - "The two legitimate `dead sidecar` claims SURVIVE: `:1015`'s historical record of the claim being corrected, and `:1047`'s `|| true` paragraph, where a dead sidecar genuinely does yield `000`. Only the two sentences that mis-attribute a 404 to a dead sidecar go."
    - "`ci.yml:450` (`producer just wrote`) is UNTOUCHED -- a different claim about the Windows reuse legs, guaranteed by a `needs:` edge rather than by this probe."
    - "`:1040` gets a premise-INDEPENDENT reason, not a phrase swap: `own_hash` is read from `integration-hash.txt`, which the run.json reader writes, so the step's INPUT does not exist before the Nx run. Placement is unchanged; only the reason is."
    - "D-3: the `test` hash rotation is accepted. `ci.yml` is a declared `test` input under PARITY-08 and the spec is project source, so `test` genuinely re-runs rather than replaying a cached PASS. That is the behaviour PARITY-08 exists to produce."
    - "D-4: every gate can SEE the change. No `codeLines`-based assertion is proposed -- `dogfood-cross-os.spec.ts:49-54` strips every `#` line, so such a gate is vacuous by construction. The lock lives in `docs-same-os-claims.spec.ts`, whose read is RAW."
    - "No sealed phase record is edited, and `REQUIREMENTS.md` / `ROADMAP.md` are NOT edited -- both already say `a known-present key` and attribute nothing, so the correction RESTORES XOS-03's own wording rather than revising a requirement."
  artifacts:
    - .github/workflows/ci.yml
    - packages/github-cache/src/docs-same-os-claims.spec.ts
    - .planning/quick/260804-lc3-close-the-ci-yml-positive-control-just-s/260804-lc3-SUMMARY.md
  key_links:
    - "`handleGet` -> `backend.get(hash)` -> an UNCONDITIONAL `cache.restoreCache` in `actions-cache-backend.ts` -> nothing on that path is save-conditioned -> the real invariant is RESTORED OR SAVED, so the narrow premise is false on every Case-B run."
    - "`server.ts`'s `handleGet` catch degrades EVERY backend fault to a 404 -> a 404 is indistinguishable from a genuine absence at the HTTP boundary -> that degradation, not a dead sidecar, is the masquerade agent this control exists to catch."
    - "curl connection-refused -> exit 7 -> swallowed by `|| true` -> `-w '%{http_code}'` prints `000` -> a dead sidecar CANNOT produce a 404; and the readiness poll accepts 404 as its own PROOF-OF-LIFE signature -> a 404 proves the sidecar ANSWERED."
    - "a token-less sidecar -> `resolveGitHubToken` undefined -> `createReadOnlyMemoryBackend` -> 404s everything -> the same cause would have MANUFACTURED the leg's MISS -> on a MISS-then-save leg a 404 makes the MISS observation INADMISSIBLE (the consequence the current prose states correctly, under the wrong agent)."
    - "the backend is chosen ONCE at sidecar startup -> a HIT proves the degraded backend was not chosen -> on a HIT leg a 404 cannot mean a degraded backend, so it reports read-path NON-DETERMINISM inside one job instead."
    - "`cacheStatus` is RECORDED and never GATED in `read-integration-hash.mjs` -> on a HIT leg this step is the ONLY thing in the job that hard-fails on a broken read path -> the control is REDUNDANT AS A LIVENESS PROOF but its ENFORCEMENT is not redundant."
    - "row D's `required` phrases are matched against RAW `ci.yml` text -> a phrase spanning a hard wrap matches NOTHING -> both new lock phrases must be authored to sit within ONE `#` line, and the literals must be copied FROM the authored lines rather than from this plan."
---

<objective>
Correct the `integration` positive-control comment block, which justifies the probe with two
independent claims that measurement and the code both contradict -- and pin the corrected reasons
with the same commit, in the one guard file that can see a comment.

Purpose: this block is the only thing standing between a reader and collapsing the two `curl` probes
into one helper with the looser acceptance set, which would leave both steps green while destroying
the control. Row D says so in its own docstring. The prose still drifted, because row D pinned the
acceptance SET (`:1001-1002`) and left the REASONS unguarded.

Output: seven corrected `ci.yml` sites, one corrected spec docstring, two new `required` phrases on
row D, and a SUMMARY. One commit.

PROSE ONLY. No job, step, acceptance set, `if:`, `needs:` edge or backend changes. Reasons change;
behaviour does not.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260804-lc3-close-the-ci-yml-positive-control-just-s/260804-lc3-CONTEXT.md
@.planning/quick/260804-lc3-close-the-ci-yml-positive-control-just-s/260804-lc3-RESEARCH.md
@.planning/STATE.md
</context>

## Decision Coverage

- **D-1:** correct the whole shared premise in ONE commit, not the one line the report named. Research
  grew the site list from five to eight: five presence-mechanism sites in `ci.yml`, two independent
  failure-implication sites in `ci.yml`, and one docstring in a LIVE guard file. All eight land
  together, per the DOCS-09 precedent CONTEXT.md cites.
- **D-2:** comment-only. The task's `<files>` is the whole surface and the gates assert nothing outside
  it moved. The control's conservatism is RESTATED, not weakened.
- **D-3:** the `test` hash rotation is accepted, not worked around. The battery is run in full for
  exactly that reason -- `test` will genuinely re-run rather than replay a cached PASS.
- **D-4:** no `codeLines` gate is proposed. Every gate reads RAW text (`git grep` on the file, or row
  D's `toContain` against `readFileSync` output), so every gate can see a `#` line.

## Established by research -- build on these, do NOT re-derive

Research settled the Q1 blocker. Treat its verdicts as established and read the full argument in
`260804-lc3-RESEARCH.md` before editing. In brief, there are TWO independent defects:

1. **The presence-mechanism claim.** The comment says the key is present because the leg's own task
   just SAVED it. The real invariant is RESTORED OR SAVED -- the probed key resolves through an
   UNCONDITIONAL `cache.restoreCache` in `actions-cache-backend.ts`, which is not save-conditioned.
   MEASURED twice: runs `30907575624` and `30910935382`, legs HIT, saved nothing, control returned 200.
2. **The failure-implication claim, a SELF-CONTRADICTION inside the same block.** `:1005-1006` says a
   404 means a dead sidecar. But `:1046-1054` -- the same block -- says a dead sidecar yields `000`,
   and the readiness poll at `:925` treats 404 as its own proof-of-life signature. A 404 means the
   sidecar ANSWERED. The real masquerade agent is `server.ts`'s SRV-05 read-fault degradation, which
   turns any backend fault into a 404 MISS. The CONSEQUENCE (`INADMISSIBLE`) is correct for a
   MISS-then-save leg; the named AGENT is not.

Per-case truth for the corrected prose, and the shape it takes:

- **Case A (MISSED and saved):** a 404 proves the read path cannot produce a key this run just wrote.
  A token-less sidecar degrades to the empty in-memory backend and 404s everything -- the same cause
  would have MANUFACTURED the MISS. `INADMISSIBLE` is right.
- **Case B (HIT, saved nothing):** the dead-sidecar framing is meaningless -- no MISS exists, liveness
  is already proved in-band, and a HIT excludes the degraded backend (chosen once at startup). A 404
  instead reports read-path NON-DETERMINISM within one job.
- **Case-B value: REDUNDANT AS A LIVENESS PROOF, not meaningless.** `cacheStatus` is RECORDED never
  GATED, so this step is the only thing in the job that hard-fails on a broken read path. The prose
  must not overclaim in either direction, and it must not say a bare "redundant".
- Use ONE shared sentence plus TWO explicit branches. House precedent for exactly that shape is at
  `ci.yml:1456-1460` (`ON A CASE-B RUN THE DELTA IS HOURS OR DAYS ... That is CORRECT and needs no
  allowance ... Do not "fix" it with an upper bound`).

Two items to carry EXPLICITLY OPEN in the shipped prose and in the SUMMARY, never closed:

- **The control's FAIL direction has never been observed.** There is no 404 on record from this step.
  So the corrected failure prose is REASONING, and only the presence disjunction is measured.
- **Fork PR behaviour stays CITED, never reproduced.** Unchanged by this task.

Legitimacy note worth recording in the SUMMARY: `REQUIREMENTS.md:590-591` and `ROADMAP.md:487` both
say `a known-present key` and attribute nothing. **XOS-03 never asserted the narrow premise** -- it was
narrowed at implementation time. This correction RESTORES the requirement's own wording rather than
revising a requirement. That is why neither file is edited.

## Three corrections to research's DRAFT PROSE, found while planning

The draft at `260804-lc3-RESEARCH.md` ("DRAFT REPLACEMENT PROSE for `:1003-1009`") is the substantive
basis for the rewrite and its clause coverage is correct. Three mechanical defects must be fixed
before it is committed:

1. **Drop both `file.ts:NN` citations.** The draft writes `actions-cache-backend.ts:219-225` and
   `select-backend.ts:68`. MEASURED: `ci.yml` carries bare filenames on 39 lines and ZERO
   `file.ext:NN` citations (`git grep -E '\.(ts|mjs|cjs)\:[0-9]+'` exits 1 on genuine no-match; the
   bare-filename control returns 39). Introducing the first two would also plant exactly the defect
   class commit `2df3af5` exists to stop. Cite the bare filename plus the SYMBOL instead --
   `cache.restoreCache` in `actions-cache-backend.ts`, `createReadOnlyMemoryBackend` in
   `select-backend.ts`, `handleGet`'s catch in `server.ts`. That is already house style at `:950` and
   `:1036`, and it is what row D's `UNCONDITIONAL cache.restoreCache` phrase pins anyway. (For the
   SUMMARY's own citations, note the real paths: `src/lib/select-backend.ts` and
   `src/server/server.ts` -- research's shorthand omits both directories.)
2. **The draft WRAPS one of the two lock phrases.** It renders as `A 404 means the sidecar DID` /
   `# answer -- ...`, so the phrase `404 means the sidecar DID answer` spans a hard wrap and
   `toContain` against RAW text would match NOTHING. The spec's own rule at `:62-66` records that this
   has already cost a red. Re-flow so BOTH lock phrases sit wholly within ONE `#` line.
3. **Drop the count from the measured sentence, or state it as leg-observations.** The draft says "all
   four integration legs took a remote-cache HIT", which reads as four legs in one run; the measurement
   is four leg-observations across TWO runs. Since the plan's single most important constraint is that
   MEASURED claims are exactly right, either say "four leg-observations across the two runs" or drop
   the number and name the two runs. Do not ship the ambiguous form.

## Shell conventions binding on every command in this plan

Windows arm64, Git Bash. The Grep tool and the `grep` command are FORBIDDEN -- `git grep` for tracked
files, `rg` for untracked or gitignored, `| rg` for every pipe filter. (The `grep` inside `ci.yml`'s
own `run:` body at `:997` is correct and untouched.) A zero-hit search needs an EXIT-CODE check before
it counts as absence: `git grep -q` exits 0 on match, 1 on genuine no-match, 2+ on error, so every
absence gate below branches on that status via `if git grep -q ...; then exit 1; fi` rather than reading
empty output. **Do NOT rewrite those gates as `cmd; test $? -eq 1`** -- measured: under `set -e` the
absence-confirming nonzero exit aborts the line before `test` ever runs, so a CORRECT absence would
false-fail. Count gates default an empty result to 0 (`|| true` plus `${N:-0}`) so a missing literal
fails the assertion cleanly instead of erroring with `integer expected`. **The audited prose wraps
across `#` lines mid-sentence**, so every phrase search in this plan is a single-line fragment; use
`-F` for
literals containing `.`, and `-U` with `\s+` if a multi-line phrase is ever unavoidable. Never `-rn`
(parses as `--replace n`). `git commit -m` FAILS on this Dev Drive (D:, ReFS) -- write the message with
the Write tool and use `git commit -F <file>`. Never `git add .` / `-A` / `-u`; stage by name. Never
prefix a command with `cd <path> &&`. No heredocs -- use the Write tool; scratch files go in the
session scratchpad, never the repo. ASCII only: `--` not an em dash, straight quotes, no emojis. Public
repo -- no work email or its bare domain in any committed file or commit message.

<tasks>

<task type="auto">
  <name>Task 1: Correct all eight sites and pin the corrected reasons on row D, in one commit</name>
  <files>.github/workflows/ci.yml, packages/github-cache/src/docs-same-os-claims.spec.ts</files>
  <action>
PRE-FLIGHT -- RE-DERIVE EVERY LINE NUMBER BEFORE TOUCHING ANYTHING. Do not trust a number quoted by
this plan, by CONTEXT.md or by RESEARCH.md. Commit `2df3af5` exists in this repo because cited line
numbers had moved. Locate every site by PHRASE:

  git -c core.pager=cat grep -n -E "own save|just saved" -- .github/workflows/ci.yml
  git -c core.pager=cat grep -n -i -e "dead sidecar" -e "masquerad" -- .github/workflows/ci.yml
  git -c core.pager=cat grep -n -i -e "just-saved" -e "dead sidecar" -- packages/github-cache/src/docs-same-os-claims.spec.ts

Expected, measured 2026-08-04 at HEAD `2df3af5`. If the shape differs, STOP and report:

  - The first search returns FIVE lines, and ALL FIVE are in scope (`:210`, `:1004`, `:1022`,
    `:1033`, `:1040`). **CORRECTED by plan-check iteration 2 -- this bullet previously said SIX and
    counted `:450` as the sixth. MEASURED at HEAD `a372b96`: the search returns FIVE.** `:450` is not
    in this result set at all: it reads `producer just wrote`, which matches neither `own save` nor
    `just saved`. Do NOT widen the regex to reconcile the count -- widening it to catch `just wrote`
    would contradict gate 346 and pull `:450` inside an absence gate this plan requires it to survive.
    Confirm `:450` under its OWN search instead, the same literal gate 347 uses:
    `git -c core.pager=cat grep -n -F 'producer just wrote' -- .github/workflows/ci.yml`, which
    returns ONE line. `:450` is OUT OF SCOPE -- a different claim about the Windows reuse legs,
    guaranteed by a `needs:` edge rather than by this probe. Leave it byte-identical.
  - The second search returns FOUR lines. TWO are defects (`:1005`, `:1031`). TWO are LEGITIMATE and
    must survive: `:1015`, the historical record of the claim being corrected, and `:1047`, the
    `|| true` paragraph where a dead sidecar genuinely does yield `000`.
  - The third search returns TWO lines, both inside row D's docstring.

Also confirm `ci.yml`'s two currently-pinned phrases sit immediately ABOVE the edit zone and match row
D's `required` array character for character. They are `the acceptance set is 200 ALONE -- a 404 here
is a control FAILURE` and `the readiness poll accepts 404 or 200 on purpose; do not collapse them`.
**They must end the task byte-identical.** The rest of the edit zone is lock-free; the nearest other
pinned phrases are far below it.

PART A -- REWRITE THE PRIMARY BLOCK. Replace the presence-mechanism sentence and the
failure-implication sentence (currently `:1003-1009`, between the two pinned lines and the
`Two probes, two acceptance sets` sentence). Use research's DRAFT REPLACEMENT PROSE as the basis,
applying the three mechanical corrections listed above this task. Preserve the draft's clause
coverage: presence (both cases), provenance, mechanism (both cases), then a Case-A bullet and a Case-B
bullet. Keep the closing `Two probes, two acceptance sets, two questions` sentence and the
`do not reuse the poll's deadbeef hash here either` instruction -- they are the block's reason for
existing.

Four properties this prose MUST carry, each one gated below:

  1. The measured half is marked as measured and names both run IDs -- `30907575624` and
     `30910935382`. Carry the literal `THE DISJUNCTION IS MEASURED` on a single `#` line.
  2. The reasoned half is marked as NOT observed. Carry the literal `NEVER OBSERVED` on a single `#`
     line, stating that this control's failure direction has never been exercised, so the
     failure-implication reasoning is derived from the code rather than measured. **This is the plan's
     most important quality constraint** -- the corrected prose replaces reasoning with better
     reasoning, and presenting it as observation would be a fresh defect of the same class as the one
     being fixed.
  3. The Case-B branch carries the literal `NON-DETERMINISM`, and carries the literal
     `REDUNDANT AS A LIVENESS PROOF` on a single `#` line rather than a bare "redundant", with the
     `cacheStatus` RECORDED-never-GATED reason that makes the enforcement non-redundant. The bare word
     alone would understate the control's value, which is the opposite overclaim from the one being
     fixed and is equally wrong.
  4. Nothing in the block claims a 404 can mean the sidecar is dead. Attribute the masquerade to
     `server.ts`'s SRV-05 read-fault degradation, naming the literal `SRV-05`, and note that 404 is the
     readiness poll's own proof-of-life signature -- the file currently uses one status code as proof
     of life at the poll and as proof of death eighty lines later.

PART B -- THE SIX REMAINING `ci.yml` SITES. Each keeps its CONCLUSION and its placement; only its
REASON changes.

**DELETION IS NOT CORRECTION.** Four of these sites could be made to satisfy a bare absence gate by
deleting the false clause and leaving a shortened or dangling sentence behind. Each therefore carries
its own REQUIRED literal, exactly as `:1040` does -- one per site, each of which must sit wholly within
ONE `#` line, and each verified ABSENT from `ci.yml` at HEAD so the gate is not vacuous.

  - `:1022` (the fork-PR resolution). It QUOTES the narrow premise inside the argument. Swap the
    quoted premise for the disjunction and add research's Q3 strengthening: the restore disjunct needs
    NO WRITE PERMISSION at all, so it carries a HIT run with zero reliance on the fork-write citation,
    while the original merge-ref argument still carries a MISS run. Two independent legs where there
    was one. Carry the literal `NO WRITE PERMISSION`. Do NOT overclaim this as "the fork question is
    closed".
  - `:1028-1031` (the escape hatch). **The escape hatch STAYS** -- a fork PR that MISSES still rests
    entirely on the cited, never-reproduced fork-write behaviour. Its closing restatement of the
    masquerade needs the same treatment as PART A: what must never be admitted into the acceptance set
    is a 404, and the reason is the read-fault degradation, not a dead sidecar. Name `SRV-05` here too.
    **Carry the literal `ADMITS THE DEGRADATION`, and author it ONLY here** -- not in PART A. Verified
    ABSENT from `ci.yml` at HEAD, with a positive control on the same path, so the gate is not vacuous.
    It must sit wholly within ONE `#` line.
    **Why this site needs its own literal (added by plan-check iteration 2):** `SRV-05 >= 2` is a
    CARDINALITY gate and cardinality cannot localize. An executor could name `SRV-05` twice inside
    PART A alone -- the mechanism sentence and the Case-A bullet both discuss the degradation, so this
    is the natural drafting outcome, not a contrived one -- and then satisfy this site by DELETING its
    false clause. Measured: dropping the trailing `, a dead sidecar masquerading as a cache MISS`
    leaves `...on the one failure it exists to detect.`, which is grammatical, complete, and carries
    NO replacement reason. Every gate in the verify block would still pass. That is the
    DELETION IS NOT CORRECTION failure this plan names above and claims to have closed, and it was the
    one PART B site without a literal of its own.
  - `:1033` (the no-retry-loop justification). The conclusion HOLDS on the widened premise. Add the
    inversion research names, or a future reader adds the retry: a transient fault that `a retry would
    HIDE` IS the read-path non-determinism this control exists to surface. Carry that literal.
  - `:1040` (the placement justification). **This is the one genuinely different site -- it needs a
    NEW reason, not a phrase swap.** Its stated reason is false on a HIT leg, where the key is present
    BEFORE the Nx run. Lead with the premise-INDEPENDENT reason: `own_hash` is read from
    `integration-hash.txt`, which the run.json reader step writes, so the step's INPUT does not exist
    before the Nx run at all. Carry the literal `INPUT DOES NOT EXIST` on a single `#` line. Keep the
    Case-A reason as the secondary one: on a MISS-then-save leg an earlier placement would 404 every
    time, breaking the control exactly where it is load-bearing. The `BEFORE cancel: cache-server`
    half and the token-masking sentence are untouched.
  - `:203-212` (the `build` job's fork-PR block). A SHORT widening only. This sentence sits INSIDE a
    `CORRECTED.` history paragraph -- **do not erase the correction record**, and do not restructure
    the paragraph. Widen the probe clause so it no longer attributes the read to a save: the probe
    reads the same merge-ref scope its own run `RESTORED OR WROTE`. Carry that literal, wholly on one
    `#` line -- the clause it replaces currently wraps across two lines, so re-flow rather than
    editing in place.
  - `:1046-1054` (the `|| true` paragraph). UNTOUCHED. It is already correct, and it is the evidence
    that a dead sidecar yields `000` -- the fact that makes PART A's correction true. PART A should
    point at it rather than restate it.

PART C -- CORRECT ROW D'S DOCSTRING AND ADD THE TWO LOCK PHRASES, in this commit. Row D
(`docs-same-os-claims.spec.ts`, "Phase 11 row D -- the positive control's ACCEPTANCE SET (D-16)") is
the EIGHTH site: its docstring repeats both defects, in a LIVE guard file.

  1. Correct the docstring's claim to the disjunction and to the 404-means-answered attribution.
     **BOTH defects live in that one sentence and BOTH are separately gated** -- adding the two
     `required` phrases below does NOT satisfy this item, and leaving the false sentence intact inside
     this task's own guard file would recreate the looks-guarded-but-isn't failure the task exists to
     close. Carry the literal `CORRECTED by quick 260804-lc3` on the line that records the correction,
     and state in one clause WHY row D did not catch this: it pinned the acceptance SET and left the
     REASONS unguarded. **Carry the literal `left the REASONS unguarded`** -- verified ABSENT from the
     spec file at HEAD, with a positive control on the same path. Added by plan-check iteration 2:
     every other spec-file gate here is satisfiable by the `required` ARRAY alone, because `git grep`
     cannot tell a docstring occurrence from an array entry -- so without this one literal the
     docstring could shed both false clauses and gain no replacement reasoning at all while every gate
     stayed green. Follow the file's house style -- a correction states a REPLACEMENT reason, it
     never merely deletes. **Do NOT quote the superseded wording in this file.** Describe the old
     claim by concept. The file's read is RAW and it is its own subject, so a quoted stale phrase would
     be indistinguishable from a regression to a repo-wide search.
  2. Add TWO strings to row D's `required` array, and NOTHING to `forbidden` -- `forbidden: []` stays,
     for the reason this file already records at three other rows: an absence check on the old wording
     is satisfied by deleting the whole comment, and a correction-history paragraph legitimately
     quotes its own superseded wording.
     - `UNCONDITIONAL cache.restoreCache` -- pins the MECHANISM that makes the premise a disjunction.
       Renarrowing the premise to a save requires deleting it, and it survives any legitimate
       rewording that stays true.
     - `404 means the sidecar DID answer` -- pins the failure-attribution correction.
     Do NOT pin the disjunction's own wording ("restored or saved"): it is short, generic, and exactly
     the kind of phrasing that gets legitimately reworded. Pin the CAUSE and the code discrimination.
  3. Extend row D's docstring with the two additive phrases' own rationale, matching the length and
     shape of the sibling rows. Keep the existing independently-deletable paragraph and add why these
     two are independently deletable from each other: the mechanism and the attribution are separate
     claims, and a failure report must tell a renarrowed premise apart from a restored dead-sidecar
     attribution.
  4. **AUTHOR THE `ci.yml` LINES FIRST, THEN COPY BOTH LITERALS OUT OF THEM character for character.**
     Do not type either phrase from this plan into the array. Row D's read is RAW, so a phrase spanning
     a hard wrap matches NOTHING; the file's own rule at `:62-66` records that this has already cost a
     red. After writing the array, verify each phrase against a SINGLE line of `ci.yml`:
     `git -c core.pager=cat grep -c -F 'UNCONDITIONAL cache.restoreCache' -- .github/workflows/ci.yml`
     and the same for the second phrase. Each must return exactly 1. This is the 11-06 idiom -- take
     the literals from the authored prose, not from the plan.

PART D -- VERIFY, THEN COMMIT ONCE. Run the full local battery, in this order:

  npm exec nx run-many -t test typecheck build lint
  npm run format:check
  npm run check:action

The battery is not ceremony here. `ci.yml` is a declared `test` input under PARITY-08 and the spec is
project source, so BOTH rotate `test`'s hash and `test` genuinely re-runs rather than replaying a
cached PASS -- which is the only thing that proves row D's two new phrases actually MATCH raw `ci.yml`
rather than silently spanning a wrap. `test` also parses `ci.yml` as YAML through
`dogfood-cross-os.spec.ts`'s job helpers, which is what catches a comment edit that broke the
document. `check:action` is run to PROVE the bundle did not drift: this task touches no
`serve()`-reachable source, so `start-cache-server/index.js` must be unchanged -- if it somehow is,
regenerate it in THIS commit rather than a later one.

If row D goes red on a phrase, the phrase wrapped -- re-flow the `ci.yml` line and re-copy the
literal. Never edit the assertion to make the suite green, and never delete a row.

Then stage the two files BY NAME, write the commit message with the Write tool, and
`git commit -F <msgfile>`. ONE commit -- the prose and the lock that pins it land together, because a
lock arriving later would not have caught this drift, and the repo's DOCS-09 precedent is explicit
that corrections land in the same commit as the guard that pins them. Do not bundle any other file.
The message must carry NO CI-skip token anywhere, not even as prose -- this repo has a recorded case
of a skip token written as prose still skipping CI, and the PR then shows no checks at all rather than
red.

FINALLY -- THE SUMMARY, WRITTEN AFTER THE COMMIT. Every line number this task cites in the SUMMARY
must be RE-DERIVED from the post-edit tree, never copied from this plan or from RESEARCH.md. **This
edit shifts the line numbers of everything below it in `ci.yml`**, so a number measured before the
edit is already wrong by the time the SUMMARY is written. Record in the SUMMARY: the eight corrected
sites at their post-edit line numbers; the two `dead sidecar` survivors and why each is legitimate;
that `:450` and `REQUIREMENTS.md` / `ROADMAP.md` were deliberately not touched, with XOS-03's
never-narrow original wording as the reason; the two EXPLICITLY OPEN items (the FAIL direction has
never been observed, and fork behaviour stays cited-never-reproduced); and the accepted `test` hash
rotation.
  </action>
  <verify>
    <automated>if git -c core.pager=cat grep -q -E "own save|just saved" -- .github/workflows/ci.yml; then exit 1; fi</automated>
    <automated>git -c core.pager=cat grep -q -F 'producer just wrote and can restore that entry' -- .github/workflows/ci.yml</automated>
    <automated>if git -c core.pager=cat grep -q -F 'a dead sidecar had masqueraded as a cache MISS' -- .github/workflows/ci.yml; then exit 1; fi</automated>
    <automated>if git -c core.pager=cat grep -q -F 'sidecar masquerading as a cache MISS' -- .github/workflows/ci.yml; then exit 1; fi</automated>
    <automated>git -c core.pager=cat grep -q -F 'blaming a dead sidecar that was' -- .github/workflows/ci.yml</automated>
    <automated>git -c core.pager=cat grep -q -F 'a DEAD SIDECAR -- curl exits 7' -- .github/workflows/ci.yml</automated>
    <automated>git -c core.pager=cat grep -q -F 'THE DISJUNCTION IS MEASURED' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F '30907575624' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F '30910935382' -- .github/workflows/ci.yml</automated>
    <automated>git -c core.pager=cat grep -q -F 'NEVER OBSERVED' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'NON-DETERMINISM' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'REDUNDANT AS A LIVENESS PROOF' -- .github/workflows/ci.yml</automated>
    <automated>git -c core.pager=cat grep -q -F 'INPUT DOES NOT EXIST' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'NO WRITE PERMISSION' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'a retry would HIDE' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'RESTORED OR WROTE' -- .github/workflows/ci.yml</automated>
    <automated>N=$(git -c core.pager=cat grep -c -F 'SRV-05' -- .github/workflows/ci.yml | rg -o '[0-9]+$' || true); test "${N:-0}" -ge 2</automated>
    <automated>git -c core.pager=cat grep -q -F 'ADMITS THE DEGRADATION' -- .github/workflows/ci.yml</automated>
    <automated>test "$(git -c core.pager=cat grep -c -F 'UNCONDITIONAL cache.restoreCache' -- .github/workflows/ci.yml | rg -o '[0-9]+$' || true)" = "1"</automated>
    <automated>test "$(git -c core.pager=cat grep -c -F '404 means the sidecar DID answer' -- .github/workflows/ci.yml | rg -o '[0-9]+$' || true)" = "1"</automated>
    <automated>git -c core.pager=cat grep -q -F 'the acceptance set is 200 ALONE -- a 404 here is a control FAILURE' -- .github/workflows/ci.yml &amp;&amp; git -c core.pager=cat grep -q -F 'the readiness poll accepts 404 or 200 on purpose; do not collapse them' -- .github/workflows/ci.yml</automated>
    <automated>SP=packages/github-cache/src/docs-same-os-claims.spec.ts; git -c core.pager=cat grep -q -F 'CORRECTED by quick 260804-lc3' -- "$SP" &amp;&amp; git -c core.pager=cat grep -q -F 'UNCONDITIONAL cache.restoreCache' -- "$SP" &amp;&amp; git -c core.pager=cat grep -q -F '404 means the sidecar DID answer' -- "$SP"</automated>
    <automated>git -c core.pager=cat grep -q -F 'left the REASONS unguarded' -- packages/github-cache/src/docs-same-os-claims.spec.ts</automated>
    <automated>test "$(git -c core.pager=cat grep -c -F 'forbidden: []' -- packages/github-cache/src/docs-same-os-claims.spec.ts | rg -o '[0-9]+$' || true)" = "14"</automated>
    <automated>if git -c core.pager=cat grep -q -F 'just-saved' -- packages/github-cache/src/docs-same-os-claims.spec.ts; then exit 1; fi</automated>
    <automated>if git -c core.pager=cat grep -q -F 'dead sidecar masqueraded as a cache MISS' -- packages/github-cache/src/docs-same-os-claims.spec.ts; then exit 1; fi</automated>
    <automated>npm exec nx run-many -t test typecheck build lint</automated>
    <automated>npm run format:check</automated>
    <automated>npm run check:action</automated>
    <automated>test "$(git -c core.pager=cat show --name-only --format= HEAD | rg -c -v '^$')" = "2"</automated>
    <automated>git -c core.pager=cat show --name-only --format= HEAD | rg -q -F '.github/workflows/ci.yml' &amp;&amp; git -c core.pager=cat show --name-only --format= HEAD | rg -q -F 'packages/github-cache/src/docs-same-os-claims.spec.ts'</automated>
    <automated>test -z "$(git -c core.pager=cat diff -U0 HEAD~1 HEAD -- .github/workflows/ci.yml | awk '/^(\+\+\+|---)/ {next} /^[+-]/ {b=substr($0,2); sub(/^[ \t]+/,"",b); if (b != "" &amp;&amp; b !~ /^#/) print}')"</automated>
    <automated>test -z "$(git -c core.pager=cat status --porcelain -- .github packages docs nx.json package.json README.md start-cache-server)"</automated>
  </verify>
  <done>
`.github/workflows/ci.yml` carries no save-attributing premise (`own save` / `just saved` return a
genuine no-match) while `:450`'s `producer just wrote` survives byte-identical as the positive control
on that search. Neither masquerade-attribution sentence survives; both legitimate `dead sidecar` claims
do -- the historical record and the `|| true` paragraph.

**Every corrected site proves its REPLACEMENT reasoning landed, not merely that the false clause was
deleted.** The block marks its MEASURED half with `THE DISJUNCTION IS MEASURED` plus both run IDs and
its REASONED half with `NEVER OBSERVED`; the Case-B branch carries `NON-DETERMINISM` and the qualified
`REDUNDANT AS A LIVENESS PROOF` rather than a bare "redundant"; `:1040` carries `INPUT DOES NOT EXIST`,
`:1022` carries `NO WRITE PERMISSION`, `:1033` carries `a retry would HIDE`, and `:210` carries
`RESTORED OR WROTE`. The escape hatch at `:1028-1031` carries `ADMITS THE DEGRADATION`, authored ONLY
there, so that site proves its own replacement reasoning rather than borrowing PART A's.

`SRV-05` appears at least TWICE. **That count proves only that the token appears at least twice --
it does NOT prove distribution, because a cardinality gate cannot localize.** This wording was
CORRECTED by plan-check iteration 2, which measured that the previous claim (that the count proved
reattribution landed in both PART A and the escape hatch) was invalid: two occurrences inside PART A
alone satisfy it, and the escape hatch could then be "fixed" by deleting its false clause, leaving a
grammatical sentence with no replacement reason and every gate green. Localization at the escape hatch
is carried by its own `ADMITS THE DEGRADATION` literal, not by the count. Every one of these literals
was verified ABSENT from `ci.yml` at HEAD, with a positive control on the same path, so none of these
gates is vacuous.

Row D's two existing pinned phrases are byte-identical. Row D's docstring carries
`CORRECTED by quick 260804-lc3` and no longer carries EITHER defect -- neither the presence-mechanism
half (`just-saved`) nor the failure-implication half (`dead sidecar masqueraded as a cache MISS`, live
at `:522` and wholly within one line). Its `required` array carries `UNCONDITIONAL cache.restoreCache`
and `404 means the sidecar DID answer` -- each matching EXACTLY ONE line of `ci.yml`, proven by count,
and both matching under row D's own `toContain` in a `test` run that genuinely re-ran on the rotated
hash.

The docstring also carries `left the REASONS unguarded`, which is the ONE spec-file literal that
cannot be satisfied by the `required` array -- `git grep` cannot tell a docstring occurrence from an
array entry, so without it the docstring could shed both false clauses and gain no replacement
reasoning while every other spec gate stayed green. `forbidden` is still `[]`, and this is now
ASSERTED rather than merely claimed: the count of empty `forbidden: []` arrays in the file is still
14, so nothing was added to row D's.

Every added or removed content line in the `ci.yml` diff is `#`-prefixed or blank, proving the change
is comment-only rather than resting on battery side effects and executor discipline. The gate is
scoped to `ci.yml` alone, because the spec file legitimately adds non-comment array strings. The full
local battery exits 0, including `check:action` proving no bundle drift. HEAD is ONE commit touching
EXACTLY those two files, with nothing else in the tree modified.

Every absence gate uses the `set -e`-exempt `if ...; then exit 1; fi` form. The `cmd; test $? -eq 1`
idiom is NOT safe here -- measured: under `set -e`, `git grep`'s correct absence-confirming nonzero
exit aborts the line before `test` runs, so a CORRECT result would false-fail and invite an executor to
weaken a working gate.
  </done>
</task>

</tasks>

<verification>
- The two defects are closed at all EIGHT sites, and the fix is not a subset: the presence-mechanism
  search returns a genuine no-match across `ci.yml` while `:450`'s out-of-scope claim survives, and
  neither masquerade-attribution sentence remains while both legitimate ones do.
- The lock is non-vacuous and cannot silently false-PASS: each new `required` phrase matches EXACTLY
  ONE `ci.yml` line by count, and row D's own RAW `toContain` runs green in a `test` execution that
  genuinely re-ran because both edited files rotated the hash.
- MEASURED and REASONED are separated in the shipped prose, gated by two distinct literals, and the
  FAIL direction is declared never observed.
- Zero behaviour change: nothing outside the two files is modified, `check:action` proves no bundle
  drift, and the acceptance set, retry decision and step placement are untouched.
- One commit contains both the prose and the guard that pins it.
</verification>

<success_criteria>
Eight sites corrected, two lock phrases added to row D with `forbidden: []`, one commit of exactly two
files, full local battery green, and a SUMMARY whose citations were re-derived from the post-edit tree
and which carries both EXPLICITLY OPEN items plus the XOS-03 legitimacy note.
</success_criteria>

<output>
Create `.planning/quick/260804-lc3-close-the-ci-yml-positive-control-just-s/260804-lc3-SUMMARY.md` when done
</output>
