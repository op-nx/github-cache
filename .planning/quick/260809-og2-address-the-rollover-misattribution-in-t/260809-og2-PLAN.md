---
phase: quick-260809-og2
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - packages/github-cache/src/publish/publish-mirror.spec.ts
  - packages/github-cache/src/publish/publish-mirror.ts
  - docs/advanced.md
  - packages/github-cache/src/docs-same-os-claims.spec.ts
requirements: []

must_haves:
  truths:
    - Neither publish-mirror warning message asserts a closed enumeration of causes; both state the list is not exhaustive.
    - Both messages name an artifact version skew between the sidecar and this publish step, in consumer-general terms naming no artifact of this repository.
    - The partial branch additionally names the first run against a new month shard as a one-time expected rise -- the misattribution this task exists for.
    - The total gate does NOT gain the month-rollover cause, and nothing claims this repository's bundle drift can trip it.
    - A guard fails if the closed-enumeration phrase returns to either message, asserted through publishMirror over mock.calls.flat(), never through a file read.
    - A guard fails if either message loses the skew cause.
    - A guard fails if the partial message loses the rollover cause, AND a separate guard fails if the total-gate message gains it -- the asymmetry is enforced from both sides, not narrated.
    - docs/advanced.md no longer asserts a cardinality the messages do not carry.
  artifacts:
    - packages/github-cache/src/publish/publish-mirror.ts (both core.warning bodies plus the comments above them)
    - packages/github-cache/src/publish/publish-mirror.spec.ts (absence plus presence pins on BOTH branch fixtures)
    - docs/advanced.md (the two cardinality sites)
    - packages/github-cache/src/docs-same-os-claims.spec.ts (the OBS-04 row docstring)
  key_links:
    - The absence assertion must run against BOTH fixtures -- runWithMisses(10, 9) and the all-MISS fixture -- because the phrase lives in both messages and only one branch fires per run.
    - The two docs cardinality sentences are pinned by NO guard row, so they go stale silently unless edited in this same commit.
    - The OBS-04 row's three required phrases and its forbidden patterns must all still hold after the docs edit; npx nx test github-cache is what proves it.
---

<objective>
Remove the false completeness claim from both publish-mirror read-miss warnings, add the causes the
messages omit (a consumer-general artifact version skew on both branches, month-shard rollover on
the partial branch only), and pin both with guards that are RED against the current tree.

Purpose: under a sidecar/publish version skew the warning today fires naming a cache-version
rotation the reader never made, while never naming the cause that occurred. The closed enumeration
is the defect, not the missing item -- this same message already shed a true cause once (`54677af`)
with nobody noticing.

Output: corrected messages and comments, new guard assertions on both branches, two consistency
edits in `docs/advanced.md`, and the guard-row docstring recording why the cardinality left the prose.
</objective>

<context>
@.planning/quick/260809-og2-address-the-rollover-misattribution-in-t/260809-og2-RESEARCH.md
@.planning/quick/260809-og2-address-the-rollover-misattribution-in-t/260809-og2-CONTEXT.md
@packages/github-cache/src/publish/publish-mirror.ts
@packages/github-cache/src/publish/publish-mirror.spec.ts
@docs/advanced.md
</context>

<authoritative_source>
RESEARCH.md OVERTURNED CONTEXT.md's one open question. Bundle drift IS consumer-reachable:
`dist/publish/publish-mirror.js` ships in the npm tarball (measured by `npm pack --dry-run`), and
`docs/advanced.md:139-145` sanctions an adopter wiring publish. Where the two artifacts differ,
RESEARCH wins and CONTEXT's `<unresolved>` block is answered, not still open.

Lift these sections VERBATIM rather than composing new prose -- each was already checked against the
guard rows and the blocklist:
- RESEARCH `## Wording the implementation may use` -- both message bodies, both docs edits.
- RESEARCH `## What the guard must assert` -- the assertions.
- RESEARCH `## Claims the implementation MUST NOT make` -- 9 items, HARD BLOCKLIST.

Line numbers below were read at HEAD. Re-locate by content; do not trust the number.
</authoritative_source>

<vacuity_checks_already_run>
Run against the CURRENT tree before this plan was written, so the executor does not re-derive them.

| Command | Result | What it settles |
|---|---|---|
| `rg -c -F 'Two candidate causes' packages/github-cache/src/publish/publish-mirror.ts` | exit 1 | A source-file gate on this phrase is SILENTLY VACUOUS -- the literal is split across a template concatenation in both branches. It would pass green against the unchanged file. |
| `rg -c -F 'candidate causes' packages/github-cache/src/publish/publish-mirror.ts` | 3, exit 0 | Positive control for the row above: the zero is real, not a tooling failure. ALSO the usable negative needle -- all three occurrences sit on one line each (the wrap falls before `candidate`), so this is the one grep-level gate on this file that is not wrap-defeated. |
| `rg -c -F 'different versions of this action' packages/github-cache/src/publish/publish-mirror.ts` | exit 1 | The presence needle is RED before the edit. |
| `rg -c -F 'not exhaustive' packages/github-cache/src/publish/publish-mirror.ts` | exit 1 | The Shape C needle is RED before the edit. |
| `rg -c -F 'publishMirror' start-cache-server/index.js` (control `createCacheServer` -> 3, exit 0) | exit 1 | The publish engine is NOT inlined in the sidecar bundle, so this change owes NO `npm run check:action` regeneration. |
| `rg -n -F 'two candidate causes' docs/advanced.md` | `:98`, exit 0 | Docs cardinality site 1 is live, on one line. |
| `rg -n -F 'the same two causes worth checking' docs/advanced.md` | `:105`, exit 0 | Docs cardinality site 2 is live, on one line. |
| `npx nx show project github-cache` | `test` present | The runnable gate is `npx nx test github-cache`, not a missing-target no-op. |
| `rg -c -F 'the first publish run against a new month shard' <src>` | exit 1 | The rollover presence needle is RED before the edit. |
| `rg -c -F 'new month shard' <src>` and `<spec>` | exit 1 on BOTH | The rollover needle is RED on both files, so the total-gate ABSENCE assertion is non-vacuous and the phrase is not already planted in the spec. |
| `rg -c -F 'a one-time rise is expected' <src>` | exit 1 | The alternative rollover needle is RED too. |
| `rg -c -F 'month shard' <src>` | **3, exit 0** | REJECTED AS A NEEDLE. The bare bigram already occurs in the asset-cap warning and the shard machinery, so a pin on it would be green before the edit. `new month shard` is the discriminating form and the one both assertions must use. |

WHAT NO GREP CAN GATE HERE, stated so nobody adds one and believes it: a re-introduced closed
enumeration written across a concatenation boundary is invisible to `rg`, and `-U` with `\s+` cannot
bridge the `' +` / `'` join or a `//` comment continuation either. The authoritative negative gate is
the spec assertion over the JOINED runtime string. The `candidate causes` grep above is a cheap
second net that happens to work today; it is not the gate.
</vacuity_checks_already_run>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the guards RED, before touching the messages</name>
  <files>packages/github-cache/src/publish/publish-mirror.spec.ts</files>
  <behavior>
    - Partial fixture `runWithMisses(10, 9)`: no recorded warning matches the closed-enumeration phrase.
    - Partial fixture: the recorded warning names an artifact version skew.
    - Partial fixture: the recorded warning names the month-shard rollover cause -- THE ASYMMETRY, POSITIVE HALF.
    - Total-gate fixture (all-MISS over two keys): the same absence plus skew-presence pair.
    - Total-gate fixture: no recorded warning names the month-shard rollover cause -- THE ASYMMETRY, NEGATIVE HALF.
    - Total-gate fixture: the two shared cause phrases pinned positively -- that case has no positive pin on its cause list today.
    - Neither fixture's warning names an internal build artifact of this repository.
  </behavior>
  <action>
Extend the existing cases inside `describe('publishMirror split metric and partial-miss guard (D4,
D5)')`. Do not add a parallel describe block.

1. In `warns ONCE just ABOVE the target rate (10 entries, 9 misses -- bound 0.5958)`, after the
   existing `toContain` pins, add the absence plus presence pair from RESEARCH `## What the guard
   must assert`: read `vi.mocked(core.warning).mock.calls.flat()` into a local and assert
   `not.toContainEqual(expect.stringMatching(...))` for the closed-enumeration phrase, then assert
   the skew needle with `toContain` on `mock.calls[0][0]`. `toHaveBeenCalledOnce()` is already
   pinned in that case -- keep it, it is the quantifier half of the pair.
2. SAME CASE, THE ASYMMETRY'S POSITIVE HALF: add a `toContain` pin on `the first publish run
   against a new month shard`. This is the ONE claim the whole task exists to add, and RESEARCH's
   `## What the guard must assert` omits it -- the omission is the reason it is called out here
   rather than lifted. Do NOT weaken the needle to a bare `month shard`: measured, that bigram
   already occurs three times in this engine (the asset-cap warning and the shard machinery), so a
   pin on it is green before the edit. `new month shard` returns exit 1 today on both the source and
   the spec.
3. In `fires the TOTAL-case branch, not the partial one, when everything missed`, add the same
   closed-enumeration absence and skew-presence pair, plus positive `toContain` pins on
   `cache-version rotation in this commit range` and on `runtime token's Actions-cache read scope`.
   Keep the existing `not.toContain('not of the restores attempted')` discriminator untouched -- it
   is what tells the two branches apart.
4. SAME CASE, THE ASYMMETRY'S NEGATIVE HALF: assert `not.toContainEqual(expect.stringMatching(/new
   month shar[d]/))` over `mock.calls.flat()`. This is what stops an executor "completing" the
   total gate with a cause it cannot have, which CONTEXT.md locked against and which no prose can
   enforce. Split per this file's single-character-class convention, same as the sibling needles.
   Pair it with the step-2 pin in the SAME commit -- alone, either half is satisfiable by putting
   the clause on neither branch.
5. In `names nothing a consumer cannot observe in their OWN repository (W3)`, add ONE further
   `not.toContainEqual` row covering this repository's internal build artifacts, alongside the three
   existing needles -- and add the SAME row to the total-gate case from step 3, because the
   blocklist scopes to either warning MESSAGE and the total gate otherwise ships with no
   distribution-constraint guard at all. Both are already green today and are load-bearing only in
   pair with the presence pins above; absence alone is satisfied by deleting the message.

THREE CONVENTIONS THIS FILE ALREADY ENFORCES; breaking any one silently vacates the guard:
- SPLIT THE FORBIDDEN PHRASE with a single-character character class, as the file does at
  `/differen[t] OS/` and `/see[d] filter/`. Spelling it whole plants it in the file that proves it
  absent, and a repo-wide search can then no longer tell the guard from a regression.
- NEGATE THE QUANTIFIER, NEVER THE PREDICATE. `expect(core.warning).not.toHaveBeenCalledWith(
  expect.stringMatching(...))` asserts "SOME call lacks the phrase", not "no call carries it". Use
  `mock.calls.flat()` plus `not.toContainEqual`. `54677af`'s commit body records rejecting exactly
  that form, and the W3 case being extended already carries the comment explaining why.
- NEVER SUBSTITUTE A `readFileSync` CHECK. Measured above: the source-file grep returns exit 1 today,
  so such an assertion is green against the UNCHANGED file.

Each new assertion carries a one-sentence comment saying what it would catch. Do not restate the
three conventions -- the file explains them at the sites being extended; point at those.
  </action>
  <verify>
    <automated>LOG=$(mktemp); npx nx test github-cache --skip-nx-cache --output-style=stream 2>&amp;1 | tee "$LOG"; st=${PIPESTATUS[0]}; echo "exit=$st"; test $st -ne 0 &amp;&amp; rg -c -F -e 'different versions of this action' -e 'new month shard' "$LOG"</automated>
  </verify>
  <done>
`npx nx test github-cache` FAILS, and the failure output names the new assertions by their own
needles -- the skew phrase and the rollover phrase, both of which are absent from the current
messages. Not a syntax error, not an unrelated case. A GREEN run here means the guard is vacuous and
must be rewritten before proceeding; do not advance to Task 2 on green. Every pre-existing case in
the file still passes.

WHAT THESE GUARDS DO NOT COVER, said plainly so a future reader does not over-trust them: they pin
the reintroduction of THIS SPECIFIC PHRASING, not the class "a closed enumeration". A message
rewritten to say "these are the only two causes" or "the following causes are exhaustive" passes
every assertion here. RESEARCH concedes the class is not mechanically checkable and this plan
accepts that -- the phrase-level pin plus the `not exhaustive` presence pin in Task 2 is the whole
of the mechanical coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite both warning messages and the comments that direct the reader</name>
  <files>packages/github-cache/src/publish/publish-mirror.ts</files>
  <action>
Replace both `core.warning` bodies with RESEARCH `## Wording the implementation may use`, verbatim.
Re-locate by content: the total gate is the `hashes.length > 0 && readMisses === hashes.length &&
mirrored === 0` branch (around `:804-816`); the partial message is the `core.warning` closing the
`else if` (around `:895-902`).

THE ASYMMETRY IS DELIBERATE AND MUST SURVIVE. The partial branch gets FOUR causes including the
month-shard rollover; the total gate gets THREE and NOT the rollover. The total gate requires
`mirrored === 0`, so its shard never resolves, so the pre-restore membership skip never runs and
rollover cannot affect it. Adding it there would be a cause that branch cannot have -- the exact
error CONTEXT.md locked against. The skew cause DOES belong on both, on the total gate for the
consumer case only.

Both messages keep every existing cause verbatim (`cache-version rotation in this commit range --
the archive path literal or the cross-OS flag changed`, `runtime token's Actions-cache read scope`)
and every currently pinned literal (`9 of 10 server-produced cache entries (90%)`, `not of the
restores attempted`, `nothing mirrored`, `Two consecutive all-miss pushes`). Reworking any of those
phrasings owes an update to the corresponding spec assertion in the SAME commit.

Then the comments. Source comments are NOT consumer-facing and may name our artifacts freely:
1. The rotation-signal instruction (around `:866`, "Treat this branch as the live rotation signal")
   currently directs a reader to the cause that is absent under drift. Say the branch also fires on
   a version skew between the sidecar artifact and this one, and name action-bundle drift as our own
   instance of that class.
2. The stranger's-CI-log block above the partial message (around `:882-894`) closes by listing what
   survives as "the two candidate causes the sibling gate above already names". That sentence goes
   stale with this edit -- drop the cardinality.
3. Record, in that same comment block, that a self-perpetuating cohort -- an entry that MISSES is
   never mirrored, so it is never in the shard, so it is re-enumerated and retried forever -- is a
   real and currently unnamed contributor; that `54677af` dropped it from the message as collateral
   of a leak removal (state it as what the diff shows, NEVER as that commit's intent); and that it
   is deliberately NOT restored as a numbered cause, because "not exhaustive" now covers it and it
   is a consequence of any miss rather than an independently actionable diagnosis for a stranger.
   This is the answer to the silent-deletion failure mode: the drop goes on the record without
   paying the consumer-log cost.

   NARRATE THAT HISTORY WITHOUT QUOTING THE RETIRED PHRASE. The natural way to write "the old
   message listed two candidate causes" reuses the exact bigram this task removes, which correctly
   reddens the `candidate causes` gate below -- the gate cannot tell a quotation from a relapse, and
   it should not try. Say "the retired enumeration" or "the closed list" instead. If the phrase is
   genuinely needed, split it with a single-character class as the spec file does.

BLOCKLIST -- RESEARCH `## Claims the implementation MUST NOT make`, all 9 items. In the MESSAGES
specifically: never `action-bundle drift`, `start-cache-server/index.js`, `dist/action/index.js`, or
any `.planning/` path. Never a restored cardinality of any size. Never eviction or branch/ref
scoping as causes -- both measured closed. Never a figure not carrying run id `31305961054`. Never a
claim that this repository's bundle drift can trip the total gate. It stays `core.warning`; nothing
here may add a red signal. ASCII only -- `--` not an em dash, `->` not an arrow.
  </action>
  <verify>
    <automated>LOG=$(mktemp); npx nx test github-cache --skip-nx-cache --output-style=stream 2>&amp;1 | tee "$LOG"; st=${PIPESTATUS[0]}; echo "exit=$st"; test $st -eq 0</automated>
    <automated>npx nx run github-cache:typecheck &amp;&amp; npx nx run github-cache:lint</automated>
    <automated>rg -c -F 'not exhaustive' packages/github-cache/src/publish/publish-mirror.ts &amp;&amp; rg -c -F 'different versions of this action' packages/github-cache/src/publish/publish-mirror.ts</automated>
    <automated>rg -c -F 'candidate causes' packages/github-cache/src/publish/publish-mirror.ts; test $? -eq 1 &amp;&amp; rg -c -F 'restored as a MISS' packages/github-cache/src/publish/publish-mirror.ts</automated>
    <automated>node -e "const s=require('node:fs').readFileSync('packages/github-cache/src/publish/publish-mirror.ts','utf8');const bad=[...s].filter(c=>c.charCodeAt(0)>126);if(bad.length){console.error('non-ASCII: '+bad.join(''));process.exit(1)}console.log('ascii-clean')"</automated>
  </verify>
  <done>
The full `github-cache` test target is GREEN, typecheck and lint pass, `candidate causes` returns
exit 1 over the source (it returned 3 before -- the paired positive control `restored as a MISS`
must still return a nonzero count, or the zero means the messages were deleted rather than
rewritten), both new needles are present, and the file is ASCII-clean.

The asymmetry is proven by the Task 1 assertions flipping green together -- the partial fixture's
rollover pin AND the total-gate fixture's rollover absence -- not by this task's greps. A source-file
count of the rollover phrase is deliberately NOT used as a gate: "appears once" cannot prove "appears
in the partial branch", and a count gate is equally satisfied by putting the clause on the wrong
branch. Only the two runtime assertions localize it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retire the cardinality from the prose and record why in its guard row</name>
  <files>docs/advanced.md, packages/github-cache/src/docs-same-os-claims.spec.ts</files>
  <action>
Two edits in `docs/advanced.md`, verbatim from RESEARCH `## Wording the implementation may use`:
- `:98` -- `and two candidate causes.` becomes `and the causes worth checking.`
- `:105` -- `the same two causes worth checking` becomes `the same causes worth checking`

Neither sentence is pinned by any guard row, which is exactly why the edit must be made deliberately
in this commit instead of being discovered stale later. RESEARCH confirmed both edits leave every
phrase the `260809-hcr` OBS-04 row requires intact (`depends on whether the publish run also wrote
entries of`, `a warning once that cohort dominates the enumeration`, `that is the shape the all-MISS
warning is reserved`) and match none of its forbidden patterns. Do not reflow the surrounding
paragraph: those three required phrases must each stay on ONE line per that table's rule.

Then, per the file's own house rule that a reworded site updates its ROW in the same commit, extend
the OBS-04 row's docstring in `docs-same-os-claims.spec.ts` (the second entry of `DOCS_08_SITES`,
the one whose `forbidden` list carries `/warning it emits names the axis/`). Record: that the prose
asserted a cardinality the messages no longer carry; that the messages dropped the closed
enumeration because a closed list already shed a true cause once without anyone noticing; and that
the cardinality is deliberately NOT re-pinned as a `required` phrase, since pinning a count is the
defect this change removes. Do not add a new row and do not add a new `required` entry.

Scope fences: `.planning/REQUIREMENTS.md` stays untouched. Nothing is merged. No new section in
`docs/advanced.md` -- the message points at no doc, so there is nothing to redirect, and a new
load-bearing doc claim would owe a new `DOCS_08_SITES` row.
  </action>
  <verify>
    <automated>rg -n -F -e 'two candidate causes' -e 'the same two causes' docs/advanced.md; test $? -eq 1 &amp;&amp; rg -c -F -e 'the causes worth checking' -e 'the same causes worth checking' docs/advanced.md</automated>
    <automated>rg -c -F -e 'depends on whether the publish run also wrote entries of' -e 'a warning once that cohort dominates the enumeration' -e 'that is the shape the all-MISS warning is reserved' docs/advanced.md</automated>
    <automated>LOG=$(mktemp); npx nx test github-cache --skip-nx-cache --output-style=stream 2>&amp;1 | tee "$LOG"; st=${PIPESTATUS[0]}; echo "exit=$st"; test $st -eq 0</automated>
    <automated>git diff --name-only | rg -c -F 'REQUIREMENTS.md'; test $? -eq 1</automated>
  </verify>
  <done>
Neither cardinality phrase survives in `docs/advanced.md` and both replacements are present; all
three OBS-04 required phrases still match, each on its own line; the full test target is green; the
OBS-04 row docstring records the change; `.planning/REQUIREMENTS.md` is absent from the diff.
  </done>
</task>

</tasks>

<verification>
1. `npx nx test github-cache` green with the new assertions present on BOTH branch fixtures.
2. `npx nx run github-cache:typecheck` and `:lint` green.
3. `rg -c -F 'candidate causes' packages/github-cache/src/publish/publish-mirror.ts` exits 1 (was 3).
4. Both new needles present in the source; the file is ASCII-clean.
5. `docs/advanced.md` carries neither cardinality phrase and still satisfies the OBS-04 row.
6. `git diff --name-only` lists exactly the four files in `files_modified` and nothing else -- in
   particular no `.planning/REQUIREMENTS.md` and no `start-cache-server/index.js` (measured: the
   publish engine is not in that bundle, so `npm run check:action` has nothing to regenerate; if it
   DOES report drift, that is a pre-existing condition of the tree, not this change).
7. Nothing is merged. `setFailed` is untouched.
</verification>

<success_criteria>
Both warnings state their cause list is not exhaustive, both name the version-skew cause in
consumer-general terms, the partial branch alone names month-shard rollover, a RED-then-GREEN guard
pair covers each branch through the engine, the prose no longer asserts a stale cardinality, and the
`54677af` cause-drop is on the record in a source comment without entering a stranger's CI log.
</success_criteria>

<output>
Commit all four files together. The docs edit and the guard-row docstring must not be split from the
message change -- the prose is only correct relative to the messages it describes.
</output>
