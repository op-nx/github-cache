---
phase: quick-260810-v1g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md
  - .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md
  - .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-SUMMARY.md
  - .planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md
  - .planning/quick/260809-2s6-stop-the-publish-mirror-from-re-enumerat/260809-2s6-SUMMARY.md
  - .planning/v0.0.2-MILESTONE-AUDIT.md
autonomous: true
requirements: [AUDIT-ITEM-A, AUDIT-ITEM-B]

must_haves:
  truths:
    - "All 22 commits in 4518787..1b06816 carry a recorded eight-gate verdict, each attributable to a captured log file by path."
    - "The per-commit result is stated as measured, with reds (if any) named as findings rather than smoothed over; no cost excuse survives in either kuo record."
    - "10-VERIFICATION.md's readMisses expectation carries a MEASURED value attributed to run 31305961054, and the reasoning for why it was left open is superseded in place, not deleted."
    - "The milestone audit no longer carries item B as open, states which of the two NOT OBSERVED sections it misread, and its item count arithmetic and Assessment agree with the corrected frontmatter."
    - "The working tree ends on branch gsd/v0.0.2-os-invariant-cross-os-sharing at its pre-sweep tip, with no tracked file left modified by the sweep."
  artifacts:
    - ".planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md (new: 22-row gate matrix plus the resolved log directory)"
    - ".planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md (edited: second addendum carrying the sweep result)"
    - ".planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-SUMMARY.md (edited: stale references reconciled)"
    - ".planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md (edited: readMisses row superseded in place)"
    - ".planning/quick/260809-2s6-stop-the-publish-mirror-from-re-enumerat/260809-2s6-SUMMARY.md (edited: pointer on the FIRST NOT OBSERVED section)"
    - ".planning/v0.0.2-MILESTONE-AUDIT.md (edited: item B closed, misreading named, counts corrected)"
  key_links:
    - "SWEEP.md data-row count <-> git rev-list --count 4518787..1b06816 (the matrix must cover the whole range, not a subset)"
    - "SWEEP.md verdict <-> kuo-VERIFICATION.md addendum status (the status follows the measurement; it is not asserted independently of it)"
    - "10-VERIFICATION.md's substituted figures <-> publish-mirror.ts's transcribed 43/112 and 43/113 with run id 31305961054 (the record must not invent a third denominator)"
    - "MILESTONE-AUDIT.md frontmatter (tech_debt, open_by_design) <-> its prose sections and the Total line (frontmatter and prose must not disagree after the correction)"
---

<objective>
Close the two substantive items of the v0.0.2 milestone audit.

Item A is a real measurement: gate all 22 commits of `4518787..1b06816` on all eight gates, then
reconcile the two kuo records that currently understate what was measured. Item B is a records
closure: the three NOT OBSERVED items are already closed by run `31305961054`, so `10-VERIFICATION.md`
and the milestone audit must be corrected to say so, including naming the audit's own misreading.

Purpose: the audit's two open items are both honest refusals to certify. One needs the measurement
that was never run; the other needs a correction, because the thing it refuses to certify was in fact
measured a day earlier and the audit read the wrong section of a summary with two same-titled
sections.

Output: one new SWEEP.md evidence matrix and five edited records. No source change, no push, no
window, no backup ref, no `gh workflow disable`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-CONTEXT.md
@.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-RESEARCH.md
@.planning/v0.0.2-MILESTONE-AUDIT.md
@.planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md
@.planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md
</context>

<facts_do_not_re_derive>
Measured already. Re-deriving any of these wastes the budget the sweep needs.

- The range is exactly 22 commits, all live ancestors of HEAD. Two commits were REWORDED; the
  pre-reword hashes (`65d36e1`, `703a261`) are dangling but still resolve in `git log --all`.
  Enumerate with `git rev-list --reverse 4518787..1b06816`; validate membership with
  `git merge-base --is-ancestor <sha> HEAD`, never with `git log --all`.
- One full uncached eight-gate battery is 13-15 seconds at HEAD, warm or cold. Whole sweep is under
  ten minutes. There is no cost reason to stop short of 22/22.
- `package.json` and `package-lock.json` are unchanged across `4518787..HEAD`. The main tree's
  existing install therefore serves every checkout: **no `npm ci` is needed at all**, not even once.
  Task 1 asserts this rather than assuming it.
- `.prettierignore` ignores `.planning/`, and `npm run format:check` measured exit 0 at HEAD with
  this task's untracked directory present. The recorded `CLAUDE-SECURITY-*` pollution trap therefore
  does not apply to this sweep, provided `git status --porcelain` shows nothing but the untracked
  `.planning/quick/260810-v1g-*/` directory before the sweep starts.
- `start-cache-server/index.js` is not touched by any commit in the range, so bundle drift at a
  mid-range commit would be a genuine finding, not noise.
- Run `31305961054`: event `push`, headBranch `main`, headSha `e3bf98b`, conclusion `success`,
  2026-08-09T09:27:44Z. `publish (ubuntu-24.04-arm)` job 93226687998: scanned 112, mirrored 10,
  readMisses 43, alreadyPresent 59. `publish (windows-11-arm)` job 93226687984: scanned 113,
  mirrored 1, readMisses 43, alreadyPresent 69. Both `publish-verify` legs (93227111969,
  93227112032) `success`. The `bead` cross-job round-trip under the new key `bead31305961054` is
  verbatim in both `dogfood-verify` logs, the windows one reading a `'linux'`-produced payload.
- `260809-2s6-SUMMARY.md` has TWO `## NOT OBSERVED` sections. The first (line ~127) is PLAN-1's,
  written before the window, and lists the three items. The second (line ~350) is PLAN-2's, written
  after, and lists two entirely different things. The audit read the first.
</facts_do_not_re_derive>

<tasks>

<task type="auto">
  <name>Task 1: Gate all 22 commits on all eight gates and record the matrix</name>
  <files>
    .planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md
    (script and logs live OUTSIDE the repo, in this session's scratchpad directory)
  </files>
  <action>
    Pre-flight, all four assertions before any checkout. (1) `git status --porcelain` lists nothing
    but the untracked `.planning/quick/260810-v1g-*/` directory -- if anything else appears, stop and
    report rather than sweeping a polluted tree. (2) `git rev-parse --abbrev-ref HEAD` is
    `gsd/v0.0.2-os-invariant-cross-os-sharing`; record `git rev-parse HEAD` as the restore target.
    (3) `git rev-list --count 4518787..1b06816` is 22. (4)
    `git diff --name-only 4518787..HEAD -- package.json package-lock.json` is empty, which is what
    licenses skipping the install; if it is non-empty, stop -- the cost model and the single-install
    assumption both break and the plan needs revising.

    Then write the sweep script to this session's scratchpad with the Write tool (never a heredoc,
    never inline `-c`) and run it with `bash`. Use the reference implementation in `<sweep_script>`
    below verbatim in shape; the only thing to substitute is the absolute scratchpad path. Record
    that resolved absolute path -- SWEEP.md must name it, because a verdict without its log path is a
    claim.

    The capture discipline in that script is load-bearing and is the whole reason the sweep is worth
    running (AGENTS.md): every gate `tee`s to its own per-iteration log; every exit status is captured
    into a VARIABLE; `exit ${PIPESTATUS[0]}` inline is forbidden because it terminates the loop after
    one iteration and reads as a clean pass over a battery that never ran; the log names carry the
    iteration index because `date +%s` has one-second resolution and 13-second iterations collapse
    into one file. Do NOT `break` on a red gate -- best effort means every commit gets a verdict, and
    Nx caches terminal output for successful runs only, so a red commit's output exists nowhere but
    its `tee` log.

    Three traps the script must honour. `--skip-nx-cache` on every invocation, or a warm `.nx/cache`
    replays a prior commit's verdict and the gate proves nothing. `nx run-many` exits 0 on a missing
    target, so assert on the printed `Successfully ran targets build, typecheck, test, integration,
    lint` line and treat `ran=0` with `nx=0` as RED, not green. `check:action` rebuilds the bundle and
    diffs it, so a genuinely drifting commit leaves `start-cache-server/index.js` modified and blocks
    the next checkout -- restore it with `git checkout -- start-cache-server/index.js` at the END of
    every iteration, unconditionally, AFTER the exit status is recorded, never before.

    If the session dies mid-sweep the recovery is `git checkout
    gsd/v0.0.2-os-invariant-cross-os-sharing`; nothing else is left behind, because the logs are
    outside the repo and no commit is made.

    The script also writes a sentinel `PATH.txt` into its own output directory, containing that
    directory's absolute path and nothing else. SWEEP.md then carries the same path on a single
    structured line beginning `LOG_DIR: `. The verify reads the structured line and cross-checks it
    against the sentinel, so a hand-typed or copy-pasted path in SWEEP.md fails rather than being
    believed. Do not make the verify regex-scan SWEEP.md's prose for a path: the script's own filename
    contains the same token as the log directory, so a prose mention would match first and a correctly
    executed sweep would fail.

    After the loop, assert HEAD is back on the branch at the recorded restore SHA, then write
    `260810-v1g-SWEEP.md` into this task's directory (ASCII only). It carries: the `LOG_DIR: ` line
    with the absolute path; the eight gate commands verbatim; a 22-row table, oldest first, with
    columns index, SHA, subject, nx exit, `Successfully ran targets` line count, format:check exit,
    check:action exit, fallow:ci exit; and a verdict paragraph.

    Two honesty rules for that verdict. If all 22 are green, say so and cite the log directory -- and
    say plainly that this supersedes kuo's "22 checkouts x 6 gates, not run" framing, which predates
    any measurement of the battery. If any commit is red, that is a real finding: name the commit, the
    gate, the log path and what the log says, and do NOT average it away or re-run until it is green.
    If a gate fails for a reason unrelated to the commit under test -- an instrument failure such as
    `nx format:check` being unable to resolve its base under a detached HEAD -- record it as an
    INSTRUMENT failure in its own column note, distinctly from a gate verdict. A red commit and a
    broken instrument are different findings and must not be merged into one number.

    Every INSTRUMENT classification must be falsifiable by a reader: cite the log file by path AND
    quote the verbatim line from that log that distinguishes an instrument failure from a real gate
    failure. No automated check can prove such a classification is CORRECT -- the only mechanical
    discriminator would be re-running until the gate goes green, which is exactly the pass condition
    the locked best-effort decision forbids. So the guarantee here is falsifiability, not automation:
    the verify asserts only that any INSTRUMENT row cites a log, and a reader can then open that log
    and disagree. State that limit in SWEEP.md rather than implying the classification was gated.

    Make no commits in this task. `.planning/` is tracked and its content changes under every
    checkout, so committing mid-sweep would corrupt the sweep; and by the time the sweep ends, the
    only new file is an untracked artifact the orchestrator owns.
  </action>
  <verify>
    <automated>SW=.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md; OUT=$(rg -N '^LOG_DIR: ' "$SW" | head -1 | cut -d' ' -f2-); [ -n "$OUT" ] && [ -d "$OUT" ] || { echo "FAIL: SWEEP.md LOG_DIR line names no reachable directory ($OUT)"; exit 1; }; [ "$(cat "$OUT/PATH.txt")" = "$OUT" ] || { echo "FAIL: SWEEP.md LOG_DIR disagrees with the sweep script sentinel PATH.txt"; exit 1; }; rows=$(rg -c '^[0-9]+ [0-9a-f]{7,40} nx=' "$OUT/RESULTS.tsv"); expected=$(git rev-list --count 4518787..1b06816); [ "$rows" -eq "$expected" ] || { echo "FAIL: RESULTS.tsv has $rows result rows, range has $expected"; exit 1; }; [ "$rows" -eq 22 ] || { echo "FAIL: expected 22 result rows, got $rows"; exit 1; }; logs=$(ls "$OUT" | rg -c '\.log$'); [ "$logs" -ge 88 ] || { echo "FAIL: only $logs gate logs captured, expected at least 88 (22 x 4)"; exit 1; }; ctrl=$(rg -l -F 'Successfully ran targets' "$OUT" | wc -l); [ "$ctrl" -ge 1 ] || { echo "FAIL: no log contains the run-many success line -- capture did not work"; exit 1; }; [ "$(git rev-parse --abbrev-ref HEAD)" = "gsd/v0.0.2-os-invariant-cross-os-sharing" ] || { echo "FAIL: not back on the branch"; exit 1; }; git diff --exit-code --quiet || { echo "FAIL: tracked files left modified by the sweep"; exit 1; }; sweeprows=$(rg -c '^\| *[0-9]+ *\|' "$SW"); [ "$sweeprows" -eq 22 ] || { echo "FAIL: SWEEP.md has $sweeprows data rows, expected 22"; exit 1; }; inst=$(rg -c -F INSTRUMENT "$SW" || echo 0); if [ "$inst" -gt 0 ]; then cited=$(rg -N -F INSTRUMENT "$SW" | rg -c -F '.log' || echo 0); [ "$cited" -ge 1 ] || { echo "FAIL: an INSTRUMENT classification cites no log file"; exit 1; }; fi; echo OK</automated>
  </verify>
  <done>
    `RESULTS.tsv` holds 22 result rows, one per commit in the range, matching `git rev-list --count`;
    at least 88 gate logs exist and at least one demonstrably captured `nx` output; HEAD is back on
    the branch with no tracked file modified; `SWEEP.md` carries 22 data rows, the absolute log
    directory, and a verdict that names any red commit or instrument failure explicitly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile item A's two kuo records against the measurement</name>
  <files>
    .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md
    .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-SUMMARY.md
  </files>
  <action>
    `260810-kuo-VERIFICATION.md` is `status: human_needed` on exactly one truth -- truth 6, "every
    commit leaves all six gates green" -- solely because the measurement did not exist. It now does.
    Append a THIRD dated section, in the same supersede-in-place style the file already uses twice,
    titled for this sweep and citing `260810-v1g-SWEEP.md` plus the absolute log directory. Record
    what was measured: 22 of 22 commits, eight gates each, uncached, on the main tree.

    Then update the frontmatter to follow the evidence, not to flatter it. If the sweep is 22/22
    green, truth 6 moves from `PRESENT_BEHAVIOR_UNVERIFIED` to verified, `score` becomes 7/7 and
    `behavior_unverified` becomes 0 -- and the second `human_verification` item (the SUMMARY
    reconciliation) is closed by this same task, so it goes too. Whether `status` then reads `passed`
    is a verdict, not bookkeeping: state the measurement and let the status follow it, and if the
    sweep found any red commit or instrument failure, `status` stays `human_needed` with the finding
    named. Do NOT flip it by fiat, and do not delete the original text of truth 6 or of the two
    `human_verification` entries -- supersede them in place with the new evidence, which is this
    project's established pattern and the reason the file is readable at all.

    Also correct the file's own now-stale cost framing where it appears: "full per-commit verification
    is 22 checkouts x 6 gates" was written without any measurement of the battery, and the measured
    figure is under ten minutes for the whole sweep. Say that, since it is the sentence that kept the
    item open.

    `260810-kuo-SUMMARY.md`: the audit names three stale references. VERIFY each before editing --
    two of the three were already fixed downstream, and blindly "correcting" a corrected file is how
    records rot. Measured now: the `read-back.spec.ts:363` citation already reads `:360` in both
    SUMMARY.md and `260810-kuo-deferred-items.md`; the frontmatter metrics already read
    `commits: 22` and `test_count: 1184`; and the bisect prose already reads "10 of the 22 commits, on
    `test` only". What IS still stale: the Self-Check section says `--is-ancestor` holds for "all 17
    commit hashes" and that `git log 4518787..HEAD` counted "exactly 17", and its last bullet says the
    working tree carries "the three untracked planning documents" where it is five; a fourth stale 17
    sits in the same Self-Check block, on the bullet counting ASCII-clean commit messages; and the
    paragraph about the other twelve commits still asserts full per-commit gating "was not run". Fix
    those four places: BOTH Self-Check bullets that count commits go to 22 (keeping the true statement
    that 17 was the count when the task closed, so the number is superseded rather than falsified), the
    untracked-document count to five, and the not-run claim replaced by a pointer to the sweep with its
    result. The ASCII bullet's claim stays true at 22 -- the re-verification already recorded the five
    review-fix commit messages as ASCII-clean with a positive-controlled scanner -- so that one is a
    count correction, not a re-audit. In the same edit, record in one line which of the audit's three
    named references were already closed and by what, so the next reader does not re-open them.
  </action>
  <verify>
    <automated>V=.planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md; S=.planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-SUMMARY.md; for needle in '260810-v1g-SWEEP.md' '22 of 22'; do c=$(git grep -c -F "$needle" -- "$V" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: $V does not cite $needle"; exit 1; }; done; c=$(git grep -c -F 'five untracked planning documents' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: SUMMARY self-check still miscounts the untracked documents"; exit 1; }; c=$(git grep -c -F '260810-v1g' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: SUMMARY does not point at the sweep"; exit 1; }; c=$(git grep -c -E 'all 22 commit hashes|22 commit hashes in the table' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: SUMMARY self-check still counts the table at 17 only"; exit 1; }; c=$(git grep -c -E '22 commit messages' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: SUMMARY self-check ASCII bullet still counts 17 commit messages"; exit 1; }; c=$(git grep -c -F ':360' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: the read-back citation regressed"; exit 1; }; echo OK</automated>
  </verify>
  <done>
    `260810-kuo-VERIFICATION.md` carries a third dated section citing `260810-v1g-SWEEP.md` and the
    22-of-22 measurement, with frontmatter `score`, `behavior_unverified`, `behavior_unverified_items`
    and `human_verification` reconciled to the evidence and `status` justified by it rather than
    asserted. `260810-kuo-SUMMARY.md`'s self-check counts 22 on BOTH commit-counting bullets (hashes
    and ASCII-clean messages) and five on the untracked documents, its not-run claim points at the
    sweep, the `:360` citation is intact, and the already-closed references are recorded as such.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close item B in the two records that carry it</name>
  <files>
    .planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md
    .planning/quick/260809-2s6-stop-the-publish-mirror-from-re-enumerat/260809-2s6-SUMMARY.md
  </files>
  <action>
    `10-VERIFICATION.md`'s first `live_ci_only_items` entry carries `expected: "... readMisses 0 ..."`
    and an `open_sub_item` that deliberately declines to substitute a DERIVED post-fix number, closing
    with an instruction to close it from the same live window that closes 260809-2s6's three
    unobserved items. That window happened: run `31305961054`, head `e3bf98b`, 2026-08-09.

    Rewrite the row to the MEASURED value, attributed. The standing rule holds and is the reason the
    row was left open: substitute only a measured value, never a derived one. The qualifying value is
    `readMisses 43 of scanned 112` on the ubuntu leg (job 93226687998) and `43 of 113` on the windows
    leg (job 93226687984), both from run `31305961054`. Carry the denominator with the count --
    `publish-mirror.ts` states the rule the codebase already follows, that the observable is
    `readMisses / scanned` and not a bare `readMisses`, because the count alone is consistent only at
    one denominator and `scanned` moves. Carry the run id, as every figure in this codebase does.

    State the provenance honestly rather than implying a fresh read: these five counts reach the job
    summary only and never the step log, so they were read from the job summary at the time by the
    task that ran the window and transcribed into tracked source with their run id. They are measured,
    not derived -- 260809-2s6 explicitly contrasts them against a derived 33% and a review's corrected
    41%, both wrong by the denominator while the miss count of 43 was right in both. A fresh
    first-hand re-read would need a browser against that run's page and is not required to close this.

    Supersede in place. Do NOT delete the `open_sub_item` reasoning -- it is the record of WHY the row
    was left un-rewritten, it is correct reasoning, and its instruction was satisfied rather than
    overruled. Append the closure to it, naming the run, and adjust the entry's `status` clause so a
    machine reading the frontmatter no longer sees an open sub-item.

    The file's top-level `score:` key goes stale in the same edit and must move with it: it currently
    reads that all three live-CI items were SAMPLED 2026-08-08 with "1 closed-with-one-clause-falsified",
    and that falsified clause is the very row being closed here. Rewrite it so all three read closed,
    naming the 2026-08-09 run as what closed the third. Leaving it would make the frontmatter
    self-contradictory against the row directly below it, which is the exact failure mode this whole
    task exists to correct elsewhere.

    Mention the irony worth having on record: the one code path that would put these numbers in the step log is the partial-read warning,
    which fires only above the threshold -- so success is exactly what makes the number unobservable
    via `gh`.

    `260809-2s6-SUMMARY.md` is the root cause of the audit's error: it has two `## NOT OBSERVED`
    sections and the audit read the first. Add a short pointer immediately under the FIRST heading --
    the one at roughly line 127, PLAN-1's, listing the three items -- recording that all three were
    closed by PLAN-2's own window, run `31305961054`, and that the second `## NOT OBSERVED` section
    later in the same file is PLAN-2's and lists two different things. Do not delete or rewrite either
    section: the first was true when written, and the pointer is what stops the next reader repeating
    the misreading. This is the cheapest of the three edits in item B and the only one that prevents a
    recurrence.
  </action>
  <verify>
    <automated>V=.planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md; S=.planning/quick/260809-2s6-stop-the-publish-mirror-from-re-enumerat/260809-2s6-SUMMARY.md; for needle in '31305961054' '43 of 112' '43 of 113' '93226687998'; do c=$(git grep -c -F "$needle" -- "$V" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: $V is missing $needle"; exit 1; }; done; c=$(git grep -c -F 'open_sub_item' -- "$V" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: the open_sub_item reasoning was deleted instead of superseded"; exit 1; }; c=$(git grep -c -F '31305961054' -- "$S" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: 2s6 SUMMARY carries no pointer to the closing run"; exit 1; }; first=$(git grep -n -F '## NOT OBSERVED' -- "$S" | head -1 | cut -d: -f2); ptr=$(git grep -n -F '31305961054' -- "$S" | head -1 | cut -d: -f2); [ "$ptr" -gt "$first" ] && [ $((ptr - first)) -le 12 ] || { echo "FAIL: the pointer is not adjacent to the FIRST NOT OBSERVED heading (heading $first, pointer $ptr)"; exit 1; }; node -e 'const y=require("js-yaml"),fs=require("fs");const fm=y.load(fs.readFileSync(process.argv[1],"utf8").split(/^---$/m)[1]);const s=String(fm.score);if(!/31305961054/.test(s)){console.log("FAIL: frontmatter score does not name the run that closed the third live-CI item");process.exit(1);}const it=fm.live_ci_only_items[0];if(!/43 of 112/.test(String(it.status))||!/31305961054/.test(String(it.status))){console.log("FAIL: the first live_ci_only_items status carries no measured figure with its run id");process.exit(1);}console.log("OK");' "$V"</automated>
  </verify>
  <done>
    `10-VERIFICATION.md`'s readMisses row carries `43 of 112` and `43 of 113` attributed to run
    `31305961054` with both job ids, the original `open_sub_item` reasoning preserved and superseded
    rather than deleted, the top-level `score:` key reconciled to all three live-CI items closed and
    naming that run, and no open sub-item left visible to a machine reading the frontmatter.
    `260809-2s6-SUMMARY.md` carries a pointer within a dozen lines of its FIRST `## NOT OBSERVED`
    heading naming the closing run and the existence of the second section.
  </done>
</task>

<task type="auto">
  <name>Task 4: Correct the milestone audit's own error and its arithmetic</name>
  <files>.planning/v0.0.2-MILESTONE-AUDIT.md</files>
  <action>
    Four coupled edits, and the framing matters: this is a correction of the audit's OWN error, not a
    quiet restatement. Say what was misread -- `260809-2s6-SUMMARY.md` carries two `## NOT OBSERVED`
    sections and the audit read the first, PLAN-1's, written before PLAN-2 opened the window that
    closed all three items.

    (1) Frontmatter. Remove the `phase: 09 + 10 (open by design, push-gated)` item from `tech_debt`
    and the single `open_by_design` entry, and add both closures to `closed_since_prior_audit`: item B
    with its evidence (run `31305961054`, head `e3bf98b`, readMisses 43 of scanned 112 on ubuntu and
    43 of 113 on windows, both `publish-verify` legs green, the `bead` cross-job round-trip under
    `bead31305961054`), and the audit's misreading named as the reason it was ever carried as open.
    Item A's `260810-kuo` entry likewise moves to closed, citing `260810-v1g-SWEEP.md` and the sweep's
    verdict -- and if the sweep found a red commit, it does NOT move: it becomes a named finding
    instead. Note that `open_by_design` becoming empty is the point; an empty key beats a stale one.

    (2) Prose. Delete the "### Open by design (1)" section's claim and replace it with the closure and
    the misreading; rewrite "### Open for the maintainer (1)" to the sweep's measured result. Both
    section headings' counts change, and if both items close, the audit has zero items in either
    category -- say so in the heading rather than leaving a "(1)" that no longer counts anything.

    (3) Arithmetic. "**Total: 12 items across 8 groupings.** One needs a maintainer decision, one
    needs a push to `main`, and the other ten are prose or cosmetic." Recount from the corrected
    frontmatter rather than by subtracting two from twelve -- the item count and the grouping count
    both move, and the "needs a push to `main`" clause is now false in its entirety, since no push was
    needed. State the new totals and drop the push clause.

    (4) Assessment. Its closing paragraph says "The two substantive items are both honest refusals to
    certify: a coverage claim narrower than its wording, and a measured expectation left un-rewritten
    rather than replaced by a computed one." Both are now closed, and by different means -- one by
    running the measurement that was missing, one by discovering the measurement already existed.
    Rewrite the paragraph to that, and keep the shape observation about artifact prose, which is still
    true and is the audit's most useful finding.

    Do not touch anything else in the audit. The ten prose and cosmetic items, the v0.0.3 deferral
    lane and the unattributed `69bd1b7` failure are all explicitly out of scope, and the requirements,
    phase, integration and Nyquist sections are untouched by either item.
  </action>
  <verify>
    <automated>A=.planning/v0.0.2-MILESTONE-AUDIT.md; c=$(git grep -c -F '31305961054' -- "$A" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: audit does not cite the closing run"; exit 1; }; c=$(git grep -c -F '260810-v1g-SWEEP.md' -- "$A" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: audit does not cite the sweep evidence"; exit 1; }; c=$(git grep -c -E 'NOT OBSERVED' -- "$A" || echo 0); [ "$c" -ge 1 ] || { echo "FAIL: audit does not name what it misread"; exit 1; }; c=$(git grep -c -F 'Total: 12 items across 8 groupings' -- "$A" || echo 0); [ "$c" -eq 0 ] || { echo "FAIL: the stale total survives"; exit 1; }; c=$(git grep -c -F 'two substantive items' -- "$A" || echo 0); [ "$c" -eq 0 ] || { echo "FAIL: the Assessment still claims two substantive items"; exit 1; }; node -e 'const y=require("js-yaml"),fs=require("fs");const t=fs.readFileSync(process.argv[1],"utf8");const fm=y.load(t.split(/^---$/m)[1]);const fail=m=>{console.log("FAIL: "+m);process.exit(1);};const groups=fm.tech_debt||[];const phases=groups.map(g=>String(g.phase));if(phases.some(p=>/open by design/.test(p)))fail("item B is still carried as a tech_debt group");if((fm.open_by_design||[]).length)fail("open_by_design is still populated");const closedList=fm.closed_since_prior_audit||[];const closed=closedList.join(" ");if(!/31305961054/.test(closed))fail("closed_since_prior_audit does not carry item B evidence run");if(!/NOT OBSERVED/.test(t))fail("the audit does not name the section it misread");if(!(closedList.length>=11))fail("closed_since_prior_audit gained no entries (was 10)");const kuo=groups.filter(g=>/260810-kuo/.test(String(g.phase)));if(kuo.length===0){if(!/260810-v1g-SWEEP/.test(closed))fail("item A left tech_debt without a closed entry citing the sweep");}else if(!/260810-v1g-SWEEP/.test(JSON.stringify(kuo)))fail("item A retained as debt without citing the sweep finding");console.log("frontmatter OK");' "$A" || exit 1; FILES=".planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-VERIFICATION.md .planning/quick/260810-kuo-apply-the-15-triaged-survivors-of-the-si/260810-kuo-SUMMARY.md .planning/phases/10-os-invariant-releases-mirror/10-VERIFICATION.md .planning/quick/260809-2s6-stop-the-publish-mirror-from-re-enumerat/260809-2s6-SUMMARY.md .planning/v0.0.2-MILESTONE-AUDIT.md"; for f in $FILES; do [ -f "$f" ] || { echo "FAIL: missing touched file $f"; exit 1; }; done; bad=$(rg -l '[^\x00-\x7F]' $FILES || true); [ -z "$bad" ] || { echo "FAIL: non-ASCII in $bad"; exit 1; }; echo OK</automated>
  </verify>
  <done>
    Item B is gone from `tech_debt` and `open_by_design` is empty; `closed_since_prior_audit` has GAINED
    at least one entry and carries run `31305961054`; item A has either left `tech_debt` with a closed
    entry citing `260810-v1g-SWEEP.md`, or been retained as a named finding that cites the sweep -- both
    directions of the frontmatter edit are checked, so a half-done edit fails. The prose sections and
    their counts agree with the frontmatter, the stale `Total: 12 items across 8 groupings` line and the
    `two substantive items` claim are both gone, the file names the two-`NOT OBSERVED`-sections
    misreading as the origin of item B's false openness, and all six touched files are ASCII-only.
  </done>
</task>

</tasks>

<sweep_script>
Reference implementation for task 1. Write this to the session scratchpad with the Write tool as
`v1g-sweep.sh` (substituting the real absolute scratchpad path for `OUT`), then run
`bash <scratchpad>/v1g-sweep.sh`. Do not paste it into an inline `bash -c`; the `${PIPESTATUS[0]}`
captures and the nested quoting are exactly what inline invocation mangles.

```bash
#!/usr/bin/env bash
# No `set -e`: a red gate must be RECORDED, not fatal.
OUT="<absolute scratchpad path>/v1g-sweep"
BRANCH=gsd/v0.0.2-os-invariant-cross-os-sharing
mkdir -p "$OUT"
: > "$OUT/RESULTS.tsv"
printf '%s' "$OUT" > "$OUT/PATH.txt"   # sentinel: SWEEP.md's LOG_DIR line is checked against this

i=0
for sha in $(git rev-list --reverse 4518787..1b06816); do
  i=$((i + 1))
  git checkout --quiet "$sha" || { echo "$i $sha checkout-failed" | tee -a "$OUT/RESULTS.tsv"; break; }

  L="$OUT/$i-$sha"
  npx nx run-many -t build typecheck test integration lint --skip-nx-cache 2>&1 | tee "$L-nx.log"
  nx=${PIPESTATUS[0]}
  ran=$(rg -c -F 'Successfully ran targets build, typecheck, test, integration, lint' "$L-nx.log" || echo 0)

  npm run format:check 2>&1 | tee "$L-format.log"; fmt=${PIPESTATUS[0]}
  npm run check:action  2>&1 | tee "$L-action.log"; act=${PIPESTATUS[0]}
  npm run fallow:ci     2>&1 | tee "$L-fallow.log"; fal=${PIPESTATUS[0]}

  # Trap: check:action rebuilds the bundle. Restore AFTER the verdict is recorded, always.
  git checkout --quiet -- start-cache-server/index.js

  echo "$i $sha nx=$nx ran=$ran format=$fmt action=$act fallow=$fal" | tee -a "$OUT/RESULTS.tsv"
done

git checkout --quiet "$BRANCH"
git rev-parse --abbrev-ref HEAD
```

Oldest-first is deliberate, not stylistic: it is the only order in which a truncated sweep is a
contiguous prefix, which is what a bisect-safety claim needs to mean anything. A cherry-picked subset
proves nothing about bisect safety no matter which subset it is.
</sweep_script>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical commit -> local working tree | 22 checkouts execute each commit's own test and lint code in the main tree |
| recorded audit artifact -> future reader | every record edited here is read later as settled evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v1g-01 | Tampering | the five edited records | medium | mitigate | Every substituted figure is a measured value carrying its run id or log path; supersede in place, never delete the superseded reasoning, so a later reader can audit the substitution |
| T-v1g-02 | Repudiation | the sweep verdict | high | mitigate | Per-gate `tee` logs outside the repo with the absolute directory named in SWEEP.md; a verdict without a reachable log is not a verdict |
| T-v1g-03 | Denial of Service | working tree left detached mid-sweep | low | mitigate | Unconditional bundle restore per iteration and a `git checkout <branch>` recovery documented in task 1; no commit is made during the sweep |
| T-v1g-04 | Elevation of Privilege | production writes to the public repo | low | accept | No push, no window, no ref creation, no workflow toggle. Item B is closed from a permanent existing run, so the whole class is out of scope by construction |
| T-v1g-SC | Tampering | package-manager installs | low | accept | No install runs: `package.json` and `package-lock.json` are measured unchanged across the range, so no package enters the tree and the legitimacy gate has nothing to audit |
</threat_model>

<verification>
1. `260810-v1g-SWEEP.md` has 22 data rows, one per commit in `4518787..1b06816`, each with five
   recorded gate results and a reachable absolute log directory.
2. Working tree ends on `gsd/v0.0.2-os-invariant-cross-os-sharing` at its pre-sweep tip, with
   `git diff --exit-code` silent -- the sweep left no tracked file modified.
3. `260810-kuo-VERIFICATION.md` cites the sweep, and its frontmatter status is justified BY the
   measurement rather than asserted alongside it.
4. `10-VERIFICATION.md` carries a measured `readMisses` figure with its denominator, both job ids and
   run `31305961054`, and still contains the original reasoning for why the row was left open.
5. `260809-2s6-SUMMARY.md` carries a pointer adjacent to its FIRST `## NOT OBSERVED` heading.
6. `.planning/v0.0.2-MILESTONE-AUDIT.md` frontmatter and prose agree: neither item is open, both are
   under `closed_since_prior_audit`, the total is recounted, and the misreading is named.
7. All authored content is ASCII across all six touched files -- wired into task 4's `<automated>` as
   its final cross-file step, so it gates during execution rather than sitting here unenforced.
8. NOT automated, deliberately: that an INSTRUMENT classification in SWEEP.md is CORRECT. The only
   mechanical discriminator is re-running until the gate goes green, which is the pass condition the
   locked best-effort decision forbids. Task 1 requires each such row to cite its log and quote the
   distinguishing line, and the verify asserts the citation exists -- so the claim is falsifiable by a
   reader. Recorded here as a known unguarded edge rather than papered over with a check that cannot
   fail.
</verification>

<success_criteria>
- 22 of 22 commits gated on all eight gates, with every verdict attributable to a named log file.
- Any red commit or instrument failure is reported as a finding, with its log, and not averaged away.
- Both audit items are closed in every record that carries them, with the audit's own misreading
  named rather than quietly overwritten.
- No push, no window, no backup ref, no workflow toggle, no source-file change, and no commit made by
  the executor -- all six touched files are `.planning/` artifacts the orchestrator commits.
</success_criteria>

<commits>
The executor makes NO commits. Nothing outside `.planning/` changes: the only working-tree writes are
the per-iteration `start-cache-server/index.js` restore (a no-op unless a commit genuinely drifts,
and reverted either way) and the sweep logs, which live outside the repo. All six touched files are
`.planning/` documents, so they land in the orchestrator's docs commit. Committing mid-sweep is
actively harmful, because `.planning/` is tracked and its content changes under every checkout.

If the sweep finds a genuine bundle drift at a mid-range commit, that is a FINDING for SWEEP.md, not
a fix to commit -- `start-cache-server/index.js` is byte-unchanged across the whole range and must
stay that way.

Use `git commit -F <file>` if any commit ever becomes necessary; `git commit -m` fails on this repo's
D: Dev Drive with a COMMIT_EDITMSG "Invalid argument".
</commits>

<output>
Create `.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SUMMARY.md`
when done.
</output>
