# v0.0.2 "OS-invariant cross-OS sharing" -- Integration Check

Scope: Phases 7-13. Per-phase VERIFICATION.md/SECURITY.md/VALIDATION.md already report
"passed" / "nyquist_compliant: true" and are NOT re-verified here. This audit traces only the
seams between phases and the E2E flows (O1-O4) named in the brief.

## Wiring Summary

Connected: 8 of 8 named cross-phase seams traced end-to-end and found WIRED.
Orphaned: 0.
Missing: 0 BLOCKER-level breaks found. 1 WARNING (documentation-only, since fixed in-branch;
recorded below for completeness).

## API / Data-flow Coverage

Consumed: every cross-phase artifact checked (lint target, hash-parity records, cache
version, Release asset name, cleanup filter, CACHE_READ_ONLY knob, docs contract) has a live
consumer verified against actual file content, not against planning prose.
Orphaned: none found.

## Auth / Trust Protection

Not in scope for this audit (covered by per-phase SECURITY.md / gsd-security-auditor per
TRUST-13, confirmed present at .planning/phases/10-os-invariant-releases-mirror/10-SECURITY.md line 76).

## E2E Flows (the milestones 4 target outcomes)

Complete: 4 of 4 (O1, O2, O3, O4) -- all PROVEN with live-CI or local-measurement evidence,
not merely planned.
Broken: 0.

---

## Detailed Findings, by seam

### Seam 1: Phase 7 -> Phase 8 (lint target folded into task-hash invariant)

Status: WIRED.

Evidence run:
- capture-hashes.mjs line 85 -- const TARGETS = [build, typecheck, test, integration, lint];
  -- lint is measured by the same Nx-native createTaskHasher().hashTask() call as the other
  four targets, not re-derived.
- packages/github-cache/src/hash-parity/compare.ts lines 82-87 -- export const INVARIANT_TARGETS
  = [build, typecheck, test, lint] -- lint is one of the FOUR targets required
  byte-identical across the two platform records (compareHashParitys clause (c) loop at
  line 528).
- .github/workflows/ci.yml lines 1839-1872 (hash-parity-compare job) -- build-gating (grepped the
  whole file for continue-on-error: 0 hits outside comments discussing its absence),
  if: cancelled() is false (never governs pass/fail), asserts on record CONTENT via
  assert-parity.js plus a grep -q anchor on the literal success line.
- 10-EVIDENCE-LIVE-CI.md line 182 -- lint was measured absent from the Releases mirror BY
  DESIGN (the lint CI job never starts the cache sidecar), which is a documented, deliberate
  scope boundary and not a coverage gap in the invariant-target set.
- No OS-divergent lint inference: plan 08-05 measured lints hash byte-identical
  (6930879416208693542) on both legs before D-21s clause was written; hash-parity-compare
  re-asserts this on every CI run, not just once historically.

Requirement IDs touched: LINT-01, LINT-04, CORR-06 (Phase 7) into PARITY-01/03/06, CORR-03
(Phase 8). All WIRED.

### Seam 2: Phase 8 -> Phases 9-12 (CORR-03 continuously guards downstream phases)

Status: WIRED.

- hash-parity-compare (ci.yml line 1839) has NO continue-on-error, no advisory period, and no
  needs.*.result reference anywhere in its if: -- verdict is derived purely from downloaded
  record content (confirmed by reading compare.ts in full: compareHashParity throws/returns
  ok:false on 8 distinct named fault reasons, none of which is bypassable by job status).
- Invariant-target set = build, typecheck, test, lint identical, integration divergent
  -- confirmed both in the comparator source and in the jobs own inline comment restating the
  same four-plus-one split.
- nx.json has exactly ONE runtime input anywhere in the file (git grep -n runtime nx.json
  returns a single hit at line 106, on targetDefaults.integration) -- confirms CORR-04s
  "integration is the ONLY OS-discriminated target" claim structurally, not just by comment.
- This job runs on every push and PR (no event gate), so it continuously guards Phases 9-12s
  changes to cache-archive-path.ts, release-asset-name.ts, cleanup.ts, and ci.yml itself
  (all of which are declared test inputs per PARITY-08, so an edit to any of them rotates the
  test hash and is caught).

Requirement IDs touched: CORR-03, CORR-04 (Phase 8) into consumed by every later phase. WIRED.

### Seam 3: Phase 9 -> Phase 10 (cache-version rotation window sequencing)

Status: WIRED, and the sequencing is explicitly instrumented, not just hoped for.

- `09-ROTATION-SIGNAL.md` was authored by plan 09-02 IN ADVANCE of the version-rotating commit
  (git-history-verified: the rotation-signal file predates plan 09-03s cache-version commit),
  and names THREE distinct all-MISS windows on two different axes (Nx task hash / Phase 7,
  `@actions/cache` version / Phase 9, Release asset name / Phase 10) with explicit non-triggers
  so a reader cannot conflate Phase 9s window with Phase 10s CORR-02 rename window.
- OBS-04s tripwire (two consecutive all-miss pushes with no version-affecting change in
  between) stays a `core.warning`, never a hard failure, so it cannot itself break CI across
  the Phase 9/10 boundary.
- `10-EVIDENCE-PRE-RENAME.md` and `10-EVIDENCE-LIVE-CI.md` both confirm the mechanism split held
  in practice: the Phase 9 rotation and the Phase 10 CORR-02 rename were measured as separate
  events with separate shard census deltas.

Requirement IDs touched: VER-01, VER-02, VER-03, OBS-04 (Phase 9) into Phase 10 CORR-02/RETAIN-04.
WIRED.

### Seam 4: Phase 10 -> Phase 11 (asset rename precedes proofs; RETAIN-04 same-commit as CORR-02)

Status: WIRED. Verified directly from git history, not from prose.

Commands run:

    git show --stat 77f675c
    git show 77f675c -- packages/github-cache/src/cleanup/cleanup.ts

Result: the SAME commit `77f675c` ("feat(10-07): collapse the Releases namespace to one asset
name per hash") touches both `release-asset-name.ts` (CORR-02, the rename) AND
`cleanup/cleanup.ts` (RETAIN-04, the filter widening to admit both `nx-cache-<hash>` and
legacy `<hash>-<os>` names). Confirmed via diff content, not via commit message alone.

    git merge-base --is-ancestor f94a8ee 77f675c   # exit 0

`f94a8ee` ("feat(10-04): give each publish leg a seed key only that leg can produce (OBS-05)")
is an ancestor of `77f675c` (CORR-02) -- OBS-05 landed BEFORE the rename, as REQUIREMENTS.md
demands (otherwise `publish-verify` would go vacuous the moment the rename lands).

Republish-before-Phase-11 precondition: `10-EVIDENCE-LIVE-CI.md` records a maintainer-authorised
temporary push to `main` (run `30471772954`, head `180c3d3`) that republished the mirror under
the new `nx-cache-<hash>` name (census: 0 into 16 new-form assets, legacy family unchanged 72
into 72, falsifier "readMisses == scanned" pre-registered and did NOT fire). `11-EVIDENCE.md`
explicitly cites this warm state as its precondition and its O1/O2 hash records match the
same shard census by exact hash equality.

Requirement IDs touched: CORR-02, RETAIN-04, RETAIN-05, OBS-05, XOS-07 (Phase 10) into XOS-01,
XOS-02 (Phase 11). WIRED.

### Seam 5: Phase 11 -> Phase 12 (load-bearing ordering: attribution before second producer)

Status: WIRED. Verified by commit timestamp, not by phase-close date alone.

    git log --format="%h %ad %s" --date=iso -1 f5dd429
    # 2026-07-30 18:05:57 "feat(12-02): add the three windows-11-arm reuse legs..."

Phase 11s `11-EVIDENCE.md` records its O1/O2/O3 measurement and its TEST-08 producer-attribution
capture (the four-means table: graph premise, terminalOutput runner-path fingerprint, Actions-
cache/shard cross-reference by job window, and hash recomputation) at 2026-07-29T21:44-22:04Z
and its O3 live push at 2026-07-29T23:39-23:57Z -- all on 2026-07-29, strictly BEFORE
`f5dd429` (2026-07-30), the commit that adds the three Windows reuse legs and makes Windows CI a
second producer. The milestones stated ordering constraint (O1 proven before O4 enabled) holds
by the actual commit graph, not merely by phase numbering.

No surviving claim says `mirrored-by` answers the producing OS: `11-EVIDENCE.md`s own
"D-14 retraction" section explicitly retracts that reading repo-wide, with a live counter-example
(`integration` hash `8137422034373911537`, Windows-produced, stamped `mirrored-by: linux`).
Grepped docs/*.md and README.md for "producing OS" / "producer OS" / "which OS produced" --
every hit is either the legitimate cacheable-output-invariance design premise ("outputs must not
depend on which OS produced them") or dogfood-verifys OWN known-fixed-producer check
(`action/index.ts` line 432, which knows its expected producer because `dogfood-seed` always
runs on a fixed ubuntu leg -- a different, valid mechanism, not `mirrored-by`-derived). No
lingering false attribution claim found.

Requirement IDs touched: XOS-01, XOS-02, XOS-03, TEST-08, TEST-09 (Phase 11) into XOS-04, XOS-05,
XOS-08 (Phase 12). WIRED.

### Seam 6: Phase 12 -> Phase 13 (read-only backend + floor-of-1 gate on the Windows legs)

Status: WIRED, confirmed at all three layers (source, workflow, spec).

- Source: `select-backend.ts` lines 71-126 -- `CACHE_READ_ONLY` is read as the LAST branch (every
  branch above it has already returned a read-only backend or thrown), narrows to
  `createReadOnlyActionsCacheBackend()`, and `actions-cache-backend.ts` lines 283-285 confirm
  `createActionsCacheBackend` SPREADS the read-only factorys `get` rather than duplicating it
  (VER-08s "exactly one restoreCache READ call site" claim verified directly in source).
- Workflow: `ci.yml`s `build-windows` (line 482), `typecheck-windows` (631), `test-windows`
  (731) each: (a) `needs: build` / `needs: typecheck` / `needs: test` respectively (XOS-08s
  producer-to-consumer edges, confirmed present), (b) write `CACHE_READ_ONLY=1` into
  `$GITHUB_ENV` in a REGULAR step before the background sidecar step starts, (c) tee their Nx
  output and gate on the `[remote cache]` count with a floor of 1, using a fail-closed
  all-decimal case guard against a non-numeric count silently passing (`ci.yml` lines 619-624
  etc.) -- this is XOS-09s inductive floor argument, present in the executable YAML, not just
  in a comment.
- Spec: `dogfood-cross-os.spec.ts` independently re-parses the comment-stripped `ci.yml` and
  asserts (a) each Windows leg tees its log and records the count (around line 1055), (b) each
  writes `CACHE_READ_ONLY` before its sidecar step (around line 1987, with an explicit ordering
  check), (c) each gates at a floor of 1 rather than only printing it (around line 1334), and
  (d) exactly 3 `CACHE_READ_ONLY` sites exist in the file (around line 2027) -- so a future edit
  that silently removes the knob from one Windows leg (making its floor-of-1 gate unsound, since
  a writable leg could launder a MISS into a self-produced HIT on a re-run) fails a spec, not
  just review.
- Live confirmation: the quick task `260803-mew` evidence file (cited from `12-UAT.md`) records
  a REAL run (`30586177358`, PR #12, first run) where the three Windows legs restored exactly
  the pre-registered per-target counts (1/2/1, total 4) against the ubuntu legs fresh-executed
  entries in the SAME run, closing what `12-VERIFICATION.md` had left as
  PRESENT_BEHAVIOR_UNVERIFIED / PENDING at phase-close.

Requirement IDs touched: VER-08, VER-09, TRUST-14, XOS-09 (Phase 13) depend on XOS-04, XOS-05,
XOS-08 (Phase 12). WIRED.

### Seam 7: E2E cross-OS read path (selectBackend into archive path into asset name into cleanup)

Status: WIRED, traced through every hop in source.

1. `selectBackend()` (`select-backend.ts`) picks one of 5 outcomes from runtime context alone
   (no caller-facing mode flag) -- confirmed 5 branches present, matching `docs/advanced.md`s
   documented table exactly.
2. The chosen Actions-cache backend calls `cache.restoreCache([path], cacheKeyFor(hash),
   undefined, undefined, true)` -- `enableCrossOsArchive: true` hardcoded at the 5th positional
   argument (`actions-cache-backend.ts` lines 219-225), and again at the 4th positional in
   `saveCache` (line 360-365) and the 5th in the disambiguation probe (line 381-389) -- THREE
   call sites, all hardcoded `true`, matching VER-03s "all three call sites" requirement.
3. `cacheArchivePath(hash)` (`cache-archive-path.ts` lines 77-79) returns a hardcoded
   forward-slash, workspace-relative literal (.nx/cache/nx-github-cache-hash.tar) with NO join,
   resolve, sep, or platform branch -- confirmed by reading the function body directly (the
   file is also comment-locked and spec-scanned for exactly those forbidden imports).
4. `releaseAssetName(hash)` (`release-asset-name.ts` lines 85-87) returns the prefix plus hash
   -- NO platform parameter exists (confirmed: the function signature takes only hash, and the
   pre-CORR-02 OS-suffixed form is gone). `cachePlatform()` still exists but its ONLY live
   consumer is the `mirrored-by: <os>` Release LABEL in `publish-mirror.ts` line 393 -- it never
   re-enters the lookup name.
5. `cleanup.ts`s delete filter calls `isServerProducedAssetName` = `isCurrentAssetName(name) OR
   isLegacyOsSuffixedAssetName(name)` -- confirmed both branches present and disjoint (the
   disjointness argument is structural: the current-shape prefix itself ends in a dash that the
   hash pattern forbids inside it, so branch As remainder can never satisfy branch Bs last-dash
   split). `cleanup.yml` line 40 actually invokes `dist/cleanup/index.js` on a real scheduled
   workflow (cron 17 3 star star star) -- not merely present in source but wired into a running
   job.
6. `integration` remains the SOLE OS-discriminated target: `git grep -n runtime nx.json` returns
   exactly ONE hit, on `targetDefaults.integration` (line 106). No other target (build,
   typecheck, test, lint) carries a runtime input anywhere in `nx.json`.

No OS component re-enters the Actions-cache key, the Release asset name, or the cleanup lookup
anywhere in this chain. WIRED end-to-end.

### Seam 8: Consumer contract (docs/cross-os.md, env-knob count, selectBackend outcome count)

Status: WIRED as of current HEAD. WARNING for the drift window that preceded it
(informational only -- already fixed on this branch).

- `docs/advanced.md` line 21 -- "selectBackend has FIVE outcomes" -- matches the 5 branches read
  directly in `select-backend.ts` (untrusted to Releases reader, malformed repo to throw, no
  token to empty memory backend, CACHE_READ_ONLY set to read-only Actions-cache, else to
  writable Actions-cache).
- `docs/configuration.md`s env-var table (lines 17-27) lists exactly NINE rows:
  NX_SELF_HOSTED_REMOTE_CACHE_SERVER, NX_SELF_HOSTED_REMOTE_CACHE_ACCESS_TOKEN, PORT,
  CACHE_MIRROR_MAX_AGE_DAYS, CACHE_MIRROR_ALLOW_AGGRESSIVE_RETENTION, CACHE_READ_ONLY,
  GH_TOKEN, GITHUB_TOKEN, GITHUB_REPOSITORY -- matching DOCS-10s "ninth env knob" language.
- WARNING (already resolved, recorded for completeness): commit `0bfc29b` ("docs: let
  advanced.md own the backend-outcome count, and stop restating it (DOCS-10)"), the
  second-to-last commit on this branch, shows the count was previously restated at 5 SITES with
  only 1 pinned by a spec -- `memory-backend.ts` line 59 needed a hand-edit that nothing in the
  tree would have caught when the count moved four to five. This is exactly the
  existence-without-integration failure mode this audit watches for, and it DID occur
  mid-milestone. It is fixed at current HEAD: `git grep` now finds the outcome-count sentence at
  exactly 2 sites (the doc sentence in `docs/advanced.md` line 21 and the regex assertion
  reading it in `docs-adoption.spec.ts` line 199), with the other three sites
  (`docs/configuration.md` line 134, `actions-cache-backend.ts` line 86, `memory-backend.ts`
  line 59) now cross-referencing the doc rather than re-stating the number. No action needed;
  flagged only because the drift window itself is a real instance of the failure class this
  audit exists to catch, and a future maintainer should not re-introduce a 6th restated count.

Requirement IDs touched: DOCS-07, DOCS-08, DOCS-09, DOCS-10. WIRED at current HEAD.

---

## E2E Flow Verdicts (O1-O4)

| Flow | Verdict | Evidence |
|---|---|---|
| O1 -- local Windows dev HITs build/typecheck/test produced by Linux CI | COMPLETE | `11-EVIDENCE.md` O1 section: cold `nx run-many` on a native Windows arm64 box, 3-of-3 targets show literal `[remote cache]`, `cacheStatus: remote-cache-hit`; `build`/`typecheck` attributed via the mechanically-asserted TEST-08 graph premise |
| O2 -- local Windows dev HITs integration produced by Windows CI | COMPLETE | Same run, `integration` HIT with `cacheStatus: remote-cache-hit` and an in-artifact runner path (Windows-produced), against the pre-rename baseline non-regression comparison |
| O3 -- Windows CI MISSes integration produced by Linux CI | COMPLETE | Push run `30500255530`: TEST-09 part 1 cites `hash-parity-compare`s own build-gating record showing `integration` hashes DIFFER at that commit; o3-witness proves the Linux entry existed before the Windows step started |
| O4 -- Windows CI HITs build/typecheck/test produced by Linux CI | COMPLETE | Left PENDING at Phase 12 close (`12-VERIFICATION.md`: PRESENT_BEHAVIOR_UNVERIFIED, honestly recorded, not fabricated); CLOSED by quick task `260803-mew`: real run `30586177358` (PR 12, first run) shows build-windows=1, typecheck-windows=2, test-windows=1 (total 4) `[remote cache]` hits, each against an ubuntu leg that MISSed-and-executed in the SAME run -- exact match to the pre-registered counts |

No broken flow found in any of O1-O4.

---

## Requirements Integration Map

Only requirements with an actual cross-phase touchpoint are traced individually below; the
remainder are phase-internal (verified by that phases own VERIFICATION.md, out of this audits
scope) and are listed in the "no cross-phase wiring" section beneath the table.

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| LINT-01 | Phase 7 lint target into Phase 8 capture-hashes.mjs TARGETS into compare.ts INVARIANT_TARGETS into hash-parity-compare gate | WIRED | -- |
| LINT-04 | Phase 7 lint Nx inputs into hash_project_config into every task hash in Phase 8s comparator | WIRED | -- |
| CORR-06 | Phase 7 mechanical enforcement into Phase 8s build-gating hash-parity-compare re-proves it every run | WIRED | -- |
| PARITY-01/03/06/07 | Phase 8 comparator into Phases 9-12 changes to archive-path/asset-name/cleanup/ci.yml (all declared test inputs, PARITY-08) | WIRED | -- |
| CORR-03 | Phase 8 hash-parity-compare job into guards every later phases edits | WIRED | -- |
| CORR-04 | Phase 8 sole-discriminator claim into confirmed structurally (1 runtime hit in nx.json) | WIRED | -- |
| VER-01/VER-02/VER-03/VER-04 | Phase 9 archive-path plus enableCrossOsArchive into Phase 13s backend construction reuses the same 3 call sites | WIRED | -- |
| VER-06 | Phase 9 dogfood-verify cross-OS proof into corroborates O2 in Phase 11 | WIRED | -- |
| OBS-04 | Phase 9 rotation-signal prediction into Phase 10s measured shard census confirms the predicted axis split | WIRED | -- |
| DOCS-08 | Phase 9 same-OS-restore doc correction into Phase 12s consumer recipe inherits the corrected premise | WIRED | -- |
| CORR-02 / RETAIN-04 | Phase 10 rename plus cleanup filter widening, same commit 77f675c into Phase 11 warm-mirror precondition for XOS-01/02 | WIRED (git-verified same commit) | -- |
| CORR-05 | Phase 10 -- all 3 violation sites closed, asserted directly | WIRED | -- |
| OBS-03 | Phase 10 mirrored-by label into Phase 11s D-14 retraction explicitly documents its limits | WIRED | -- |
| OBS-05 | Phase 10 leg-distinguishable seed, landed BEFORE CORR-02 (git-verified ancestor) into makes publish-verify non-vacuous for Phase 11 | WIRED | -- |
| XOS-06 / XOS-07 | Phase 10 max-parallel 1 plus widened needs into Phase 11s XOS-07 live measurement confirms non-overlap and correct enumeration ordering | WIRED | -- |
| TRUST-10/11/12/13 | Phase 10 threat-model updates into classified in 10-SECURITY.md (confirmed present) | WIRED | -- |
| XOS-01/02/03 | Phase 11 live proofs into consumed as Milestone O1/O2/O3 | WIRED | -- |
| TEST-08 | Phase 11 producer-attribution capture, dated BEFORE Phase 12s Windows-leg commit (git-verified) into preserved as the frozen record Phase 12s graph-premise assertion now cites instead of re-deriving | WIRED | -- |
| TEST-09/TEST-10 | Phase 11 O3 negative proof plus cold-start proofs into cited by Phase 12s ordering rationale | WIRED | -- |
| OBS-02 | Phase 11 per-target remote-cache count naming into reused verbatim as the counting convention for Phase 12/13s Windows-leg gates | WIRED | -- |
| XOS-04/05/08 | Phase 12 Windows legs plus needs edges into gated by Phase 13s read-only backend plus floor-of-1 | WIRED | -- |
| DOCS-07 | Phase 12 consumer recipe into drift-guarded by docs-cross-os.spec.ts, portability checklist derived from PARITY-01 | WIRED | -- |
| VER-08/VER-09 | Phase 13 writable-composes-read-only backend into confirmed single get closure via object spread in source | WIRED | -- |
| TRUST-14 | Phase 13 CACHE_READ_ONLY knob into confirmed as selectBackends LAST branch, selectBackend.length stays 0 | WIRED | -- |
| XOS-09 | Phase 13 floor-of-1 gate into confirmed present in ci.ymls executable YAML on all 3 Windows legs, cross-checked by dogfood-cross-os.spec.ts | WIRED | -- |
| DOCS-09/DOCS-10 | Phase 13 doc corrections into confirmed consistent at current HEAD (5 outcomes, 9 env knobs, single-sourced via commit 0bfc29b) | WIRED (WARNING for the pre-fix drift window, already closed) | Previously restated at 5 sites with only 1 guarded; fixed same-branch, no residual gap |

### Requirements with no cross-phase wiring (phase-internal only)

These are self-contained within their own phase and have no expected touchpoint in another
phases code -- not a coverage gap, just outside this audits cross-phase scope:

- Phase 7: LINT-02, LINT-03, LINT-05, LINT-06 (ESLint rule mechanics, scoped to Phase 7s own
  lint config and specs).
- Phase 8: PARITY-04 (explicitly deferred as a separate named question, not this milestones
  close), PARITY-05 (native-vs-windows-11-arm integration parity, self-contained to Phase 8s
  measurement).
- Phase 9: VER-05 (compression-method surfacing in the publish summary, Phase 9/10-internal
  observability only).
- Phase 10: XOS-06 (max-parallel 1 comment-lock, internal to Phase 10s own workflow edit,
  though its live effect was independently confirmed in Phase 11s evidence as noted above).

None of these were flagged as at-risk during this audit; they are listed here only to satisfy
the "flag requirements with no cross-phase wiring" instruction.

---

## Commands run (for reproducibility)

    git grep -n "^  hash-parity" .github/workflows/ci.yml
    git grep -n "continue-on-error" .github/workflows/ci.yml
    git log --oneline -- packages/github-cache/src/lib/release-asset-name.ts
    git show --stat 77f675c
    git show 77f675c -- packages/github-cache/src/cleanup/cleanup.ts
    git merge-base --is-ancestor f94a8ee 77f675c
    git log --format="%h %ad %s" --date=iso -1 77f675c
    git log --format="%h %ad %s" --date=iso -1 f5dd429
    git grep -n "runtime" nx.json
    git grep -n "outcome" docs/advanced.md docs/configuration.md
    git show --stat 0bfc29b
    git grep -n "TRUST-11" -e "TRUST-12" .planning/phases/10-os-invariant-releases-mirror/10-SECURITY.md

Files read in full: capture-hashes.mjs, compare.ts, select-backend.ts,
actions-cache-backend.ts, cache-archive-path.ts, release-asset-name.ts, ci.yml (two passes),
cleanup.yml, windows-regression-detector.yml, 09-ROTATION-SIGNAL.md,
10-EVIDENCE-PRE-RENAME.md, 10-EVIDENCE-LIVE-CI.md, 11-EVIDENCE.md (partial, 779 of 1380
lines -- the O1/O2/O3/TEST-08 sections needed for this audit), 12-UAT.md (partial),
docs/advanced.md (partial), docs/configuration.md (partial).
