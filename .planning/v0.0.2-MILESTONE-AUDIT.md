---
milestone: v0.0.2
milestone_name: OS-invariant cross-OS sharing
audited: 2026-08-04T10:10:48Z
audited_at_commit: 2df3af5
status: tech_debt
scores:
  requirements: 51/51
  phases: 7/7
  integration: 8/8
  flows: 4/4
  nyquist: 7/7
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-cutting (bookkeeping)
    items:
      - "REQUIREMENTS.md: 9 requirement checkboxes still `[ ]` (CORR-03, CORR-04, PARITY-01..07) although 08-VERIFICATION.md:188-196 marks all nine SATISFIED"
      - "REQUIREMENTS.md: 12 traceability status cells read `Pending` against closed work -- the 9 Phase 8 rows plus CORR-05, RETAIN-05, TEST-08 (whose checkboxes are already `[x]`)"
      - "ROADMAP.md:713-719 traceability rows for Phase 8 are ID-SHIFTED against REQUIREMENTS.md; ROADMAP's PARITY-02/03/04/05 note text describes REQUIREMENTS' PARITY-03/05/06/07. Known and deliberately deferred (08-ROOT-CAUSE.md:2027 item 5)"
      - "ROADMAP.md Phase 8 count says 7 requirements; REQUIREMENTS.md assigns 9 to Phase 8 (PARITY-01..07 + CORR-03 + CORR-04)"
      - "SUMMARY.md `requirements_completed` frontmatter absent for 30 of the 51 roadmapped REQ-IDs (all of Phase 7, all of Phase 8, all of Phase 9), so the audit's third cross-reference source is structurally non-discriminating on this project"
  - phase: 12-windows-ci-reuse-o4-consumer-recipe
    items:
      - "12-VERIFICATION.md frontmatter says `status: passed` while its body says `Status: human_needed`, `score: 9/13`, `behavior_unverified: 4`. All four items ARE closed downstream (12-VALIDATION.md:113, 12-UAT.md tests 1/4, quick 260803-mew) but the artifact was never reconciled"
      - "ROADMAP.md:94 Phase 12 checkbox is `[ ]` while the Progress table (:915) says Complete 2026-07-31"
  - phase: 13-read-only-actions-cache-backend
    items:
      - "ROADMAP.md:639-644 all five Phase 13 plan checkboxes still `[ ]` although the phase is complete"
  - phase: 08-nx-task-hash-parity
    items:
      - "One unattributed `test` failure at commit 69bd1b7, not reproducible in 7 attempts and no output captured (deferred-items.md). Actionable only on a second occurrence WITH output captured"
  - phase: 11-live-proofs-o1-o2-o3
    items:
      - "SUPERSEDED by quick 260804-h3b -- CLOSED IN CODE by `40e4d21` and `e5d3cd3`, both landed 2026-08-03 AFTER the note (`da462b5`), and pinned by 24 passing assertions. Not open debt. Original text, accurate when written: `o3-witness` asserts a CREATION ordering, so on a Case-B run (every producer HITs, nothing is created) it reddens with no event to observe. A FALSE red the first post-Phase-13 PR touching no declared input will hit (ROADMAP.md:700-705, superseded in place by the status block that follows it)"
  - phase: process (main-window procedure)
    items:
      - "The temporary-main-window restore force-push fires a full production `ci.yml` run (both `publish` legs writing for real) because `[skip ci]` is structurally unavailable to a re-push of an existing commit. Sole `human_needed` of quick 260803-mew; five stale `refs/backups/*` on origin suggest five prior undisclosed occurrences"
  - phase: 09-os-invariant-actions-cache-version
    items:
      - "One live-CI confirmation open by design: the FIRST post-merge push's `publish-verify (windows-11-arm)` leg. Code/fixture/mutation halves are closed; same class as VER-06's and OBS-04's own live closures"
  - phase: 260726-pjz (quick)
    items:
      - "Still `Needs Review` in STATE.md: residue item 4 is a labelled duplicate retained against the file's own criterion, and four removed sentences have no coverage row"
open_by_design:
  - "Phase 9: first post-merge `publish-verify (windows-11-arm)` observation"
  - "Phase 10: OBS-01 summary + shard census on the first post-merge push (OBS-05 and XOS-07 already closed live on run 30471772954)"
---

# Milestone v0.0.2 -- OS-Invariant Cross-OS Sharing: Audit

**Audited:** 2026-08-04T10:10:48Z at `2df3af5`, branch `gsd/v0.0.2-os-invariant-cross-os-sharing`
**Status:** `tech_debt` -- 51/51 requirements satisfied, zero critical blockers, real accumulated
bookkeeping debt plus two genuine open follow-ups.

## Definition of Done

From ROADMAP.md:30-42 -- "A Windows developer reuses Linux CI's portable task artifacts, and Windows
CI reuses them too, with the OS-sensitive target still provably separated. Proven by dogfooding this
repo, then documented as a recipe consumers can copy."

| # | Outcome | Layer | Proven by | Verdict |
|---|---------|-------|-----------|---------|
| O1 | Local Windows dev HITs `build`/`typecheck`/`test` produced by Linux CI | Releases mirror | Phase 11 | ACHIEVED |
| O2 | Local Windows dev HITs `integration` produced by Windows CI | Releases mirror | Phase 11 | ACHIEVED |
| O3 | Windows CI MISSES `integration` produced by Linux CI | Actions cache | Phase 11 | ACHIEVED |
| O4 | Windows CI HITs `build`/`typecheck`/`test` produced by Linux CI | Actions cache | Phase 12 | ACHIEVED |

The load-bearing ordering held **by the commit graph, not by phase numbering**: the integration
checker proved Phase 11's producer-attribution capture (2026-07-29) predates Phase 12's Windows-leg
commit `f5dd429` (2026-07-30) via `git log --date=iso`. O1's attribution was captured before O4
destroyed it permanently.

## Phase Verifications

| Phase | VERIFICATION | Status | Score | Nyquist | Security |
|-------|--------------|--------|-------|---------|----------|
| 7. Lint Toolchain + Ambient-Platform-Read Ban | present | passed | 22/22 must-haves, 2/2 prohibitions, 5/5 SC | COMPLIANT | present |
| 8. Nx Task-Hash Parity | present | passed | 7/7 must-haves (9/9 requirements SATISFIED) | COMPLIANT | present |
| 9. OS-Invariant Actions-Cache Version | present | passed | 11/11 requirements code-verified | COMPLIANT | present |
| 10. OS-Invariant Releases Mirror | present | passed | 13/13 code-level must-haves | COMPLIANT | present |
| 11. Live Proofs -- O1, O2, O3 | present | passed | 12/12 must-haves | COMPLIANT | present |
| 12. Windows CI Reuse (O4) + Consumer Recipe | present | passed (frontmatter) / human_needed (body) | 9/13, behavior_unverified 4 | COMPLIANT | present |
| 13. Read-Only Actions-Cache Backend | present | passed | 7/7 must-haves | COMPLIANT | present |

**7/7 phases verified. 0 unverified phases. 7/7 Nyquist COMPLIANT (`nyquist_compliant: true`,
`wave_0_complete: true` on every VALIDATION.md). 7/7 SECURITY.md present.**

### Phase 12's contradiction, resolved

12-VERIFICATION.md is the one internally inconsistent artifact in the milestone: frontmatter
`status: passed`, body `Status: human_needed` with four `human_verification` items and
`behavior_unverified: 4`. All four are closed by downstream work, which is why the frontmatter was
flipped -- but the body, score and counters were never reconciled:

| Item | Closed by | Evidence |
|------|-----------|----------|
| 1. XOS-05 per-leg `[remote cache]` counts on a FIRST same-repo PR run | Phase 12's own landing | Run `30586177358`, PR #12, counts 1/2/1 total 4 against counts pre-registered in `f5d03b0` |
| 2. `windows-regression-detector.yml` dispatch after merge | quick `260803-mew` | Runs `30825110047` PASS at `41f65e1` and `30825602626` FAIL at the needle's grep with nx at exit 0 -- both directions, on the FOUR-target needle at HEAD |
| 3. RESEARCH assumption A1 (`--no-warnings` leaves stderr empty on `linux/arm64`) | 12-VALIDATION.md audit | Both `hash-parity-<runner>` artifacts from run `30586177358` record the HARDENED command; `stderr` length 0 on both legs; `stdout` `linux\n` vs `win32\n`. Closed by measurement, not inference |
| 4. `docs/cross-os.md` prose accuracy review | 12-UAT.md test 4 | Reviewed after code-review finding CR-01 was fixed |

Item 2's closure is worth noting for its own sake: the PASS half on record (`30603713356` at
`e757d4c`) could no longer speak to the gate at HEAD, because `9e79009` added `lint` and made the
needle FOUR-target -- `git merge-base --is-ancestor 9e79009 e757d4c` is false. The gate as it exists
had never run on a real runner in *either* direction until `260803-mew`.

## Requirements Coverage -- 3-Source Cross-Reference

**51/51 satisfied. 0 unsatisfied. 0 orphaned.**

Every one of the 51 roadmapped REQ-IDs appears in its assigned phase's VERIFICATION.md with a
rendered verdict (checked by REQ-ID sweep over all seven files). No requirement is assigned in the
traceability table but absent from every VERIFICATION.

| Phase | REQ-IDs | In VERIFICATION | In SUMMARY frontmatter | REQUIREMENTS checkbox | Final |
|-------|---------|-----------------|------------------------|-----------------------|-------|
| 7 | LINT-01..06, CORR-06 (7) | 7/7 | 0/7 | 7/7 `[x]` | satisfied |
| 8 | PARITY-01..07, CORR-03, CORR-04 (9) | 9/9 SATISFIED | 0/9 | 0/9 `[x]` | satisfied (checkboxes stale) |
| 9 | VER-01..06, OBS-04, DOCS-08 (8) | 8/8 | 0/8 | 8/8 `[x]` | satisfied |
| 10 | CORR-02, CORR-05, RETAIN-04, OBS-03, OBS-05, XOS-06, XOS-07, TRUST-10..13 (11) | 11/11 | 6/11 | 11/11 `[x]` | satisfied |
| 11 | XOS-01, XOS-02, XOS-03, TEST-08, TEST-09, TEST-10, OBS-02 (7) | 7/7 | 5/7 | 7/7 `[x]` | satisfied |
| 12 | XOS-04, XOS-05, XOS-08, DOCS-07 (4) | 4/4 | 3/4 | 4/4 `[x]` | satisfied |
| 13 | VER-08, VER-09, TRUST-14, XOS-09, TEST-11, DOCS-09, DOCS-10 (7) | 7/7 | 7/7 | 7/7 `[x]` | satisfied |

### Why 30 requirements are `satisfied` and not `partial`

The status matrix maps `VERIFICATION passed + SUMMARY frontmatter missing` to **partial (verify
manually)**. That row fires for 30 of the 51 IDs -- but not because the work is unconfirmed. This
project's SUMMARY house style records `provides:` / `affects:` / `requires:` and does not populate
`requirements_completed` at all in Phases 7-9; only 21 of 51 IDs appear in any SUMMARY frontmatter.
The source is structurally non-discriminating here, so it cannot separate a real gap from a
convention.

The matrix's own instruction was therefore executed rather than waived: each of the 30 was verified
manually against its phase's VERIFICATION.md verdict plus the REQUIREMENTS.md status note. All 30
carry an explicit rendered verdict. The absent frontmatter is recorded as tech debt above, not as a
requirement gap.

### The 9 unticked Phase 8 checkboxes

CORR-03, CORR-04 and PARITY-01..07 are all `[ ]` in REQUIREMENTS.md and all read `Pending` in its
traceability table -- while 08-VERIFICATION.md:188-196 renders **SATISFIED** for each of the nine
with re-derived evidence (ordering proof re-run, `hash.details.nodes` at `capture-hashes.mjs:305`,
M3 both workstation graph states byte-identical at HEAD, `git diff --name-only 7bfe64f..HEAD` empty
over the public-surface files, exactly one `{ "runtime": ... }` entry in `nx.json`). Per the matrix
this is `passed + [ ]` -> **satisfied (update checkbox)**. Bookkeeping, not coverage.

### The ROADMAP/REQUIREMENTS Phase 8 ID shift

ROADMAP.md:713-719's Phase 8 rows are shifted against REQUIREMENTS.md: ROADMAP's PARITY-02/03/04/05
note text describes the *content* of REQUIREMENTS' PARITY-03/05/06/07, and REQUIREMENTS' PARITY-02
(the per-NODE `details` instrument) and PARITY-04 (the warm-local-vs-cold-CI named question) have no
ROADMAP row at all. ROADMAP counts Phase 8 as 7 IDs; REQUIREMENTS assigns 9.

This is **already known and deliberately deferred**, not a new discovery: 08-VERIFICATION.md:239
records it as "Surfaced, not fixed", with the exact off-by-N mapping, and states that "an audit
against that table would have mis-reported PARITY-02" -- which is precisely the trap this audit hit
and cross-checked out of. REQUIREMENTS.md is authoritative (ROADMAP.md:755 says so in as many
words). It hides no coverage hole: all nine IDs are verified.

## Cross-Phase Integration

Full report: `.planning/v0.0.2-INTEGRATION-CHECK.md` (gsd-integration-checker, 349 lines).

**8/8 seams WIRED. 4/4 E2E flows COMPLETE. 0 BLOCKERs. 1 WARNING, already resolved at HEAD.**

| Seam | Verdict | Evidence the checker actually ran |
|------|---------|-----------------------------------|
| 7 -> 8 | WIRED | `capture-hashes.mjs` and `compare.ts` both fold `lint` into the invariant-target set; `lint` ENFORCED, not measured once |
| 8 -> 9/10/11/12 | WIRED | `hash-parity-compare` in `ci.yml` has zero `continue-on-error` hits and derives its verdict from record content |
| 9 -> 10 | WIRED | VER-01 rotation window and Phase 10's republish sequencing recorded as designed |
| 10 -> 11 | WIRED | `git show --stat` proves CORR-02 and RETAIN-04 landed in the **same commit `77f675c`**; `git merge-base --is-ancestor` proves OBS-05 is an ancestor of it (landed first, as RETAIN-04/OBS-05 both require) |
| 11 -> 12 | WIRED | `git log --date=iso` proves the attribution capture (07-29) predates Windows-leg commit `f5dd429` (07-30) |
| 12 -> 13 | WIRED | `select-backend.ts` read directly: `CACHE_READ_ONLY` narrows as the LAST branch; `actions-cache-backend.ts` spreads the read-only factory (VER-08); the floor-of-1 gate is live in executable `ci.yml` YAML, not just documented |
| E2E read path | COMPLETE | `cache-archive-path.ts` and `release-asset-name.ts` read in full -- no OS component re-enters the cache key or asset name; `git grep -n "runtime" nx.json` confirms exactly one target (`integration`) carries an OS discriminator |
| Consumer contract | WIRED | Env-knob count and `selectBackend` outcome count agree across doc surfaces |

**The one WARNING:** commit `0bfc29b` shows the "`selectBackend` has five outcomes" claim was at one
point restated at 5 sites with only 1 spec-guarded -- a genuine existence-without-integration gap
that did occur mid-milestone. Resolved at current HEAD (2 sites, both guarded).

## Genuine Open Follow-Ups

These are not requirement gaps -- all 51 are satisfied -- but they are real and worth a decision
before the milestone closes.

### 1. `o3-witness` reddens falsely on a Case-B run (ROADMAP.md:700-705)

**STATUS: CLOSED IN CODE -- SUPERSEDED by quick 260804-h3b.** Two commits fixed this on 2026-08-03,
both AFTER the note this item repeats: `40e4d21` removed the server-side `&ref=` narrow that filtered
out the row proving prior existence, and `e5d3cd3` admitted the default-branch scope to the client-side
ref allowlist. 24 assertions in `dogfood-cross-os.spec.ts` pin the result. This item was never open at
the time this audit was written; it was inherited from a note nobody had retired. The body below is
kept for the reasoning it records -- including its `Trigger is concrete` sentence, which is now
HISTORICAL rather than forward-looking: the trigger it predicts was already disarmed in code before
this audit was authored, so no future PR will hit it. The live observation is in
`.planning/quick/260804-h3b-fix-o3-witness-case-b/260804-h3b-EVIDENCE.md`.

`o3-witness` asserts a CREATION ordering: that the linux entry came into existence before the
Windows `integration` step began. On a Case-B run every producer HITs, so nothing is created, the
assertion has no event to observe, and the job reddens. The cross-OS property still holds -- the
three green read-only legs in run `30768540898` show it. The witness silently assumes the Case-A
shape.

**Trigger is concrete:** the first post-Phase-13 PR touching no declared input will hit it.

### 2. The main-window restore force-push fires production CI (quick 260803-mew, sole `human_needed`)

The temporary-main-window procedure's STEP 7 restore force-push fires a full `ci.yml` run
(`30825636788`, both `publish` legs RED on real production writes), because `[skip ci]` is
structurally unavailable to it -- the restore re-pushes an *existing* commit whose message cannot be
changed. Pre-existing and already tracked, no new regression, but it is a property of the shared
`main`-window PROCEDURE, and five stale `refs/backups/*` on origin suggest five prior undisclosed
occurrences.

### 3. Live-CI items open by design

| Item | Phase | Why it cannot close pre-merge |
|------|-------|-------------------------------|
| First post-merge `publish-verify (windows-11-arm)` observation | 9 | `publish` / `publish-verify` are push-gated to `main`; no PR run samples them |
| OBS-01 summary + shard census on the first post-merge push | 10 | Same push gate. OBS-05 and XOS-07 already closed live on run `30471772954` |

Both are the same class as VER-06's and OBS-04's own live closures. Phase 13's two Live-CI items are
**both CLOSED** (ROADMAP.md:679-688): XOS-09's gate on run `30744366870` (green at 1/2/1 against
counts pre-registered in the same head `631a2e7`) with the FAIL direction on `30745558383`, and the
Q4 base-scope READ half (Case B) PROVEN by quick `260802-toz` on run `30768540898`.

### 4. Unreproducible `test` failure at `69bd1b7` (Phase 8 deferred-items.md)

`npm run test` exited 1 once; output was discarded by the battery loop's redirect and is
unrecoverable. Nx's flaky-task detection then fired -- a FAILURE and a SUCCESS at the same task
hash, i.e. non-determinism rather than a regression. Seven consecutive local runs passed at that
hash; CI's `test` job at the same tree is `success`. Actionable only on a second occurrence with
output captured.

## Tech Debt Summary

**11 items across 7 areas.** Nine are documentation/bookkeeping drift with no runtime effect; two
(`o3-witness` Case-B, the main-window restore push) are real behaviours with concrete triggers.

SUPERSEDED by quick 260804-h3b, on the second clause only: the `o3-witness` Case-B item is CLOSED IN
CODE (`40e4d21`, `e5d3cd3`, 24 assertions), so **ONE** real behaviour with a concrete trigger remains
-- the main-window restore push. The counts in the paragraph above are as-audited at `2df3af5` and are
deliberately NOT restated: re-arithmetic is out of scope for this correction, and the paragraph's own
"11 items across 7 areas" already disagrees with the frontmatter's 13 items across 8 areas, which is
PRE-EXISTING drift this task does not touch.

The bookkeeping cluster is worth closing in one pass, because it is all the same failure: artifacts
that were correct when written and never reconciled after downstream work closed their items --
9 checkboxes, 12 status cells, 6 ROADMAP plan/phase checkboxes, and one VERIFICATION body whose
frontmatter already disagrees with it.

## Verdict

Milestone v0.0.2 **achieved its definition of done**. All four target outcomes are proven live with
pre-registered evidence; the load-bearing O1-before-O4 ordering held by the commit graph; the store
is OS-invariant end to end with `integration` the sole OS-discriminated target; and the consumer
recipe ships drift-guarded.

Status is `tech_debt` rather than `passed` because of the accumulated bookkeeping drift and the two
open behavioural follow-ups above -- not because any requirement is unmet.

SUPERSEDED by quick 260804-h3b, on the "two open behavioural follow-ups" clause only: follow-up 1
(`o3-witness` Case-B) was already CLOSED IN CODE when this verdict was written, so ONE behavioural
follow-up remains. `status: tech_debt` is UNCHANGED and still correct -- the accumulated bookkeeping
drift and the main-window restore push are untouched by this task, and neither depended on follow-up 1.

## Bookkeeping debt closed -- quick task 260808-lpt, 2026-08-08

Superseding IN PLACE, per this file's own convention (the three 260804-h3b blocks above). The
`tech_debt` frontmatter list and `audited_at_commit: 2df3af5` are the AS-AUDITED record and are
deliberately untouched; this note supersedes them rather than rewriting them. `status: tech_debt`
is likewise unchanged -- closing this cluster is not a re-audit.

### 1. Disposition of the eight items

| # | Area | Disposition |
|---|---|---|
| 1 | cross-cutting (bookkeeping), 5 sub-items | **CLOSED**, four of five. The 9 requirement checkboxes and the 12 traceability status cells closed in `c3fe74d`, each read individually against `08-VERIFICATION.md`'s own Requirements Coverage row (plus `10-VERIFICATION.md` for CORR-05 and RETAIN-05, `11-EVIDENCE.md` for TEST-08). The ROADMAP Phase 8 ID shift and the 7-vs-9 count closed in `d5841c2`. The fifth, `requirements_completed`, is RECLASSIFIED rather than closed -- see section 2 |
| 2 | 12-windows-ci-reuse-o4-consumer-recipe, 2 sub-items | **CLOSED.** `12-VERIFICATION.md`'s frontmatter, body Status line and counters now agree, with the four closures re-derived from their own named artifacts and appended under `## Human Verification Closed`. Phase 12's ROADMAP checkbox ticked in `d5841c2`, dated from the Progress table row |
| 3 | 13-read-only-actions-cache-backend | **CLOSED, and WIDENED** -- see section 4. Six Phase 13 plan checkboxes, not the five the item names, plus fourteen the item did not name |
| 4 | 08-nx-task-hash-parity, the unattributed `test` failure at `69bd1b7` | **STILL OPEN**, untouched. Actionable only on a second occurrence with output captured; nothing in this task bears on it |
| 5 | 11-live-proofs-o1-o2-o3, `o3-witness` Case-B | **ALREADY CLOSED** before this task, by `40e4d21` and `e5d3cd3` per the 260804-h3b supersede. Not open debt; not re-touched |
| 6 | process, the main-window restore force-push firing production CI | **STILL OPEN**, untouched. A procedural decision for the operator, outside a bookkeeping pass |
| 7 | 09-os-invariant-actions-cache-version, the first post-merge `publish-verify (windows-11-arm)` leg | **STILL OPEN BY DESIGN.** Live-CI, listed in `open_by_design`; nothing here closes it |
| 8 | 260726-pjz (quick), still `Needs Review` in STATE.md | **RESOLVED, honestly and partially.** Re-derived by executed probe: follow-up (a), the labelled duplicate, is CLOSED -- removed in `83ac4fd`, all three of its tokens now a genuine no-match against a positive control. Follow-up (b) is PARTIALLY closed and the remainder accepted as non-operative: `raw-count` and `self-inflicted` now have homes, `safety-weighted` and `re-populate` remain homeless. The row's Status cell and OPEN clause carry the full re-derivation |

### 2. `requirements_completed` -- reclassified as house-style CONVENTION, not a defect

The fifth cross-cutting sub-item is the explicitly out-of-scope one, and this note is its
deliverable. It is RECLASSIFIED from a defect to a `house-style` convention of this project.

The convention: this project's SUMMARY frontmatter records `provides:` / `affects:` / `requires:`
and does not populate `requirements_completed`. The audit's third cross-reference source is
therefore structurally non-discriminating here BY CONVENTION, not by omission, and the
**two sources of record** for requirement closure on this project are the phase VERIFICATION.md
verdict and REQUIREMENTS.md's traceability table. Both are populated, both were used as the sources
for the closures in `c3fe74d`, and the cross-reference is sound on two sources.

**NO sealed SUMMARY was back-edited by this task, and backfilling the REQ-IDs was DECLINED for that
reason.** Retro-fitting frontmatter into shipped SUMMARYs would rewrite the record of what each plan
claimed at the time it landed, which is the same failure class this whole task exists to close.

The figure is the AUDIT's, quoted rather than re-asserted: "30 of the 51 roadmapped REQ-IDs (all of
Phase 7, all of Phase 8, all of Phase 9)". It is not re-derivable here and this note does not adopt
it. Measured at the current tree, ROADMAP's Phases 7-9 carry 22 traceability rows and REQUIREMENTS'
carry 27, so 30 matches neither table nor its own stated scope; and this task's own Phase 8 fix
moves the roadmapped Phase 7-9 figure again, 22 to 24, so any re-derived number written here would
be stale on arrival.

### 3. The roadmapped-subset arithmetic moved

`ROADMAP.md`'s roadmapped subset moved 51 to 53 and the documented difference against
`REQUIREMENTS.md`'s DEFINED set moved SIX IDs to FOUR (`57 - 53 = 4`), because the Phase 8
traceability block gained the two rows it was always assigned. The audit's own as-audited
`scores.requirements: 51/51` is the value at `2df3af5` and is superseded by this note rather than
restated. No requirement was added, removed or rescoped.

### 4. A completeness finding about THIS AUDIT, in three measured parts

Recorded as a finding about the audit's item list, not as a defect in the phases. An item list that
catches 6 of 20 instances of one defect is the interesting result.

1. Item 7 says "all five Phase 13 plan checkboxes". There are **SIX**, `13-01` through `13-06`,
   corroborated by STATE.md's "Plan: 6 of 6" and by the Progress table's Phase 13 row. The item's
   own cited range, `ROADMAP.md:639-644`, spans six rows while calling them five.
2. **Phase 10 and Phase 11 were omitted entirely** -- fourteen more unticked plan checkboxes of the
   identical class, seven in Phase 10 and seven in Phase 11, while each phase's own `**Plans**:`
   line already declared `8/8 plans complete` and `7/7 plans complete` respectively. That is the
   same internal contradiction item 6 fixes between Phase 12's checkbox and the Progress table.
3. **Phase 10's block has EIGHT rows, not seven** -- `10-01` was already ticked, so the closing gate
   asserts all eight are present-and-ticked rather than seven, which also catches an accidental
   untick of `10-01`.

All twenty were closed by this task, in `d5841c2`.

### 5. Unticked checkboxes elsewhere under `.planning/` -- a different class, out of scope

Unticked checkboxes remain elsewhere under `.planning/`: in the research documents under
`.planning/research/`, in phase `*-RESEARCH.md` files, in `PROJECT.md`, and scattered through phase
SUMMARY / PLAN / LEARNINGS / VERIFICATION and deferred-items files. None of them is a phase or plan
completion marker. They are research checklists, roadmap-adjacent progress lists and per-plan
scratch -- a different class, and out of scope for this pass. Every one of them is left as it is.

**No count is recorded here deliberately.** The measured figure, with the command that produced it
and its date, lives in this task's SUMMARY at
`.planning/quick/260808-lpt-fix-the-v0-0-2-milestone-audit-bookkeepi/260808-lpt-SUMMARY.md`, and
nowhere in this sealed artifact. The reason is the reason this whole task exists: a precise count
written into an archived artifact rots the moment anyone adds a checklist item, and an audit
carrying a rotted count is what produced these eight items in the first place. The near-miss is
worth recording -- an earlier revision of this task's plan was about to write "twenty" here against
a figure that measured 93 at the time, and the figure then moved again twice before execution.

### 6. One box left unticked in ROADMAP.md, by design

Exactly one unticked checkbox survives in `ROADMAP.md`: the `**v0.0.2 OS-invariant cross-OS
sharing**` milestone rollup entry. `/gsd:complete-milestone` owns it. Ticking it here would assert
the milestone is closed, while this task is the work that makes closing it safe. The closing gate
asserts positively that it is still present AND still unticked, so it can be neither ticked nor
deleted.

### 7. The 11-vs-13 paragraph drift is NOT re-arithmetic'd

`## Tech Debt Summary`'s "11 items across 7 areas" disagrees with the frontmatter's 13 items across
8 areas. That drift is PRE-EXISTING, predates this task, and is deliberately left alone -- matching
the scope note the 260804-h3b supersede block already carries on that same paragraph. This task
closed items; it did not recount them.
