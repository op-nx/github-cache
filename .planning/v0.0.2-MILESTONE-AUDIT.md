---
milestone: v0.0.2
milestone_name: OS-invariant cross-OS sharing
audited: 2026-08-10T20:13:41Z
audited_at_commit: 0dbee20
supersedes: "audit of 2026-08-04T10:10:48Z at 2df3af5 (110 commits stale)"
status: tech_debt
scores:
  requirements: 57/57
  phases: 7/7
  integration: 9/9
  flows: 5/5
  nyquist: 7/7
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 08-nx-task-hash-parity
    items:
      - "One unattributed `test` failure at commit `69bd1b7`, not reproducible in 7 attempts, no output captured (`deferred-items.md`). Actionable only on a SECOND occurrence WITH output captured; the capture procedure is now codified in `AGENTS.md`"
  - phase: cross-cutting (artifact prose behind the tree)
    items:
      - "`11-EVIDENCE.md`'s O4 verdict slot still literally reads `PENDING` although the observation was made and correctly recorded in `12-UAT.md` and `REQUIREMENTS.md`. Already self-disclosed by `12-VERIFICATION.md`"
      - "`.planning/codebase/INTEGRATIONS.md` still says `selectBackend` has four outcomes; Phase 13's `CACHE_READ_ONLY` branch made it five. This file sits OUTSIDE the guarded consumer-doc surface (`docs-adoption.spec.ts` covers `docs/configuration.md`, `docs/advanced.md`, `docs/versioning.md` only), so no gate catches it -- new finding, from this audit's integration check"
      - "`08-VERIFICATION.md` records the ROADMAP Phase 8 ID shift as `surfaced, not fixed`. It WAS fixed by quick 260808-lpt (`d5841c2`); the artifact carries the pre-fix note"
      - "`10-VERIFICATION.md`'s ticked/not-ticked tally says TRUST-13's auditor `has not run` and `no 10-SECURITY.md on disk`. `10-SECURITY.md` now exists and `REQUIREMENTS.md` marks TRUST-13 Complete against its section 1"
  - phase: 10-os-invariant-releases-mirror
    items:
      - "Deferred item D2 is substantially closed -- `publish-mirror.ts` carries no same-OS-restore-MISS claim and D2's own suggested guard (a `publish-mirror` row in `DOCS_08_SITES`) is in place -- but one test title at `publish-mirror.spec.ts:437` still pairs `foreign-OS` with a restore MISS, which is the stale-CAUSE class D2 named. Prose only, LOW"
      - "Deferred item D1 (ROADMAP's descriptive plan list 7 rows behind its checkbox list) is CLOSED: `ROADMAP.md` now carries exactly one unchecked box, the milestone's own"
  - phase: 13-read-only-actions-cache-backend
    items:
      - "Deferred item D1: a redundant untracked `.gitkeep` in the phase directory. Trivial"
  - phase: process (audit instrumentation)
    items:
      - "SUMMARY `requirements_completed` frontmatter is absent for 34 of the 57 requirements (all 7 of Phase 7, all 9 of Phase 8, 10 of Phase 9's 11, 5 of Phase 10's 12, 2 of Phase 11's 7, 1 of Phase 12's 4). The audit's THIRD cross-reference source is therefore structurally non-discriminating on this project. All 34 were verified MANUALLY against their phase's VERIFICATION.md requirements-coverage table instead -- every one carries an explicit EARNED / SATISFIED / Verified verdict"
  - phase: v0.0.3 (deliberately deferred, not v0.0.2 debt)
    items:
      - "Nine UNRESOLVED `/simplify` items were filed to a v0.0.3 deferral record (`b6580ad`). Recorded here so the lane is not lost at milestone close, NOT counted as v0.0.2 debt"
open_by_design: []
# EMPTY IS THE POINT, and an empty key beats a stale one. This audit's single open_by_design entry
# read "The post-fix readMisses value on the first real push to main (260809-2s6's three NOT OBSERVED
# items plus 10-VERIFICATION's un-rewritten expectation row)". It was NEVER open: run 31305961054 on
# 2026-08-09 measured all three, a day before this audit ran. Corrected 2026-08-10 by quick 260810-v1g;
# see closed_since_prior_audit and the "Both substantive items, and why one of them was never open"
# section below.
closed_since_prior_audit:
  - "REQUIREMENTS.md bookkeeping: 9 unticked checkboxes and 12 stale `Pending` traceability cells -> all 57 `[x]`, all rows `Complete` (260808-lpt)"
  - "ROADMAP Phase 8 traceability ID shift and the seven-vs-nine count -> fixed; roadmapped subset 51 -> 53 (260808-lpt, `d5841c2`)"
  - "ROADMAP Phase 12 checkbox, Phase 13 plan checkboxes, Phase 10 descriptive plan list -> one unchecked box remains, the milestone's own"
  - "12-VERIFICATION.md frontmatter-vs-body contradiction (`passed` vs `human_needed`, 9/13) -> reconciled, all four items closed downstream (260808-lpt)"
  - "The temporary-main-window restore force-push firing the production `publish` legs -> gated on `!github.event.forced` (260808-u2q, `92998de`) and OBSERVED in BOTH directions in 260808-wxg's window: executable `ci.yml` held at 823/823 identical, non-forced `publish` RAN, forced `publish` SKIPPED"
  - "Phase 9's and Phase 10's open-by-design live observations -> sampled in 260808-wxg's window, run `31281406708`. O-A superseded in place because its expectation (a `linux` producer on the Windows publish-verify leg) is the exact condition `assertPublishedByThisLeg` exists to reject"
  - "readMisses 63 -> root-caused and fixed (260809-2s6): 48 of the 63 were prior runs' single-use CI seed entries the mirror enumerated as cache content; only 15 were real Nx hashes, all pre-rotation"
  - "The o3-witness Case-B false-red note -> superseded; closed in code by `40e4d21` and `e5d3cd3` BEFORE the note was written (260804-h3b)"
  - "Quick 260726-pjz stuck at `Needs Review` in STATE.md -> resolved (260808-lpt)"
  - "260810-pvw's two `human_needed` items -> both closed: P18's composition question resolved BY MEASUREMENT (a mutation severing the spread reddens 12 assertions), and the orphaned disjointness comment FIXED in `4664855`, not waived"
  - "ITEM B, and THIS AUDIT'S OWN ERROR. Carried above as `open_by_design` and as the `09 + 10 (open by design, push-gated)` tech_debt group, it was ALREADY CLOSED when this audit ran, by run `31305961054` at head `e3bf98b`, 2026-08-09: `readMisses` 43 of `scanned` 112 on `publish (ubuntu-24.04-arm)` job 93226687998 and 43 of 113 on `publish (windows-11-arm)` job 93226687984, both `publish-verify` legs green (93227111969, 93227112032), and the `bead` cross-job round-trip HIT under the new key `bead31305961054` on both `dogfood-verify` legs, the windows one reading a `'linux'`-produced payload. WHY IT WAS CARRIED AS OPEN: `260809-2s6-SUMMARY.md` has TWO `## NOT OBSERVED` sections and this audit read the FIRST -- PLAN-1's, written BEFORE PLAN-2 opened the window that closed all three items. The second section is PLAN-2's and lists two entirely different things. No push was needed to close this; the closing window had already happened. Corrected 2026-08-10 by quick 260810-v1g: `10-VERIFICATION.md`'s row and `score:` key rewritten to the measured figures, and a pointer added under 2s6's FIRST `## NOT OBSERVED` heading so the misreading cannot recur"
  - "ITEM A -> CLOSED by measurement. The `every commit leaves all six gates green` claim on quick 260810-kuo's range is now measured: all 22 commits of `4518787..1b06816`, oldest first, on all EIGHT gates, uncached, on the main checkout. Result 22 of 22 GREEN, 88 recorded exit codes all 0, zero bundle drift at any commit, every verdict attributable to a captured log by path. Evidence: `.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md`. Eight gates is a superset of any reading of the unenumerated `six`, so the claim is discharged rather than approximated. The `22 checkouts x 6 gates` cost framing that kept it open was written without ever timing the battery: the whole sweep took 369 seconds, 6 minutes 9 seconds of wall clock end to end, about 17 seconds per commit including the checkout. `260810-kuo-VERIFICATION.md` now reads `status: passed`, 7/7, `behavior_unverified: 0`, and its second human item -- the SUMMARY reconciliation -- closed in the same task, where all three named stale references turned out to be already fixed downstream"
---

# Milestone v0.0.2 -- OS-Invariant Cross-OS Sharing: Audit

**Audited:** 2026-08-10T20:13:41Z at `0dbee20`, branch `gsd/v0.0.2-os-invariant-cross-os-sharing`
**Status:** `tech_debt` -- 57/57 requirements satisfied, zero critical blockers. **CORRECTED
2026-08-10 by quick 260810-v1g: both substantive items are CLOSED.** As first written this line read
"one item genuinely open for the maintainer and one open by design"; neither is open. The remaining
debt is 10 items across 6 groupings, none of which needs a maintainer decision or a push to `main`.

This audit SUPERSEDES the 2026-08-04 pass at `2df3af5`. That pass is 110 commits stale and most of
what it recorded as debt has since been closed; see `closed_since_prior_audit` above for the delta.

## Definition of Done

From `ROADMAP.md:29-48`:

> A Windows developer reuses Linux CI's portable task artifacts, and Windows CI reuses them too,
> with the OS-sensitive target still provably separated. Proven by dogfooding this repo, then
> documented as a recipe consumers can copy.

**Verdict: MET.** All four target outcomes are proven live, the fifth (Phase 13's read-only Actions
cache backend) is wired and counted consistently, and the mandatory ordering constraint held.

| # | Outcome | Layer | Phase | Status |
|---|---------|-------|-------|--------|
| O1 | Local Windows dev HITs `build`/`typecheck`/`test` produced by Linux CI | Releases mirror | 11 | PROVEN |
| O2 | Local Windows dev HITs `integration` produced by Windows CI | Releases mirror | 11 | PROVEN |
| O3 | Windows CI MISSES `integration` produced by Linux CI | Actions cache | 11 | PROVEN |
| O4 | Windows CI HITs `build`/`typecheck`/`test` produced by Linux CI | Actions cache | 12 | PROVEN |
| O5 | Windows CI legs read the cache through a backend with no writable surface | Actions cache | 13 | PROVEN |

**The mandatory ordering constraint held.** O1 had to be PROVEN before O4 was ENABLED, because
enabling O4 makes Windows CI a second producer of those hashes and permanently destroys the producer
attribution O1's proof rests on. The integration checker re-verified this independently from git
history, not from prose: Phase 11's TEST-08 evidence capture at `ada339a` precedes Phase 12's
second-producer commit `f5dd429`, and the two never shared a phase.

## Phase Verification

All seven phases carry a VERIFICATION.md with `status: passed`. No unverified phase.

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 07 lint-toolchain-and-the-ambient-platform-read-ban | passed | 22/22 must-haves, 2/2 prohibitions, 5/5 ROADMAP criteria | Re-derived live at HEAD, every mutation reverted byte-identical |
| 08 nx-task-hash-parity | passed | 7/7 | Human verification CLOSED by executing the named check, not waiving it: run `30367663950`, both leg artifacts compared field by field |
| 09 os-invariant-actions-cache-version | passed | 11/11 requirements code-verified | Re-verified a second time at `3d127b2`; the first-pass gap closed at code, fixture and mutation level |
| 10 os-invariant-releases-mirror | passed | 13/13 code-level; 3/3 live-CI items sampled | Live items sampled 2026-08-08 under a temporary main window, run `31281406708` |
| 11 live-proofs-o1-o2-o3 | passed | 12/12 | |
| 12 windows-ci-reuse-o4-consumer-recipe | passed | 9/13 at verification time, 13/13 after downstream closure | All four human-verification items closed 2026-08-08 by 260808-lpt, each from its own named artifact |
| 13 read-only-actions-cache-backend | passed | 7/7 | Verified at `4ac20dc` |

## Requirements Coverage (3-source cross-reference)

**57/57 satisfied. Zero unsatisfied. Zero orphaned. The FAIL gate does not fire.**

| Source | Result |
|--------|--------|
| REQUIREMENTS.md traceability | 57 rows, all `Complete`; 57 of 57 checkboxes `[x]`, zero `[ ]` |
| Phase VERIFICATION.md coverage tables | all 57 IDs present in their OWN assigned phase's report, each with an explicit satisfied verdict |
| SUMMARY.md `requirements_completed` frontmatter | 23 of 57 listed; 34 absent -- see the caveat below |

**Orphan detection: zero orphans.** Every one of the 57 traced IDs resolves in its assigned phase's
VERIFICATION.md. Checked mechanically, with a bogus-ID positive control confirming the search was
capable of returning nothing (exit 1, no output) -- a zero-hit sweep is not evidence of absence
without it.

**Distribution:** Phase 7 = 7, Phase 8 = 9, Phase 9 = 11, Phase 10 = 12, Phase 11 = 7,
Phase 12 = 4, Phase 13 = 7. Sums to 57, matching REQUIREMENTS.md's own coverage line.

### The third source is non-discriminating on this project

Per the status matrix, `passed` + frontmatter `missing` reads as **partial (verify manually)**. That
applies to 34 of the 57 IDs, because Phase 7's and Phase 8's SUMMARY frontmatter schema predates the
`requirements_completed` key entirely and later phases populated it unevenly. Treating those 34 as
`partial` would be an artifact of the instrument, not a finding about the work -- so all 34 were
verified manually against their phase's requirements-coverage table:

- **Phase 7 (7 IDs)** -- LINT-01..06, CORR-06: all `[x] EARNED`, each with live-test evidence.
- **Phase 8 (9 IDs)** -- PARITY-01..07, CORR-03, CORR-04: all `SATISFIED`, against a real two-leg run.
- **Phase 9 (10 IDs)** -- VER-01..07, ROBUST-04, OBS-04, DOCS-08: all `Verified`; VER-06 closed live;
  OBS-04 recorded with an honest partial mismatch rather than a clean claim.
- **Phase 10 (5 IDs)** -- OBS-05, TRUST-10..13: OBS-05's code half verified and its live half since
  CLOSED under the 2026-08-08 window; TRUST-10..12 verified in round 2; TRUST-13 was correctly
  unticked at verification time (the auditor had not run) and is now Complete against
  `10-SECURITY.md` section 1.
- **Phase 11 (2 IDs)** -- XOS-03, TEST-09: both `SATISFIED`, proven on push run `30500255530`.
- **Phase 12 (1 ID)** -- DOCS-07: `SATISFIED`, fully code-verified.

The remaining reservation is recorded as tech debt, not as a coverage gap.

## Cross-Phase Integration

Checked by `gsd-integration-checker` against code and config at HEAD, not against SUMMARY prose.
Full report: `.planning/v0.0.2-INTEGRATION-CHECK.md`.

**9/9 seams WIRED. 5/5 flows COMPLETE. Zero blockers.**

| Seam | Result |
|------|--------|
| Phase 7 -> 8 -> 9 single OS-discriminator hand-off | WIRED -- one `{ "runtime": ... }` in `nx.json`, enforced by a live exact-equality spec |
| Releases mirror vs Actions cache independence | WIRED -- genuinely two layers, as the ROADMAP claims |
| Phase 13 read-only backend composing the Phase 9/10 read path | WIRED -- spreads rather than duplicates; exactly one `restoreCache` read call site survives |
| Nine-knob consumer contract single-sourced across code, docs and tests | WIRED -- `CACHE_READ_ONLY` mechanically verified as the last, strictly-narrowing branch |
| `ci.yml` needs-graph, forced-push gate, remote-cache floor gates | WIRED |
| `windows-regression-detector.yml` dispatch | WIRED |
| Consumer recipe (`docs/cross-os.md`) vs actual behaviour | WIRED -- no dogfood-only optimization has leaked into the consumer contract |
| ROBUST-04 recursion from Phase 9 into Phase 10 | WIRED -- handled, not dropped |
| Post-phase consolidation commits (~110) vs cross-phase seams | WIRED -- every phase-owned caller routes through the new shared modules (`repo-file.ts`, `mirrored-by-label.ts`) with no orphaned references |

All 44 test files / 1145 tests pass at HEAD.

The checker's two findings are both bookkeeping, not broken seams, and are carried as tech debt: the
`11-EVIDENCE.md` O4 `PENDING` slot, and the stale four-outcomes claim in
`.planning/codebase/INTEGRATIONS.md`. It also confirmed that the "fifth outcome" language in the
Phase 13 work refers to `selectBackend`'s branch count (DOCS-10) and was never conflated with the
milestone's four-row O1..O4 table.

## Nyquist Coverage

**7/7 compliant.** Every phase has a VALIDATION.md with `nyquist_compliant: true` and
`wave_0_complete: true`. No phase needs `/gsd:validate-phase`.

| Phase | VALIDATION.md | Compliant | Detail |
|-------|---------------|-----------|--------|
| 07 | exists | true | 2 gaps found, 2 filled, 0 escalated |
| 08 | exists | true | 19 rows; 13 to a real test, 6 to a recorded run; 2 gaps filled, 0 open |
| 09 | exists | true | baseline 675 tests / 38 files, all nine battery commands exit 0 |
| 10 | exists | true | 33/33 rows green, 0 manual-only, 0 open |
| 11 | exists | true | 3 gaps found, 3 filled, 1 escalated; 848 -> 856 unit, 4 -> 15 integration |
| 12 | exists | true | 13 rows, 8 green at audit; remainder closed downstream |
| 13 | exists | true | audited at `eb57440` |

## Tech Debt

**CORRECTED 2026-08-10 by quick 260810-v1g.** Both of this audit's substantive items are now closed,
by different means -- one by running the measurement that was missing, one by discovering the
measurement already existed and correcting THIS AUDIT'S OWN misreading. Nothing is genuinely open for
a human and nothing is open by design; what remains is artifact prose sitting behind a tree that
moved, plus one standing non-actionable failure and two cosmetics.

### Open for the maintainer (0 -- was 1, CLOSED by measurement)

**Per-commit gating on quick 260810-kuo's 22-commit range: CLOSED, 22 of 22 green.** The sweep was
run. All 22 commits of `4518787..1b06816`, oldest first, on all EIGHT gates (`build`, `typecheck`,
`test`, `integration`, `lint` via `nx run-many --skip-nx-cache`, plus `format:check`, `check:action`,
`fallow:ci`), uncached, on the main checkout: **22 of 22 green**, 88 recorded exit codes all 0, zero
bundle drift at any commit, every verdict attributable to a captured log file by path. Evidence:
`.planning/quick/260810-v1g-close-the-two-substantive-v0-0-2-audit-i/260810-v1g-SWEEP.md`. Eight is a
superset of any reading of the "six gates" the artifacts never enumerate, so the claim is discharged
rather than approximated. `260810-kuo-VERIFICATION.md` now reads `status: passed`, 7/7,
`behavior_unverified: 0`.

The item as this audit stated it also carried a false cost premise: "22 checkouts x 6 gates and was
not run" was written without anyone ever timing the battery. Measured from the sweep's own file
timestamps: the entire 22-commit sweep took **369 seconds, 6 minutes 9 seconds** of wall clock end to
end, about 17 seconds per commit including the checkout, with no `npm ci` needed at all because
`package.json` and `package-lock.json` are byte-unchanged
across the range. The maintainer decision this item asked for was never actually required. The
accompanying `260810-kuo-SUMMARY.md` staleness closed in the same task, where all three references
named above turned out to be already fixed downstream and were re-measured rather than re-edited.

One honest limit: the sweep is a local Windows-arm64 measurement, not CI. Nothing was pushed.

### Open by design (0 -- was 1, and it was never actually open)

**The post-fix `readMisses` value: CLOSED, and it was closed a day BEFORE this audit ran.** Run
`31305961054` at head `e3bf98b`, event `push`, headBranch `main`, conclusion `success`, 2026-08-09
measured all three items: `readMisses` **43 of `scanned` 112** on `publish (ubuntu-24.04-arm)` job
`93226687998`, **43 of 113** on `publish (windows-11-arm)` job `93226687984`, both `publish-verify`
legs green (`93227111969`, `93227112032`), and the `bead` cross-job round-trip HIT under the new key
`bead31305961054` on both `dogfood-verify` legs -- the windows one reading a `'linux'`-produced
payload. `10-VERIFICATION.md`'s row and its `score:` key now carry those measured figures with their
denominators and their run id. No push to `main` was needed to close this, and none was made.

**What this audit misread, named plainly.** `260809-2s6-SUMMARY.md` contains TWO `## NOT OBSERVED`
sections, one per plan. The FIRST is PLAN-1's, written BEFORE the window, and it lists the three
items. The SECOND is PLAN-2's, written AFTER the window that closed them, and it lists two entirely
different things. This audit read the first and inherited a claim that was already stale, then
recorded it as `open_by_design` and as a `tech_debt` grouping. Quick 260810-v1g added a pointer
directly under that first heading so the next reader cannot repeat the misreading -- the cheapest of
the three edits that closed this item, and the only one that prevents a recurrence.

The reasoning that kept `10-VERIFICATION.md`'s row un-rewritten was CORRECT and is preserved rather
than deleted: it refused to substitute a DERIVED figure into a measured-evidence record, and its own
instruction was to close the row from the same live window that closes 2s6's three items. That
instruction was satisfied, not overruled. The substituted figures are measured, transcribed into
tracked source with their run id by the task that ran the window, because these five counts reach the
job summary only and never the step log. The irony is worth keeping: the one code path that would put
them in the step log is the partial-read warning, which fires only above the threshold -- so success
is exactly what makes the number unobservable via `gh`.

### Artifact prose behind the tree (4 sites)

Each is a true statement that a later commit falsified, in a file nothing gates:

- `11-EVIDENCE.md`'s O4 verdict slot reads `PENDING`; the observation exists in `12-UAT.md`.
- `.planning/codebase/INTEGRATIONS.md` says `selectBackend` has four outcomes; Phase 13 made it five.
- `08-VERIFICATION.md` calls the ROADMAP Phase 8 ID shift "surfaced, not fixed"; `d5841c2` fixed it.
- `10-VERIFICATION.md` says TRUST-13's auditor has not run and `10-SECURITY.md` is not on disk.

### Standing, not actionable (1)

**The unattributed `test` failure at `69bd1b7`.** Not reproducible in seven attempts, with nothing
recoverable afterwards because Nx caches terminal output for successful runs only, so the re-run
destroyed the evidence. Nx classified it flaky (a FAILURE and a SUCCESS at the same task hash) and
the commit was docs-only. Actionable only on a second occurrence WITH output captured -- the capture
procedure is now codified in `AGENTS.md`, which is the whole of the intended response.

### Low / cosmetic (2)

- Phase 10 deferred D2 is substantially closed, but `publish-mirror.spec.ts:437`'s test title still
  pairs `foreign-OS` with a restore MISS -- the stale-CAUSE class D2 named. D2's own suggested guard
  (a `publish-mirror` row in `DOCS_08_SITES`) is in place.
- Phase 13 deferred D1: a redundant untracked `.gitkeep` in the phase directory.

### Deferred to v0.0.3, deliberately (1)

Nine UNRESOLVED `/simplify` items have their own deferral record (`b6580ad`). Not v0.0.2 debt;
recorded here so the lane survives milestone close.

**Total: 10 items across 6 groupings.** Recounted from the corrected `tech_debt` frontmatter rather
than by subtracting two from the previous line, which is how a third error surfaced: the previous line
read "12 items across 8 groupings" while the frontmatter it was summarising held **13** items across
those 8. The two closures removed one whole grouping each -- `260810-kuo (quick)` with 2 items and
`09 + 10 (open by design, push-gated)` with 1 -- so 13 minus 3 is 10, across 6.

**Zero need a maintainer decision and zero need a push to `main`.** The "needs a push" clause is now
false in its entirety: item B's closing run had already happened, so no window was ever required. All
10 remaining items are artifact prose, one standing non-actionable failure, two cosmetics, one audit
instrumentation note, and one deliberate v0.0.3 deferral.

## Assessment

The milestone achieved its definition of done. Every requirement is satisfied and independently
verified, every cross-phase seam is wired against the code rather than the claims, and all five
outcomes are proven live -- including the ordering constraint that made the Phase 11 -> 12 boundary
non-negotiable.

What is notable about the debt is its shape. There are no unsatisfied requirements, no broken seams
and no uncovered code. What remains is almost entirely artifact prose that was TRUE when written and
that a later commit falsified -- the predictable cost of ~110 post-phase commits of multi-agent
review fallout landing on a closed milestone's records. That observation is this audit's most useful
finding and it still stands.

**Both substantive items are now closed, and by different means.** Each began as an honest refusal to
certify -- a coverage claim narrower than its wording, and a measured expectation left un-rewritten
rather than replaced by a computed one -- and each closed differently. The first closed by RUNNING the
measurement that was missing: 22 of 22 commits, all eight gates, uncached, 6m09s, and the cost
framing that had deferred it was measured false. The second closed by DISCOVERING that the measurement
already existed, on a run from the day before this audit, and that the only thing genuinely missing was
a correction to this audit's own reading -- it had stopped at the first of two same-titled
`NOT OBSERVED` sections in one summary. So one item needed work and the other needed a retraction, and
this audit's shape observation applies to the audit itself: the stale artifact was this file.

Neither closure required a push, a window, a backup ref or a workflow toggle. Nothing was merged.

The branch is not merged. `PR #16` remains open.
